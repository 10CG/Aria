---
track-id: pre-merge-completeness-gate-change-scope
owner-container: simonfish/bfe8285d
phase: A.2
status: active
updated-at: 2026-09-24T13:43:07Z
---

# Aria — Session Handoff (2026-09-17 / 18) — `10CG/Aria#199` A.2/A.3: 接手双子星 → C1 核验 → v2.1 返修 → post_planning R2



## 一句话

接双子星交接 (`docs/handoff/2026-09-17-199-a2-a3-v2-r1-paused-handover-to-twin.md`) → 以原串重新认领 (`s-73b9@1606`, A.2) → **C1 主控核验 v2** → 发现证据层两处不可复现, owner 裁「先修证据再开 R2」→ 执笔实例返修出 **v2.1** → 主控四项独立核验全过 → 推送 (`5d435e9`, 两端 MATCH) → 发两件单 → **开 R2** (五席, 2 席滑动窗口)。

## C1 核验结论 (详见 scratch `C1-verification-record.md`)

- 生成器 `REGEN_IDENTICAL`; `a2_state_runs` 复跑逐字节一致; `revision_log` 抽查 11 条落点全部属实; C2 两处自报风险属实且已有书面局限。
- `v2_state_runs` 复跑**四处不一致**, 全部归因: 三处 N6 证据与执笔容器身份**及其认领状态**耦合 (换容器必然翻转), 一处 N9 fixture 未钉时间戳致同秒排序漂移。**计划内容本身没查出新 Major。**

## v2.1 返修 (执笔实例做, 主控只核验)

- 只改 `metadata.v2_state_runs` 的 script 与 output; 判据代码与 `gen_yaml.py` 一字未动。
- N6: fixture 自造本轨 claim (raw id 每跑唯一, 容器身份由 `phase1_gate` 自解析回填) + 等秒边界; N9: 提交时间钉死并单调递增 60 秒。
- 主控独立核验四项全过: 脚本 diff 54 行与自述一致且未触判据; 重生成 yaml 结构化比对无夹带; **自建独立副本复跑与执笔产物逐字节一致**; 主仓零写入。
- 执笔实例另报两条主控该知道的: (1) **还有第 4 处同源耦合主控没撞上** (`own release` 格也吃真仓认领状态, 若派给没接过本轨的容器会从「拦住」翻成「放行」), 已一并解耦; (2) 同秒心跳会写出空 diff commit 是**真竞态** —— 他先做反事实发现全绿, 因其为 no-op 而不采信, 改用直接调库探针才实证出来。

## 本次落地

| 时间 | 事件 | 证据 |
|---|---|---|
| 09-17 | 以原串重新认领 199 轨 | claim `s-73b9@1606` (A.2), push_success true |
| 09-17 | C1 核验 v2 (生成器 / a2 复跑 / v2 复跑 / revision_log 抽查 / C2 风险) | scratch `C1-verification-record.md` |
| 09-18 | v2.1 本地提交 + 推送 | `5d435e9`, origin 与 github 各自 ls-remote MATCH |
| 09-18 | C6 追评论 (VERSION 子模块表无机械覆盖) | `10CG/Aria#177` comment 24863 |
| 09-18 | C7 开新单 (phase1_gate self-resume 按 session 匹配) | `10CG/aria-plugin#202` |
| 09-18 | 心跳刷新 (precheck 退出 0 后) | 协调 ref `6ddf089`, 两端一致 |
| 09-18 | 开 post_planning R2, 入口探针 no_sibling_found | 派单 scratch `199-r2-prep/r2-prompts/` |

## AI 流程判断 (Rule #10, 请 owner 复议)

1. **C6 的归宿与 carry 清单写的不同**: 双子星 C6 写「开单」, 主控定向查重后发现 `10CG/Aria#177` 正是该类的单且评论区已有同形补充先例 ⇒ 改为追评论。已如实记录。
2. **为保住 claim 主动刷了心跳**: 心跳停在 09-17T16:06Z, 已近 SWEEP_TTL (24h), R2 派发期间丢 claim 会很麻烦。按 owner 2026-09-17 裁定 (本容器已有 claim 的心跳免逐次授权) + 先跑 `coord_ref_precheck` 退出 0 后执行。
3. **R2 派单用文件而非内联**: 派单全文落 scratch, 席位自己读。省 token 且留下可审计原件; 但席位读文件失败时会静默降级, 主控须在收报告时核对它是否真按派单执行。
4. **审计轮进行中冻结工作区**: `latest.md` 的 track 表里本轨 owner-container 仍写 `023236f2` (已过时), 明知有误但等五席返回 + 聚合落盘后再改, 以免污染「porcelain 只多了它自己那份报告」的核验判据。

## 待办

- R2 五席 → 聚合 → 收敛判定 (注意: R1 有 10 个 Major 簇, 比较键集合结构上不可能与 R2 相等 ⇒ 不可能判 converged; 别在 R2 改口径)
- R2 的 Major 若过半由 v2 返修自身引入 ⇒ R3 换新执笔实例 (R1 定的判据)
- 执笔实例提的三条待裁项 (都要改 `gen_yaml.py`): 同秒心跳的生产含义写进 `cannot_catch` / 第 99 行真仓路径依赖如何处置 / `own_claim_files` 补注生产用法 vs fixture 做法
- `latest.md` 本轨行的 owner-container 与状态更新 (冻结解除后)
- C4 (B.1 入口前置: 10CG/Aria#195 完成 C.2 或 owner 改序) / C5 (10CG/Aria#195 的 Phase B) 仍在 carry


## R2 结果 (2026-09-18, 五席已齐)

聚合: `.aria/audit-reports/post_planning-R2-2026-09-18T162427-983Z-pre-merge-completeness-gate-change-scope-aggregated.md` (133 行)

- verdict **PASS_WITH_WARNINGS** · 去重前 0C/6M/11m (17 条) → 去重后 **0C/5M/10m (15 键)** · vote **3 REVISE / 2 PASS** · `converged: false` · `oscillation: false`
- 五条 Major: TASK-001 的 claim 身份写死且对 `yielded` 一态完全失明 (两席同 scope 不同 category, 交叉注明同题) · `commit_attribution` 对发版同步面偏松 · `baseline_rebase` 的 standards 零 diff 断言在 v2.1 落盘时已为假 (两席独立算出同一 finding id) · 读前必看第 8 条对 `spec_level_undetermined` 未收窄
- **五条 Major 无一由 v2.1 返修引入** ⇒ 按 R1 判据不触发换执笔实例
- 主控逐条独立复核, 修正五处「实质成立、概括失准」的席位措辞; 两处席位分歧经核均非对称分歧, 不列 Conflicted
- 五席 frontmatter 的 `drift_check_skipped` 一致写 `false`, 聚合按口径重算 `true`, 报告原文不改

## v2.2 返修 (2026-09-19, 执笔实例出稿 + 主控核验)

R2 五条 Major 全部闭合, 经生成器产出 (禁手改 yaml 的纪律未破)。M1+M2 合并修: claim 身份改按 (本容器, 归一 track_id, active) 三元组运行时解析, owner_gates 第 14 项扩到四态。M3: 路径三分 exclusive / shared / foreign, 判 own 需「无 foreign」且「有 exclusive 或带本轨 `Spec:` trailer」。M4: standards 断言改实测值加范围限定, `rule6_note` 按 SOT §4.1 五字段重写。M5: explicit-only 扩到 `spec_level_undetermined`, 并给 SC-9(4) fixture 钉显式 checkpoint 才让断言脱离空集恒真。

**主控核验逐项自跑** (未采信执笔自报): 越界写入 0 · REGEN_IDENTICAL · M3 十一例对照全符合 (含两条历史无此形态、由主控在核验副本里构造的样本) · trailer 支撑独立复算 (近 300 提交纯 shared 集 11 条, 带本轨 trailer 0 条) · a2 证据逐字节不变 / v2 证据仅 N9 新增 3 态 · 归档门 31 task 且与 v2.1 同态 · 新增行裸引用 0 · 禁用字形 0 · HEAD 与 gitlink 未动。

**主控自己的一处判据错误 (记录以免复现)**: 核验脚本把「旧 claim 路径 `s-86f7@1836` 残留须为 0」写成全称句, 实际五处全落在「记录时事实 / 实测锚 / 用法示例 / revision_log」语境, 保留是对的 —— 正是 M4 要求别人改的那类没带范围限定的全称句, 判据已修正。

## 执笔实例提请裁定 (六条, 未自行决断)

1. `rule6_note.scenario1` 在 A.3 只能是占位 (结果目录要到 TASK-024 才存在), 回填与齐备性断言放在 TASK-031 —— 计划期填占位是否可接受。
2. `scenario4b: not_required` / `negctrl: n/a` 的成立完全依赖「description 零改动」, 若 B.2 真改了 description 必须回头改 `rule6_note` 并补跑场景 4b, **目前没有机械触发器**, 只写进字段依据。
3. M3 的 trailer 是**声明不是证明**: 他轨若在纯 shared 提交里写上本轨 trailer 会被判 own。执笔实例判可接受 (需伪造意图, 且第 2/9 项仍呈递 `git log` 清单); 结构更强的替代是砍掉 trailer 分支、要求本轨每个提交都含 exclusive 路径 (代价: TASK-029 的台账须并进同一提交)。**这是执笔实例自报最不放心的一条。**
4. M3 新增停点: 本轨 5.7 发布同步提交漏写 trailer 会停在等待点 16 (方向 fail-closed)。
5. M1 多条 active claim 选「全部纳入 + 上报」, 不挑不删 (依据 `heartbeat_by_track` 的 all-matching 语义), 与 `10CG/aria-plugin#202` 强相关。
6. TASK-030 原句同时记了 aria-orchestrator「detached」(属未动的 minor) 与 standards SHA, 只改了 standards 半句, 刻意没碰未裁的 minor —— 请确认这个边界切法。

另有一处超出可写范围的陈旧: `.aria/notes/2026-09-17-199-a2-a3-tooling/README.md` 仍写「对应 v2 (主仓 `caa40c8` 起)」, 流程描述未过时, 不在允许写的三文件内故未动。

## R3 结果 (2026-09-19, 五席已齐)

聚合: `.aria/audit-reports/post_planning-R3-2026-09-19T044500-000Z-pre-merge-completeness-gate-change-scope-aggregated.md` (152 行)

- verdict **PASS_WITH_WARNINGS** · raw 0C/8M/7m (15 条) → 席位登记层去重 0C/6M/7m (13 键) → **聚合归并后 0C/4M/7m** · vote **5 REVISE / 0 PASS** · `converged: false`
- **R2 五条 Major 五席一致判 closed** (code-reviewer 对 PP2-M3 判 partially, 理由是收紧带出的新洞而非旧洞未闭, 该部分独立计为 R3-M2)
- 本轮四题 Major, **主控全部独立复现**: R3-M1 TASK-001 基线复核缺 standards 重测 (四席同题, 本轮最强跨席佐证) · R3-M2 `commit_attribution` 收紧后误伤本轨自己 (TASK-023 一切做对仍恒红; 工具目录未登记致真提交 `12c870d` 判 foreign) · R3-M3 Rule #6 的 `description_changed` 只验四份被改 SKILL.md 中的三份 (fail-open) · R3-M4 心跳推送是全计划唯一无推后核验的推送类 (与多远程约束 2 冲突)
- 七条 minor 新增; R2 未动的 8 条 minor 状态不变
- Major 题数 **10 (R1) → 5 (R2) → 4 (R3)**; 本轮仅 1/4 由 v2.2 返修自身引入, **未过半 ⇒ 按 R1 判据不触发换执笔实例**
- 聚合另记: 两处 finding id 登记纠正 (backend-architect 错用 R2 的 id; `9122f4a9` 两轮内容相反属真撞车, 并列保留) · 五席 `drift_check_skipped` 连续第三轮一致写错, 由聚合按口径重算 · **主控自身两处判据错误** (「旧 claim 路径残留须为 0」的全称句; 数本轨报告时 grep 漏了 spec_id 捞回 43 份他轨报告), 均已修正并记入流程记录
- 审计期冻结守住: 五席逐一返回各核一次, 工作区只增席位报告、无越界; HEAD `5fd7e08` 与三个子模块 gitlink 全程未动; 共享审计副本零改动

## v2.3 返修 (2026-09-19, 执笔实例出稿 + 主控核验)

R3 四题 Major 全部闭合, 经生成器产出。M1: 新增机读清单 `baseline_rebase.standards_files` (七条) 与 `standards_files_basis`, TASK-001 按它跑第四组命令, 与 aria / 主仓两组同构; 含 v2.2 漏列的 `conventions/git-commit.md`。M2: `TOOLING` 常量进 `exclusive()` 前缀集 + TASK-023 补 trailer 义务; `cannot_catch` 的代价陈述按全量分类改写。M3: frontmatter 比对面三份改四份 + TASK-017 补断言。M4: 新增 `metadata.coord_push_verify` 五键并接进心跳 / 重认领 / release 三处, 断言含 `push_success` 与推后 `ls-remote` 比对, 并单列 no-push 分支。

**主控核验逐项自跑** (未采信执笔自报): 越界 0 (恰三文件) · `REGEN_IDENTICAL` · M2 十三例对照全符合 (含主控构造的五条样本) · M2 全量核对独立复算一致 · M4 心跳条四项断言 False → True · M1 / M3 落地逐项核 · 归档门 31 task 且与 v2.2 同态 · 顶层最后一个键仍是 `tasks` · 裸引用 0 / 禁用字形 0 · HEAD 与三个 gitlink 未动 · 共享副本零改动。

**主控自己的两处期望写错 (记录以免复现)**: 我在建 M2 对照期望表时把 `1ab72cc` 与构造样本 `f1030e0` 的期望写成「应转 own / 不应再恒红」, 核验时两者皆未如期翻转。逐文件复算后确认**两条都是我错**: `1ab72cc` 含 `docs/handoff/latest.md` (track-id 不符), 判 foreign 是设计里有意的 fail-closed; `f1030e0` 无 trailer, 判 `shared-only` 正是应有行为 —— 我另造带 trailer 的同形态实测, 在 **v2.2 代码下就已 `own-release-sync`**。另一处: 我的归因脚本用正则只抓字面量前缀, 抓不到 `p.startswith(TOOLING)` 这种变量写法, 一度把「已纳入 exclusive」误读成「未纳入」; 改用构造样本 E (纯工具目录) 实证翻转 `foreign` → `own`。

## 执笔实例提请裁定 (三条)

1. **「R3-M2 由 v2.2 返修自身引入」的计数口径**。它实测证明形态 1 (TASK-023 恒红) 在带 trailer 时于 v2.2 代码下**本就通过** ⇒ 形态 1 不是收紧引入的代码缺陷, 而是收紧带出的连带义务没写进 TASK-023; 真正由 v2.2 代码引入的只有形态 2 (工具目录)。**主控复核: 成立, 我已独立复现**。这不改变返修内容, 但 R3 聚合里「四题中 1 题由本轮返修引入、未过半」的分子口径据此仍成立 (形态 2 属实), 只是该题的性质描述应更精确。
2. **ab-results 不进静态 exclusive 集**。理由是写死会把他轨的 ab-results 一并放行 (样本 `9de3074` 正是他轨 ab-results, 现判 foreign), 取舍为 fail-closed; 代价是 TASK-030 调用点漏传 `extra` 就会停。请 owner 确认接受。
3. **`git-commit.md` 进 `standards_files` 但其被引措辞仍是未处置的 minor `31b4c0f1`**。若后续裁该 minor 要改写引用, `standards_files` 里这一条的说明需同步改。

另: 执笔实例自报「本轮唯一被推翻的自造错误 (初稿写过一条未实跑的断言: ab-results 与台账同提交即判 own) 是靠对照实验而非自检发现的」, 并建议下一轮复核席至少一席不读其报告结论、直接对三文件 diff 独立复算 —— 与本仓 memory 记过的「勘正动作由原作者执笔时错误系统性逃逸」同型, 建议采纳。

## R4 结果 (2026-09-20, 五席已齐)

聚合: `.aria/audit-reports/post_planning-R4-2026-09-20T033000-000Z-pre-merge-completeness-gate-change-scope-aggregated.md` (159 行)

- verdict **PASS_WITH_WARNINGS** · raw 0C/5M/10m (15 条) → 席位登记层去重 0C/4M/9m (13 键) → **聚合归并后 0C/4M/8m** · vote **4 REVISE / 1 PASS** · `converged: false`
- **R3 四题**: M2/M3/M4 五席一致 closed; M1 四席 closed + km 判 partially (理由即其自立的 M1, 已独立计数 ⇒ 聚合判 closed)
- **本轮四题 Major, 主控全部独立复现**: R4-M1 standards 重测组在「standards 真动过」时静默判过 (未 fetch 的 gitlink 使 stdout 空串 rc=128, 与零 diff 逐字节相同; **检查的失效条件与它要检测的条件是同一个**) · R4-M2 手写 Phase D 丢掉 Rule #9 五字段自校验与 `latest.md` 两子步 (tl+cr 跨席同键) · R4-M3 skill-creator 工作区漏计 (从不进 deliverables 字段, 按字段扫描结构上看不见) · R4-M4 `standards_files` 漏 `session-handoff.md` (求法找「被点名的」, 判据写「实际依赖的」)
- **引入来源**: R4-M1 与 R4-M4 **由 v2.3 返修自身引入**, R4-M2 / M3 为 v1 遗留 ⇒ **2/4 恰好一半、未过半**, R1 判据不触发换执笔实例
- Major 题数 **10 (R1) → 5 (R2) → 4 (R3) → 4 (R4)**, 且本轮四题与 R3 四键**交集为空** (无一前轮遗留)
- **本轮特有流程见效**: 按执笔实例自己的建议, 指派 code-reviewer 先不读其返修报告、直接对 diff 逐 hunk 独立复算。结论「独立复算没有推翻它任何一条返修结论, 出入全部落在两类: 它**没看到的面**(两条 Major)与**自报与产物之间的偏差**(两条 minor)」。四条 Major 里有两条是该席发现的
- **主控自身三处方法缺口** (已记入聚合流程记录): 曾报「全量分类独立复算一致」实为同一方法两次执行、不构成交叉验证 · 用英文 `workspace` 检索中文「工作区」得零命中差点判席位编造 · 用全 yaml 搜字符串判引入来源、检索面选错层级
- 审计期冻结守住: 五席逐一返回各核一次, 工作区只增席位报告、无越界; HEAD `a182ba2` 与三个 gitlink 全程未动; 共享副本零改动

## v2.4 返修 (2026-09-21 / 22, 执笔实例出稿 + 主控核验)

owner 裁定 (原话): 2026-09-21 在 /state-scanner 推荐中选「[1] 裁 R4 四题 Major → 交执笔实例返修 v2.4」; 范围追问选「四题 Major + 四条同处 minor」; R4-M3 二选一选「执笔实例按证据裁, 主控核验」。2026-09-22 追加: 执笔实例请裁第 9 条 (`metadata.container`) 照补后落仓; 落仓后双推。

- **执笔实例 = 本会话新派的 tech-lead 实例** (跨会话必然换人 ⇒ H3「是否换执笔实例」事实上已换)。工作面 = 返修副本 (真仓 `71c500e` 的 `cp -a` 全量副本; standards 子模块的 `.git` 是**绝对** gitdir 指针, 不改的话副本里的 git 操作会写进真仓, 已改为相对; 主仓与三个子模块全部 remote 的 push URL 已禁)。派单原件落 scratch, 以 sha256 前 16 位 (`d44cccba2eb42056`) 核对其确按原件执行。
- **四题 + 四条同处 minor 全部闭合, 经生成器产出** (`b686185`): R4-M1 基线复核三组同口径 (standards 组先 fetch; 两端点先 `cat-file -e`; 零 diff = 退出码 0 且输出为空; gitlink 取 `ls-tree` 第三字段) · R4-M2 TASK-031 补 D.3 起稿 / 五字段 E1 自校验 (命令逐字照 SOT) / `latest.md` 两子步, 判断清单第 25 条列全 phase-d-closer 全部子步 (含 D.post) · R4-M3 按 (b) 裁: 工作区会产生但不入库 (放 aria `skills/*-workspace/` 或主仓 `ab-workspace/`, 两处均已忽略), TASK-030 删第二个 extra, TASK-024 快照比较改为 porcelain 出现 `*-workspace/` 行即停 · R4-M4 `standards_files` 补 `conventions/session-handoff.md` (零 diff) · `f5b3afad` / `0f027861` 求法改为可复跑两步 (字面计数 + Rule #N 语义补足, 输入钉冻结快照), 新增 `standards_files_derivation` (command / script / output) · `cb1529a3` 粗体范围句改「六个文件」并全文计数联动 · `f0e78a1e` v2.3 条原文不改、v2.4 条勘正, TASK-029 deliverables 去台账 (实证判定该改)。判断清单补第 35–39 条 (v2.4); `revision_log` 24 → 29; `metadata.container` 补 v2.4 执笔记录。

**主控核验逐项自跑** (未采信执笔自报): 越界 0 (恰三文件, 子模块零改动) · 真仓零写入 (派单后无 fetch) · 派单指纹与三文件 sha256 与报告一致 · `REGEN_IDENTICAL` · 31 TASK、顶层末键 `tasks` · 归档门与 v2.3 同态 (`complete=False` / `verdict=pass`) · **三态证据**: 先在主控自建环境 (主仓 `a563192` + aria `1cb3872` 嵌套 clone) 用 v2.3 输入校准、逐字节复现, 再用 v2.4 终版输入复跑, 两份均逐字节一致 · **R4-M1 五态反事实** (缺对象 + 真零 diff / 缺对象 + 真有 diff / 对象齐两态 / 整行误喂): 旧判据三态假绿 —— 其中「缺对象 + 真有 diff」是 `content-integrity.md` 的 +56/-2 被读成零 diff, 比 R4 原文所述更重; 新判据三态全停、正常两态不误停 · R4-M2 E1 与 SOT 逐字节一致, 「整份检索 5 / `head -8` 得 1」复现 · R4-M3 取证 (两条忽略规则与注释原文 / skill-creator SKILL.md「as a sibling to the skill directory」/ `check-ignore` / `2a46d08` 零 workspace、2026-04-10 后 51 个 AB 结果提交零 workspace、23 个被追踪 workspace 文件全来自 04-09 的 `2892c6f`) 全部属实 · 求法脚本独立复跑逐字节一致 (保留的 7 族均为单成员) · 计数联动全文一致 · 新增行禁用字形 0、裸 issue 引用 0 (正则 28 处命中全为 `Rule #N`; 项目检查器两版均 0) · `revision_log` 前 24 条逐字不变 · 补遗只动 `metadata.container` 与 `revision_log[28]` 两键。

**主控另记** (不计入本轮返修, 供 R5 或 owner 复议):

1. TASK-031 第 10 条「track-id 写错 ⇒ 停在 owner_gates 第 16 项」在 Phase D **不可达**: `commit_attribution` 全计划只在 TASK-001 / TASK-030 调用, 第 16 项也只挂这两处。v2.3 起即有, 执笔实例与主控各自独立发现, 范围外未改 (执笔实例请裁第 7 条)。
2. 与 R4-M1 **同族** (命令自身失败时空输出被当通过) 的还有两处 (执笔实例范围外观察): TASK-027 第 4 步 (b) 的「有输出 ⇒ 停」与 `metadata.sc12_liveness.guard_config_hooks` 的「须无输出」。实际风险低。
3. R4-M1 的发生条件比审计原文窄: 主仓 `git fetch` 在默认 on-demand 递归下会顺带拉取 gitlink 有变且已检出的子模块 (执笔实例实测); 修法不受影响, 计划原文已按实测改写。
4. 本次开工时本容器 claim 心跳已 **26.4h** (超 SWEEP_TTL 24h)。原因之一: state-scanner SKILL.md 的心跳触发条件写的是「本会话持 active claim」, 新会话尚未认领即不触发 —— 容器的 claim 跨会话老化无人刷。已按 owner 2026-09-17 裁定刷新 (`coord_ref_precheck` 退出 0 → `refreshed` / `push_success=true` / `push_skipped=false` → 两端 `375517c`)。

## 执笔实例提请裁定 (九条, v2.4)

1. **R4-M3 裁为 (b)** —— 主控已核取证属实, 建议接受。
2. `latest.md` 改动单独成提交, 在 owner_gates 第 13 项的授权请求里点明其 diff (未新增等待点、未改第 13 项原文); 若要在 owner_gates 显式写出, 需改第 13 项。
3. 基线复核的 aria 组与主仓组按 standards 组同口径一并改 (而非只论证不受影响)。
4. TASK-029 deliverables 去掉台账 (另一选项: 保留台账、改 owner_gates 第 16 项措辞)。
5. `latest.md` 的 History 落点按本仓现行版式 (track 表本轨行 + 说明段) —— SOT 写「History 表格」, 本仓 `latest.md` 无此表。
6. D.post 写进手写路径, 执行时读配置判定 (当前 `post_closure = off` ⇒ 跳过, Rule #10 白名单第一类)。
7. TASK-031 track-id 那句的停点不可达 (见上文主控另记第 1 条): 是否让 TASK-031 在推 Phase D 提交前跑一次 `commit_attribution`。
8. 审计报告里 R4-M1 前提 (on-demand) 的勘正是否另记 —— 主控已记于上文主控另记第 3 条。
9. ~~`metadata.container` 未更新~~ → ✅ owner 2026-09-22 裁补, 已补 (主控初版派单漏列)。

## R5 结果 (2026-09-22, 五席已齐, max_rounds 耗尽)

聚合: `.aria/audit-reports/post_planning-R5-2026-09-22T034038-000Z-pre-merge-completeness-gate-change-scope-aggregated.md` (155 行), 提交 `fffc5da`

- verdict **PASS_WITH_WARNINGS** · raw 0C/6M/9m (15 条) → **聚合归并后 0C/5M/7m** · vote **4 REVISE / 1 PASS** · `converged: false` · `oscillation: false`
- **R4 四题 + v2.4 处置的四条 minor**: 五席一致 closed
- **本轮五题 Major, 主控全部独立复现**: R5-M1 第 13 项同批授权与 v2.4 新增的 `latest.md` diff 呈递时序冲突 (v2.4 引入) · R5-M2 claim 只在 TASK-001 核一次存活 (本轨心跳间隔三段超 24h, 协调 ref 历史 sweep 6 次) · R5-M3 C.2.4.5 子模块指针闸 (缺省 block) 零提及 (前四轮 24 份报告也零提及) · R5-M4「空输出即通过」同族两处 (qa / km 判 major, tl / cr 风险节判 minor; 五簇中最软) · R5-M5 aria 侧工作区躲过 porcelain 却污染 SC-13 递归检索 (v2.4 引入)
- **引入来源**: 2/5 由 v2.4 返修引入 (R5-M1 / M5), 未过半 ⇒ R1 判据不触发
- Major 题数 **10 → 5 → 4 → 4 → 5**, 相邻两轮键集**轮轮交集为空** (每轮都是新面, 不是同一批问题打转)
- **本轮流程**: 派单文件 + sha256 指纹, 五席全部吻合; knowledge-manager 登记的 id `af970df1` 在 288 种组合下不可复算, 聚合不采信; 去重解析器补第五格式族 (backend-architect 首跑零命中); 冻结纪律守住 (工作区 1 → 5, HEAD `cf05d8b` 与三个 gitlink 未动, 两份共享副本零改动)
- **主控自身漏核**: 2026-09-21 核验 v2.4 时没看到 R5-M1 / M5 —— 两者都是修法带出的新面 (聚合流程记录第 8 条)
- **席位新证据**: `latest.md` 的 History 节曾按 SOT 存在, 在合并提交 `ecb6296` 中丢失 (tl, 主控核实); R4 minor `0dd2d3f2` 的不可复现根因是其 R4 用错了副本, 嵌入证据本身未失实 (qa)

## v2.5 返修 (2026-09-22, 执笔实例出稿 + 主控核验)

owner 裁定 (原话, 2026-09-22): 降级策略「[2] 增加轮次」; v2.5 范围「Major + 同处 minor + 同族扫描」; 执笔「沿用 v2.4 执笔实例」。

- **执笔 = 本会话 v2.4 的同一执笔实例** (保留上下文)。新返修副本 = 真仓 `ad35d83` 的全量副本 (建副本时 `cp -a` 撞上瞬时 `index.lock` 致 `&&` 链静默跳过 gitdir 修正与禁推, 主控发现后补做, 并在副本目录内逐个核验 gitdir; 已记 memory)。派单指纹 `f0a1fd574e122484` 核对一致。
- **闭合 (`e7a1782`)**: R5-M1 原等待点 13 拆为 13a / 13b (13a 批、13b 未批的中间态记台账) · R5-M2 Phase B–D 每个会话入口核一次 claim (前置检查 → 强制对齐 → 重解析 → 心跳 → `coord_push_verify`; 两处例外: AB 会话 / release 之后; 证据 N11) · R5-M3 TASK-030 显式跑 C.2.4.5, 放行须退出 0 且见逐子模块结论行, 新立第 17 项 · R5-M4 TASK-027 第 4 步 (b) 与 TASK-001 同口径、(c) 补 feature 一侧 (`1453c41f`), guard 钉主仓根并验退出码、三个调用点同步 (证据 N10 重写) · R5-M5 选 (a): 工作区只放主仓 `ab-workspace/` · `118d1d64` TASK-031 deliverables 补 `latest.md`。
- **同族扫描**: 160 个候选 (机器生成, 漏 0 多 0), 改 41 条 + 经所引规则改 5 条, 114 条不受影响; 三段嵌入代码 (`coord_ref_precheck` / `commit_attribution` / `crlf_guard`) 补退出码; `hard_constraints` 第 14 条立判据命令通则。**交互表 9 张**, 执笔借此自己查出五处修法新面 (本地协调 ref 落后致心跳推送失败 / release 之后的入口核验 / 13a–13b 中间态 / TASK-027 第 7 步原本没跑 guard / C.2.4.5 在子模块目录里 trivially passes)。判断清单补第 40–45 条 (v2.5), 旧条目只加联动注记; `revision_log` 29 → 37。

**主控核验逐项自跑** (未采信执笔自报): 越界 0 (恰三文件, 子模块零改动) · 真仓零写入 · `REGEN_IDENTICAL` · 31 TASK、顶层末键 `tasks` · 归档门与 v2.4 同态 · **三态证据独立复跑逐字节一致** (a2 不变; v2 含新增 / 重写的 N9 / N10 / N11) · 计划正文无悬空的「第 13 项」引用 (剩余三处都在 `revision_log`: 一处 v2.4 历史条目、两处为 v2.5 条目里加引号的检索词) · C.2.4.5 所据 `submodule_gate.sh` 事实 (环境变量缺省 block / 头注释与代码不符 / exit 65 / trivially passes) 与 R5-M5 所据事实 (本机确有被忽略的 `aria/skills/issue-triage-workspace/`) 属实 · 三段嵌入代码 diff 实读, `commit_attribution` 反事实: 坏 ref 下 v2.4 退出 0 判 ok (假绿)、v2.5 退出 2 · **同族清单换方法独立扫描** (按判据用语直扫 v2.4 判据文本) 35 个位置 35/35 在执笔清单内 · 新增行禁用字形 0、裸引用 0 · `revision_log` 前 29 条逐字不变。

**主控另记**:

1. 执笔范围外观察第 1 条值得 owner 注意: **生产心跳在落后的本地协调 ref 上会静默失败** —— state-scanner 入口心跳 fail-soft 且不自己 fetch, 非快进推送失败只记遥测; 可能是本轨心跳间隔多次超 24h 的原因之一, 与 10CG/aria-plugin#197 相关。插件侧没有「先对齐再心跳」, 本轮只在计划侧补; 是否开 issue 由 owner 定。
2. `aria/skills/phase-c-integrator/scripts/submodule_gate.sh` 头注释 (「读 .aria/config.json, fallback warn」) 与代码 (只读环境变量, 缺省 block) 不符 —— aria 侧文档缺陷, 计划以代码为准。

## 执笔实例提请裁定 (十条, v2.5)

1. **R5-M5 裁为 (a)** (工作区只放主仓 `ab-workspace/<本次结果目录名>/`)。
2. C.2.4.5 的等待点**新立第 17 项** (不复用第 8 / 9 项), 放行判据在退出码之外要求逐子模块结论行。
3. R5-M2 的**强制对齐步** (席位修法之外新增) 与会话入口核验的两处例外。
4. **13a 批、13b 不批的中间态只记台账**, 不另设防止机制。
5. `hard_constraints` 第 14 条通则的范围与措辞 (尤其「运行目录钉死」与「子模块先确认已检出」是否过宽)。
6. TASK-027 第 4 步 (c) 的主仓侧**比到工作树** (席位写的是比到 HEAD)。
7. `latest.md` History 节恢复与否放到 13b 的请求里由 owner 裁。
8. guard 补了**第三个调用点** TASK-027 第 7 步。
9. 前轮遗留的「TASK-001 第 1 条」差一 (`metadata.claim` 与 `coord_ref_precheck.own_claim_files` 两处, 应为第 2 条), 未改, 是否顺手勘正。
10. `.aria/state-checks.yaml` 的 `no-unresolved-version-placeholder` 命令本身会把 grep 的出错码反转成 0, 不在可写集, 计划侧已兜住; 是否由他轨改该检查。

**v2.4 九条请裁的现状**: 第 2 条 (latest.md 在第 13 项请求里点明) **已被本轮取代**; 第 1 条的选址部分被 R5-M5 取代 (「不入库」仍成立); 第 5 条前提已变 (History 节是被合并吞掉的, 恢复与否转入上面第 7 条); 第 9 条已处置; 其余 (3 / 4 / 6 / 7 / 8) 仍成立。

## R6 结果 (2026-09-22, 五席已齐; 本周期 max_rounds 7 的第 6 轮)

聚合: `.aria/audit-reports/post_planning-R6-2026-09-22T142418-000Z-pre-merge-completeness-gate-change-scope-aggregated.md` (119 行), 提交 `111d0ca`

- 五席: tech-lead 0C/0M/2m PASS · backend-architect 0C/0M/0m PASS · qa-engineer 0C/0M/0m PASS · code-reviewer 0C/0M/2m PASS · knowledge-manager 0C/1M/0m REVISE ⇒ raw **0C/1M/4m**, vote **4 PASS / 1 REVISE**
- **R5 九项** (五题 Major + 两条同处 minor + 同族扫描覆盖的两条): 四席全 closed; qa-engineer 对账表**漏列 R5-M5** (四席已覆盖、主控核验 v2.5 时已独立复核)
- **唯一被立为 major 的是已知项 A** (TASK-031「track-id 写错 ⇒ 停在第 16 项」不可达; cr 新证据: v2.5 在同任务里另写「本任务自身不跑 commit_attribution」, 明文互斥; 值非空检查对拼错的值同样得 5)。tl / qa / cr 三席判低于 major。**主控按后果定级为 minor** (后果是一份 handoff 的归属元数据出错、可事后更正, 不跳过任何闸; 与 R5-M4 取 major 用的是同一原则), **呈 owner 复议** ⇒ 聚合 **0C/0M/5m**, verdict PASS
- 五条 minor 中三条 (`6ad0a84b` 序号引用错位 / `2c2e8931` 强制对齐未推广到 release / `749f8d15` C.2.4.5 override 描述与调用时机不适配) 长在 v2.5 新建机制的接缝上 —— 修法带出新面的模式仍在, 但降到 minor; `29325b2c` 为同族扫描漏网项
- **收敛**: R6 键集 (空集或 `{591d8667}`) 都不等于 R5 五键 ⇒ `converged: false`。**R7 是本周期最后一轮**: 按主控定级, R7 若无 Major 且全票 PASS 即收敛; 若维持 major, R7 结构上不可能收敛
- Major 题数 10 → 5 → 4 → 4 → 5 → **0** (主控定级; km 定级为 1)
- **可审计性缺口 (cr 指出)**: 「同族扫描 160 个候选 / 交互表 9 张」只在执笔报告里, 执笔报告在主控 scratch、不在仓内; 建议把 v2.4 / v2.5 执笔报告与扫描脚本落进本轨工具目录 —— 待 owner 定
- **开轮前运维事实**: claim 心跳派发前已达 23.6h (距 SWEEP_TTL 0.4h), 按 v2.5 的会话入口顺序刷新 (协调 ref 两端 `61f8e76`); 第六批推送**半推** (github 成功、origin `websocket: bad handshake` 失败), 以普通快进补推修复, 未 force

## v2.6 返修 (2026-09-24, 执笔实例出稿 + 主控核验)

owner 2026-09-24 裁定 (原话): 已知项 A 定级「minor (主控定级)」; R7 路径选「修全部五条 minor 后开 R7」; 第七批「双推」。

- **闭合 (`320d523`), 五条全闭合**: `354faf33` TASK-031 起稿条删去不可达的「停在第 16 项」、改为明写本任务不跑该判据, track-id 取值改由写后自校验条的 `grep -cxF` 逐字断言守 · `6ad0a84b` 条目序号引用改锚点式 (机器清单 342 行, 实测 10 处指错全改, 含 v2.5 请裁第 9 条的「TASK-001 第 1 条」; 指对的两处保留, 主控已核这两处确实指对) · `2c2e8931` 强制对齐从会话入口心跳**推广到全部写协调 ref 的动作** (release / 重认领等四处), `coord_ref_precheck` 用法注释改为「退出 0 = 可强制对齐」, 证据新增 N12 · `749f8d15` C.2.4.5 的 override 按调用时机改写 (trailer 须在 PR head 或只认 PR 标签), 放行判据补收 `ALLOW:` 行, 第 17 项同步 · `29325b2c` custom checks 一律看输出首行、`##SKIP##` 视为没跑成, 形态清单补该形
- **三份机器清单** (owner 点名的同族扫描要求): 序号引用 342 行 / 协调 ref 写入路径 8 条加 45 个候选单元 / custom checks 16 条加 7 个机械探针。判断清单补第 46–50 条 (v2.6), 五条旧条目只加「v2.6 链接注」不改原意; `revision_log` 37 → 43

**主控核验逐项自跑**: 越界 0 (恰三文件, 子模块零改动) · 真仓零写入 · `REGEN_IDENTICAL` · 31 TASK、顶层末键 `tasks` · 归档门与 v2.5 同态 · **三态证据独立复跑逐字节一致** (含本轮新增的 N12) · 五项修法逐条实读确认落地 (起稿条的假承诺已改成正确的否定陈述; release 条含对齐步、理由与 N12 实测) · **换方法独立扫描**: 活文本里条目序号引用只剩两处, 逐条核实均指对; 调用 `phase1_gate` / `release_gate` 的四处全部带对齐或豁免说明 · 新增行禁用字形 0、裸引用 0 · `revision_log` 前 37 条逐字不变 · 结构化比对 30 条键路径全部落在范围内

**执笔实例自报薄弱点 (六条, 择要)**: track-id 断言只守文件内容、不守提交归属 (5.9 的提交仍无 `commit_attribution` 覆盖, km 的另一修法本轮按派单未采) · 对齐通则只枚举了两处例外 · C.2.4.5 的 PR 标签一路没有实跑 (只按代码断言) · custom checks 首行口径依赖现有 16 条的实际形态 · 序号引用清单里 38 行是人工判定 (判定词写在脚本字典里可复核) · N12 的 fixture 用 `update-ref` 拨回状态, 是取证手法非生产路径。

## 执笔实例提请裁定 (两条, v2.6)

1. **C.2.4.5 的 override 走哪一路**: (a) trailer 放 PR head = 要改写并强推已推送分支, 与 owner_gates 第 8 项冲突且三项核验都要重做; (b) PR 标签不动提交, 但依赖执行环境有 `forgejo` CLI 与 `ARIA_PR_NUMBER` (API 失败按无标签, 方向安全但会挡住)。计划已把两路与代价并列并写了「先请 owner 考虑标签」一句; 是否固化为默认路径请 owner 裁。
2. **v2.5 十条 / v2.4 九条请裁本轮未覆盖的项**按派单不动; 其中 v2.5 请裁第 9 条 (「TASK-001 第 1 条」指向) 已被本轮 `6ad0a84b` 实际改掉, 可视为消解。

## R7 结果 (2026-09-24, 五席已齐) — **post_planning 收敛**

聚合: `.aria/audit-reports/post_planning-R7-2026-09-24T121027-000Z-pre-merge-completeness-gate-change-scope-aggregated.md` (122 行), 提交 `765b73b`

- 五席: tech-lead 0C/0M/2m PASS · backend-architect 0C/0M/0m PASS · qa-engineer 0C/0M/0m PASS · code-reviewer 0C/0M/5m PASS · knowledge-manager 0C/0M/1m PASS ⇒ raw **0C/0M/8m**, vote **5 PASS / 0 REVISE**, 聚合归并 **0C/0M/6m**
- **收敛判定**: `conclusions_stable` = (R7 Major 键集 `{}` == R6 Major 键集 `{}`) = True; `unanimous_pass` = True ⇒ **`converged: true`**。判定口径与 R1–R6 完全一致, 未作任何调整; 派单里明写「这不构成应当投 PASS 的暗示, 不因是最后一轮而放宽或收紧」
- **R6 五条**: 四条五席一致 closed; `29325b2c` 四席 closed、tech-lead 判 partially (操作面闭合、完备性声明不实, 已另计为 finding)。**knowledge-manager 在 R6 立的唯一 Major (已知项 A) 由其本人本轮独立复核判 closed**, 并写明同意 owner 的 minor 定级
- 六条 minor 全为措辞 / 事实断言 / 信息缺口级: 同族清单声明不实 (实测至少四条探针有 `##SKIP##` 配退出 0, 含计划要跑的 `plugin-cache-currency`) · 「首行须为 OK」按字面是全等而实际首行带后缀 · CR 注解在本机 ugrep 包装下不成立 (主控实测 CRLF 态 shell grep 得 1 / GNU grep 得 0) · 授权口径 yaml 侧未同步「并已强制对齐」· 判断清单第 47 条的「一律」是全称句 · PR 标签 override 不按子模块分这一代价未写
- Major 题数轨迹: **10 → 5 → 4 → 4 → 5 → 0 → 0**
- **一条与既有经验不同的数据点**: 本仓 memory 记「收敛只发生在无 Major 轮 + 下一轮零返修」; 本轮实际路径是「无 Major 轮 (R6) → 只含 minor 的返修 (v2.6) → R7 仍零 Major 且全票 PASS」—— 纯 minor 返修没有破坏收敛, 尽管它又带出 8 条新 minor。判据应修正为「无 Major 轮 + 下一轮无 Major 且全票 PASS」
- **开轮前运维实证**: claim 心跳曾停 35.2h (超 SWEEP_TTL 11 小时) 仍为 active —— 未被扫只是没人跑 sweep, 不构成安全边界; 这正是 R5-M2 指出的风险在真实环境里的一次实证

## 待 owner 裁定

1. ~~R2 五条 / R3 四题 / R4 四题 / R5 五题 / R6 五条的返修~~ → ✅ **均已完成 (v2.2 / v2.3 / v2.4 / v2.5 / v2.6)**; v2.5 连带两条同处 minor (`1453c41f` / `118d1d64`) 与同族扫描覆盖的 `ac2e8dcb` / `11c3a29f`; v2.4 连带 R4 四条同处 minor (`f5b3afad` / `0f027861` / `cb1529a3` / `f0e78a1e`)。**余**: R4 另四条独立 minor (`9c0dcb27` / `d931db51` / `0dd2d3f2` / `ea958583`) + 前轮未动 minor (聚合写 15 条; 执笔实例指出与 v2.3 `revision_log` 所记口径有出入, 见其范围外观察第 6 条); R5 另三条 minor (`34b92188` / `27cee280` / `ae4753f5`) 未动
2. ~~是否开 R3 / R4 / R5~~ → ✅ **均已跑完** (owner 2026-09-22 裁「开 R5」); R5 未收敛且 `max_rounds` 已耗尽 ⇒ owner 2026-09-22 按 audit-engine 降级策略裁 **[2] 增加轮次** —— 本审计周期 `max_rounds` 5 → 7 (全局 config 不改); v2.5 → R6 (`111d0ca`, 未收敛) → owner 2026-09-24 裁「已知项 A 为 minor」+「修全部五条 minor 后开 R7」→ v2.6 已落仓 (`320d523`) → **R7 已跑完 (`765b73b`): `converged: true`** (五席全票 PASS, 0C/0M/6m) ⇒ **post_planning 检查点收敛, A.2/A.3 审计流程完成**
3. 执笔实例先前提的三条 (都要改 `gen_yaml.py`): 同秒心跳的生产含义写进 `cannot_catch` · 三态脚本对真仓路径的依赖如何处置 · `own_claim_files` 补注生产用法与 fixture 做法的关系 (本轮 (d) 条已采纳补反向指针)
4. ~~推送授权~~ → ✅ **已清 (2026-09-19, 三批)**: 第一批 v2.2 (`12c870d`) 与返修记录双推至 `bfc6949`; 第二批 R3 三个提交 (`49e41c0` 六份审计产物 / `a4df8ef` 轨级 handoff / `75264dd` latest.md 同步) 双推至 `75264dd`; 第三批 v2.3 (`a71c94e`) 与本轨 handoff (`5eb2a49`) 双推至 `5eb2a49`。三批均逐 remote `ls-remote` 独立核验两端 MATCH, 三个子模块 gitlink 两端可达。**第四批** (v2.4 `b686185` + 本 handoff 与 `latest.md` 同步) owner 2026-09-22 已授权, **已双推至 `322c6df`** (推前两端均为 `71c500e`、确认快进; 推后 origin 与 github 各自 `ls-remote` 与本地 HEAD 逐字 MATCH; 三个子模块 gitlink 未动); 回填提交 `cf05d8b` 同样双推并核验。**第五批** (R5 审计产物 `fffc5da` + 本 handoff 与 `latest.md` 同步 `20bc77a`) owner 2026-09-22 已授权, **已双推至 `20bc77a`** (推前两端均为 `cf05d8b`、确认快进; 推后 origin 与 github 各自 `ls-remote` 与本地 HEAD 逐字 MATCH)。**第六批** (R5 裁定写回 `ad35d83` + v2.5 `e7a1782` + 本 handoff 与 `latest.md` 同步 `37335d4`) owner 2026-09-22 已授权, **已双推至 `37335d4`** —— 首推半推 (github 成功、origin `websocket: bad handshake` 失败), 以普通快进补推修复、未 force; 推后两端 `ls-remote` 与本地一致 (origin 的 `ls-remote` 间歇失败, 重试后取到)。**第七批** (R6 审计产物 `111d0ca` + 本 handoff 同步 `5f39105`) owner 2026-09-22 已授权, **已双推至 `5f39105`** (两端首次尝试即 MATCH)。**第八批** (v2.6 `320d523` + 本 handoff 同步 `9a3ac24`) owner 2026-09-24 已授权, **已双推至 `9a3ac24`** (两端首次尝试即 MATCH)。**第九批** (R7 审计产物 `765b73b` + 本 handoff 与 `latest.md` 同步) **待授权推送**
5. **v2.4 执笔实例九条请裁** (现状见「执笔实例提请裁定 (十条, v2.5)」节末: 第 2 条已被取代, 第 9 条已处置)
6. **v2.5 执笔实例十条请裁** (第 9 条已被 v2.6 消解)
7. **v2.6 执笔实例两条请裁** (见上节; 其一为 C.2.4.5 的 override 路径)
8. **执笔报告与扫描脚本是否落仓** (R6 聚合流程记录第 5 条: 三份清单与交互表只在 scratch, 仓内无法复核)

## 入口门提醒 (不要把审计收敛读成可以开工)

owner_gates 第 1 项 = 「10CG/Aria#195 已完成 C.2 合并或 owner 明示改序」; `docs/handoff/latest.md` 现标该轨为 `yielded`、B.1 待起 ⇒ 即使 R3 通过, 下一步仍是 owner 门。
