先给结论: **现在不要开始起草 proposal.md**。

你这次的输出落在四态表里的「已检测到、有碰撞」态, 而且同时踩中了「有 N 条读不懂 schema 的 claim」这一行。按 SKILL.md 的措辞规范, 这轮的正确读法是:

> 已检测到 3 条可解析的 overlap claim (状态各不相同) + **1 条无法解析的 claim —— 存在性已确认、内容未知, 按存在处理**。

认领这道闸门问的不是「这份产物做得对不对」, 而是「**它该不该存在**」。它的全部价值来自「早于投入」——你现在恰好还没投入, 这是最便宜的时刻; 一旦先起草再核实, 它记录的就只是既成事实了。所以顺序是: 先核实这四条, 再决定起不起草。

另外两点前置说明:

- 本次跑的是 `--mode advisory`, 它**不会阻塞**你。但 advisory 的意思是「不机械挡你」, 不是「可以忽略」。
- 你自己的 claim 已经拿到了。**不要因为看到碰撞就再跑一次 acquire** —— 幂等 check 的判据是本 session 的 active claim 是否已存在, 已存在就跳过。

---

## 逐条处置

| # | track_id | 容器 | status | 性质 | 处置 |
|---|----------|------|--------|------|------|
| 1 | `oauth-login-aaaa1111` | cA | active | **真碰撞, 唯一可能让答案变成「不该存在」的一条** | 停下, 核实 cA 进度, 上呈 owner 裁决 |
| 2 | `social-login-bbbb2222` | cB | done | **比 active 更强的信号: 可能已经交付完了** | 去实读它的成果, 确认 #174 还剩什么范围 |
| 3 | `oauth2-spike-cccc3333` | cC | abandoned | 不阻塞, 但是有价值的输入 | 读它放弃的原因, 喂进 Constraints |
| 4 | (空) | (空) | unknown | 不可解析, **按存在处理** | 人工去 coordination ref 看原始 yaml |

### 【1】`oauth-login-aaaa1111` (cA, active, 今天 08:00Z) —— 最高优先级

这是四条里唯一「此刻有另一个容器正在做同一个 issue」的。`claimed_at` 是**今天**, 不能按陈旧 claim 处理。

要做的核实 (只读, 不要动 cA 的任何东西):

- cA 的 claim yaml 本体 (它的 track 目录 / 有没有写 scope)
- 远端有没有 `oauth-login` 相关分支 / 在制 proposal / PR
- 它的 slug 是 `oauth-login`, 你的是另一个 —— slug 不同**只是提示**范围可能可分割, 不是证据, 必须实看

然后这件事**不是我能替你裁的**: 「这份 Spec 该不该存在 / 谁来做」是产品级决策。完整选项集 (不预先替你收窄, 也不藏推荐):

- **A. 让路** —— 我方 `release_gate.py --raw-track-id <你这串> --status abandoned`, #174 归 cA。适合 cA 的范围已经完整覆盖你要做的事。
- **B. 分范围** —— 与 cA 划清边界, 你只做它不做的那部分。**注意**: 如果因此改了 Spec 目录名, 那就是换了 track-id, 必须 `release 旧 + acquire 新` 两步走, 不能只改目录。
- **C. 由 cA 让路** —— 如果核实下来 cA 那条 track 其实已经停摆。但**释放动作只能由 cA 那侧做**, 你不能替别的容器 release; 需要走人工协调, 不是你单方面判定「它没动就当它没了」。
- **D. 确实是两件事** —— 核实后发现只是 issue 挂错 / 两条 track 讲的不是同一件事, 那就照常起草, 并把这个判断写进 proposal 留痕。

我的建议是先做完核实再选, 因为 A/B/C/D 之间的差别完全取决于 cA 实际做到哪一步 —— 现在选任何一个都是猜。

### 【2】`social-login-bbbb2222` (cB, done, 昨天)

**别因为 status 是 done 就当它翻篇了。** done 在这里意味着「同一个 issue 昨天已经有人做完了」, 这比 active 更可能让你的起草整个变成重复劳动。

要做的核实:

- #174 本身现在什么状态 (被关了吗 / 被部分满足了吗 / 有没有 cB 的收尾评论)
- cB 那条 track 的产物落在哪 (spec 归档 / 合并了的 PR), 实际交付的范围是什么

判据是**去看产物**, 不是从 `status: done` 这个字段推断 —— done 只说明那条 track 走完了它自己的流程, 不说明 #174 的需求被覆盖了多少。核实完可能有三种结果: 你的 Spec 整个不必要 (走【1】的 A) / 只剩残余范围 (走 B) / cB 做的是另一个切面 (照常起草, 并在 proposal 里引用 cB 的成果做背景)。

### 【3】`oauth2-spike-cccc3333` (cC, abandoned, 前天)

**不阻塞**, 也不需要你做任何释放动作 (那不是你的 claim, 你也无权释放别人的)。

但它不是噪音: 一个被放弃的 spike, 放弃原因往往正是你要踩的坑。如果它留下了记录 (决策记录 / 分支 / issue 评论), 值得读一遍, 把结论喂进 proposal 的 `Constraints` 或背景段。读不到就算了, 不要卡在这里。

### 【4】空 track_id / status `unknown` —— 对应 `unknown_schema_claims: 1`

这条**不能渲染成「无碰撞」, 也不能当脏数据丢掉**。规范措辞是: 存在性已确认、内容未知, **按存在处理**。

具体含义: 它的 `linked_issue` 是 `null`, 意思不是「它跟 #174 无关」, 而是「**我们读不出它跟 #174 有没有关**」。零证据不是正证据, 所以保守地当成可能的第 4 个碰撞。

要做的:

- 人工去 coordination ref 里翻这条 claim 的原始 yaml, 看它到底是谁、什么 schema 版本、在做什么
- 顺带确认一件事: 数组里这条空记录**是不是**就是 `unknown_schema_claims` 计的那 1 条。很可能是 (计数对得上), 但**别假定** —— 如果不是同一条, 那你面对的其实是 4 条 overlap + 另外 1 条不可解析, 一共 5 条

如果实在读不出内容, 它就停留在「存在但内容未知」, 需要连同【1】一起上呈 owner: 在有一条身份不明的 claim 挂在同一个 issue 上时要不要开工。

---

## 汇总: 建议的动作顺序

1. **暂停起草** (不写 proposal.md, 不建目录, 不改 slug)。
2. 只读核实【2】cB 的产物 + #174 现状 —— 这条最可能直接把问题解掉 (如果已交付, 后面都不用谈了)。
3. 只读核实【1】cA 的进度与范围。
4. 翻【4】的原始 yaml; 顺手扫一眼【3】的放弃理由。
5. 把 1–4 的核实结果连同上面 A/B/C/D 四个选项一起交给 owner 裁决 —— 这一步我不会替你决定。
6. 按裁决结果执行**退出义务**, 两条缺一就会留下永不释放的僵尸 claim:
   - 改名 (含缩范围换 slug) ⇒ `release` 旧 track-id + `acquire` 新 track-id, 两步走。
   - 放弃方向 ⇒ `release_gate.py --raw-track-id <与 acquire 时逐字相同的那一串> --status abandoned`。

最后提醒一句口径: 对外汇报这轮结果时, 请照四态表的措辞写, 不要把「1 条无法解析」压缩成「无碰撞」或「共 3 条碰撞」—— 那正好把四态压成一态, 是这个闸门最容易失效的方式。
