先说结论: **四条全部命中, 我不能自行放行, 起草在这里暂停。** 按 spec-drafter 的 A.1 入口认领约定, `linked_issue_overlap` 非空时必须**按对方 claim 的 `status` 分档上呈请裁**(经 `AskUserQuestion`), 由你裁定后我才继续写 `proposal.md`。认领的作用是「早于投入地发现撞车」, 不是「记录既成事实」—— 所以现在停, 比起草完再发现便宜得多。

先纠正你原话里的一处: 第 4 条的 `linked_issue` 是 `null`, **不是** `10CG/Aria#174`。它是那条 `unknown_schema_claims = 1` 对应的读不懂 schema 的 claim —— 存在性已确认、内容未知, 按存在处理; 但不能说它「和你同一个 issue」, 也不能因为它 `linked_issue` 是 null 就当作无关放掉。

---

## 一、告警(五要素逐字回显)

`claimed_at` 不截断成日期, `linked_issue` 逐字回显原串(不缩成 `#174`)。我方 `linked_issue` 原始串 = `10CG/Aria#174`。

| # | 对方 `track_id` | owner-container | `claimed_at` | 对方 `linked_issue` 原串 | 我方 `linked_issue` 原串 | `status` |
|---|---|---|---|---|---|---|
| 1 | `oauth-login-aaaa1111` | `cA` | `2026-09-05T08:00:00Z` | `10CG/Aria#174` | `10CG/Aria#174` | `active` |
| 2 | `social-login-bbbb2222` | `cB` | `2026-09-04T10:00:00Z` | `10CG/Aria#174` | `10CG/Aria#174` | `done` |
| 3 | `oauth2-spike-cccc3333` | `cC` | `2026-09-03T09:00:00Z` | `10CG/Aria#174` | `10CG/Aria#174` | `abandoned` |
| 4 | `""`(空) | `""`(空) | `""`(空) | `null` | `10CG/Aria#174` | `unknown` |

为什么坚持逐字回显: 匹配时 **org 段不参与匹配**, 所以「原串对照」是人工判别**误配**的唯一手段。这里 1–3 条的原串与我方逐字相同(含 `10CG/` 段), **可以排除误配**, 确实是同一个 issue 上的三条 claim。第 4 条三个字段都是空串、issue 为 null, 五要素里有三要素不可得 —— 这本身就是「schema 读不懂」的表现, 我不会拿空串当值去比对。

---

## 二、逐条处置

**第 1 条 · `oauth-login-aaaa1111` (`cA`, `active`)**
有人**正在做**。今天 08:00 认领, 是四条里最新的一条, 也是唯一活跃的一条 —— 它是本次的主要碰撞方。处置: 按 `active` 档请裁, 三个方向 —— 合并方向(并到 cA 那条轨上去) / 换方向(我改做别的) / 确认确实是两件事(同 issue 但不同切面, 各自继续)。在你裁定前我不起草。

**第 2 条 · `social-login-bbbb2222` (`cB`, `done`)**
同一件事**可能已经做完**。处置: **按 `active` 同档请裁**, 不因为它是终态就自动放行。要点: `done` 这个终态**也可能是 GC 产物, 而不是真的做完** —— 需要你去核实 cB 的产物是否真的落地(有没有 ship / 有没有 PR), 而不是凭状态字面结论。若确已做完, 我这条大概率是重复投入。

**第 3 条 · `oauth2-spike-cccc3333` (`cC`, `abandoned`)**
同一件事**可能已被放弃**。处置同上: **按 `active` 同档请裁**, 并同样注明 `abandoned` 也可能是 GC 产物而非真的主动放弃。这条对你的价值可能是**负向情报**: 如果 cC 是真放弃, 值得先看它为什么放弃(可能踩到了我这次会同样踩的坑), 再决定我要不要接着做。

**第 4 条 · 空 track_id (`unknown`, `unknown_schema_claims = 1`)**
读不懂它的 schema ⇒ **视同 `active` 处理**(存在性已确认、内容未知)。措辞上准确的说法是「已检测到 **1** 条无法解析的 claim —— 存在性已确认、内容未知, 按存在处理」, **不能**写成「无碰撞」。处置: 按 `active` 同档请裁, 但选项里要多一档「先去人工核实这条 claim 到底是什么」—— 因为连 owner-container 都取不到, 无法直接联系对方。

**四条共同的一条禁令**: 我**不会提议去 release / 清理对方任何一条 claim**(包括看起来是 GC 残留的第 2、3、4 条)—— 那是对方容器的东西, 只有它自己能释放。

---

## 三、请裁(这是 `AskUserQuestion` 该问的四组, 我不替你选)

**【问 1】cA 的 `active` claim(`oauth-login-aaaa1111`, `2026-09-05T08:00:00Z`, `10CG/Aria#174`)怎么处理?**
- 【A】合并方向 —— 我不另起 Spec, 并入 cA 那条轨, 由它统一交付
- 【B】换方向 —— 我放弃 `10CG/Aria#174` 这条线, 换别的任务
- 【C】确实是两件事 —— 同 issue 不同切面, 双方各自继续(请说明切面边界怎么分)

**【问 2】cB 的 `done` claim(`social-login-bbbb2222`, `2026-09-04T10:00:00Z`, `10CG/Aria#174`)怎么处理?**
- 【A】它确已做完 —— 我这条属重复, 停止起草
- 【B】`done` 是 GC 产物 / 未真正交付 —— 视同活跃碰撞, 与 cA 一并按问 1 处理
- 【C】确实是两件事 —— 与它无关, 继续

**【问 3】cC 的 `abandoned` claim(`oauth2-spike-cccc3333`, `2026-09-03T09:00:00Z`, `10CG/Aria#174`)怎么处理?**
- 【A】它确已放弃 —— 我接手这个方向(建议先读它放弃的原因)
- 【B】`abandoned` 是 GC 产物 / 状态不可信 —— 视同活跃碰撞, 按问 1 处理
- 【C】确实是两件事 —— 与它无关, 继续

**【问 4】那条无法解析的 claim(`unknown`, 三要素为空, `linked_issue` = `null`)怎么处理?**
- 【A】按存在处理、视同 `active` —— 先人工核实它是什么, 核实前我不起草
- 【B】你已知它是什么且与本 Spec 无关 —— 记录后继续
- 【C】按存在处理但接受风险 —— 你明确授权我在未核实的情况下继续

---

## 四、裁定回来之后我会做什么

- 裁定为 **合并方向 / 放弃本方向** ⇒ 我要履行**退出义务**: `release_gate.py --raw-track-id <与认领时逐字相同的那串> --status abandoned`。不释放 = 留下一条永不释放的僵尸 claim。
- 裁定为 **确实是两件事, 但需要换 Spec 目录名以区分切面** ⇒ 改名就是换了 track-id, 必须**两步走**: release 旧串 + acquire 新串, 不能只改目录。
- 裁定为 **继续原方向** ⇒ 我才进入 A.1 起草(`openspec/changes/<slug>/proposal.md`, 落在本项目仓内), 并在头部 blockquote 写 `> **Linked Issue**: \`10CG/Aria#174\``, 同时把这次 overlap 的四条五要素和你的裁定记进 proposal, 供后续 post_spec 审计追溯。

一句话: 现在球在你那边, 四条我一条都没有自行放行。
