# 复跑说明 (诊断用, 不改任何制品)

首轮结果 with 1/3 vs old 2/3, 差异点 = with 臂整段未输出 `output-formats.md` 规定的
`📝 README 同步状态` 区块 (实测: with 0 次 / old 1 次; with 318 行 vs old 258 行, 多出篇幅
落在本轮新增的 Layer L heartbeat 面上)。

**每臂仅 1 sample 不足以区分「回归」与「采样波动」** —— 同一天在 eval 2 上已经栽过一次
(看似回归, 横向比对后是噪声)。故对同一 prompt 复跑两轮 (`-rep2` / `-rep3`), 两臂制品与首轮**完全相同**,
唯一变量是随机性。

判据 (跑前写死):
- with 臂 3/3 轮都丢该区块 ⇒ **确认回归**, 回改 state-scanner/SKILL.md 后重跑全套
- 时有时无 ⇒ 采样波动, **不改制品**, 但把「输出区块遗漏无稳定断言可测」记进套件缺口
