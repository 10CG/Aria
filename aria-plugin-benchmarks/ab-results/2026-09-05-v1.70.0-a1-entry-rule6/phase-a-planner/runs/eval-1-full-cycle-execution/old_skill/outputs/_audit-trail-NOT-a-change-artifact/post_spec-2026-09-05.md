# post_spec 审计留痕 — oauth2-social-login

> ⛔ **本文件不是 change 产物**，请勿把它当作 `oauth2-social-login/` 的一部分扫描或审阅。
>
> 沿革 (R3 → R5 两次纠正): audit-engine 的 SOT 报告位置是 `.aria/audit-reports/post_spec-{timestamp}.md`，按 agent × round 拆分。R3 指出原位置 (变更目录下 `audit-post-spec.md`) 无 SOT 依据；R3 的处置只是把它挪进变更目录下的 `audit-reports/` 子目录并加免责声明 —— R5 判定**这不够**: 自认偏离不等于合规，一个放在变更目录里、名字与 SOT schema 相仿的文件仍会被当作该 change 的产物去扫描。Level 3 产物定义 (`standards/openspec/templates/README.md`) 只有 proposal.md + tasks.md (+ A.2 生成的 detailed-tasks.yaml)，没有第四类产物或子目录。
>
> 现处置: 移出变更目录树，放在 `outputs/_audit-trail-NOT-a-change-artifact/`；proposal.md 的「审计留痕」节只留一两句 verdict 叙述，不再指向本文件为产物。真实运行时本记录应落 `.aria/audit-reports/` 并按 SOT 粒度拆成 per-agent-per-round 多文件 —— 单文件跨轮汇总这一点仍偏离 SOT，如实留在这里。

> **性质**: append-only 审计叙事。与收敛型交付面 (proposal.md / tasks.md) **刻意分居**，两者不同步、不互为镜像。
> **checkpoint**: `post_spec` | **mode**: `convergence` | **max_rounds**: 5 (来自 `.aria/config.json`)
> **team**: `aria:tech-lead` / `aria:backend-architect` / `aria:qa-engineer` / `aria:code-reviewer` / `aria:knowledge-manager` (5 席，来自 config `audit.teams.post_spec`)

不同步声明:

1. 本文件只增不改，历史轮次结论即使后来被推翻也保留原文。
2. 本文件里的行号引用是**当轮快照**，proposal 修订后即失效，不回填。
3. proposal.md 才是设计的 SOT；本文件不承载任何设计决策。
4. 本文件不参与归档完成度判定。

---

## Round 1 (5 席全出, 对象: proposal.md 初稿)

**结论集合**: 5 席共 37 条 finding — Critical 8 / Major 21 / Minor 8。verdict = **FAIL** (阻塞进入 A.2)。

按缺陷簇归并 (跨席位重复计一次):

| # | 簇 | 报出席位 | 严重度 |
|---|---|---|---|
| C1 | `identities` 唯一约束语义歧义 (三元组 vs 二元组), 直接放大账户接管 | tech-lead / backend-architect / code-reviewer (3 席独立报出) | Critical |
| C2 | 关联决策矩阵分支缺口 (未验证邮箱且无既有用户 = 不可执行指引; provider 无可用邮箱; email 被他人占用; identity 命中但邮箱漂移) | tech-lead / backend-architect / code-reviewer | Critical |
| C3 | SC-2 的「4xx」判定域含 404, 功能未实现时天然通过 = 恒绿 | qa-engineer / code-reviewer | Critical |
| C4 | SC-3 / SC-4 是纯负向断言, 基线天然为真 = 恒绿 | qa-engineer | Critical |
| C5 | unlink 可解绑最后一个登录方式 ⇒ 账户永久锁死, 且与「不做口令回填」非目标共存无解 | backend-architect / tech-lead / code-reviewer | Critical |
| C6 | Level 3 产物悬空: proposal 以「见某文件」引用 A.2 才生成的 detailed-tasks.yaml | tech-lead / code-reviewer | Critical |
| M1 | 非目标用「OIDC 之外」当判据, 反而排除掉 in-scope 的 GitHub; Apple 举例与判据反向 | tech-lead / code-reviewer | Major |
| M2 | 短时存储未定介质 (多实例下进程内存直接错) 与非原子消费竞态 | backend-architect / tech-lead | Major |
| M3 | `OAuthProvider` 只有名字没有契约, 归一化形状缺失 ⇒ 决策矩阵的输入也跟着欠定 | tech-lead | Major |
| M4 | `redirect_uri` 与登录后 return-URL 无约定 = 开放重定向面 | tech-lead | Major |
| M5 | FK on-delete 语义未定义 (孤儿行 / 阻塞删除) | backend-architect | Major |
| M6 | nonce 在交付物里承诺、在存储契约里失踪、在验收里无覆盖; PKCE 降级为 `plain` 与 verifier 不匹配均无负控 | code-reviewer | Major |
| M7 | 前端交付物零 SC 覆盖 | code-reviewer | Major |
| M8 | 「provider 不可用」风险无 SC 追踪 | qa-engineer / knowledge-manager | Major |
| M9 | TTL 时序与并发 callback 原子性未被任何 SC 覆盖 | qa-engineer / tech-lead | Major |
| M10 | 文档同步义务 (Rule #3) 无 SC, 且 Key Deliverables 无文档产出 ⇒ 与 tasks.md Phase 6 不同构 | knowledge-manager | Major |
| M11 | 可追溯性缺内部锚点 (既有 users 表 / 会话签发器 / 既有登录用例集均无路径) | knowledge-manager | Major |
| M12 | GitHub 无可信邮箱路径未定义分支 | backend-architect / tech-lead | Major |
| m1 | 限流未提 (callback 是外部可达滥用面) | backend-architect | Minor |
| m2 | unlink 未声明所有权校验 (IDOR) | backend-architect | Minor |
| m3 | SC-5 覆盖率可被弱断言灌水, 四条决策路径未点名 | qa-engineer | Minor |
| m4 | SC-6 基线天然为绿, 角色未标注 | qa-engineer | Minor |
| m5 | GitHub 多邮箱边界 (primary 未验证但另有 verified) 未表态 | qa-engineer / backend-architect | Minor |
| m6 | change_id `add-oauth2-social-login` 含动词前缀且切分为 4 段, 超「最多 3 topic」 | knowledge-manager | Minor |

**处置**: 全部 24 簇吸收，proposal.md 改写为 R1-fixed；tasks.md 追加 1.4 / 3.6 / 5.6-5.9 六项 (编号不可变，未重排既有项)；SC 由 7 条扩到 16 条。

**m6 的独立核验**: 未采信席位转述，回 `standards/openspec/templates/README.md` §Change ID 命名约定实读 —— §2 明确「最多 3 个 topic」，全部正例 (`user-otp-mvp` / `tam-trigger-polish` / `schema-migration-hardening`) 无动词前缀。改名 `oauth2-social-login` (3 topic, 19 字符, 无 descriptor tail, 首版无需 version 前缀) 成立。

---

## Round 2 (5 席全出, 对象: R1-fixed 的 proposal.md + tasks.md)

R2 的镜头刻意偏向「R1 修法本身引入的新缺陷」——结构性重写是新交付面，不是清理；上一轮审过的那版不能替这版背书。

**结论集合**: 5 席共 34 条 finding — Critical 6 / Major 25 / Minor 3，归并为 15 簇。verdict = **FAIL** (继续第 3 轮)。

R2 的关键观察: **绝大多数 finding 落在 R1 新写的文本上** (现状锚点段 / Provider 抽象契约 / 决策矩阵表 / 新 SC-8~SC-16)，而非 R1 未动的部分。这符合「结构性重写是新交付面，不是清理」—— 上一轮审过的那版不能替这版背书。

| # | 簇 | 席位 | 严重度 | 处置 |
|---|---|---|---|---|
| R2-1 | 决策矩阵缺 `flow` 维度: 已登录用户在设置页关联时复用 login 规则 ⇒ 会话被静默切到他人账户 | tech-lead / backend-architect (2 席独立报出) | Critical | 矩阵拆 login / link 两表，link 流永不切换会话主体 |
| R2-2 | 现状锚点自锁: 门槛说「Phase B 不得开工」，而解除它的 1.4 本身是 Phase B 任务 | tech-lead / code-reviewer | Critical | 门槛收窄为「除 1.4 外」，1.4 标为零号任务 |
| R2-3 | SC-10 的 404 与「路由不存在」不可分 = SC-2 已修的 404 冒充形状在新位置原样复现，且无 unlink 成功正控 | qa-engineer | Critical | 加成功正控 + 越权 error code |
| R2-4 | D4a 断言「该用户确实有账户可登」不成立: D3 建的纯 OAuth 用户无密码，「先用邮箱密码登录」对他们同样不可执行 | backend-architect | Critical | D4a 拆 `-pw` / `-nopw` 两分支 |
| R2-5 | SC-15 / SC-14(a) 是纯负向断言，基线零命中恒绿 (违反本章自己写的 born-red 前言) | tech-lead / qa-engineer | Critical | 各补正向锚 + 坏实现样本 |
| R2-6 | `ProviderTokens` 被消费却从未定义；Google `id_token` 缺失时存在悄悄降级为「不验签放行」的路径 | tech-lead | Major | 补形状定义 + `id_token_missing` fail-closed |
| R2-7 | `user_session_id` 在首登路径无载体 ⇒ 跨会话绑定在最主要路径上失效，SC-2 的跨会话用例不可构造 | tech-lead / backend-architect | Major | 改 `binding_id` 恒非空 + pre-auth 绑定 cookie |
| R2-8 | `has_password` 谓词无来源；`users` 密码列可空性从未核实 ⇒ D3 可能在 Phase B 直接撞墙 | tech-lead | Major | 锚点表加 A-4 + DEC-3 分支后果 |
| R2-9 | SC-7/8/9/10/15/16 六条无任务承载 (R1 只给 SC-11~14 补了任务，补齐是选择性的) | tech-lead / code-reviewer | Major | 追加 5.10-5.15 + 6.3 |
| R2-10 | 派生数字未重算: proposal 硬编码「22 项」vs tasks 28 项；Summary 分项之和 ≠ Total | tech-lead / code-reviewer / knowledge-manager (3 席) | Major | proposal 改为「以 tasks.md Summary 为准」不再持副本 + 逐格核对留痕 |
| R2-11 | 依赖图把 Phase 5 钉在 Phase 3 之后，与 born-red 义务结构性冲突 | code-reviewer | Major | 图下显式声明负控不受本图时序约束 + 列出成对关系 |
| R2-12 | D2 与 D2' 在四个声明维度上取值相同，实际路由靠塞在第四列自然语言里的第五布尔量 ⇒「全枚举」断言不成立 | backend-architect | Major | 加第五列，改称五元组 |
| R2-13 | SC-16(b) 两侧皆空时空集比空集假绿；从中文散文抽端点名的方法未定义 | qa-engineer / knowledge-manager | Major | 要求围栏代码块 route 列表 + 抽取数 == 3 前置断言 |
| R2-14 | SC-5 是覆盖率维度，对 D4a/D4b **文案对调**这类方向性错误天然免疫 | qa-engineer | Major | 加 SC-5b 直接断言文案差异 |
| R2-15 | 若干新 SC 的可证伪性细节: SC-9(b) 靠调度碰运气、SC-12/13 缺配对正控、SC-15 字符串 grep 被同义写法绕过、DEC-2 应用层竞态无验收、GitHub 非 primary 邮箱回退禁令无 SC | qa-engineer / backend-architect | Major/Minor | 同步屏障强制竞态 / 各补正控 / 检查面扩到 AST 级 / 新增 SC-22 / 新增 SC-23 |
| R2-16 | 锚点空洞只挡 Phase B 而不挡 Status→Approved，与 Rule #2 方向相反 | knowledge-manager | Major | Status 字段写死「锚点未全填实前不得进入 Approved」 |

**处置**: 全部吸收。proposal 改写为 R2-fixed (SC 由 16 条扩到 23 条，矩阵拆 login/link 双表并加第五列)；tasks.md 追加 3.7 / 5.10-5.15 / 6.3 (共 8 项，28→36)，新增「SC ↔ 任务映射」表把「有 SC 无任务」变成可机械核对的一张表。

**R2 遗留至 R3 的已知项** (tech-lead 因篇幅上限未展开，此处显式转交): `{provider}` 未知值路由行为 (已在 R2-fixed 补 SC-21)、D0 不可达与「分支覆盖 ≥85% 无豁免」的口径冲突 (已补覆盖率口径段)、`next` 白名单的 author-to-match-checker 形状 (已在 SC-13 要求含未列举形态)。

---

## Round 3 (5 席全出, 对象: R2-fixed 的 proposal.md + tasks.md)

**收敛判据说明** (写在结论之前，避免事后解释): 按 audit-engine convergence 语义，收敛 = 相邻两轮结论集合一致。附加的停轮判据取「本轮 finding 中有多少落在**上一轮 fix 新写的文本**上」—— 该比例过半即说明每轮 fix 在引入约等量同形状缺陷，边际产出转负，此时应**换新鲜视角或上呈 owner**，而不是继续加轮。R1→R2 该比例已经过半 (R2 的 15 簇里 11 簇针对 R1 新文本)。R3 若重复此形状，按上述判据停轮并把残余项转为 Phase B 的显式跟踪项，而非继续研磨。

**席位实况 (不粉饰)**: R3 只跑成 **3/5 席** (tech-lead / code-reviewer / knowledge-manager)。backend-architect 与 qa-engineer 两席被 harness 的并发 subagent 上限挡下 (非本人裁量跳过, 非 Rule #10 意义上的自行豁免), 已转入 R4 在 R3-fixed 文本上补跑。**在这两席回来之前, R3 不构成一个完整轮次, 不得据此宣布收敛。**

**结论集合 (3 席)**: 15 条 finding — Critical 3 / Major 8 / Minor 4，归并 11 簇。verdict = **FAIL** (继续)。

| # | 簇 | 席位 | 严重度 | 处置 |
|---|---|---|---|---|
| R3-1 | Approved 门槛与 1.4 循环死锁: R2 把闸门从 Phase B 上提到 Approved, 但解除它的 1.4 仍是 Phase B 任务 —— **自锁换了个位置重犯** | tech-lead / (code-reviewer R2 同形) | Critical | 门槛拆三条, 明写 1.4 在 Draft 态执行; 同批删掉旧措辞的段标题 |
| R3-2 | 矩阵第 4 列是布尔「有/无」, 把「同一 email 匹配到 ≥2 个 user」静默吞进「有」⇒ 实现者取第一条 = 任意选中受害者; D0 抓不到 (它落在表内) | tech-lead | Critical | 第 4 列改基数 0/1/≥2, 新增 D6 fail-closed + SC-24; 新增锚点 A-5 |
| R3-3 | 审计留痕文件 `audit-post-spec.md` 的名称与位置**无 SOT 依据** (SOT = `.aria/audit-reports/post_spec-{timestamp}.md`, 且按 agent×round 拆分), 会让依赖该路径的下游机制看不见这份留痕 | knowledge-manager | Critical | 迁到 `audit-reports/post_spec-2026-09-05.md` 并在文件头与 proposal 双处显式声明「仍偏离 SOT (沙箱限制 + 单文件汇总)」, 不当作已解决 |
| R3-4 | Status 字段被塞入条件语句; 本项目已两次因「Status 当 changelog 用」出机读分类 bug (#101 / #50) | knowledge-manager | Major | Status 还原为纯 `Draft`, 门槛移入「现状锚点」节, 并在头部写明为什么 |
| R3-5 | link 流承重变量 `U` 有两个相反口径 (会话 vs 记录里的 `link_user_id`); 「start 后登出换人再 callback」binding cookie 判别不了, SC-17 四条对此全盲 | tech-lead | Major | 钉死 U = `link_user_id` + callback 断言其等于当前会话; SC-17 加 (b) 主体更替负控 |
| R3-6 | 矩阵缺「目标 user 账户状态」维 ⇒ 社交登录成为绕过既有账户状态校验的旁路; D1 的 email 漂移「记审计事件」无消费方 = 静默 | tech-lead | Major | 加账户状态列 + D1-inactive / D2-inactive + SC-25 + 锚点 A-6; 漂移事件改为「投递到风控/告警队列」并由 SC-5 断言被投递 |
| R3-7 | SC-20 → 5.13 是**名义映射**: 5.13 正文只写 link 流与 DEC-2 竞态, 与 SC-20 两子项词面交集为 0; 全部 Phase 5 任务里 `D0`/`resolver`/`NormalizedIdentity` 命中数 = 0 | tech-lead / code-reviewer (2 席独立算出) | Major | SC-20 拆 20a/20b; 新增 tasks 5.16 承接 20a/24/25; 20b 落 2.2/2.3 的 verification |
| R3-8 | SC-15 自留后门: 「AST 级**或**穷举等价形态清单」二选一, 且 (c) 的坏样本恰好取自 (b) 逐字列举的清单 ⇒ 走清单支时是同一人按同一张表写检查器与被检代码 | tech-lead | Major | 删掉清单支, 判据单一化为 AST 级; 坏样本要求取自未逐字列举的写法 |
| R3-9 | SC-13 的条件**不可满足**: 要求「≥2 条未逐字列举的形态」, 而给出的两个样例恰好已被同段逐字覆盖 ⇒ 合格样例集 = 空集 | code-reviewer | Major | 文档刻意不给样例, 改为实现者自选并在 PR 列出 |
| R3-10 | 依赖图豁免列表漏 5.4 (Phase 5 共 15 项, 图中 5 + 豁免 9 = 14, 差集 {5.4}) ⇒ 5.4 按图落回 Phase 3 之后, 与其 born-red 义务相反 | code-reviewer | Major | 补入豁免列表 + 给成对关系 |
| R3-11 | 三处小账: SC-12 标称 4 条实列 5 条; Note 2 说「逐项估时见 detailed-tasks.yaml」而该文件当时不存在 ⇒ 分项估时只自洽不可证伪; 留痕表有一行未编号 | code-reviewer / knowledge-manager | Minor | SC-12 改 5 条并逐条编号; Note 2 补「A.2 前为直估, 生成后回填」; 该行补记为 R2-16 |

**R3 的机械核对 (code-reviewer 逐格实算, 无 finding 的部分也留痕)**: Summary 分项 4/4/7/3/15/3 与实数全等; Total 36 = 实数 36; 估时 min/max 与分项和相等且等于 frontmatter; 编号 1.1-6.3 共 36 号无缺无重; SC 清单 24 条无缺号; 正文 SC 引用差集 = 空; 正文 tasks 编号引用差集 = 空; 映射表双向差集 = 空。**该核对仅对当时那对字节成立** (审计期间文件被并发改写, 席位已自行标注), R3 fix 之后的数字由本席重算并写在 tasks.md 的「派生数字核对」行。

**处置**: 11 簇全部吸收。proposal → R3-fixed (SC 26 条; 矩阵 login 11 分支 + link 4 分支 + D0/L0; 锚点 4→6 项); tasks.md 追加 5.16 (36→37 项, 估时重算 106-169h); detailed-tasks.yaml 生成 (A.2/A.3) 并含 TASK-037。

---

## Round 4 (补跑 R3 被并发上限挡下的 2 席: backend-architect / qa-engineer, 对象: R3-fixed)

**停轮判据 (延续 R2 段的预登记, 不事后改口)**: 若 R4 两席的 finding 仍以「上一轮 fix 新写的文本」为主且严重度不再下降, 即判定边际产出转负, **停轮**并把残余项转为 Phase B 的显式跟踪项 + 上呈 owner 复议, 而不是继续加轮研磨。已观测趋势: Critical 8 → 6 → 3, finding 数 37 → 34 → 15 (3 席)。

**结论集合 (2 席)**: 11 条 finding — Critical 2 / Major 7 / Minor 2，全部为前三轮未报过的新点。verdict = **FAIL** (继续 R5)。

| # | 簇 | 席位 | 严重度 | 处置 |
|---|---|---|---|---|
| R4-1 | `provider_user_id` 「稳定不可变」是无条件信任假设, 零防线。唯一相关信号 (D1 email 漂移事件) 是**事后检测**且**只在 email 也变时触发** —— 企业 Workspace 账号转让恰好 ID+email 一起转移, 接管无声且不可检测 | backend-architect | Critical | 写进 Risk 表, 明确列为**需 owner 签字接受的残留风险**; 不假装已缓解 |
| R4-2 | SC-13 的「由实现者自选未列举形态并在 PR 自报」是 author-to-match-checker 的**镜像形态** —— 检查内容由被检者事后挑选, 第三方无法复核 | qa-engineer | Critical | 判据改落外部基线: OWASP WSTG-CLNT-04 条目编号, 或由不参与 3.6 的一方开工前封存到受保护路径 |
| R4-3 | binding cookie 用固定名 ⇒ 同浏览器多标签并发两个 OAuth 流互相覆盖, 误杀合法登录 | backend-architect | Major | cookie 名按 state 命名空间化 `oauth_binding_{state}` + SC-26(a) |
| R4-4 | 浏览器禁用 cookie 时恒定 `403 invalid_state`, 且不区分「缺失」与「不符」⇒ 该用户群体永久不可用且无自解释 | backend-architect | Major | 拆 `binding_cookie_missing` / `binding_mismatch` 两码 + 前端提示 + SC-26(b) |
| R4-5 | `SameSite=Lax` 在 provider 302 顶层导航上被携带 —— 文中当作理所当然, 未要求实测 | backend-architect | Major | 明写为「整条链成立的前提」并要求真实浏览器 e2e 实测 (SC-26c) |
| R4-6 | link 表缺账户状态维: 若既有签发器只在登录时查一次状态而非每请求中间件校验, 被停用但持旧会话的用户仍可经 L4 关联新身份 | backend-architect | Major | 加 L-inactive 行 + A-6 需明确是否 continuous middleware, 二选一留痕 |
| R4-7 | D1-inactive / D2-inactive 的错误码整体外包给尚未解析的 A-6 ⇒ 是**落不到任何已枚举码上**的两条分支 | backend-architect | Major | 登记占位码 `account_inactive` 并注明最终取值取决于 A-6 |
| R4-8 | SC-20a 在 5.13 与 5.16 被各写一遍且互不知情, 映射表因此不是唯一权威 | qa-engineer | Major | 5.16 为唯一执笔任务, 5.13 改为复用不重写 |
| R4-9 | SC-11 有两条约束, 但执笔任务只给了第一条的基线与坏实现样本 ⇒ 名义全覆盖实际写一半 | qa-engineer | Major | 两条各自负控 + 各自坏实现样本; 只写一条视为未完成 |
| R4-10 | SC-15(c) 要「≥2 个未列举写法」, 而给的样本之一「字面比较」正是被检基线形态本身 ⇒ 实际只交付 1 个 | qa-engineer | Major | 明写字面比较不占名额, 两个名额都必须是未点名的分派写法 |
| R4-11 | 429 缺命名 error_code, 与其余分支的 `{status, error_code}` 模式不一致; SC-5 与 SC-20a 缺「坏实现样本」句式 (违反本文档自己的 born-red 前言) | backend-architect / qa-engineer | Minor | `429 rate_limited` + 两条各补坏实现样本 |

**处置**: 11 簇全部吸收。SC 26 → 28 条 (新增 SC-26 binding cookie 契约三子项)；tasks 37 → 38 项 (新增 5.17)；估时重算 109-173h；detailed-tasks.yaml 同步至 TASK-038。

**趋势 (供停轮判断, 数字实计非印象)**: finding 数 37 → 34 → 15(3席) → 11(2席)；Critical 8 → 6 → 3 → 2；按席均算 7.4 → 6.8 → 5.0 → 5.5。**严重度在降, 席均产出基本持平** —— 每轮 fix 仍在自己新写的文本上引入约等量同形状缺陷 (R4 的 11 条里 9 条打在 R2/R3 新增文本上)，符合「边际产出转负」的形状。

**为什么仍然跑 R5 而不是就此停轮**: 停轮判据是本席自己预登记的**启发式**，而 `.aria/config.json` 的 `audit.max_rounds = 5` 是 owner 的**配置决定**。convergence 模式未收敛时的配置行为就是继续到上限轮 —— 用自己的边际收益判断把它截短，属于 CLAUDE.md 规则 #10 明令禁止的「AI 自行豁免已启用闸门」，四类豁免白名单 (config 显式 off / adaptive_rules / 成文 lane / 结构性前提不成立) 无一适用。因此 R5 照跑；R5 之后若仍未收敛，那是**配置定义的终止**(触及 max_rounds)，不是本席的裁量。

---

## Round 5 (max_rounds 上限轮, 5 席全出, 对象: R4-fixed)

**结论集合 (5 席全出)**: 21 条 finding — Critical 6 / Major 13 / Minor 2，归并约 18 簇。5 席里 4 席明确回 `NOT_CONVERGED`，第 5 席 (code-reviewer) 亦回 NOT_CONVERGED。

| # | 簇 | 席位 | 严重度 | 处置 |
|---|---|---|---|---|
| R5-1 | **`flow` 自身无校验**: 它是第一分流维却来自请求参数, 无取值域、无缺省、无「与会话状态相容」规则。`flow=login` + 已有会话 ⇒ `binding_id` 由受害者自己的会话派生、比对必然通过 ⇒ 递一个 `?flow=login` 链接即可把受害者会话静默换成攻击者身份。R2-1 拆双表只挡住了 link 那一侧 | tech-lead | Critical | start 端点封闭 `flow ∈ {login,link}`；缺失/未知 `400 invalid_flow` fail-closed (禁止默认 login)；两条越界组合各钉死处置；新增 SC-27 + tasks 5.18 |
| R5-2 | SC-2 仍写 error code ∈ {`invalid_state`} 覆盖「binding 不符」, 与 R4 新拆的 `binding_cookie_missing`/`binding_mismatch` **互斥** ⇒ 任何实现都必然让其中一条判红 (恒红 = 零信息) | tech-lead / backend-architect (2 席) | Critical | SC-2 五条负控逐条给码, binding 两态分列 |
| R5-3 | SC-13 的「实现者自选并自报」改法被 R4 判为 author-to-match-checker 镜像后, R5 发现新给的替代路径之一同样不成立: WSTG-CLNT-04 是**叙述性测试用例编号, 不是逐条可寻址的 payload 清单**, 实现者可给任意自选 payload 贴一个查不了的外部标签 ⇒ 刚堵上的洞以「引用权威」的名义重开 | qa-engineer | Critical | 删掉 OWASP 分支, 只留「非实现者封存到受保护路径」一条 |
| R5-4 | 审计留痕的 R3 处置 (挪进变更目录下 `audit-reports/` + 免责声明) **不够**: 自认偏离不等于合规, 放在变更目录里、名字与 SOT schema 相仿的文件仍会被当作该 change 产物扫描; Level 3 产物定义里没有第四类产物或子目录 | knowledge-manager | Critical | 移出变更目录树到 `outputs/_audit-trail-NOT-a-change-artifact/`; proposal 的「审计留痕」节只留 verdict 叙述 |
| R5-5 | 交付面已按 R4 fix 更新, 而留痕里 R4 段当时仍是占位符 ⇒ 「只增不改的历史账本」本身失真, 读者无法判断 SC-26/5.17 是否真经过 5 席收敛 | knowledge-manager | Critical | R4 段补齐 (本文件上一节); 此后每轮 fix 前先落轮次记录 |
| R5-6 | 零号任务 1.4 在 Layer 1 仍写「A-1~A-4」, 而门槛 2 要求六项全解才可 Approved ⇒ 一个实现者解 4 项就认为闸门解除、另一个按 yaml 解 6 项, 对「能不能开工」给出相反结论 | tech-lead | Major | 1.4 标题与依赖段同步为 A-1~A-6 并补列 A-5/A-6 |
| R5-7 | **D1 漂移事件的「投递到风控/告警队列」没有代码宿主**: 不在 Key Deliverables、锚点表无它、39 项任务无人建或接 ⇒ 「断言消费方收到」要么不可满足, 要么由实现者自造 stub 消费方 (自造 stub 恒绿 = 本 Spec 反复点名的假绿) | tech-lead | Major | 选「诚实降级」: 改为写 error 级结构化事件并在 Risk 表登记「本期无消费方」为残留风险; SC-5 改断言事件已写出且字段完整, **不**断言消费方收到 |
| R5-8 | 5.4 被 R3-10 补进 born-red 名单, 但 SC-4(a) 的正向锚是「测试期 OAuth 日志行数 ≥ 1」—— 实现不存在时它必红, 红的原因恰是它自己要排除的「扫描目标为空」⇒ born-red 义务结构上不可满足; 且 yaml 侧 TASK-022 的依赖顺序与之相反, 两层互斥 | tech-lead | Major | 5.4/5.5 从 born-red 名单撤回, 显式标注为 tripwire (实现后立即执行) |
| R5-9 | `provider_user_id` 残留风险写了「需 owner 签字」, 但 Approved 门槛只锁六项锚点 ⇒ 可在无人签字下进 Approved (R2-16「空洞不挡 Approved」同款复发) | backend-architect | Major | 新增门槛 4, 与锚点走同一道门 |
| R5-10 | SC-5 的「共 15 条分支」未随 L-inactive 重算 (应 16); 429 的「响应体给出触发维度」新承诺未写进 SC-19 ⇒ 不可验收 | backend-architect | Major | 各自同步 |
| R5-11 | SC-13 的新修法**只改了 proposal 没改 detailed-tasks.yaml** —— TASK-025 的 verification 仍是旧句。真正驱动实现的是 Layer 2, SOT 改了执行层没改 = 修法未传导 | qa-engineer | Major | TASK-025 verification 逐字同步 |
| R5-12 | SC-15(c) 的「未点名写法」示例之一「装饰器注册的策略对象」仍是 (b) 已点名的「字典/注册表分派」的语法变体 ⇒ 实质只交付 1 个新形态 (与刚修好的「字面比较不占名额」是同一形状换位置) | qa-engineer | Major | 明写它也不占名额, 给出真正不同范畴的合格例 |
| R5-13 | **5.4 第二次被挤出依赖图** (加 5.15/5.18 时把它挤掉了); 5.15 同时出现在图分支列与 born-red 豁免列, 两处对它的时序说法相反 | code-reviewer | Major | 5.4 补回图分支列并加成对维护警告; 5.15 只留 born-red 列 |
| R5-14 | `born_red_pairs` 与散文里的项数不一致; 且 `038→009` 这对在依赖图上**没有边** (TASK-009 的 dependencies 不含 TASK-038, 两者可并发) ⇒ 声称的 born-red 先后不成立 | code-reviewer | Major | 数字对齐; TASK-009 的 dependencies 补 TASK-038 / TASK-039 |
| R5-15 | **SC-17(b) 无 Layer 2 落点**: `session_subject_changed` 在 proposal 出现 2 次、在 detailed-tasks.yaml 命中 0 次, 承载任务 TASK-031 的 verification 无一覆盖 ⇒ R3-7 的「名义映射」形状在 SC-17 上原样复现 | code-reviewer | Major | TASK-031 verification 增该条 + 坏实现样本 |
| R5-16 | proposal 出现 `TASK-009`/`TASK-014`, 违反同文件自定的「一律用 Layer 1 编号」; SC-26c 的实测义务被派给 3.1 而映射表指向 5.17, 两处冲突; 「A.1 阶段尚不存在」已过期 | code-reviewer | Minor | 三处同步 |
| R5-17 | SC-27 定义在 SC-26 之前, 是清单里唯一乱序处 | code-reviewer | Minor | 移到 SC-26 之后 |

**处置**: 17 簇全部吸收。SC 27 → 29 条 (新增 SC-27 flow 校验)；tasks 38 → 39 项 (新增 5.18)；估时重算 111-176h；detailed-tasks.yaml 至 TASK-039。

---

## 终局 verdict

**post_spec = 未收敛 (NOT CONVERGED)，因触及 `max_rounds = 5` 而终止 —— 不是「通过」。**

按 phase-a-planner SKILL.md 的 `on_audit_fail: 阻塞进入 A.2`，本闸门的结论是**阻塞**。

### 趋势 (实计, 非印象)

| 轮 | 席数 | finding | 每席 | Critical | 每席 Critical |
|----|------|---------|------|----------|--------------|
| R1 | 5 | 37 | 7.4 | 8 | 1.6 |
| R2 | 5 | 34 | 6.8 | 6 | 1.2 |
| R3 | 3 | 15 | 5.0 | 3 | 1.0 |
| R4 | 2 | 11 | 5.5 | 2 | 1.0 |
| R5 | 5 | 21 | 4.2 | 6 | 1.2 |

**诚实读法**: 每席总 finding 数从 7.4 降到 4.2 (约 −43%)，但**每席 Critical 自 R2 起就卡在 1.0~1.2 之间，五轮没有向零收敛**。不能说「严重度在下降」—— R5 的绝对 Critical 数 (6) 比 R3/R4 都高，只是因为席位数不同；按每席归一化后是平的。

### 为什么五轮压不下来

R5 的 17 簇里，绝大多数打在**前一两轮 fix 新写的文本**上，而不是初稿遗留:

- `flow` 维是 R2 引入的 → R5 才发现它自己没被校验
- binding cookie 契约是 R4 引入的 → 同轮就带出 SameSite/多标签/禁用 cookie 三个洞
- SC-13 改了三版: 逐字列举样例 (条件不可满足) → 实现者自选自报 (author-to-match-checker 镜像) → 引外部权威 (那个"权威"不是逐条可寻址的清单)
- 「5.4 掉出依赖图」在 R3 和 R5 各发生一次，第二次是加新任务时把它挤掉的

这是「结构性重写是新交付面, 不是清理」的教科书形状: 每轮 fix 都在自己新写的段落里重犯它要治的病。**继续用同一批席位加轮, 预期还是这个结果**; 有效的做法是换新鲜视角, 或承认设计面已超出单轮可收敛的规模。

### 移交给 owner 的判断 (不由本席自行降级)

1. R5 的 17 簇**已全部吸收进文档**，但**没有第 6 轮验证这批修法本身**。按前五轮规律, 这批修法里大概率仍埋着同形状的新缺陷。是否加轮 (超出 config 的 max_rounds) 需 owner 决定。
2. 两条残留风险等签字: `provider_user_id` 稳定性假设 (无声接管路径)、身份漂移事件无消费方。
3. 六项现状锚点全部未解析 (本仓无应用代码)，Spec 因此停在 `Draft`。

