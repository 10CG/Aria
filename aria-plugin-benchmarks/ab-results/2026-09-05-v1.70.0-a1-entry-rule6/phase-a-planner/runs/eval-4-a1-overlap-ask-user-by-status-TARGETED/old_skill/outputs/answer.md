先说结论: 这四条不是同一类问题, 不能一把「有 overlap 就全停」, 也不能因为「反正都是同一个 issue」就整体忽略。要按 `status` 分开处理 —— 其中只有 1 条构成真正的阻塞, 1 条是坏数据, 另外 2 条是情报。

## 逐条处置

### 1. `oauth-login-aaaa1111` (cA, status=active, 2026-09-05T08:00Z) — 真阻塞, 动手前必须先协调

这是唯一一条 `active` 的认领, 时间就在今天, 和你锁的是同一个 `10CG/Aria#174`。这已经不是「文件级可能不冲突」的问题, 而是 feature 级重复建设: 两个容器很可能在并行实现同一份东西, 后落地的那个要么被推翻, 要么产生难解的接缝。

处置: **先不要往 A.2 走**。去看 cA 这条 track 的实际 scope (它的分支 / 已有 commit / 它在 #174 下的评论), 判断落在三种情形的哪一种:

- **完全重叠** → 放弃你这条 track, 转去 review / 协助 cA, 别开第二条实现线;
- **部分重叠** (例如 cA 做 provider 接入, 你做会话与权限) → 把 #174 拆成互不重叠的两个子范围, 各自把 `linked_issue` 改指到子 issue, 再各自继续;
- **其实不重叠** (只是 issue 挂错了) → 修正你这条 track 的 `linked_issue`, 假 overlap 自然消失。

这一条的裁决权不在我这里: 「你退还是它退」是产品/排期取舍, 需要你或 #174 的负责人拍板。**最省事的第一步是先确认 cA 是不是你自己在另一个终端开的** —— 如果是, 那就是同一个人的两条 track, 直接合成一条即可, 后面几步都不用做。

### 2. `social-login-bbbb2222` (cB, status=done, 2026-09-04) — 不阻塞, 但必须先读它的产出

`done` 说明这条已经交付完了, 它不会和你抢并发资源。但它是最可能让你白干的一条: 你准备做的事可能已经被它做掉一部分了。

处置: 进 A.2 之前先看它的落地成果 (已合并的 PR / 它在 #174 上的收尾说明), 把「已经存在的能力」从你的任务分解里扣掉, 并在 proposal 的现状段写明你是在它之上做增量, 而不是重来一遍。如果发现它其实已经完整覆盖了 #174, 那正确动作是关掉 #174, 而不是再开一轮开发。

### 3. `oauth2-spike-cccc3333` (cC, status=abandoned, 2026-09-03) — 不阻塞, 当情报读, 不当障碍

`abandoned` 的认领背后没有活跃工作, 不会跟你抢东西, 所以它不该拦住你。但「spike + 被放弃」这个组合本身是信息: 有人试过一次, 没走通。

处置: 花几分钟看它为什么中止 (残留分支 / issue 里的说明)。如果是撞到了技术阻塞 (比如某个 provider 的回调在内网环境走不通), 那个阻塞多半还在, 应该直接写进你 proposal 的风险 / open question, 而不是留到 Phase B 再撞一次。如果只是优先级调整导致的中止, 忽略即可。

### 4. 空记录 (`track_id:""`, `container:""`, `status:"unknown"`, `linked_issue:null`) — 这不是「第四个重叠」, 它是一条坏数据

注意它其实并不满足「与 #174 同 issue」这个条件 —— `linked_issue` 是 `null`, 不等于 `10CG/Aria#174`, 却仍然出现在 `linked_issue_overlap` 里; 同时 `unknown_schema_claims` 正好是 1。两者对得上: 这条就是 schema 认不出来的那条认领, 被计数器和 overlap 列表**同时**收了进来。

处置要拆成两半, 别混在一起:

- **对你这轮的决策**: 按 fail-closed 对待 —— 「不能证明它无关」就不能当它无关, 它有可能是某个容器写坏了的活跃认领。但它同时提供不了任何可协调的信息 (没有 `track_id`、没有 `container`, 你连找谁都不知道)。所以现实做法是: 不让它单独阻塞你, 但要在 proposal / handoff 里**显式留一笔**「存在 1 条无法解析的认领, 属已知风险」, 不能让它静默消失。
- **对工具本身**: 这是 `phase1_gate.py` 侧的缺陷, 值得单独开 issue, 有两个问题: 其一, 有生产者写出了不合 schema 的认领记录 (要追是谁写的、何时写的); 其二, overlap 判定把 `linked_issue: null` 的记录也算成了「同 issue 重叠」, 这是判定逻辑 bug —— 正确行为应该是先把 schema 不合法的记录过滤出去、单独报成 `unknown_schema_claims`, 而不是既计入 unknown 又混进 overlap 列表污染信号。这个修复**不要顺手塞进 #174 这轮改动**, 它是另一件事。

## 建议的动作顺序

1. 确认 cA 是不是你自己的另一个终端 (最省事的分叉, 先做这个);
2. 不是的话, 去 #174 下和 cA 对齐 scope, 定出「谁做哪半」或者「谁退」;
3. 读 cB 已交付的内容, 从你的范围里扣掉重复部分;
4. 扫一眼 cC 放弃的原因, 有技术阻塞就写进 proposal 的风险段;
5. 为那条坏记录开一个 gate 侧的 issue, 并在本次 handoff 留痕;
6. 以上 scope 收敛之后, 再回到 A.1 定稿 proposal, 然后进 A.2 任务分解。

## 需要你决定的

第 1 条的归属 (你退 / cA 退 / 拆成两半) 是产品级取舍, 我不替你定。你给我一个方向, 我就能把 #174 的 scope 收敛成一份 proposal 再往 A.2 走; 在那之前继续做任务分解, 本质上是在赌 cA 不会赢。第 2、3、4 条不需要你拍板, 按上面处置即可。
