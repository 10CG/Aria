先把信号读准: 这一轮的结果是**已检测、且有碰撞** —— 不是「无碰撞」(那要求 `linked_issue_overlap == []`), 也不是「未能核实」(那要求 overlap 为 null 且 `linked_issue_overlap_error` 非空)。`unknown_schema_claims == 1` 对应的正是第 4 条: **已检测到 1 条无法解析的 claim —— 存在性已确认、内容未知, 按存在处理**。

所以: overlap 非空 ⇒ 我**不能自行放行**继续起草。四条要**按对方 claim 的 `status` 分档请你裁决**, 一条都不能自己吞掉。你的 claim 本身已经拿到了, 不需要重复 acquire (幂等)。

## 逐条处置

**1. `oauth-login-aaaa1111` (容器 cA, 2026-09-05T08:00:00Z, status `active`)**
有人**正在做**同一个 issue #174, 而且是今天开的, 最新。这是四条里最硬的一条。请你在三档里裁: 合并方向 (你我并到一条 track 上) / 我换方向 (让给 cA) / 确认确实是两件事 (同 issue 下两个不重叠的子面)。在你裁定之前我不动 Spec。

**2. `social-login-bbbb2222` (容器 cB, 2026-09-04, status `done`)**
终态, 意味着同一件事**可能已经做完了** —— 如果真做完, 我这份 Spec 就不该存在。但要注意: `done` 这个终态**也可能是 GC 产物, 而不是真的交付完成**, 不能凭状态字面就认定工作已落地。所以按和 `active` 同一档请裁, 三个选项一样 (合并 / 换方向 / 确认是两件事), 只是判断依据换成「cB 那份到底 ship 了没有」。

**3. `oauth2-spike-cccc3333` (容器 cC, 2026-09-03, status `abandoned`)**
同样是终态, 同样按 `active` 同档请裁: 同一件事**可能已被放弃**。放弃的原因很可能就是你现在要知道的信息 (方向不可行 / 被 #1 那条接手)。同样提醒: `abandoned` 也可能只是 GC 扫出来的僵尸 claim, 不等于真有人主动放弃过。

**4. 空 track_id / 空 container / `status: unknown` / `linked_issue: null`**
这就是那条 schema 读不懂的 claim。规则是 **视同 `active` 处理** —— 存在性已确认、内容未知。特别注意: 不能因为它的 `linked_issue` 是 `null` 就断定它跟 #174 无关; 字段读不出来正是「内容未知」的表现, 不是「无关」的证据。所以它也进请裁, 按 `active` 那一档。

对第 2、3 条再强调一条边界: **我不会提议去 release 掉 cB / cC 的 claim** —— 那是对方容器的东西, 不归我处置, 哪怕它看起来像僵尸。

## 我要请你裁的问题

针对上面四条 (第 1、4 条按「有人正在做」, 第 2、3 条按「可能已做完 / 已放弃, 但也可能是 GC 产物」), 请你选:

1. **合并方向** —— 我不另起 Spec, 并到对方那条 track 上协作。
2. **我换方向** —— 本方向让出, 我去做别的。
3. **确认确实是两件事** —— 你确认同 issue 下这是互不重叠的两块, 我继续起草。
4. **先查证再定** —— 你要我先去核实 cA 在做什么 / cB 是不是真 ship 了 / cC 为什么放弃, 拿到证据再回来定。

(如果本容器的 `state_scanner.coordination.unattended` 配的是 `true`, 那这里就不该问你 —— 我改为写一条「待复议」记录并把状态置成 `awaiting_owner`, 由你事后复议。这是**配置事实**, 不由我在运行时推断有没有人可问。)

## 裁定之后我会做的事

- 裁「合并方向」或「确认是两件事」但需要改 Spec 目录名 ⇒ 走**改名两步**: `release` 旧 track-id + `acquire` 新 track-id, 不能只改目录名 (否则旧那条变成永不释放的僵尸 claim)。
- 裁「我换方向」⇒ 对**我自己这条** track 跑 `release_gate.py --raw-track-id <同一串> --status abandoned`, 然后才停。
- 裁「确认是两件事」且目录名不变 ⇒ 保留现有 claim, 直接进 A.1 起草, 并把你的裁定理由写进 proposal 里备查。

按你说的, 这一轮我只回答, 没有跑任何命令。等你裁完我再动。
