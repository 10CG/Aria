## Triage Report

**Verdict**: `confirmed` | **Severity**: `medium` | **Recommended Action**: `open-spec`

> Note: teammate referenced "#89 or #90". Both are state-scanner in-flight surfacing
> issues. **#89** (this issue) is the primary match for "不显示 in-flight 改动" —
> surfacing mid-implementation carry-forward items. **#90** is a closely related,
> narrower variant (inline `tasks.md` annotation surfacing) and should be
> cross-linked / de-duplicated when a spec is opened.

---

### Version

| Field | Value |
|-------|-------|
| Reported | `0.3.2` (TH example-project context, not Aria's own version) |
| Current | `1.21.2` |
| Gap | behind (informational only — this is a feature request, not a version regression) |

This is an enhancement proposal, not an outdated bug report; the version gap is not
material to the verdict.

### Code Path

Cited paths are illustrative placeholders/globs (`openspec/changes/<change>/tasks.md`,
`project_v<X>_carry_forward.md`, `openspec/changes/*/tasks.md`) — expectedly absent in
the Aria repo. Only `.aria/state-checks.yaml` exists (Phase 1.11 hook, option D candidate).
`matches_description=false` is expected for a feature request.

### Git History

No recent commits matched on cited files (`likely_fix_candidates=[]`). No fix has landed.

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | none (only main `master` @ 91370db) |

**No in-flight work addresses #89.** Field is clear to open a new cycle.

### Reproduction

**Mode**: `auto` | **Hit rate**: `1/1`

- Input: `grep -rn 'carry-forward|carry_forward' aria/skills/state-scanner/scripts/scan.py`
- Expected: surfacing logic present if gap were already closed
- Actual: 0 matches; Phase list 1.4–1.15 has no phase scanning active
  `openspec/changes/*/tasks.md` for inline carry-forward annotations
- Match: **true** — gap confirmed

### Verdict Rationale

The reported gap is real and independently verified: state-scanner's scan.py contains
no carry-forward surfacing logic across any of its phases (1.4–1.15), and the ship-time
consolidation pattern (`project_v<X>_carry_forward.md`) is confirmed by the issue's own
history. No in-flight PR/branch/worktree addresses it. This is a `confirmed` feature
request; severity `medium` (impacts multi-session handoff continuity but no data
corruption / hard breakage); recommended action: open an OpenSpec proposal evaluating
options A–D (A: new Phase 1.X grep; D: `.aria/state-checks.yaml` config — likely lowest-risk),
and cross-link #90 to avoid duplicate work.

---

*Generated by `/issue-triage` v1.21.2 — Ref: 10CG/Aria#89*
