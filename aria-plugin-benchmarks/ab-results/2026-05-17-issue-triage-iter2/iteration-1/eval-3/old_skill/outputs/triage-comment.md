## Triage Report

**Verdict**: `confirmed` | **Severity**: `medium` | **Recommended Action**: `open-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `0.3.2` |
| Current | `1.21.2` |
| Gap | behind |

> Version gap is a **project-context artifact**, not an outdated-report signal. `0.3.2` is the downstream TH project version cited as the example case, not an Aria version. The capability gap exists in **current** Aria state-scanner — this is not a stale report.

### Code Path

Cited paths are templated/example paths (`openspec/changes/<change>/tasks.md`, `project_v<X>_carry_forward.md`) and TH-project paths (`openspec/changes/v0-3-2-chat-mvp/tasks.md`) — none resolve in the Aria repo, which is expected: this is a **missing-capability feature request**, not a code-bug citation. `.aria/state-checks.yaml` exists (74 lines) and is the closest existing surface (Option D self-config), but no built-in carry-forward scan exists.

### Git History

No recent commits matched on cited files. `likely_fix_candidates` empty.

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none (the `feature/m3-carryover-*` remote branches are unrelated M3 HCL-crons / result-path work) |
| Worktrees | only main `/home/dev/Aria` @ master |

**Nothing in-flight addresses #89.**

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/2`

- Code inspection: `grep` over `aria/skills/state-scanner/` finds only `SKILL.md` reading `handoff.latest_path` (session handoff doc) and a schema-doc mention of prior-session priority. `scan.py` has **no** scan of inline `[carry-forward: ...]` markers in active `openspec/changes/*/tasks.md`. Capability gap confirmed.
- In-flight scan: no PRs/branches/commits referencing #89/#90 or this feature.

### Duplicate

**#90 is a near-duplicate of #89** — same reporter, filed 7 minutes apart (2026-05-08), same TH v0.3.2 case, same root problem. #89 carries the broader framing (options A–D); #90 is the more specific Phase 1.6 / Phase 2 framing. Recommend consolidating: keep #89 as primary, fold #90's Phase-specific detail in, and close #90 as duplicate.

### Verdict Rationale

Confirmed capability gap: state-scanner structurally cannot surface mid-implementation carry-forward debt accumulated as inline `tasks.md` annotations (verified by code inspection — Step 6 hit 2/2). Not an outdated report (version gap is project-context noise from the TH example). Nothing in-flight. Severity `medium`: real handoff-blindness risk across multi-session cycles (the reporter's TH v0.3.2 case lost ≥7 items including a race condition), but no data corruption and a manual `grep` workaround exists. Recommended action `open-cycle`: warrants an OpenSpec proposal (option A — a Phase 1.X active carry-forward scan — is the most aligned design), de-duplicating #90 into the same cycle.

---

*Generated by `/issue-triage` v1.21.2 — Ref: 10CG/Aria#89*
