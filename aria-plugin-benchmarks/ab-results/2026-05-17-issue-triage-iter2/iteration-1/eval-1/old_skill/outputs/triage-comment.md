## Triage Report

**Verdict**: `fixed-in-1.20.0` | **Severity**: `n/a` | **Recommended Action**: `close`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.17.3` |
| Current | `1.21.2` |
| Gap | `behind` |

Issue filed against v1.17.3. The fix landed in **v1.20.0**; current release is v1.21.2. This is an already-resolved report — re-triaged to confirm closure rather than to open a new cycle.

### Code Path

`aria/skills/state-scanner/scripts/collectors/_status.py` — confirmed present (113 lines). The cited bare path `skills/state-scanner/scripts/collectors/_status.py` (no `aria/` prefix) does not exist; the `aria/`-prefixed path is correct.

Critical evidence: `_status.py:55-61` docstring now reads:

```
def _normalize_status(raw: str | None) -> str:
    """Normalize Status string to OpenSpec-aligned lifecycle states.

    Priority order (refined per Forgejo Aria #101 fix, 2026-05-13):
      1. Terminal (archived / deprecated) — irreversible
      2. Pending family (draft / pending / placeholder)
      3. In-progress family ...
```

The unconditional `for token in ("done", "complete"): if token in low: return "done"` short-circuit described in the issue is gone — replaced with priority-ordered normalization.

### Git History

`triage.py` Step 4 `likely_fix_candidates: []` (keyword-collector limitation — fix commit subject is `fix(state-scanner): #101 ...`). Manual `git log` on the cited file reveals the fix definitively:

- `154775a` — `fix(state-scanner): #101 _normalize_status substring shadow + add implemented token`
- Merged via PR **#44** (`feature/aria-issue-101-status-normalize` → `master`, merge `79f9f1d`)
- Released in **v1.20.0** (`1b7d570`, release PR **#45** merge `899f3fa`)

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | `feature/aria-issue-101-status-normalize` (already merged to master — not active work) |
| Worktrees | `/home/dev/Aria` @ `master` (main) |

No in-flight fix work. The fix branch exists only because it has been merged; nobody is currently working on this.

### Reproduction

**Mode**: `auto` | **Hit rate**: `0/4`

Imported `_normalize_status` from current master code and ran the 4 real Status strings from the issue:

| case_id | Input | Expected | Actual | Match |
|---------|-------|----------|--------|-------|
| case-1-docs-marketplace | `Approved … — Phase A done, ready for Phase B` | not `done` | `approved` | ✅ fixed |
| case-2-existing-data-migration | `Implemented … post-deploy 验证后归档` | not `done`/`unknown` | `implemented` | ✅ fixed (secondary bug also addressed — `implemented` token added) |
| case-3-pricing-marketplace-redo | `Implemented … post-monitoring 后归档` | not `done`/`unknown` | `implemented` | ✅ fixed |
| case-4-terms-of-service | `⏸ DRAFT pending lawyer review — … done …` | `pending`/`draft` | `pending` | ✅ fixed |

0/4 reproduce. Both the primary substring-shadow bug and the secondary `Implemented`→`unknown` gap (raised in the prior manual triage at issuecomment-5972) are resolved.

### Verdict Rationale

The bug is **already fixed and shipped**. Fix commit `154775a` was merged to `master` via PR #44 and released in v1.20.0; issue #101 is already `closed` (2026-05-13). Reproduction against current code shows 0/4 hit rate — all 4 real-world Status strings now normalize to correct lifecycle states. No in-flight duplicate work. No new OpenSpec cycle needed: this issue is correctly closed and the teammate can be told the fix is already merged and released.

---

*Generated by `/issue-triage` v1.0.0 — Ref: 10CG/Aria#101*
*Re-triage / closure confirmation — 2026-05-17*
