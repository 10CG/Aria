# Run Notes — issue-triage 10CG/Aria#101

**triage.py run?** Yes. Command:
`python3 aria/skills/issue-triage/scripts/triage.py --issue "10CG/Aria#101" --output <outputs>/triage-report.json`
**Exit code:** 0 (`steps_with_data == 5`, all collectors ok).

**Hand-authored JSON?** No. JSON was machine-generated by triage.py; only the
`repro`/`verdict`/`severity`/`recommended_action` fields were filled in Stage 3
via Edit (per skill — Steps 1-5 untouched, AI-only fields). Anti-hand-author
gate passed (triage_tool_version=1.21.2, fresh generated_at, step1_issue.body present).

**Verdict:** `already-fixed` | severity `minor` | action `close`.

Issue is already `closed`. Code `_normalize_status` rewritten with `_has_token`
word-boundary matching + reordered priority; fix archived
(`openspec/archive/2026-05-13-aria-issue-101-status-normalize`), shipped in
plugin v1.21.2 (filed against v1.17.3). Reproduction of the 4 real Status
strings: 0/4 hits — both primary substring-shadow bug and secondary
`Implemented`→`unknown` defect resolved.
