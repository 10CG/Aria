## Triage Report

**Verdict**: `already-fixed` | **Severity**: `minor` | **Recommended Action**: `close`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.17.3` |
| Current | `1.21.2` |
| Gap | `behind` (+several minor/patch versions) |

Issue was filed against v1.17.3. Current release is v1.21.2. The `_normalize_status` logic was reworked under the `aria-issue-101-status-normalize` cycle (2026-05-13) and shipped before the current version. Issue state is already `closed`.

### Code Path

`aria/skills/state-scanner/scripts/collectors/_status.py` (the bare `skills/...` and `_status.py` citations in the issue body do not resolve; the `aria/`-prefixed path is the correct one — confirmed present, 113 lines).

`_normalize_status` (L55-111) has been rewritten:
- Word-boundary matching via `_has_token` replaces the old `if token in low` substring test (roots out the entire substring-shadow class).
- Priority reordered: terminal → pending family → in-progress → approved → **implemented (new state)** → reviewed/active/ready → `done`/`complete` **LAST** as fallback.
- Docstring explicitly references "refined per Forgejo Aria #101 fix, 2026-05-13" and the archived proposal.

### Git History

`likely_fix_candidates: []` from the collector (commits predate the report window on this file path), but the fix is independently confirmed via: (a) archived OpenSpec change `openspec/archive/2026-05-13-aria-issue-101-status-normalize/`, (b) the rewritten code with `_has_token` word-boundary logic, (c) plugin version bump to v1.21.2.

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none (0 open PRs) |
| Local branches | `feature/aria-issue-101-status-normalize` (origin + github) — the merged fix branch, not active WIP |
| Worktrees | `/home/dev/Aria` (master, main) only |

No duplicate in-flight work. The fix cycle is complete and archived (Phase D.2 done per recent master history).

### Reproduction

**Mode**: `auto` | **Hit rate**: `0/4`

Imported `_normalize_status` from current `aria/skills/state-scanner/scripts/collectors/_status.py` and ran the issue's 4 real Status strings:

| case_id | Input | Expected | Actual | Match |
|---------|-------|----------|--------|-------|
| case-1-docs-marketplace | `Approved (Rev2 CONVERGED) — Phase A done, ready for Phase B` | `approved` | `approved` | bug not reproducible |
| case-2-existing-data-migration | `Implemented (...PR-A merged...) — post-deploy 验证后归档` | `implemented` | `implemented` | bug not reproducible |
| case-3-pricing-marketplace-redo | `Implemented (...UAT PASS; post-monitoring 后归档` | `implemented` | `implemented` | bug not reproducible |
| case-4-terms-of-service | `⏸ DRAFT pending lawyer review — Phase B PR-A done...` | `pending` | `pending` | bug not reproducible |

All 4/4 now resolve to the correct semantic status; none returns `done`, so none false-positives into `pending_archive`. The secondary `Implemented`→`unknown` defect raised in the manual triage (issuecomment-5972 / 6019) is also fixed via the new `implemented` lifecycle state.

### Verdict Rationale

Verdict is **`already-fixed`**: the primary substring-shadow bug AND the secondary missing-`Implemented`-token bug are both resolved in current code (v1.21.2). Reproduction of the issue's exact 4 real-world Status strings yields 0/4 hits — every case now normalizes to its intended semantic state. The fix is shipped, archived (`openspec/archive/2026-05-13-aria-issue-101-status-normalize`), and the issue is already `closed`. No new cycle needed; recommended action is `close` (already closed — no further work).

---

*Generated by `/issue-triage` v1.21.2 — Ref: 10CG/Aria#101*
*Step 6 performed by AI (auto mode) — 2026-05-17*
