## Triage Report

**Verdict**: `confirmed` | **Severity**: `minor` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `0.3.2` |
| Current | `1.21.2` |
| Gap | behind |

Note: reported `0.3.2` is the cited **TH project** case version (TH v0.3.2 chat MVP context), not an Aria-plugin version. The version gap is informational only — this is a structural Aria state-scanner gap independent of plugin version, not an outdated-report artifact.

### Code Path

Cited paths are template placeholders (`openspec/changes/<change>/tasks.md`, `project_v<X>_carry_forward.md`, `openspec/changes/*/tasks.md`, `<change>/_carry_forward.md`) describing the TH project layout — not present in the Aria repo (expected). `.aria/state-checks.yaml` exists (Option D surface mechanism referenced in the issue). The substantive code check is a grep of `aria/skills/state-scanner/scripts/` (13 collectors + scan.py): **zero** collector greps inline `[carry-forward:]` / `code-ready` / `PASS-with-note` annotations in `tasks.md`.

### Git History

No recent commits matched on cited files (`likely_fix_candidates: []`).

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none directly addressing inline carry-forward surfacing |
| Worktrees | master only (main) |

No in-flight PR/branch addresses this gap. The closed #85/#88 work (state-scanner v3.0 G2/G3/G4) covers UPM Pending Followups + handoff pointer + in-progress US — explicitly **not** inline `openspec/changes/*/tasks.md` carry-forward annotations. This gap remains unaddressed.

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/2`

- **case-1**: grep of all state-scanner collectors → no handler for inline `tasks.md` carry-forward markers. Only refs: SKILL.md L178 (read handoff doc) + schema-doc L588 (ship-time consolidation). Structural blindness confirmed. `match: true`
- **case-2**: #88 spec scope cross-check → G2/G3/G4 do not cover inline tasks.md carry-forward; #89 gap uncovered by shipped work. `match: true`

### Verdict Rationale

Reproduced 2/2: state-scanner has no collector that surfaces in-flight inline carry-forward annotations, exactly as the issue describes, and the closed #85/#88 surfacing work does not cover this path. The gap is real and confirmed. **Severity `minor`** — the issue self-classifies as a process/hygiene gap that breaks no workflow (advisory). **Action `next-cycle`** — a genuine enhancement worth a scheduled OpenSpec cycle (one of Options A–D), not a hotfix.

**Duplicate note**: #90 ("surface in-progress openspec/changes/*/tasks.md inline carry-forward annotations") is a duplicate of this issue — same author, same TH v0.3.2 case, filed 7 min after #89 (#89 23:27:37Z / #90 23:34:30Z), with a more detailed Phase 1.6.1 proposal. Recommend consolidating: keep **#89** as canonical (earliest), close **#90** as `duplicate-of-#89` while folding its concrete regex/output spec into #89's design.

---

*Generated by `/issue-triage` v1.21.2 — Ref: 10CG/Aria#89*
