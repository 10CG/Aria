## Triage Report

**Verdict**: `fixed-in-X` | **Severity**: `major` | **Recommended Action**: `close`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.17.3` |
| Current | `1.21.2` |
| Gap | behind (reported 4 patch/minor behind current) |

Issue filed against v1.17.3. Fix shipped in v1.20.0 (release commit `055be0f`); current release is v1.21.2. This is now an outdated report — the defect no longer exists in any current version.

### Code Path

`aria/skills/state-scanner/scripts/collectors/_status.py` (correct full path; the issue's bare `skills/...` and `_status.py` citations do not resolve, but the `aria/`-prefixed path exists, 113 lines).

`_normalize_status` has been **refactored**. Its docstring explicitly states "Priority order (refined per Forgejo Aria #101 fix, 2026-05-13)". Changes vs. the buggy version:
- Word-boundary matching via `_has_token` replaces unbounded `if token in low` substring matching — roots out the entire substring-shadow class.
- `done` / `complete` moved to the LAST fallback (was first, the original root cause).
- Pending family + `approved` evaluated before `done`.
- New `implemented` lifecycle token added (the secondary bug from the prior triage).

### Git History

`likely_fix_candidates: []` from the collector (collector keyword scan), but full git history confirms the fix landed:
- `3e9d0e1` feat(aria-issue-101-status-normalize): Phase A.1 proposal + A.2 tasks
- `ec207af` feat(aria-issue-101-status-normalize): Phase A+B complete
- `09f5e32` Merge pull request #103 `feat(aria-issue-101-status-normalize): cycle complete (fix + tests + docs + benchmark)`
- `e80f55d` chore(spec): archive aria-issue-101-status-normalize (Phase D.2)
- `055be0f` release: bump aria submodule to v1.20.0 (ships the fix)

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none (PR #103 already merged + closed) |
| Local branches | `feature/aria-issue-101-status-normalize` (origin + github) — merged, retained, not active work |
| Worktrees | only main `/home/dev/Aria` @ master |

No in-flight work — the cycle is fully closed (PR merged, OpenSpec archived under `openspec/archive/2026-05-13-aria-issue-101-status-normalize/`, issue closed 2026-05-13T16:04:39Z).

### Reproduction

**Mode**: `auto` | **Hit rate**: `0/4`

Imported `_normalize_status` from current `aria/skills/state-scanner/scripts/collectors/_status.py` and ran the issue's 4 real Status strings:

| case_id | Input | Expected | Actual | Match |
|---------|-------|----------|--------|-------|
| case-1-docs-marketplace | `Approved … Phase A done, ready for Phase B` | not `done` | `approved` | false |
| case-2-existing-data-migration | `Implemented … post-deploy 验证后归档` | not `done` | `implemented` | false |
| case-3-pricing-marketplace-redo | `Implemented … UAT PASS; post-monitoring 后归档` | not `done` | `implemented` | false |
| case-4-terms-of-service | `⏸ DRAFT pending lawyer review — … PR-A done …` | not `done` | `pending` | false |

`match: false` for all 4 means the original bug does NOT reproduce — none of the 4 cases is misclassified as `done`. 0/4 hit rate confirms the fix.

### Verdict Rationale

The bug (substring `"done"` short-circuit + missing `Implemented` token) is fully fixed and shipped. Current code uses word-boundary `_has_token`, reorders `done`/`complete` to last fallback, and adds the `implemented` lifecycle state — directly addressing both the primary and secondary defects from the prior manual triage (issuecomment-5972) and dogfood triage (issuecomment-6019). The 4 real project Status strings now normalize correctly (0/4 reproduce). PR #103 merged, OpenSpec archived, issue already closed 2026-05-13. Verdict `fixed-in-X` (shipped v1.20.0); recommend `close` (already closed — no further action). Severity `major`: the original defect caused silent false-positive archive recommendations of WIP specs (functional error blocking correct workflow guidance), but no irreversible data corruption.

---

*Generated by `/issue-triage` v1.21.2 — Ref: 10CG/Aria#101*
