## Triage Report

**Verdict**: `fixed-in-v1.20.0` | **Severity**: `minor` | **Recommended Action**: `close`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.17.3` |
| Current | `1.21.2` |
| Gap | `behind` |

Issue filed against v1.17.3; current aria-plugin is v1.21.2. The fix landed in **v1.20.0** (`release: bump aria submodule to v1.20.0`, commit `055be0f`), so the current version is well past the fix point. This is now an outdated / already-resolved report.

### Code Path

`aria/skills/state-scanner/scripts/collectors/_status.py` — exists. `_normalize_status()` docstring (L55-61) now reads: *"Priority order (refined per Forgejo Aria #101 fix, 2026-05-13)"* with explicit ordering: terminal → pending family → in-progress family. The pre-fix `for token in ("done","complete"): if token in low: return "done"` substring short-circuit is gone. The issue's bare path `skills/state-scanner/scripts/collectors/_status.py` (no `aria/` prefix) does not exist; correct path is the `aria/`-prefixed one (file lives inside the `aria` submodule).

### Git History

`likely_fix_candidates: []` on the cited path is a collector artifact (file is inside the `aria` submodule, not tracked by the superproject path). Superproject history confirms the fix cycle:

- `09f5e32` Merge PR #103 `feat(aria-issue-101-status-normalize): cycle complete (fix + tests + docs + benchmark)`
- `e80f55d` chore(spec): archive aria-issue-101-status-normalize (Phase D.2)
- `055be0f` release: bump aria submodule to v1.20.0
- `2902787` docs(handoff): 2026-05-13 issue #101 cycle closeout

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none (PR #103 already merged) |
| Local branches | `feature/aria-issue-101-status-normalize` (merged into master) |
| Worktrees | `/home/dev/Aria` (master, main) — no active fix worktree |

No new in-flight work — the fix cycle is complete and merged. Issue #101 is `closed` (closed_at 2026-05-13T16:04:39Z).

### Reproduction

**Mode**: `auto` | **Hit rate**: `0/4`

Imported `_normalize_status` from current master and ran the 4 real Status strings from the issue:

| case_id | Actual | Expected | Match | Notes |
|---------|--------|----------|-------|-------|
| case-1-docs-marketplace | `approved` | `approved` | bug not reproduced | substring short-circuit removed |
| case-2-existing-data-migration | `implemented` | `implemented/in_progress` | bug not reproduced | secondary `Implemented` token gap also fixed |
| case-3-pricing-marketplace-redo | `implemented` | `implemented/in_progress` | bug not reproduced | same as case-2 |
| case-4-terms-of-service | `pending` | `pending/draft` | bug not reproduced | pending family now precedes done |

All 4 cases (including the 2 secondary `Implemented` cases from the original `partial-repro` triage) now normalize correctly. 0/4 reproduce the defect.

### Verdict Rationale

The `_normalize_status` substring-ordering bug **and** the secondary missing-`Implemented`-token defect (both identified in the original 2026-05-13 `partial-repro` triage, issuecomment-6019) were fixed in PR #103, shipped in aria-plugin v1.20.0, spec archived (Phase D.2), and issue #101 closed on 2026-05-13. Reproduction on current master (v1.21.2) shows 0/4 hit rate. No in-flight or duplicate work needed — recommend no action (already closed/fixed).

---

*Generated by `/issue-triage` v1.21.2 — Ref: 10CG/Aria#101*
*Step 6 performed by AI (auto mode) — 2026-05-17*
