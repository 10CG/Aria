# Branch Manager 模拟结果

## 输入参数
- module: backend
- task_id: TASK-008
- description: api-breaking-change refactor
- mode: auto

## 模式决策分析

### 自动决策算法执行

根据 mode=auto，执行五维度评分：

| 评分因素 | 权重 | 评分规则 | 得分 |
|---------|------|---------|------|
| `file_count` | 低 | 1-3 个文件 | 0 (默认值，未指定) |
| `cross_directory` | 中 | 跨目录 | 0 (backend 模块，单目录) |
| `task_count` | 低 | 1-3 个任务 | 0 (默认值，未指定) |
| `risk_level` | 中 | 根据描述检测 | **high** (包含 "api" 和 "breaking" 和 "refactor" 关键词) → **+3** |
| `parallel_needed` | 高 | 不需要并行 | 0 (默认值，未指定) |

**总分**: 0 + 0 + 0 + 3 + 0 = 3

### 决策结果
- **阈值**: score >= 3 → Worktree, score < 3 → Branch
- **实际得分**: 3 (>= 3)
- **决策**: **Worktree 模式**
- **理由**: 包含 API 破坏性变更和重构关键词，风险等级高，使用隔离环境避免污染主分支

## 风险检测详情

### 关键词匹配
检测到以下高风险关键词：
- "api" → API 变更
- "breaking" → 破坏性变更
- "refactor" → 重构

根据规范，包含这些关键词的任务风险等级为 **high**。

### 高风险任务特征
- 涉及 API 接口变更
- 可能破坏现有功能
- 需要隔离开发环境
- 需要更严格的测试验证

## 生成分支名

根据命名规范：`{branch_type}/{module}/{task_id}-{description}`

- branch_type: feature (默认)
- module: backend
- task_id: TASK-008
- description: api-breaking-change-refactor

**分支名**: `feature/backend/TASK-008-api-breaking-change-refactor`

## 预期执行流程 (Worktree 模式)

1. **环境验证**:
   - 检查 Git 状态 (当前分支、工作目录)
   - 验证 .gitignore 规则
   - 检查开发环境 (如果适用)

2. **Worktree 创建**:
   - git checkout develop
   - git pull origin develop
   - git worktree add .git/worktrees/TASK-008-api-breaking-change-refactor feature/backend/TASK-008-api-breaking-change-refactor
   - git push -u origin feature/backend/TASK-008-api-breaking-change-refactor

3. **输出结果**:
   ```
   mode: "worktree"
   branch_name: "feature/backend/TASK-008-api-breaking-change-refactor"
   worktree_path: ".git/worktrees/TASK-008-api-breaking-change-refactor"
   location: "worktree"
   remote_push: "success"
   decision_reason: "包含 API 破坏性变更和重构，使用隔离环境确保安全"
   next_step: "cd 到 worktree 路径开始开发"
   ```

## 断言验证

✅ **风险检测**: 成功检测到 high 风险 (api, breaking, refactor)
✅ **模式选择**: Worktree 模式 (score >= 3，符合预期)
✅ **分支名格式**: `feature/backend/TASK-008-api-breaking-change-refactor` (符合规范)
✅ **关键词识别**: 正确识别所有高风险关键词

## 结论

成功通过风险等级检测，识别出 "api-breaking-change refactor" 为高风险任务。由于涉及 API 破坏性变更和重构，系统选择 Worktree 模式创建隔离的开发环境，确保主分支不受影响，同时提供安全的开发空间。