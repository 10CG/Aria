{
  "_fixture_id": "NEG-1-malformed",
  "_description": "Negative fixture per Spec QA-2 R2 patch: malformed verdict / unknown enum value. Expected: pre_merge_gate.py routes to verdict=fail (not unhandled exception, not silent skip).",
  "_consumed_by": "pre_merge_gate.py via ARIA_AETHER_MOCK_RESPONSE_FILE env var",
  "_expected_helper_behavior": "compute_verdict() routes unknown pr_ci_status via _normalize_pr_ci_status() default → pending → wait verdict (not crash). Or if verdict field is malformed/missing, helper falls back to fail with raw_message error.",
  "_mock_latency_ms": 200,
  "verdict": "unknown_state_completely_invalid",
  "pr_ci_status": "totally_unrecognized_status",
  "raw_message": "malformed JSON simulation: verdict + pr_ci_status outside enum"
}
