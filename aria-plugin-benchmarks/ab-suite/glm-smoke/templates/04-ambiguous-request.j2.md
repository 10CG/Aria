{# 场景: 开发者给出**极短、模糊、无上下文**的请求, 模拟"随口一说"的触发 #}
{# 目的: 压力测试 state-scanner 是否能在请求几乎没信息量时仍自动介入 #}
你是一位虚构软件项目的开发者。请生成**一段第一人称的口头请求**, 发送给一个 AI 助手, 但要求是: **极其简短、模糊、无上下文**, 像是开发者"随手敲一句, 让 AI 自己琢磨"的风格。

## 严格约束

1. **禁止出现任何具体行业/领域名词**, 包括但不限于:
   - 电商 / 博客 / 待办 / 天气 / 聊天机器人 / 社交平台 / CMS / 管理后台 / 论坛 / 地图 / 支付 / 物流 / 教育 / 医疗 / 金融 / 游戏
   - TodoApp / UserService / OrderService / PaymentGateway 等常见练习项目名
2. **禁止虚构具体功能**
3. **禁止明确指令**, 如 "请帮我做 X", "列出 Y", "运行 Z"
4. 允许模糊词: "继续", "走一遍", "开始吧", "看一下", "整下"
5. **长度必须极短**, {{ length_hint | default("5-20 字") }} (这是本模板区别于其他模板的关键特征)
6. 语气**懒散、含糊、假设 AI 懂你**
7. 语言: {{ language | default("中文") }}

## 输出格式 (关键)

- 仅输出任务描述本身, 直接以第一人称或无主语开头
- **禁止前缀**如 "好的, 这是..."
- **禁止后缀**如 "..."
- **禁止 markdown 代码块包裹**
- 不要加引号
- **禁止任何解释**, 只给 prompt 本身

## 示例 (仅示范风格, 不要复用具体措辞)

{%- if language == "en" %}
> continue
{%- else %}
> 继续走一下吧
{%- endif %}

## 变量种子

seed: `{{ seed | default("d4") }}`

---

现在开始生成 (只给一条, 极短):
