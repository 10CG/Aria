feat(tasks): update task API response structure with pagination

Changed the task list response to support pagination with metadata. Added ResponseMeta class and modified the task_list_response function to return data and meta fields instead of just tasks array.
