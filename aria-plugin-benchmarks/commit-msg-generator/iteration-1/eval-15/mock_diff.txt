diff --git a/src/services/report_exporter.py b/src/services/report_exporter.py
new file mode 100644
index 0000000..abc1234
--- /dev/null
+++ b/src/services/report_exporter.py
@@ -0,0 +1,45 @@
+"""
+PDF report exporter service
+"""
+import os
+from typing import Optional
+from reportlab.lib.pagesizes import letter
+from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer
+from reportlab.lib.styles import getSampleStyleSheet
+
+
+class ReportExporter:
+    """Service for exporting reports to PDF format"""
+
+    def __init__(self, output_dir: str = "./exports"):
+        self.output_dir = output_dir
+        os.makedirs(output_dir, exist_ok=True)
+
+    def generate_pdf(self, content: str, filename: str) -> Optional[str]:
+        """Generate PDF from content
+
+        Args:
+            content: HTML/formatted content to export
+            filename: Output filename
+
+        Returns:
+            Path to generated PDF or None if failed
+        """
+        styles = getSampleStyleSheet()
+        filepath = os.path.join(self.output_dir, filename)
+
+        try:
+            doc = SimpleDocTemplate(filepath, pagesize=letter)
+            story = []
+
+            for line in content.split('\n'):
+                if line.strip():
+                    story.append(Paragraph(line, styles['Normal']))
+                    story.append(Spacer(1, 12))
+
+            doc.build(story)
+            return filepath
+        except Exception as e:
+            print(f"PDF generation failed: {e}")
+            return None
diff --git a/src/templates/pdf_template.html b/src/templates/pdf_template.html
new file mode 100644
index 0000000..def5678
--- /dev/null
+++ b/src/templates/pdf_template.html
@@ -0,0 +1,32 @@
+<!DOCTYPE html>
+<html lang="zh-CN">
+<head>
+    <meta charset="UTF-8">
+    <title>Report PDF Template</title>
+    <style>
+        body { font-family: Arial, sans-serif; margin: 20px; }
+        .header { border-bottom: 2px solid #333; padding-bottom: 10px; }
+        .content { margin-top: 20px; line-height: 1.6; }
+        .footer { margin-top: 30px; font-size: 12px; color: #666; }
+    </style>
+</head>
+<body>
+    <div class="header">
+        <h1>Analysis Report</h1>
+        <p>Generated on: {{ date }}</p>
+    </div>
+    <div class="content">
+        {{ content }}
+    </div>
+    <div class="footer">
+        <p>Confidential - Internal Use Only</p>
+    </div>
+</body>
+</html>
diff --git a/src/utils/__init__.py b/src/utils/__init__.py
index 1234567..9876543 100644
--- a/src/utils/__init__.py
+++ b/src/utils/__init__.py
@@ -1,3 +1,4 @@
 """Utility modules"""
 from .formatters import format_date
+from .export_helpers import sanitize_filename
diff --git a/src/utils/export_helpers.py b/src/utils/export_helpers.py
new file mode 100644
index 0000000..4567890
--- /dev/null
+++ b/src/utils/export_helpers.py
@@ -0,0 +1,15 @@
+"""Export helper utilities"""
+import re
+
+
+def sanitize_filename(filename: str) -> str:
+    """Sanitize filename for safe file system usage
+
+    Args:
+        filename: Raw filename
+
+    Returns:
+        Sanitized filename
+    """
+    # Remove invalid characters
+    return re.sub(r'[<>:"/\\|?*]', '', filename)
