Here's the decomposed task list from the Phase 3 UX Optimization spec:

**FR-FE-003: 暗色模式 (P1)**
1. `1.1` 重构 `design-tokens.css`，建立完整亮色/暗色 CSS 变量体系
2. `1.2` 实现主题切换组件（header 开关 + `prefers-color-scheme` 检测 + `localStorage` 持久化）
3. `1.3` 适配所有页面暗色样式（`todos.css`, `statistics.css`, `auth.css`, `base.css` + 所有 HTML）

**FR-FE-004: 响应式布局 (P1)**
4. `2.1` 移动端响应式断点 + 底部导航栏
5. `2.2` 触摸目标 ≥44px、表格/列表小屏适配、滑动删除手势

**FR-FE-005: 加载状态 (P2)**
6. `3.1` 新增 `skeleton.css`、页面骨架屏占位、顶部进度条指示器
7. `3.2` 按钮加载态（disabled + spinner）、网络异常提示

**FR-FE-006: 可访问性 (P2)**
8. `4.1` 语义化 HTML（`nav`, `main`, `section`, `article`）+ 完整 ARIA 属性
9. `4.2` 键盘导航（Tab/Enter/Escape）+ 焦点管理（模态框、动态内容）

P1 任务（暗色模式 + 响应式）应优先实施，P2 任务（加载状态 + 可访问性）随后跟进。要开始实现哪个任务？