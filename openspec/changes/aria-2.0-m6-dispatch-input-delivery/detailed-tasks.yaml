# Detailed Tasks — M6 Dispatch Input Delivery (C' dual-channel)
# Phase A.3 output (task-planner v2.0). Datasource: tasks.md (path A, dual-layer).
# Spec: openspec/changes/aria-2.0-m6-dispatch-input-delivery/{proposal.md,tasks.md}
# Scope repo: aria-orchestrator @ daf7c79 (all paths verified against this HEAD).

metadata:
  feature: aria-2.0-m6-dispatch-input-delivery
  title: "Layer1→Layer2 Dispatch Input Delivery (C' dual-channel)"
  level: 3
  datasource: "tasks.md"
  scope_repo: "aria-orchestrator"
  scope_repo_head: "daf7c79"
  decision_source: "docs/decisions/DEC-20260702-001-layer2-input-delivery-architecture.md"
  total_tasks: 30
  task_groups: 6
  status: "A.3 complete — post_planning R1→R2 CONVERGED 2026-07-04 (6 Major+minors fixed, R2 all-PASS); ready for B.1"
  new_agents_required: false
  agent_roster: [backend-architect, qa-engineer, knowledge-manager]
  estimation_note: >
    Complexity S/M/L/XL is the primary sizing signal (file-impact + dependency count).
    Hour estimates are coarse anchors (S≈3h / M≈6h / L≈10h); the project's canonical
    effort axis is token-based (ai-native-estimator) — collected at phase-d-closer, not
    re-derived here.
  co_dependency_note: >
    TG-1 (container) and TG-2 (Layer 1) are co-dependent — the Step-1 regex fires before
    input load, so a container-only ship stays 100% S_FAIL. They MUST land in one
    integrated change. TG-1.7 (assertion false-green fix) is RED-first before container
    fetch is exercised.

# ---------------------------------------------------------------------------
# TG-1 — Container side (initial.sh + compute-assertions.sh) — requires image rebuild
# ---------------------------------------------------------------------------
tasks:
  - id: TASK-001
    parent: "1.1"
    task_group: TG-1
    title: "Align Step 1 ISSUE_ID regex to accept ARIA-<repo>-<number>"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: []
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh   # regex at :106
      - aria-orchestrator/docker/aria-runner/tests/             # regex accept/reject test
    verification:
      - "AC-2: accepts ARIA-<repo>-<number>; still rejects bare numeric; DEMO-/TEST- preserved (namespace-disjoint)"
    notes: "Regex is baked into the deployed M5 image (byte-confirmed :106). Letter-prefix invariant."

  - id: TASK-002
    parent: "1.2"
    task_group: TG-1
    title: "Step 2 dual-mode input resolution (file vs always-fetch)"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-001]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh
      - aria-orchestrator/docker/aria-runner/tests/
    verification:
      - "DEMO-/TEST- + file exists → validated file read (non-empty + YAML-parseable, NO silent fallback; parse failure = explicit error)"
      - "ARIA- prefix → always-fetch, ignores any pre-existing file (kills node-pinned stale-read)"

  - id: TASK-003
    parent: "1.3"
    task_group: TG-1
    title: "Fetch title/body from ISSUE_URL + read metadata from Nomad META"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-002]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh   # container has NO fetch code today (PAT only for clone :251 + PR API :396)
      - aria-orchestrator/docker/aria-runner/tests/
    verification:
      - "AC-3: title/body fetched from ISSUE_URL with FORGEJO_BOT_PAT"
      - "AC-5: target_repo/base_branch/files_hint read from Nomad META (not global env)"

  - id: TASK-004
    parent: "1.4"
    task_group: TG-1
    title: "Fetch validation + retry classification (retriable vs non-retriable)"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-003]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh
      - aria-orchestrator/docker/aria-runner/tests/
    verification:
      - "AC-3: HTTP 2xx + legitimate JSON (Content-Type/schema check rejects CF-Access pseudo-success) + non-empty body"
      - "retriable (timeout/5xx/429) → bounded exponential backoff (finite) then give up; non-retriable (404/401/pseudo-success/empty) → immediate INPUT_FETCH_FAILED"
      - "NEVER `|| true`, never silent continue; retriable-exhausted vs non-retriable logged distinctly"
    notes: "168h transient-blip resilience rationale. Feeds C.4 fetch-failure marker (TASK-010)."

  - id: TASK-005
    parent: "1.5"
    task_group: TG-1
    title: "Title/body sanitization pipeline"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-003]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh   # envsubst whitelist at :286
      - aria-orchestrator/docker/aria-runner/tests/
    verification:
      - "AC-7: YAML-safe escape + CRLF→LF + length cap (truncate marker) + injection isolation (wrap as user-content-not-instructions)"
      - "AC-7: body flows through existing envsubst whitelist ($ARIA_ISSUE_TITLE/$ARIA_ISSUE_DESCRIPTION) and is NOT re-expanded"

  - id: TASK-006
    parent: "1.6"
    task_group: TG-1
    title: "base_branch from META with Forgejo default_branch fallback"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: [TASK-002]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh
      - aria-orchestrator/docker/aria-runner/tests/
    verification:
      - "AC-5: base_branch taken from Nomad META; container fallback to Forgejo repo default_branch API; NEVER hardcode master"

  - id: TASK-007
    parent: "1.7"
    task_group: TG-1
    title: "RED-first: reproduce empty-expected_changes false-green at real initial.sh call-site; fix compute-assertions"
    status: pending
    complexity: M
    est_hours: 6
    agent: qa-engineer
    dependencies: []
    deliverables:
      - aria-orchestrator/docker/aria-runner/lib/compute-assertions.sh   # false-green at :94-120 (HIT init true, empty loops skip)
      - aria-orchestrator/docker/aria-runner/tests/compute-assertions/   # fixtures dir has ca-01..ca-07 — add empty-expected fixture as ca-08
    verification:
      - "AC-4(a): RED test at the REAL initial.sh call-site proves pre-fix empty-expected false-green, then passes"
      - "empty expected_file_touched[]/expected_diff_contains[] → emit unknown/skip, NEVER default true"
    notes: "OBJECTION ② (qa). File-mode defense-in-depth (upstream validator enforces non-empty in file mode). RED-first is a hard gate before fetch exercised."

  - id: TASK-008
    parent: "1.8"
    task_group: TG-1
    title: "Fetch mode skips compute-assertions.sh call entirely"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: [TASK-002, TASK-007]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh   # call site :514 (guarded by -f at :513)
    verification:
      - "fetch mode has no issue.yaml → compute-assertions.sh must be SKIPPED, not fed a stub; skip wired at initial.sh:513-515"
      - "RED test MUST exercise the REAL initial.sh call-chain (not isolated compute-assertions.sh): compute-assertions.sh exits 1 internally (guard :37, exit :39), but the call at :514 has `| tail -5 || true` (under set -euo pipefail :26) that SWALLOWS it — container does NOT die there. The externally-observable pre-fix failure is: assertion-results.json never written → FILE_TOUCHED_HIT/DIFF_CONTAINS_HIT stay false (:517-518) → 5-AND fails → FINAL_OUTCOME=ASSERTION_MISMATCH (:534) → exit 1 (:595) → S_FAIL(CONTAINER_CRASH). Post-fix: AUTONOMOUS_COMPLETED → exit 0."
    notes: >
      R1 qa: naive empty-guard would collide with state machine → ASSERTION_MISMATCH → exit1 → S_FAIL in a new form. Skip, don't stub.
      mid_post_spec dogfood (2026-07-04): corrected "dies at :37" imprecision — the die is swallowed by `|| true`; real fail path is ASSERTION_MISMATCH downstream, NOT a crash at the call. An isolation-only RED test (compute-assertions.sh with a bad path) would pass in both pre/post-fix states → white-washes the fix (feedback_test_mock_pattern_hides_prod_bug). Same real-call-site risk as TASK-007.

  - id: TASK-009
    parent: "1.9"
    task_group: TG-1
    title: "Define AUTONOMOUS_COMPLETED outcome (fetch mode)"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-008]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh   # 5-AND at :524-536; exit map :591-596
      - aria-orchestrator/docker/aria-runner/tests/
    verification:
      - "AC-4(b): AUTONOMOUS_COMPLETED = claude_exit==0 AND commit AND PR (three conditions, no file/diff hits)"
      - "AUTONOMOUS_COMPLETED → exit 0 → S6_REVIEW→S9 (does NOT dead-end at ASSERTION_MISMATCH/S_FAIL)"
    notes: "Distinct explicit branch, not a byproduct of the 5-AND. Amends AD-M1-4."

  - id: TASK-010
    parent: "1.10"
    task_group: TG-1
    title: "Emit outcome-class stderr marker on the channel Layer 1 reads"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-004, TASK-009]
    deliverables:
      - aria-orchestrator/docker/aria-runner/modes/initial.sh
      - aria-orchestrator/docker/aria-runner/tests/
    verification:
      - "success: distinguish AUTONOMOUS_COMPLETED vs file-mode SUCCESS via stderr marker"
      - "fetch failure (C.4): emit INPUT_FETCH_FAILED marker + exit 1"
      - "channel = stderr grepped by get_alloc_logs (redo.sh precedent, alloc_status_provider.py:251); DO NOT rely on result.json (Layer 1 never reads it; cross-node-unreadable)"
    notes: "Contract handshake with TASK-016 (Layer 1 consumer). Marker string must match on both ends."

  # -------------------------------------------------------------------------
  # TG-2 — Layer 1 side (extension.py + schema migration) — same scope as TG-1
  # -------------------------------------------------------------------------
  - id: TASK-011
    parent: "2.1"
    task_group: TG-2
    title: "Add additive nullable columns + seed writes them"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: []
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/migrations/008_schema_vNEXT_additive.sql
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/schema_migrate.py  # _MIGRATIONS list + _LATEST_SCHEMA_VERSION @ :58-67 (register new tuple + bump — else existing prod DB no-op-skips @ :105)
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/schema.sql        # schema_meta version (currently 5.0)
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/extension.py      # _phase1_scan_and_seed @ :1110
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "AC-12: additive nullable columns raw_issue_number/target_repo/base_branch/files_hint + outcome_class (NOT optional — TASK-016 is the single carrier and has no migration of its own, so the column MUST land here) on dispatches; established migrations/00N_additive.sql pattern (M3 v2/M4 v3/M5 precedent)"
      - "post_planning R1 (backend-architect): new to_version tuple registered in schema_migrate.py _MIGRATIONS + _LATEST_SCHEMA_VERSION bumped (else apply_migrations no-op-skips on existing prod DB @ :105)"
      - "seed (_phase1_scan_and_seed) writes them; historical rows (NULL) degrade gracefully"
    notes: >
      Migration file: next numeric prefix is 008 (007 is latest). CONFIRM in B: exact schema_version
      label + to_version MUST bump (memory feedback_schema_migration_to_version_bump: current==_LATEST →
      silent no-op skip; SQLite has no ADD COLUMN IF NOT EXISTS). Pair each new column with a dataclass
      field + from_row() (memory feedback_schema_column_dataclass_field_pair).

  - id: TASK-012
    parent: "2.2"
    task_group: TG-2
    title: "Dispatch ISSUE_ID = ARIA-<repo>-<number> (composite value)"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: [TASK-011]
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/extension.py   # issue_id build @ :1176
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "AC-5: letter-prefixed; uses issue NUMBER not internal id; <repo> component prevents cross-repo number collision"

  - id: TASK-013
    parent: "2.3"
    task_group: TG-2
    title: "Extend Nomad META builder with persisted target_repo/base_branch/files_hint"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: [TASK-011]
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/extension.py   # _handle_s4_launch / build_nomad_meta
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "AC-5: META fields read from persisted columns (2.1) via dispatch_row — NOT global env; each tiny (well within 100KB/field META cap, prompt_render.py:42)"
      - "AC-12 (read half): _handle_s4_launch reads the persisted columns from dispatch_row (pairs with TASK-011 write half)"

  - id: TASK-014
    parent: "2.4"
    task_group: TG-2
    title: "Rebuild ISSUE_URL from persisted raw_issue_number + target_repo columns"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-011]
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/extension.py   # ISSUE_URL @ :2147-2152
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "AC-11: ISSUE_URL = {forgejo_base}/{target_repo}/issues/{raw_number} from persisted columns — NOT parsed from composite issue_id, NOT hardcoded FORGEJO_ORG/FORGEJO_REPO env"
      - "AC-11: verified on an issue where id != number (proves fix not masked by early id==number cases); cross-repo verified structurally by unit test (live cross-repo deferred if 168h seed is single-repo)"
    notes: "R1 4-agent Major. Current bug: interpolates internal cross-repo id + hardcoded org/repo → /issues/ARIA-Aria-147 = 404, breaking the fetch centerpiece."

  - id: TASK-015
    parent: "2.5"
    task_group: TG-2
    title: "Unify head_branch = aria/{issue_id} with container BRANCH"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: [TASK-012]
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/extension.py   # head_branch @ :2989
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "container BRANCH=aria/${ISSUE_ID} must match under new ARIA-<repo>-<number> scheme; S6_REVIEW PR binding intact"

  - id: TASK-016
    parent: "2.6"
    task_group: TG-2
    title: "Consume container outcome-class marker + add FailReason.INPUT_FETCH_FAILED + persist outcome_class (fail-closed)"
    status: pending
    complexity: L
    est_hours: 10
    agent: backend-architect
    dependencies: [TASK-010, TASK-011]
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/extension.py            # _handle_s5_await @ :2593-2640
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/interfaces.py           # FailReason enum @ :67 (full :67-102; NO audit/ — mid_post_spec dogfood 2026-07-04 fix; no INPUT_FETCH_FAILED member yet)
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/alloc_status_provider.py # get_alloc_logs @ :251
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "AC-6: exit_code!=0 → read marker → route FailReason.INPUT_FETCH_FAILED DISTINCT from CONTAINER_CRASH (infra-fail vs agent-fail)"
      - "AC-4(c): exit_code==0 → read marker to distinguish AUTONOMOUS_COMPLETED vs file-mode SUCCESS → PERSIST outcome_class into DB (single carrier)"
      - "AC-4(d) fail-closed: marker absent/malformed on exit 0 → outcome_class=UNKNOWN (NOT SUCCESS); fixture covers absent + malformed"
    notes: >
      R1 qa Critical + R2 backend-architect Critical A. SINGLE CARRIER (R3 tech-lead M-1): use the outcome_class
      column added by TASK-011 (now UNCONDITIONAL per post_planning R1) — NOT dispatch_audit_log payload. This task
      has no migration of its own, so the column MUST come from TASK-011 (post_planning R1 qa: else stuck, no escape).
      Column is simpler to stratify in acceptance SQL. This is the biggest task.

  - id: TASK-017
    parent: "2.7"
    task_group: TG-2
    title: "Make Spec #2 acceptance query outcome-class-aware (cross-Spec)"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-016]
    deliverables:
      - aria-orchestrator/acceptance/check-m6-e2e-acceptance.py   # total_s9 = COUNT(*) WHERE state='S9_CLOSE'
      - aria-orchestrator/tests/acceptance/test_m6_e2e_acceptance.py   # existing TestDispatchStrat — add case per verification
    verification:
      - "AC-4(c): AUTONOMOUS_COMPLETED excluded from verified-SUCCESS counts / stratified separately"
      - "query on state='S9_CLOSE' alone must NOT conflate AUTONOMOUS_COMPLETED with verified SUCCESS"
      - "post_planning R1 (qa): add a case to existing test_m6_e2e_acceptance.py::TestDispatchStrat asserting a mixed-in outcome_class='AUTONOMOUS_COMPLETED' row is excluded/stratified (code-only edit = paper-fix of the very corpus-poisoning defense this Spec exists for)"
    notes: >
      CROSS-SPEC coordination — edits Spec #2 (aria-2.0-m6-e2e-resilience) file, which is not yet archived.
      Delivered by THIS Spec (it is the mechanism that makes the label load-bearing). Without it R2 Critical A
      recurs at the acceptance layer. post_planning R1 (km): coordinate by checking recent commits / open branches
      on check-m6-e2e-acceptance.py before editing — do NOT rely on #138 (its scope is Spec #2 TG-B crash-recovery,
      NOT this TG-A acceptance query; Spec #2 tasks.md A-acceptance items lag the already-landed code).

  # -------------------------------------------------------------------------
  # TG-3 — Key format value migration + query survey
  # -------------------------------------------------------------------------
  - id: TASK-018
    parent: "3.1"
    task_group: TG-3
    title: "Reformat issue_id VALUE to ARIA-<repo>-<number> (value-level, key not restructured)"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-012]
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/schema.sql   # issue_id TEXT, PK (issue_id,dispatch_id), partial-unique uq_issue_active_partial @ :61,245,273
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "AC-8: value-level reformat; key itself NOT restructured (no PK/index change); partial-unique-active invariant preserved on the string"
    notes: "Distinct from TG-2.1 additive columns (those are separate additive migrations, not a key change)."

  - id: TASK-019
    parent: "3.2"
    task_group: TG-3
    title: "Decide + document clean-DB vs historical-migration for existing issue_id rows"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: [TASK-018]
    deliverables:
      - openspec/changes/aria-2.0-m6-dispatch-input-delivery/tasks.md   # §3.2 decision note (main-repo Spec), or the 008 migration header comment
    verification:
      - "clean-DB vs historical-migration decision recorded with rationale"

  - id: TASK-020
    parent: "3.3"
    task_group: TG-3
    title: "Survey every acceptance/dispatch query keying on issue_id"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-018]
    deliverables:
      - aria-orchestrator/acceptance/           # survey all *.py/*.sql keying on issue_id
      - aria-orchestrator/hermes-extensions/aria-layer1/aria_layer1/db.py
    verification:
      - "AC-8: every issue_id-keyed query tolerates new format"
      - "AC-8: #147 issue_type_hint stratification UNAFFECTED — it reads json_extract('$.issue_type_hint') from dispatch_audit_log.payload_json (db.py:622), NOT an issue_id join"

  # -------------------------------------------------------------------------
  # TG-4 — Image rebuild + freeze (gates on TG-1 only)
  # -------------------------------------------------------------------------
  - id: TASK-021
    parent: "4.1"
    task_group: TG-4
    title: "Rebuild aria-runner via aether-build-container after TG-1 lands"
    status: pending
    complexity: M
    est_hours: 6
    agent: backend-architect
    dependencies: [TASK-001, TASK-002, TASK-003, TASK-004, TASK-005, TASK-006, TASK-007, TASK-008, TASK-009, TASK-010]
    deliverables:
      - aria-orchestrator/docker/aria-runner/           # image build via /aether:aether-build-container
    verification:
      - "AC-9: only container-side code baked into image (TG-2/TG-3 are Hermes/light-1, deployed separately); pushed to internal registry"
    notes: >
      Gates on TG-1 ONLY. Uses aether-build-container skill (Aria #120 GA path). image_sha256 digest — NOT git
      short-sha (m5-handoff F1 bug, PR #30). post_planning R1 (tech-lead): aether-build-container is an
      OWNER-TRIGGERED primitive — this is a 2nd owner gate (besides S7 merge); model as owner-action (AI preps,
      owner triggers build), task stays in_progress after AI prep (feedback_t15_owner_blocking_pattern) so
      phase_4→phase_5 does not silently stall.

  - id: TASK-022
    parent: "4.2"
    task_group: TG-4
    title: "Capture immutable image_sha256 + freeze single IMAGE_SHA for 168h run + record rollback"
    status: pending
    complexity: S
    est_hours: 3
    agent: backend-architect
    dependencies: [TASK-021]
    deliverables:
      - aria-orchestrator/docs/           # frozen IMAGE_SHA + rollback (old sha) record
    verification:
      - "AC-9: single frozen IMAGE_SHA recorded for entire 168h run (mid-run hotfix would confound AC-5/AC-6 corpus); rollback path (old sha) documented"

  # -------------------------------------------------------------------------
  # TG-5 — Contract + doc sync (Rule #3 — delivery items, can parallel TG-3)
  # -------------------------------------------------------------------------
  - id: TASK-023
    parent: "5.1"
    task_group: TG-5
    title: "Write AD-M6-10 (six-section) — C' dual-channel + single-node bind-mount scope"
    status: pending
    complexity: M
    est_hours: 6
    agent: knowledge-manager
    dependencies: []
    deliverables:
      - aria-orchestrator/docs/architecture-decisions.md
    verification:
      - "AC-10: AD-M6-10 written (six-section: decision/background/alternatives/rationale/risks/rollback); includes single-node scope of bind-mount input assumption"
    notes: "AD-M6-10 reserved (next available; AD-M6-8 Retired, AD-M6-3 skipped)."

  - id: TASK-024
    parent: "5.2"
    task_group: TG-5
    title: "Correct the AD4 risk-table cell (:384) mislabel"
    status: pending
    complexity: S
    est_hours: 3
    agent: knowledge-manager
    dependencies: [TASK-023]
    deliverables:
      - aria-orchestrator/docs/architecture-decisions.md   # cell @ :384
      - docs/decisions/DEC-20260702-001-layer2-input-delivery-architecture.md   # main-repo; forward-correction footnote at amend-AD-M0-5 lines (22/69/88) — see notes
    verification:
      - "AC-10: fix the 'AD-M0-5 约定' mislabel + scope bind-mount premise to single-node + xref AD-M6-10"
      - "DO NOT touch the AD-M0-5 body (:1035, m0-handoff schema — unrelated decision)"
      - "post_planning R1 (km): add forward-correction footnote to DEC-20260702-001 lines 22/69/88 (they still say 'amend AD-M0-5' — the misattribution this Spec debunks)"
    notes: >
      R1 2-agent Critical/Major. post_planning R1 (km): proposal says "DEC line 22 corrected here" but
      DEC-20260702-001 still says "amend AD-M0-5" at lines 22/69/88 (the very misattribution this Spec debunks).
      Add a forward-correction footnote at those DEC lines (feedback_inline_deferral_notation pattern): the
      bind-mount premise is an AD4-cell mislabel, not an AD-M0-5 decision — see AD-M6-10 + this AD4 correction.
      (Do NOT touch AD-M0-5 body.)

  - id: TASK-025
    parent: "5.3"
    task_group: TG-5
    title: "Amend AD-M1-4 — scope 5-AND to file mode; document AUTONOMOUS_COMPLETED + INPUT_FETCH_FAILED"
    status: pending
    complexity: M
    est_hours: 6
    agent: knowledge-manager
    dependencies: [TASK-023]
    deliverables:
      - aria-orchestrator/docs/architecture-decisions.md   # AD-M1-4 body @ :1360
    verification:
      - "AC-10: scope 5-AND SUCCESS to file mode; document AUTONOMOUS_COMPLETED (assertion_verified:false) + INPUT_FETCH_FAILED"
    notes: >
      R2 km caveat: AD-M1-4 body (:1360) has pre-existing doc/code drift (records a 9-enum/6-AND
      entrypoint-m1.sh version vs current 5-AND initial.sh:524) — verify the AD's CURRENT literal content
      before editing to avoid conflating the two generations.

  - id: TASK-026
    parent: "5.4"
    task_group: TG-5
    title: "Add layer-boundary-contract.md §5 'Task Content Delivery Mechanism'"
    status: pending
    complexity: M
    est_hours: 6
    agent: knowledge-manager
    dependencies: []
    deliverables:
      - aria-orchestrator/docs/layer-boundary-contract.md
    verification:
      - "AC-10: §5 dual-channel field schema table (file vs fetch) + file-mode lifecycle declaration"
      - "post_planning R1 (km): also bump the file's **Last Updated** header + add §5 to the ## Overview enumeration sentence (else the new section is orphaned from the overview)"
    notes: "Pre-existing gap — contract currently says nothing about how the container obtains full issue content."

  - id: TASK-027
    parent: "5.5"
    task_group: TG-5
    title: "Update CLAUDE.md M6 status — input-delivery ↔ telemetry dependency chain"
    status: pending
    complexity: S
    est_hours: 3
    agent: knowledge-manager
    dependencies: [TASK-023, TASK-025, TASK-026]
    deliverables:
      - CLAUDE.md   # main repo M6 status section (line ~529)
    verification:
      - "AC-10: input-delivery ↔ telemetry dependency chain recorded"
    notes: >
      HIGH-CONTENTION region — fetch before edit (memory feedback_claude_md_project_status_high_contention);
      project-status is live-overwrite not append (claude-md-hygiene Option A). Concurrent-write safety
      (standards/conventions/concurrent-session-write-safety.md).

  # -------------------------------------------------------------------------
  # TG-6 — E2E dogfood + pre-run verification (gates on TG-1+2+3+4 deployed)
  # -------------------------------------------------------------------------
  - id: TASK-028
    parent: "6.1"
    task_group: TG-6
    title: "Live-test heavy-node Forgejo egress/auth (fetch reachability) before any run"
    status: pending
    complexity: S
    est_hours: 3
    agent: qa-engineer
    dependencies: [TASK-021]
    deliverables:
      - aria-orchestrator/           # live probe (one-shot nomad docker job or heavy-node exec)
    verification:
      - "heavy-node → Forgejo egress + auth reachable; fetch returns legitimate JSON (not CF-Access pseudo-success)"
    notes: "Risk mitigation — fetch adds a new container outbound dependency (§Risks). Secret hygiene Rule #7: probe uses metadata, not secret values."

  - id: TASK-029
    parent: "6.2"
    task_group: TG-6
    title: "E2E dogfood: real numeric-id autonomous dispatch → S9_CLOSE with merged PR"
    status: pending
    complexity: L
    est_hours: 10
    agent: qa-engineer
    dependencies: [TASK-013, TASK-014, TASK-015, TASK-016, TASK-017, TASK-020, TASK-022, TASK-028]
    deliverables:
      - aria-orchestrator/           # E2E dogfood run evidence (dispatch row → S9_CLOSE, merged PR)
    verification:
      - "AC-1 (THE gate): autonomous dispatch of a real numeric Forgejo issue reaches S9_CLOSE with a merged PR — E2E, not unit-only"
    notes: >
      Gates on TG-1+TG-2+TG-3+TG-4 all deployed (container image + Layer1 to Hermes/light-1 + schema migration).
      NOT scorable for AC-5/AC-6 until telemetry Spec ships (Out of Scope) and Blocker 4 (Luxeno latency)
      cleared — but AC-1 loop-closure itself is provable here. smoke pattern: SQL-inject target state + dummy
      PR if needed (memory feedback_smoke_dispatch_sql_inject_pattern).

  - id: TASK-030
    parent: "6.3"
    task_group: TG-6
    title: "Verify fetch-failure classes distinguishable to Layer 1 (infra-fail vs agent-fail)"
    status: pending
    complexity: M
    est_hours: 6
    agent: qa-engineer
    dependencies: [TASK-016, TASK-022, TASK-028]
    deliverables:
      - aria-orchestrator/hermes-extensions/aria-layer1/tests/
    verification:
      - "AC-6: container emits stderr marker → get_alloc_logs → _handle_s5_await routes INPUT_FETCH_FAILED distinct from CONTAINER_CRASH; fixture covers both branches so infra failures don't pollute AC-5 corpus attribution"

# ---------------------------------------------------------------------------
# Agent allocation summary (A.3)
# ---------------------------------------------------------------------------
agent_allocation:
  backend-architect:
    count: 21
    tasks: [TASK-001, TASK-002, TASK-003, TASK-004, TASK-005, TASK-006, TASK-008, TASK-009, TASK-010,
            TASK-011, TASK-012, TASK-013, TASK-014, TASK-015, TASK-016, TASK-017, TASK-018, TASK-019,
            TASK-020, TASK-021, TASK-022]
    rationale: "Container runtime bash logic + Layer 1 Python state machine + schema/migration + query survey + image build."
  qa-engineer:
    count: 4
    tasks: [TASK-007, TASK-028, TASK-029, TASK-030]
    rationale: "RED-first assertion false-green (test-design), egress live-test, E2E dogfood, fetch-failure-class fixtures."
  knowledge-manager:
    count: 5
    tasks: [TASK-023, TASK-024, TASK-025, TASK-026, TASK-027]
    rationale: "ADR (AD-M6-10) + AD4-cell correction + AD-M1-4 amend + layer-boundary-contract §5 + CLAUDE.md — code-grounded per R2 caveats."
  new_agents: []
  note: "21 (backend-architect) + 4 (qa) + 5 (km) = 30. Roster covers full scope, no new agent per tasks.md:80."

# ---------------------------------------------------------------------------
# Execution phasing (topological — respects TG co-dependency + gates)
# ---------------------------------------------------------------------------
execution_order:
  phase_1_integrated_code:
    desc: "TG-1 + TG-2 land together (co-dependent — regex fires before input load). TG-1.7 RED-first up front."
    parallelizable:
      - "TASK-007 (RED-first assertion) — start immediately, gates container fetch exercise"
      - "TASK-001 → TASK-002 → {TASK-003 → TASK-004/005, TASK-006} → TASK-008 → TASK-009 → TASK-010 (container chain)"
      - "TASK-011 → {TASK-012 → TASK-015, TASK-013, TASK-014} (Layer 1 seed/META/URL chain)"
    barrier: "TASK-010 (container marker) + TASK-011 (outcome_class column) → TASK-016 (marker consumption) → TASK-017 (acceptance stratify)"
  phase_2_key_migration:
    desc: "TG-3 — parallel to TG-5; depends on TASK-012 (id format)."
    tasks: [TASK-018, TASK-019, TASK-020]
  phase_3_doc_sync:
    desc: "TG-5 — parallelizable with TG-3; TASK-027 (CLAUDE.md) last (depends on AD finalization)."
    tasks: [TASK-023, TASK-024, TASK-025, TASK-026, TASK-027]
  phase_4_image:
    desc: "TG-4 — gates on TG-1 (container code) ONLY."
    tasks: [TASK-021, TASK-022]
  phase_5_e2e:
    desc: "TG-6 — gates on TG-1+2+3+4 ALL deployed."
    tasks: [TASK-028, TASK-029, TASK-030]

# ---------------------------------------------------------------------------
# Complexity summary
# ---------------------------------------------------------------------------
summary:
  by_complexity:
    S: {count: 11, est_hours: 33}   # 001,006,008,012,013,015,019,022,024,027,028
    M: {count: 17, est_hours: 102}  # 002,003,004,005,007,009,010,011,014,017,018,020,021,023,025,026,030
    L: {count: 2, est_hours: 20}     # 016 (marker consume/persist), 029 (E2E dogfood)
    XL: {count: 0, est_hours: 0}
  total_est_hours: "~155h coarse (token-axis is canonical — see metadata.estimation_note)"
  critical_path: "TASK-001→002→003→004→010 (container) ‖ TASK-011→016 (Layer1) → 016 barrier → 021 image → 029 E2E dogfood (AC-1)"
  highest_risk_tasks:
    - "TASK-016 (L) — state machine + closed-enum extension + DB persist + fail-closed; the load-bearing consumer"
    - "TASK-029 (L) — AC-1 the single most important gate; needs full stack deployed + Blocker 4 tolerance"
    - "TASK-017 — cross-Spec edit into unarchived Spec #2 file (coordinate with #138)"
    - "TASK-007 — RED-first at REAL call-site (isolation-only test masks the call-site gap)"

# ---------------------------------------------------------------------------
# Out-of-scope reminders (do NOT let these silently drop)
# ---------------------------------------------------------------------------
out_of_scope:
  - "Telemetry Spec (container → Layer 1 cost/model via logs marker) — SEPARATE Spec; 168h run not scorable for AC-6 cost gate without it."
  - "Blocker 4 (Luxeno/GLM backend latency 45-54s) — owner/infra/SilkNode (#147 / SilkNode #830). Not code."
  - "manual dispatch tool-chain repair (checklist Menu A) — owner chose autonomous path (Menu B)."
