# pre_merge Completeness Gate 加 change 维度: 按被审 change 匹配报告 + 显式 not_applicable 三态 (Aria #199 / aria-plugin #161)

> **Level**: Minimal (Level 2 Spec)
> **Status**: Draft
> **Created**: 2026-09-06
> **Linked Issue**: `10CG/Aria#199, 10CG/aria-plugin#161`
> **Issue**: [Aria#199](https://forgejo.10cg.pub/10CG/Aria/issues/199) (2026-09-06 立案, aria-report 自动生成; triage 22288: confirmed / major / next-cycle) · [aria-plugin#161](https://forgejo.10cg.pub/10CG/aria-plugin/issues/161) (同缺陷在插件仓的记录, 标题「audit-engine pre_merge completeness gate 按文件名 glob 不按 spec_id 匹配 — 历史 spec 报告使门恒通过 (真实漏拦案例)」, open, 起草时 `forgejo GET` 复核)
> **认领**: 已于 2026-09-06T14:47Z 经 `phase1_gate.py --phase A.1 --mode advisory` 认领, track_id `pre-merge-completeness-gate-change-scope-bfe8285d`, `linked_issue_overlap=[]`, `unknown_schema_claims=0`
> **基线冻结**: aria 子模块 `origin/master` **`301641b`** (= v1.71.1); 本文所有行号对此 SHA (实读副本 = 插件缓存 `~/.claude/plugins/cache/10CG-aria-plugin/aria/1.71.1/`, R1 rework 时对 `execution-modes.md` / `audit-engine/SKILL.md` / `phase-c-integrator/SKILL.md` / `report-storage.md` / `pre-write-validation.md` / `report-format.md` 六文件与 `git show 301641b:` 逐字 diff 为空)。**行号有效范围 (R1 勘正, 原文写成「全部触点文件 diff 为空」过宽)**: `git diff --stat 0545f86 301641b` 对上述**代码/规程六文件**为空, 行号在两 SHA 通用; 但 `CHANGELOG.md` (+71 行, 完整性门条目 @`0545f86` 在 `:2949` / @`301641b` 在 `:3020`)、`.claude-plugin/plugin.json`、`VERSION`、`README*.md`、`state-scanner/scripts/lib/spec_complete.py` (改 25 行, `if name == "SKILL.md":` @`301641b` 是 `:924` / @`0545f86` 是 `:903`) **均非空** —— 这几处的行号**只对 `301641b`** 成立, 追引用必须先 checkout 到 `301641b`。
> **gitlink 现况 (R2 rework 重测 2026-09-07)**: 远端与本地**已全面对齐** —— R1 写的「本地 checkout 仍停在 `0545f86`」「本地 master 与 origin/master 已分叉」「否则主仓同步会把 gitlink 回退到 v1.70.0」三句**均已不成立**, 一并删除 (分叉已在 `ecb6296` 合掉)。实测: `git submodule status aria` = ` 301641b… (v1.71.1)` **无 `+` 前缀**; `git -C aria rev-parse HEAD master origin/master` 三者同为 `301641b`; 主仓 `master` = `origin/master` = `2f7ad0c`; `git ls-tree HEAD aria` = `301641b`; 工作区干净。同伴 v1.71.1 的主仓同步 PR **#202 已 merged**。Phase B/C 开工前仍应 `git fetch --all` 复核 —— 那是常规卫生, **不是**在处置某个已知的回退风险。Phase B 在 `301641b` 起分支
> **代码落点**: `aria/skills/audit-engine/references/execution-modes.md` (§Pre-merge gate Step 3-5 + 校验失败输出) · `aria/skills/audit-engine/SKILL.md` (输入参数表 + Bash 调用块 + 配置注释) · **新增** `aria/skills/audit-engine/scripts/completeness_gate.py` + `tests/test_completeness_gate.py` · `references/report-storage.md` §向后兼容 · `references/pre-write-validation.md` 关联行 · `aria/skills/phase-c-integrator/SKILL.md` (C.2 pre_hook 传 change_id / repo_path / diff_repo_path / base; `:157` 旧 schema 勘正) · **旧 schema 散文残留同族三处** `phase-a-planner/SKILL.md:267` + `phase-b-developer/SKILL.md:204,277` (与 `:157` 逐字同构, 形态族穷举见 §4 —— R2 rework 补: 原头部只列 `phase-c-integrator` 一处, 与 §4/Tasks 口径分叉, A.3 派单按头部读会漏三处) · `aria/CHANGELOG.md` + 版本引用点 · `aria-plugin-benchmarks/ab-suite/audit-engine.json` (+1 定向 eval) + `version.yaml`。Spec 落主仓 (Rule #5)
> **Rule #6 判定**: `execution-modes.md` Step 3-5 与 SKILL.md 调用块是**处方性 · 运行时指令面** (audit-engine 规程由 AI 编排执行)。**档位争议不影响执行面 (R1 rework)**: 一席判第二行 (照跑 AB)、两席判第三行 (套件覆盖外 + substitute), 本 spec **取两读法的并集, 不取任何豁免** —— 既照跑两个真 eval 套件验漂移 (照跑面 R2 rework 已收敛, 见下), 又补定向 fixture + 缺口 issue (rule6_note 见文末; 档位标签本身列 待 owner 复议 #5)。相关套件实读: `ab-suite/audit-engine.json` 文件内 `version` 字段 = **`1.0.0`** (2 evals, 全是竞品探针场景, `grep -i "completeness\|missing_checkpoint\|allow_incomplete"` 零命中) 与 `ab-suite/phase-c-integrator.json` (3 evals) 是**真 eval 套件, 两者照跑**。`ab-suite/phase-c-integrator-pre-merge-gate.json` **不是 eval 套件** (R2 rework 三席逐字实读 + 本轮复核: `type: "workflow_skill_subextension"`, **无 `evals` 键**, 8 条 `fixtures[]` 每条带 `test_case_in_unit_tests` 指向 `test_pre_merge_gate.GateCheckTests.*`; `version.yaml` 的 `total_eval_cases` 按 `sum(len(evals))` 对它计 **0** ⇒ 对 0 个 eval 断言「无回归」恒真 = 假证据), 且其 `spec`/`issue` 指向 C.2.4 CI 门 (Aria#60) 而非本 spec 改的 audit-engine pre_hook ⇒ 在它上面「跑 with/without 臂」**无结构对应物**, 改为**跑其 8 条 fixture 绑定的单测**并在 rule6_note 点名形态差异 (见 待 owner 复议 #9)。**注**: `ab-suite/version.yaml` 的**套件**版本现值 **1.5.0** (R2 rework 重取实测, `last_modified: 2026-09-05`), 与 `audit-engine.json` 文件内 `version` 是两个不同的 `version`, 全文点名区分。脚本 + schema + 勘正 = 描述性 ⇒ substitute = **SC-1~SC-10 + SC-13 + SC-15~SC-17 + SC-19~SC-22** (与文末 rule6_note 逐字同集 —— R2 rework 勘正: 原写「SC-1~SC-13」把 SC-11 活体 / SC-12 既有测试 / SC-14 AB 本体卷进 substitute, 又漏掉 R1/R2 新增的 SC-15~SC-17 / SC-19~SC-22, 使 Rule #6 合规证据面自身不自洽)
> **A.1.0 头脑风暴**: 未跑 — `audit.checkpoints.post_brainstorm = off` (Rule #10 白名单第一类)
> **审计计划**: post_spec convergence 5 席 (config enabled) → post_planning convergence (config enabled); mid_implementation / post_implementation / pre_merge / post_closure 均 config off (白名单第一类)

> **基线复核 (2026-09-10, 周限额中止后续跑前)**: 同伴容器在 09-07…09-09 期间把 aria 推进到 **v1.73.0** (`f314785`), 主仓 gitlink 已随之前进 (本地已 merge 到 `c115fd4` 并双推 MATCH)。实测 `git -C aria diff --stat 301641b f314785 -- <本 spec 全部触点文件>` **输出为空** —— 期间 21 个文件变更、895 增 63 删, 但**没有一个落在本 spec 的触点上** ⇒ 本文全部行号在 `f314785` 上继续有效, 基线冻结不需重取。**版本号目标随之顺延**: 原写 v1.71.2 已被 v1.72.x / v1.73.0 越过, 本 spec 的 PATCH 候选改为 **v1.73.1**; bump 前仍须 `git -C aria ls-remote --tags origin` 复核并读同伴 handoff 的 `<vNEXT>`。
> **审计轨迹**: post_spec R1 (2026-09-06) 票型 REVISE 5 / PASS 0 (单席 verdict FAIL 4 + PASS_WITH_WARNINGS 1), 缺陷计数 **C5 / M9 / m10** (另 7 条 decision 不计入), 聚合报告 `post_spec-R1-2026-09-06T155000-000Z-R1-pre-merge-completeness-gate-change-scope-aggregated.md` ⇒ **v2**; 三条 conflicted / 档位分歧转 待 owner 复议 #5-#7。post_spec R2 (2026-09-07) 票型 **REVISE 5 / PASS 0** (单席 verdict FAIL 3 + PASS_WITH_WARNINGS 2), 缺陷计数 **C2 / M9 / m16** (另 12 条 decision 不计入; **conflicted 0**), 聚合报告 `post_spec-R2-2026-09-07T044500-000Z-R2-pre-merge-completeness-gate-change-scope-aggregated.md` ⇒ 本文为 **v3 (R2 rework 后; 执笔席非原作者、非本轮任何审计席位, 依 memory `feedback_author_and_verifier_must_differ_for_corrections`)**, 逐条处置记录见 rework 清单; 新增 待 owner 复议 **#8-#9**

---

## Why

### 症状 (issue + triage 复现 case, 起草时对 `301641b` 逐条复核)

`audit-engine` 的 pre_merge **Checkpoint Report Completeness Gate** (`execution-modes.md:23-82`, 2026-04-23 #26 落地) 判「该跑的 checkpoint 都跑了」, 但 Step 4 (`:54-61`) 的匹配模式是:

```
- {checkpoint_name}-*.md         (无 change_id 变体)
- {checkpoint_name}-*-*.md       (含 change_id 变体)
任意文件匹配 → 该 checkpoint 通过
```

两条通配都不含被审 change 的任何标识; Step 5 (`:63-65`) 只看 `missing_checkpoints` 是否为空。于是:

| case | 输入 | 实况 (起草时实读/实跑) | 判定 |
|---|---|---|---|
| case-1 (SOT 文本) | `execution-modes.md:54-61` | 与 issue 引用逐字一致; 全节不出现 `change_id` / `spec_id`; Step 3 (`:46-52`) 的排除清单 (pre_merge / post_closure / mid_post_spec) 只处理「启用但合法不产出 → 误阻」, 对「误放」零条款 | match |
| case-2 (本仓语料模拟) | 本仓 `.aria/audit-reports/` 780 条目 (778 `.md` + 2 目录; **R1 rework 时已长到 786 `.md`** —— 本轮 post_spec 五席 + 聚合各写 1 份, 语料是活体, 见 SC-2 的冻结产物要求), 假想新 change (自身零份报告) | `post_spec-*.md` 命中 **499** / `post_planning-*.md` **209** / `post_implementation-*.md` **3** (归属 `state-scanner-mechanical-t3` / `aria-secret-guard-plugin-default-orchestrator` / `audit-drift-guard`); 属于假想 change 的 **0** 份 → 按原文三者均「通过」 | match |
| issue 报告方 | 37 份他人 `post_implementation-*.md`, 被审 change `synth-honesty-gaps` Phase A-only 自身 0 份 | 门 PASS; 编排者靠人工核对报告归属才发现 PASS 无效, 改按「Phase A-only ⇒ post_implementation 不适用」独立论证放行 (**没有采信门的绿**) | 同型 |

**根因有两层**:

1. **匹配面无 change 维度**: 门问的是「目录里有没有这个 checkpoint 的任何报告」, 与被审 change 无关。项目历史越长越必然 PASS —— 与它要防的失效 (「该跑的没跑」) 同型: 零相关证据也判绿。
2. **change_id 在 pre_merge 根本没被投递**: `audit-engine/SKILL.md:49-54` 输入参数只有 `checkpoint / mode / context / agents_config`, 没有 `change_id`; `report-storage.md:18` 说 `{spec_id}` 「从 dispatch context」取, `pre-write-validation.md:14` 说 change_id「从调用方 context 读取」—— 而 phase-c-integrator 在 pre_merge 传的 `context` 是 **`PR diff (branch_name vs base)`** (`phase-c-integrator/SKILL.md:136`; `:137` 是「5. 处理 verdict」—— R1 勘正, 原写 `:137`), 不是 spec 路径。post_spec 那条链是通的 (`phase-a-planner/SKILL.md:250` 传 `openspec/changes/{spec_id}/proposal.md`; R1 勘正, 原写 `:248`), pre_merge 这条链结构上就没有 change_id 可用。Step 4 不带 change 维度不是疏忽, 是**没有输入**。

### 起草期实读补充的事实 (issue / triage 没写, 改变修法形态)

| # | 事实 | 出处 | 含义 |
|---|---|---|---|
| F1 | 本仓 `audit.checkpoints.pre_merge = "off"` | `.aria/config.json` audit 块 (起草时 `python3 -c json.load`) | 该门在 Aria 自身**从不执行** (触发条件 `execution-modes.md:32` 仅 pre_merge)。triage 注「本仓 pre_merge 目前只会把 post_spec 纳入校验」漏了这一层; 且本仓 `post_planning = convergence`, 若开 pre_merge, Step 3 会纳入 post_spec **和 post_planning** 两项。⇒ 本 spec 的活体验证只能手动跑脚本 (SC-11), 不能靠生产 pre_merge 调用 |
| F2 | 真实文件名**不服从** `report-storage.md:8` 的 5-field schema | 本仓语料 `ls .aria/audit-reports \| sed 's/[0-9]+/N/g' \| uniq -c`: 时间戳段有 epoch ms (`1787379154696`) / ISO (`2026-05-23T0900Z`) / 纯日期 (`2026-05-23`) / 小整数 (`R4-4-…`); round 段有 `R5.5` / `R1prime` / `FINAL`; role 段有席位前缀 `A1-tech-lead`、`aggregated` / `aggregate` / `orchestrator` / `usN-mN`; `post_spec-*` 499 份中 24 份不匹配 `-R\d+-`, post_planning 2 / pre_merge 1 / mid_implementation 1 | triage 修法 1 写的 `{checkpoint_name}-*-{change_id}-*.md` 按**位置**取 spec_id 不可靠; 匹配必须是**连字符界定的子串**, 不解析段序。**R1 勘正**: 原文写「子串 `-{change_id}-` … 不依赖前后段形态」自相矛盾 —— 双侧连字符恰恰依赖「id 后面还有一段」。真实语料里 id 落**末段** (`…-{id}.md`, 无 role 段) 的报告有 **62 份**, 故归属谓词必须**左侧连字符界定 + 右侧连字符或字符串末尾**二选一 (见 §1.2 规则 2)。先例同向: state-scanner `collectors/audit.py:62-69` 只合成**首**连字符做左侧界定, 正是为了不假设尾段存在 (`_CHECKPOINT_PREFIX` + `"-" + name[m.end():]`) |
| F3 | change_id 集 `C` = **152** 个, **分解式随快照变** (R2 rework 钉 SHA): @冻结快照 `3f4b379` = `openspec/changes` **9** 目录 + `openspec/archive` **143** 目录 (archive 下共 144 个条目, 多出的是 `README.md` 不是 change); @`813e82c` 与 @HEAD (2026-09-07) = **8 + 144** (archive 145 条目), 因 `a1-entry-claim-duplicate-work-guard` 由 changes 迁入 archive ⇒ **总数守恒 152**。R1 写的 9+143 对冻结快照成立、对 R1 rework 落盘树已过期, 本轮改为两个快照都写明。其中**前缀碰撞 1 对** (`aria-orchestrator` ⊂ `aria-orchestrator-divestiture`), **后缀碰撞 0 对, 中缀碰撞 0 对** (R1 全枚举 + R2 复算一致) | R1 rework python 全枚举 + R2 rework 对 `3f4b379`/HEAD 各复算一次 | 子串规则须加「有界包含排除」: 存在更长的 `c' ∈ C` 使 `-{id}-` 是 `-{c'}-` 的子串时, 命中 `c'` 的文件**不计入** `id`。现存实例只有前缀型, 但规则发给所有采用方 ⇒ 谓词须对称覆盖前缀/后缀/中缀三型 (见 §1.2 规则 3) |
| F4 | 旧 schema `{checkpoint}-{timestamp}.md` 无 spec_id, 本仓实测 **6 份**; 而「不含任何 `-{c}-` 子串」的报告实测 **238 份** (= 6 份真 legacy + 62 份 id 落末段 + **170 份 id 段不在 `C`**: 截断/别名/自造 id, 如 `post_planning-R2-CONVERGED-dispatch-input-delivery.md` 之于 `aria-2.0-m6-dispatch-input-delivery`) | `report-storage.md:37` (reader 视为 R1/legacy) + R1 rework 全枚举 | 「无 change 段」≠「旧 schema」, 差 **39.7 倍**。故输出必须**拆两个计数**: `excluded_legacy_count` 只统计真 2-field `{checkpoint}-{timestamp}.md`; `unattributed_count` 统计「有 id 段但该 id 不在 `C`」并把文件名列进 audit trail。混成一个数会把归属失败伪装成历史包袱 (R-a 的显影缓解会反向失效) |
| F5 | phase-c-integrator 已有 `not_applicable` 语义 = 「被审对象结构性不存在, 没有可等的 CI」(`phase-c-integrator/SKILL.md:252,260,299`; `path_coverage.py:8,547-549`), 且 C.2.4 步骤 6 规定 green 来源为 not_applicable 时**必须 surface 警告行** (`:265`) | 实读 | 本 spec 的 `not_applicable` 沿用同一语义边界: 只在「对象整个未产生」时产生, 且必须进 audit trail; 与 CLAUDE.md Rule #10 白名单第四类「闸门自身的结构性前提不成立」(`configured-gate-authority.md:38`) 同义, 其边界注「A.2 没做 = 合法; A.2 做了但很简单 = 不合法」(`:40`) 直接约束本 spec 的判据设计 |
| F6 | audit-engine 已有**机械化 diff 判据**: file-scope 二次过滤 `changed_files = git diff --name-only $(git merge-base HEAD <base>)`, 全部 ⊆ `audit.scope_skip_paths` ⇒ docs/ops-only (`audit-engine/SKILL.md:403-419`; `DEFAULTS.json:118-123` 已注册 `scope_skip_paths` = `deploy/` `docs/` `.forgejo/workflows/` `.github/workflows/` `*.md`) | 实读 | **只复用 diff 取法, 不复用 `scope_skip_paths` 谓词 (R1 rework 反转)**: 该键的 SOT 语义唯一 —— `SKILL.md:398-400` #58 DEC-4 明写「**降级非 skip**」, 且实证 deploy 脚本改动 challenge 能抓到真退化。把同一谓词提升为整个 checkpoint 免检, 后果从「少跑一档」变成「零审计通过」; 叠加缺省含 `*.md` 而 aria-plugin 产品本体即 markdown 处方 ⇒ 纯 `.md` Skill 变更会常态「零自身证据也 PASS」= 与被修 bug 同型的新假绿。§1.3(b) 已改用与「对象未产生」同义的判据, `scope_skip_paths` 在本 spec **零消费** |
| F7 | 归档门 liveness 分类器只认 `SKILL.md` 内 **Bash 代码块**里的真调用为 alive, `references/*.md` 一律 prose | `state-scanner/scripts/lib/spec_complete.py:924-930` | 新脚本的调用行必须同时落在 SKILL.md 的 fenced bash 块 (sibling_spec_probe 先例 `audit-engine/SKILL.md:123-125`), 否则 D.2 判 dead-code |
| F8 | `.aria/config.template.json` **无 `audit` 块**; `config-loader/DEFAULTS.json` 的 audit 键集不含 `allow_incomplete_checkpoints` / `allow_dangling_change_ids` (仅 `audit-engine/SKILL.md:381-388` 文档化) | 实读 | 既有注册面缺口, **本 spec 不新增 config 键**以免扩大它; 缺口记 Phase D 待办 |

### 候选方案与否决理由

| 方案 | 内容 | 裁决 |
|---|---|---|
| A · 只改散文 (Step 4 通配换成 `{checkpoint}-*-{change_id}-*.md`) | issue 修法 1 的最小形态 | **否决**: (1) 仍是纯散文, 无法写 hermetic 红转绿测试, Rule #6 substitute 不成立; (2) F2 证明位置通配对真实语料不可靠; (3) 没解决根因 2 (change_id 在 pre_merge 无输入), 编排者仍要「自己猜」change_id |
| B · 机械执行器 + 三态 + 显式 change 作用域 (**本 spec**) | 新增 `completeness_gate.py` 实现 Step 3-5 (stdlib, 镜像 `sibling_spec_probe.py` 形态: stdout JSON 契约 + exit code + 消费方 fail-closed 义务); audit-engine 输入加 `change_id`; per (checkpoint, change_id) 三态 present / missing / not_applicable; not_applicable 只由机械结构事实产生 | **采用**: 修法 1 + 2 组合, 判据可测、可证伪; 修法 3 的「下界」在按 change 收窄后天然 = 「≥1 份属于本 change」 |
| C · B + 可配置下界 N (`audit.completeness_gate.min_own_reports`) | issue 修法 3 参数化 | **暂不做**: 需要新 config 键 (F8 注册面缺口会扩大); N>1 的语义不清 (一轮 convergence 5 席就是 5 份, 「份数」不是「轮数」); 留 待 owner 复议 #3 |
| D · 改用报告 frontmatter `context:` 字段做归属 | 78 份报告有 `spec_id:`/`change_id:` 行, `context:` 形态 8+ 种 (路径 / 裸 id 混用) | **否决**: 要打开每个文件读 frontmatter 且形态不统一; 文件名是 writer 侧唯一契约 (`report-storage.md:39`), 归属判定以文件名为准 |

## What

### 1. 新增 `skills/audit-engine/scripts/completeness_gate.py` (Step 3-5 的机械执行器)

**调用** (SKILL.md fenced bash 块 + execution-modes.md 各一份, 同字面):

```bash
python3 "${CLAUDE_PLUGIN_ROOT:-aria}/skills/audit-engine/scripts/completeness_gate.py" \
  --repo-path "<主仓 root>" --diff-repo-path "<C.2 合并目标仓 root>" --base "<main_branch>" \
  [--change-id <id> ...] [--no-spec]
```

- `--repo-path`: **锚点面 = 主仓根**, 承载三样东西: 报告目录 `.aria/audit-reports/`、config、`openspec/{changes,archive}/` 锚点链。
- `--diff-repo-path`: **diff 面 = 本次 C.2 实际合并的目标仓根**, 只用于 `git diff` / `merge-base`。缺省 = `--repo-path`。
- **为什么必须拆 (R1 rework)**: `phase-c-integrator/SKILL.md:252` 的执行上下文契约明写同类脚本「在执行 C.2 合并的**目标仓根**内调用 (子模块合并 → 子模块根)」, 而**子模块 PR 正是本项目 pre_merge 的主力场景**; 实测 `aria/` 子模块内 `openspec/` 与 `.aria/` **均不存在** (`test -d` 两项皆假)。单参数两个方向都坏: 传子模块根 ⇒ 零报告 + S1 恒 `change_id_unanchored` exit 2 (该错**不受** `allow_incomplete_checkpoints` 豁免) 的假红; 传主仓根 ⇒ diff 取错仓 (本 cycle 主仓 diff 全是 `openspec/**` 而代码全在子模块) 的假绿。
- **fail-closed 附加条款**: `--diff-repo-path != --repo-path` 时 (跨仓形态), `not_applicable` 的 (b) 通道 **禁止触发** —— 主仓 diff 的路径形态无法代表子模块内的实现工序; 一律走 `present`/`missing`。同理, diff 中出现 gitlink/submodule 条目时 (b) 亦不触发。
- config 由脚本**直读** `<--repo-path>/.aria/config.json` 并内联 config-loader 的缺省与旧配置兼容映射 (R2 rework 反转 R1 的「一律经 config-loader」—— 该 Skill 无程序化入口), 见 §1.4「config 读取」。
- `--base`: 供 diff 的 base revision, **无缺省** (#137 教训, `phase-c-integrator/SKILL.md:253`, 缺失即 argparse exit 2)。**绑定 (R2 rework 勘正)**: 生产调用方**必须传远程跟踪 ref** (本项目 `origin/master`), 与它自称同命令的那段 SOT 一致 —— `audit-engine/SKILL.md:404-405` 的 base 取「config 的 base 或 `git symbolic-ref refs/remotes/origin/HEAD`, fallback `origin/main`→`origin/master`」, 全是远程跟踪 ref。原写「主干真实名 (本项目 `master`)」= 裸本地分支名, 在子模块 detached HEAD / 本地 `master` 陈旧 (本项目常态) 下 `merge-base HEAD master` 会**偏移且不报错** ⇒ diff 偏小 ⇒ S3 的机械交叉核验假通过 ⇒ 全部 checkpoint 记 `not_applicable/level1-no-spec` 的假绿。脚本侧不做补全 (hermetic fixture 用本地 `master` 仍须能跑), 但**必须**在 `--base` 不含 `/`、同名远程跟踪 ref 存在且两者 SHA 不同时输出 `[WARN] base ref 陈旧: <base>=<sha1> != origin/<base>=<sha2> — merge-base 可能偏移, S2/S3 的 diff 判据会被弱化` (不阻断); `--base` 解析失败 → `git_failed` exit 2。SC-22 锁这条。
- `--change-id` (可重复): 显式作用域; `--no-spec`: Level 1 声明 (见 1.1 S3)。

#### 1.1 change 作用域解析 (S1 → S4, first-match)

| 步 | 条件 | 结果 |
|---|---|---|
| S1 | `--change-id` 给出 | 每个 id 必须有锚点: `openspec/changes/{id}/proposal.md`, 或 `openspec/archive/` 下**目录名去掉 `^\d{4}-\d{2}-\d{2}-` 日期前缀后逐字等于 `{id}`** 的目录里的 `proposal.md` (复用 `pre-write-validation.md:20-26` 锚点链的**语义**)。**逐字比对, 不用 glob (R2 rework 收窄)**: 该 SOT 写的是 glob `archive/*-{change_id}/` (`pre-write-validation.md:25`), 前缀不设界 —— `--change-id orchestrator` 会命中 `2026-…-aria-orchestrator`, 拼错的短 id 于是变成「有锚 + 零报告 = `missing` 假红」而不是 `change_id_unanchored`, 与 S1 的设计目的 (把拼写错拦成 error) 相反。任一无锚 → `error_kind=change_id_unanchored`, exit 2 (拼写错不能变成假红, 也不能变成假绿)。**豁免继承 (R2 rework)**: `audit.allow_dangling_change_ids=true` 时该 error 降为 `[WARN] dangling change_id: {id} (allow_dangling_change_ids=true)` 并按「该 id 零报告」继续评估 —— S1 复用的这条链, 其 Step 1 就是这个开关 (`pre-write-validation.md:16-18` 逐字: `config-loader → audit.allow_dangling_change_ids` / `== true → 跳过校验`), `audit-engine/SKILL.md:381-384` 明写它用于「遗留 change_id 迁移期」; 新门比它复用的既有链更严却不继承既有豁免面 ⇒ 迁移期采用方「写盘侧放行、新门 exit 2 硬阻」且无任何逃生舱 |
| S2 | 无显式 id | 从 `git diff --name-only --no-renames $(git merge-base HEAD <base>)` 取路径前缀 `openspec/changes/<id>/` 的去重 id 集; 非空 → 作用域, `scope_source=diff`。基础命令与 `audit-engine/SKILL.md:406` 同源; **`--no-renames` 是 R2 rework 补的**, 与同 skill 姊妹门 `path_coverage` 的取法对齐 (`phase-c-integrator/SKILL.md:252` 逐字 `git diff --name-only --no-renames`) —— 不加时纯归档移动的 PR (`openspec/changes/<id>/` → `openspec/archive/YYYY-MM-DD-<id>/`) 在 rename 检测开启时只输出目的侧路径, S2 取不到 `openspec/changes/` 前缀 ⇒ 落 S4 `change_scope_unresolved` exit 2, 是未成文的假红路径 (方向 fail-closed, 但契约未定义) |
| S3 | S2 为空且 `--no-spec` | 机械交叉核验: diff **不触** `openspec/changes/**` (触了即与声明矛盾 → `error_kind=no_spec_contradicted`, exit 2); 通过 → 全部纳入校验的 checkpoint 记 `not_applicable(reason=level1-no-spec)` (Level 1 无 spec ⇒ 报告文件名无 spec_id 可写, `pre-write-validation.md:28-30` 会拒绝写盘 ⇒ 对象结构性不存在), `scope_source=no_spec`, `change_ids=[]`。**`results` 里的 `change_id` 取 `null`** (R2 rework 补定义 —— 原稿只定义了 S4-bypassed 的 `results=[]`, 这一格漏了), trail 行相应渲染为 `{cp} (无 change_id)` 而非 `{cp}@{change_id}`; **Level 视为 1** (供 §1.3 Step 3 的 adaptive 推导用); audit trail 必须留 `[INFO]` 行 (见 1.4) |
| S4 | 其余 | `error_kind=change_scope_unresolved`, exit 2, 文案给三个 fix: 传 `--change-id` / 在分支里带上 change 目录变更 / Level 1 加 `--no-spec`。**不放行** (零证据不当正证据) |

`allow_incomplete_checkpoints=true` 时 S4 / missing 均降为 `verdict=bypassed` + exit 0; S3 的 `no_spec_contradicted` 与 S1 在 `allow_dangling_change_ids=false` 下的 `change_id_unanchored` **不被该键豁免** (输入矛盾不是「不完整」) —— S1 的逃生口是它自己那条链上的 `allow_dangling_change_ids` (见上表 S1)。**与「无前置 checkpoint」格的交互 (R2 rework 补定义)**: `no_prior_checkpoints=true` 时不存在可降级的 missing, 该键取值不改变结果 (恒 `verdict=pass` + `bypassed=false`)。

**豁免文案 (R1 勘正 —— 原稿造了第三种拼法)**: 现行 SOT 有两种字面, `execution-modes.md:44` = `[WARN] incomplete checkpoint gate bypassed by config` (无 missing 列表), `:82` = `[WARN] incomplete checkpoint gate bypassed: missing={checkpoint_names}`。本 spec **不新造措辞**, 统一为 `:82` 的形态并把两处 SOT 对齐成同一句 (列入 §4 同步表, SC-13 逐字 grep):

```
[WARN] incomplete checkpoint gate bypassed: missing={cp}@{change_id},... ; scope_unresolved={0|1}
```

`scope_unresolved=1` 即 S4 被豁免的那一路。**S4-bypassed 的字段取值 (原稿未定义)**: `verdict=bypassed`, exit 0, `error_kind=null`, `scope_source=null`, `change_ids=[]`, `checked_checkpoints` 照常枚举, `results=[]` (无作用域 ⇒ 无逐对结果), `bypassed=true`。

#### 1.2 报告归属匹配规则 (改前 → 改后)

改前 (`:57-60`): `{checkpoint_name}-*.md` 或 `{checkpoint_name}-*-*.md` 任意命中 → 通过。

改后, 文件 `f` 归属 `(checkpoint, change_id)` 当且仅当**全部**成立:

1. `f.startswith(f"{checkpoint}-")` 且 `f.endswith(".md")`;
2. **双侧界定 或 末段界定** (R1 rework 放宽): 令 `stem = f[:-3]` (去 `.md`), 则 `f` 命中 `change_id` 当且仅当 `f"-{change_id}-" in f` **或** `stem.endswith(f"-{change_id}")`。**逐字**, 大小写敏感, 不做归一化 (`openspec/changes/<id>` 目录名就是契约, 写侧 `report-storage.md:18` 也逐字取)。
   > **为什么改 (本轮 5/5 全席独立命中的 Critical)**: 原稿只写「含 `-{change_id}-`」, 漏掉 change_id 落**末段**的 `…-{id}.md` 形态 (无 `agent_role` 段)。本仓实测该形态 **62 份**, 覆盖 **25 个 (checkpoint, change_id) 组合的全部自有报告** —— 这些 change 明明跑过审计却会被判 `missing` 假红**阻断合并**。含在飞 change (`aria-2.0-m6-release-closeout` / `aria-2.0-m6-cost-acceptance` / `aria-2.0-m6-e2e-resilience`)。**R2 rework 勘正**: 原稿写「与**全部 3 份** `post_implementation` 报告」不成立 —— 3 份里只有 `post_implementation-R1-2026-06-11-audit-drift-guard.md` 属末段族; 另两份 `…-state-scanner-mechanical-t3.md` 与 `…-aria-secret-guard-plugin-default-orchestrator.md` 按**旧的纯中缀规则**即已命中 `state-scanner-mechanical` / `aria-secret-guard-plugin-default` (两者均 ∈ `C`, `-t3` / `-orchestrator` 是同 change 的任务/席位后缀), 不在「会被判 missing 假红」的集合里。该句是 R1 聚合原话被抄进正文的逃逸, 已按逐字实测改写。该形态仍在产生 (最新一例 `post_spec-R2-2026-06-25-session-closer-synthesis.md`)。左侧连字符界定与 `collectors/audit.py:62-69` 的单侧合成先例同向。
3. **有界包含排除 (对称, R1 rework)**: 令 `C` = `openspec/changes/*` ∪ `openspec/archive/*` (去 `YYYY-MM-DD-` 前缀) 的 id 集。若存在 `c' ∈ C`, `c' != change_id`, 使 `f"-{change_id}-"` 是 `f"-{c'}-"` 的子串 (等价: `c'` 以 `{change_id}-` **开头**、以 `-{change_id}` **结尾**、或含 `-{change_id}-`), 且 `f` 按规则 2 命中 `c'`, 则 `f` **不**归属 `change_id`。
   > 原稿只写 `c'.startswith(change_id + "-")` 即**只挡前缀扩展**, 后缀型与中缀型碰撞下他人 change 的报告仍会计入本 change —— 与被修 bug **同型** (假绿方向)。本仓现存 152 个 id 中前缀碰撞 1 对、后缀 0 对、中缀 0 对 (R1 全枚举), 故这是**潜在缺陷而非现存实例**; 但规则是发给所有采用方的通用契约, 必须对称封死并由 SC-3 锁定不变量。

不要求 `-R\d+-` 段, 不解析时间戳与 role (F2: 位置不可信)。

**两个排除计数 (R1 rework 拆分, 原稿合并成一个 `excluded_legacy_count` 会掩盖 Critical 1)**:

| 计数 | 定义 | 本仓实测 | 输出义务 |
|---|---|---|---|
| `excluded_legacy_count` | 真 2-field 旧 schema: `f` 匹配 `^{checkpoint}-[0-9TZ:.\-]+\.md$` 且不含任何 `C` 中 id (F4) | **6** | 计数即可 |
| `unattributed_count` | 以 `{checkpoint}-` 开头、按规则 2 对**任何** `c ∈ C` 都不命中、且不属于上一行 | **170** (截断/别名/自造 id) | 计数 **+ 文件名进 audit trail**, 逐字**一行**: `[WARN] unattributed reports (checked={cp1},{cp2},…): N 份 — <按字典序前 20 个文件名>[, … 其余 K 份见 stdout 的 `unattributed`]`。**R2 rework 统一**: 原稿在本表与 §1.4 造了 `[WARN] unattributed reports: …` 与 `[WARN] unattributed reports ({cp}): …` **两种拼法**且未定义多 checkpoint 下 `{cp}` 取值 —— 计数本身是全局单值 (§1.4), 故文案也**全局一行, 不按 checkpoint 重复输出**。**截断到 20** 是量级需要: 本仓实测 170 份且写侧强制被 defer (R-a), 不会自然收敛, 每次运行倒 170 个文件名会让 R-a 依赖的「显影」退化成被跳读的固定噪声 |

> 原稿定义「以 `{checkpoint}-` 开头但不含任何 `-{c}-`」= 本仓 **238 份**, 会把上面那 62 份**真报告**吞进 legacy 计数 ⇒ R-a 依赖的「legacy 计数显影」不但不显影, 反而把归属 bug 伪装成历史包袱, 编排者读到「legacy 不计入: N」会得出相反结论。

#### 1.2b 枚举范围: 两条 SOT 未定义、语料里却有实例的边界 (2026-09-07 主 loop 冻结快照实测补)

SOT Step 4 只写「扫描目录: `{project_root}/.aria/audit-reports/`」, **没说递归与否, 也没说不以 checkpoint 名开头的文件怎么算**。两条都不是假想问题, 冻结快照 `3f4b379` 里各有实例:

| 边界 | 语料实况 (冻结快照 `git ls-tree -r --name-only 3f4b379 -- .aria/audit-reports/`) | 本 spec 定义 |
|---|---|---|
| **子目录** | 存在两个子目录 `pr19-submodule-scan/` 与 `wf-r1fix/` (各含 `AGGREGATE.md` 等)。同一份 SOT 被两个实现分别读成「递归」与「不递归」时, 完整性判决会不同 | **只扫顶层, 不递归** (与现行 glob `{checkpoint}-*.md` 的语义一致, 是「不改变现状」的那一侧)。`completeness_gate.py` 用 `iterdir()` 而非 `rglob()`, 并在 stdout 契约里输出 `scanned_dir_depth: 1`; **落点文件 = `report-storage.md` §向后兼容** (R2 rework 点名, 见 §4 同步表), 逐字写明「子目录内的报告不计入完整性证据」, SC-13 加对应 grep。**理由 (R2 rework 换掉原理由)**: 原写「递归会把上述两个一次性目录突然算成证据」与语料不符 —— 冻结快照里 `pr19-submodule-scan/` 内是 `AGGREGATE.md` + 五份 `*.yaml`、`wf-r1fix/` 内是 `PLAN-*.json` / `VERDICT-*.json`, **无一以 checkpoint 名开头**, 换 `rglob()` 也是 0 份计入。真实理由是**面向未来**: 目录形态无契约, 一旦有人把合规命名的报告归置进子目录, 递归实现会把它算成证据而非递归实现不会, 两个实现对同一份 SOT 判决不同; 本 spec 选「不改变现状」且**不放宽**的那一侧 |
| **无 checkpoint 前缀的形态** | **`*-audit-trail.md` 族恰 5 份** (R2 rework 逐字重列, 原稿列错了成员): `a1-entry-claim-audit-trail.md` · `linked-issue-field-availability-audit-trail.md` · `linked-issue-normalization-audit-trail.md` · **`premerge-gate-mainbranch-failclosed-audit-trail.md`** (原稿漏列) · `sibling-spec-probe-audit-trail.md`; 原稿列进来的 `phase-b-review-179-secret-guard-manifest-precision.md` **不属 audit-trail 族**, 但它同样是「含 change 语义、不以 checkpoint 名开头」的实例, 故一并保留为第 6 个样本。**含 change_id 但不以任何 checkpoint 名开头** | **不计入任何 checkpoint 的证据** (规则 1 的 `f.startswith(f"{checkpoint}-")` 已自然排除), 且**也不进** `unattributed_count` (该计数按定义只收以 checkpoint 前缀开头的), 也不进 `excluded_legacy_count` —— **三个桶都不收**。**落点文件 = `report-storage.md` §向后兼容** (R2 rework 点名, 见 §4 同步表), SC-13 加对应 grep; 运行时不变量由 SC-19(b)(c) 锁 |

> **为什么第二条值得成文**: 它解释了一个会误导人的观测 —— `sibling-spec-probe` 这个**已 ship 并归档**的 change, 按逐字子串 `-sibling-spec-probe-` 在冻结快照里命中 **0 份**。不是它没跑过审计, 是它的记录落在 `*-audit-trail.md` 这个族里。若不成文, 采用方会把「0 份」读成「这个 change 从未审计」并据此改写侧约定, 或反过来要求把 audit-trail 计入证据 —— 后者会让**任何**带 change_id 的散记文件成为完整性凭据, 正是本 spec 要封的假绿口子。

#### 1.3 per (checkpoint, change_id) 三态 (改前: 二态 通过 / missing)

**Step 3 的枚举源必须过 SOT 优先级链 (R2 rework, 原稿直接枚举 `audit.checkpoints` 字面值 = 新假绿)**: 三处 SOT 同写「`checkpoints` 显式值 > `adaptive_rules` 推导值 > 默认 `off`」(`audit-engine/SKILL.md:391` · `config-loader/config-example.md:280` · `execution-modes.md:15`), 而散文 Step 3 (`execution-modes.md:47`) 写的是「对 `audit.checkpoints` 中每个 key」—— 散文时代编排者能看原始 `config.json`, 脚本拿到的却是与 `DEFAULTS.json` 合并后的对象, 字面枚举两个方向都坏, 官方 `config-example.md` 的两个场景各击穿一个:

- 场景 B (`:402-417`, `mode=adaptive` 且**无** `checkpoints` 块): 与 `DEFAULTS.json` 合并后八键字面**全 `"off"`** (实测) ⇒ 纳入集空;
- 场景 C (`:419-440`, `mode=adaptive` + 只显式写 `post_spec`/`post_closure`): `post_implementation` 由 `adaptive_rules.level_2/3` 启用, 却因字面值是 DEFAULTS 填的 `"off"` 而**不被枚举** ⇒「该跑的没跑」查不出 = 与被修 bug **同型**的新假绿。

脚本对**每个 checkpoint × 每个 change_id** 解析 `resolved_mode`, 三级 first-match:

| 级 | 判据 (对**原始** `.aria/config.json` + 旧配置兼容映射后的视图求值 —— 合并 DEFAULTS 后的对象区分不了「用户显式 off」与「DEFAULTS 填的 off」, 见 §1.4「config 读取」) | `enabled_by` |
|---|---|---|
| 1 | 原始 config 的 `audit.checkpoints` **自身含该键** ⇒ 取其值 | `explicit` |
| 2 | 否则 `audit.mode == "adaptive"` ⇒ 取 `audit.adaptive_rules.level_{N}`, N = 该 change 的 OpenSpec Level | `adaptive:level_{N}` |
| 3 | 否则 `"off"` | `default` |

`resolved_mode` 非 `"off"` 且 key 不在排除清单内 ⇒ 该 (checkpoint, change_id) 纳入校验。**Level N 的取法**: 读 `openspec/changes/{id}/proposal.md` (归档态 `archive/<日期前缀>-{id}/proposal.md`) 头部 Level 行, 正则 `Level\s*\**\s*[:：].*?([123])`; `--no-spec` ⇒ N=1。**解析不到 ⇒ `error_kind=spec_level_undetermined`, exit 2** (fail-closed, 不猜; 文案给两个 fix: proposal 头部补 `> **Level**:` 行 / 在 `audit.checkpoints` 里显式写该 checkpoint)。顶层 `checked_checkpoints` = 各 change_id 纳入集的**并集** (`sorted()` 字典序), 逐对 `results` 里的 `enabled_by` 记明来源, 使「这对为什么被查 / 没被查」可核对。

**Step 3 枚举 (`:46-52`) 追加一条排除: `mid_implementation`** (R1 rework)。它与已排除的 `mid_post_spec` **同因** —— `DEFAULTS.json` 里 `audit.mid_implementation = {trigger: task_progress, threshold: 50, unit: percent_tasks_completed}`, `audit-engine/SKILL.md:63` 标「条件触发」: 启用但任务进度未过 50% 阈值时**合法不产出报告**, 落 `execution-modes.md:51-52` 的「启用即会误阻」原句。原稿把它交给 1.3(b) 兜底, 但 (b) 只在「diff 全是 docs」时生效, 而最常见场景恰是「code diff + 进度未过阈值」⇒ 仍判 `missing` 假红。排除条款与 `mid_post_spec` 同构书写。另有一条候选追加 `post_brainstorm` (待 owner 复议 #1)。

对每个纳入的 checkpoint × 作用域内每个 change_id:

| 状态 | 判据 (按序 first-match; 全部是机械结构事实, 无 AI 价值判断) |
|---|---|
| `present` | 1.2 规则命中 ≥ 1 份 |
| `not_applicable` | (a) `scope_source=no_spec` (S3); 或 (b) checkpoint = `post_implementation` **且** `--diff-repo-path == --repo-path` **且** diff 中无 gitlink/submodule 条目 **且** diff 非空 **且** 全部变更文件路径都在 **`openspec/changes/{作用域内某个 id}/**` ∪ `.aria/audit-reports/**`** 之下 (即 **Phase A-only PR**: B.2 实现工序整个未发生) → reason `phase-a-only-no-implementation`; **R2 rework 两处收窄**: (1) **删掉 `openspec/archive/**`** —— 纯归档移动是 D.2 工序, 它恰恰**证明实现已发生**, 本仓 `62de051` (Level 3 已 ship 的 `owner-container-identity-key-and-collision-parser` 的归档-only 提交, 三文件全在 `openspec/archive/` 下) 按原判据会拿到免检票, 而三态 first-match 下 `present` 优先 ⇒ (b) 唯一被触发的时刻正是「post_implementation 报告确实缺失」= 门该拦的那一次; 同理, 交付物本身就是 openspec 语料的 cycle (批量勘正归档 proposal) 也会触碰 `openspec/archive/**`, 收窄后一律落 `present`/`missing`。(2) **加入 `.aria/audit-reports/**`** —— 本仓真实 Phase A 分支惯例把审计报告与 triage 一并落该目录 (实测 `813e82c` 12 份 .aria / 2 份 openspec, `f634d83` 6/1, `2c8eaa6` 1/1), 该目录不在 `openspec/**` 下, 不加则 (b) 在本仓的规范工作流里几乎不可能触发, 与保留 (b) 的理由 (「对应 issue 里真实发生过的 Phase A-only PR 场景」, 待 owner 复议 #7) 自相冲突; 或 (c) checkpoint = `post_planning` 且**不存在任何 A.2 产物**: `openspec/changes/{id}/tasks.md` 与 `detailed-tasks.yaml` 都不存在 **且** `proposal.md` 无 `^#{2,}\s*Tasks` 小节 (归档态取 `archive/*-{id}/`) → reason `no-a2-artifact` (Rule #10 `:38` 的原例) |
| `missing` | 其余 (含 diff 为空: `SKILL.md:410` 防 vacuous-true 同款, 空集不放行) |

**(b) 为什么不再用 `scope_skip_paths` (R1 rework, 反转原设计)**: 见 F6 含义列 —— 该键 SOT 语义是「降级非 skip」(DEC-4), 缺省又含 `*.md`, 用作 checkpoint 免检会给 aria-plugin 这类「产品本体即 markdown 处方」的仓造出与被修 bug 同型的新假绿, 且落在 Rule #10 白名单第四类的**禁止侧** (`configured-gate-authority.md:40`「存在但简单」不算)。改用的新判据与 issue 报告方**自己独立论证放行的那一格逐字同义** (「`synth-honesty-gaps` 是 Phase A-only ⇒ post_implementation 不适用」), 属于「对象整个未产生」的合法侧。跨仓形态下禁触发 (见 §1 fail-closed 条款)。

**(c) 为什么改成「任何 A.2 产物」(R1 rework)**: 原稿只看 `tasks.md`/`detailed-tasks.yaml` 两个**文件**。但 Level 2 的 A.2 产物结构上就是**内联**的 —— `standards/openspec/project.md:117` 规定 Level 2 输出 = `proposal.md` (Level 3 在 `:118`, 输出 = `proposal.md + tasks.md`; **R2 rework 行号勘正**, 原写 `:118` 错格一行, 而 `:117` 正是本段核心论证的锚), `templates/proposal-minimal.md:27-31` 自带 `## Tasks` 小节。按文件判 ⇒ **每个 Level 2 change 恒成立** ⇒ 自动豁免采用方已 enabled 的 post_planning, 正是 Rule #10 要禁的形态。**本 spec 自身即此形态** (内联 `## Tasks`、无 `tasks.md`, 却在头部声明跑 post_planning): 按原稿判据本 spec 会给自己发一张 not_applicable 免检票 —— 自相矛盾。改后本 spec 走 `present`/`missing`, 与头部声明一致。非真空: 本仓归档语料实测「三样 A.2 产物全无」的 change 有 **37** 个 (52 个无 A.2 文件的 change 中 15 个有内联 `## Tasks`), (c) 在它们上仍会正常触发。

`post_spec` 无 not_applicable 通道 (有 change_id 即有 proposal, 对象必存在)。「做了但很简单」(如 tasks.md 只有 3 行、内联 Tasks 只有 2 条) 恒 `present`/`missing`, 不进 not_applicable (`configured-gate-authority.md:40`)。

#### 1.4 路由 + stdout 契约 (改前: `:63-65` 二路由 + `:70-80` 单一 ERROR 模板)

**config 读取 (R2 rework 反转 R1 的「一律经 config-loader」)**: 脚本**直读** `<--repo-path>/.aria/config.json` (stdlib `json`), 并在脚本内**内联实现** config-loader 的两块语义 —— (a) `DEFAULTS.json` audit 子集的缺省 (`enabled=false` / `mode="adaptive"` / `adaptive_rules` 三档 / 八个 checkpoint 全 `"off"`), (b) 旧配置兼容映射 (`config-loader/SKILL.md:305-331` 逐字: `experiments.agent_team_audit === true` **且**配置内不存在 `audit` 块 ⇒ `audit.enabled=true` + `mode="manual"` + `agent_team_audit_points` 里的 `post_spec` / `post_implementation` / `pre_merge` 三元素各映射为 `"convergence"`, 未出现的 checkpoint 保持 `"off"`)。

**为什么反转 (三席独立实读, 本轮复核逐条命中)**: `config-loader` **没有程序化入口** —— 目录内只有 `SKILL.md` + `DEFAULTS.json` + `config-example.md`, **零 `.py`** (`find skills/config-loader -name '*.py' | wc -l` = 0); `SKILL.md:8-10` 是 `user-invocable: false` / `disable-model-invocation: true` / `allowed-tools: Read, Glob` 的散文 Skill (给 AI 读的规程), stdlib-only 的脚本无从「调用」它; 全插件 `.py` 对 `agent_team_audit` 零命中。同仓先例同向: `sibling_spec_probe.py:380` 与 `pre_merge_gate.py:721` 都直读 `.aria/config.json`, `multi_remote.py:107-113` 明写运行时不读 `DEFAULTS.json`、Python 常量才是缺省。R1 反转掉的那条失效 (旧配置采用方直读 ⇒ `audit` 块不存在 ⇒ 空纳入集) 由**内联映射 + SC-21 的相等性断言**堵住, 不靠一个不存在的调用面。

**第二副本的漂移风险不靠「不复制」消解, 靠相等性断言封住 (R1 `013d0274` 的原始修法)**: SC-21 在**测试期**读插件树的 `config-loader/DEFAULTS.json` 与 `config-loader/SKILL.md` §旧配置兼容层, 断言脚本内联的缺省集与映射表**逐字相等**; 运行时不依赖这两个文件存在 ⇒ 调用契约无需 `--defaults-path` 参数, `error_kind` 也不需要 DEFAULTS 缺失/损坏一格。

**坏 JSON 的语义刻意与 config-loader 散文不同**: `config-loader/SKILL.md:37` 规定「JSON 格式错误 ⇒ 警告用户 + 返回默认值」, 而默认值 = audit 全 off ⇒ 门会拿到空纳入集; 本门取 **`error_kind=config_unreadable`, exit 2** (fail-closed)。若真「一律经 config-loader」, `config_unreadable` 这一格**结构上不可达** (该输入只会落成空纳入集), SC-10 的对应断言恒绿 —— 这也是反转的理由之一。该差异列入 §5 行为变更。**config 文件缺失** (非损坏) 时: 取全套缺省 (audit 全 off) ⇒ `enabled=false` ⇒ 落下面的格 A。

**报告目录不存在 (R2 rework 补, 原稿全文未定义 —— 首次启用 pre_merge 的采用方正是这种状态)**: `<--repo-path>/.aria/audit-reports/` 不存在时**视为零份报告**, 不抛 `FileNotFoundError`、不新增 `error_kind`, 并在 audit trail 留 `[WARN] completeness gate: .aria/audit-reports/ 不存在, 按零报告评估`; 后续照常走 `missing` (fail-closed 方向) 或 `not_applicable`。`scanned_dir_depth` 仍输出 `1`。
**两个 `allow_*` 键的缺省源 (R1 勘正)**: `allow_incomplete_checkpoints` / `allow_dangling_change_ids` 实测**不在** `DEFAULTS.json` 的 audit 键集内 (F8), 故「缺省值与 DEFAULTS.json 一致」对它们**无源** —— 唯一出处是 `audit-engine/SKILL.md:385-388` 散文「默认 false」。本 spec 按该散文取 `false`, 并把「补进 DEFAULTS.json」写进 F8 的 Phase D 缺口 issue。

- 任一 `missing` → `verdict=fail`, exit **1**; 全部 `present`/`not_applicable` → `verdict=pass`, exit 0; 豁免 → `verdict=bypassed`, exit 0; 输入/环境错 → `verdict=error`, exit **2**。
- **纳入集为空: 按成因分三格 (R2 rework 重写; R1 的「一律 error」是方向相反、量级同级的新失效)**: 在受调用方守卫的路径上 (`phase-c-integrator/SKILL.md:131` 步骤 2 对 `audit.enabled=false`、`:132` 步骤 3 对 `pre_merge="off"` 各有一次早退), `checked_checkpoints == []` **只可能由合法配置产生** —— Step 3 排除 `pre_merge` 自身 + `post_closure` + `mid_post_spec` + `mid_implementation` 后, 空集等价于「audit 已启用、只开 pre_merge、其余全 off」。一律判 error 会把官方 `config-loader/config-example.md:381-400` 场景 A「仅启用合并前审计 (低成本起步)」、`:402-417` 场景 B、以及 legacy `experiments.agent_team_audit_points: ["pre_merge"]` 经兼容映射 (`config-loader/SKILL.md:316-327`) 落到同一格的采用方 —— **恰是同一条 rework 引入内联映射要保护的那批** —— 变成 `verdict=error` + exit 2 + 消费方 fail-closed + `on_fail: 阻塞合并` 的**永久硬阻**, 且 §1.1 的豁免面只覆盖 S4/missing (`allow_incomplete_checkpoints` 明示不救) ⇒ **无任何配置逃生口**; ERROR 文案「audit 未启用或全 off」对他们还是误诊。改前行为是通过 (`execution-modes.md:64`)。三格:

| 格 | 机械判据 (对原始 config + 兼容映射后的视图求值) | 结果 |
|---|---|---|
| A | `audit.enabled != true` (含 config 文件缺失 / 无 `audit` 块且不触发兼容映射) | `verdict=error`, `error_kind=audit_not_enabled`, exit **2**, 文案 `[ERROR] completeness gate: audit 未启用 (audit.enabled != true) — 调用方本应在 phase-c-integrator 步骤 2 早退, 到此说明接线断裂`。**生产不可达** (调用方早退), 只覆盖直调与误接线, fail-closed |
| B | `audit.enabled=true` **且** `resolved(pre_merge) != "off"` **且** 纳入集空 | `verdict=pass`, exit **0**, `no_prior_checkpoints=true`; audit trail **必须**留 `[INFO] completeness gate: 无前置 checkpoint 纳入校验 (checked_checkpoints=[]) — 依 audit.checkpoints/adaptive_rules 的配置放行 (Rule #10 白名单第一类「配置显式关闭」, configured-gate-authority.md:35), 非零证据判绿`。先例同向: `audit-engine/SKILL.md:410-411` 对空 `changed_files` 的处置是 `pass-through (不降级)` 而非中止 |
| C | `audit.enabled=true` 但 `resolved(pre_merge) == "off"` | `verdict=error`, `error_kind=pre_merge_not_enabled`, exit **2** (调用方步骤 3 本该早退)。同样生产不可达 |

  「零证据不当正证据」这条原则由格 A / 格 C 与 §1.3 的 `missing` 兜住 —— 它约束的是**证据面**, 不是 owner 主动关掉的**校验面**; 把「owner 显式 off」也算成缺证据, 与 Rule #10 白名单第一类正面冲突。格 B 的语义选择 (`pass` 还是「保留 error + 另设可配置逃生口」) 仍列 **待 owner 复议 #8**; 两分支的可证伪断言见 SC-15。
- stdout 恰一个 JSON (`schema_version "1"`, `gate "completeness_gate"`), 顶层键集**逐字 15 项** (R2 rework: 补 `scanned_dir_depth` 与 `no_prior_checkpoints`; 原稿的 13 项列表不含 `scanned_dir_depth`, 而 §1.2b / SC-19(a) 又要求输出它 ⇒ SC-10 与 SC-19 结构上不可能同绿, 任一实现必红一条): `schema_version` / `gate` / `verdict` / `error_kind` (str|null, 封闭集 `change_id_unanchored` / `no_spec_contradicted` / `change_scope_unresolved` / `audit_not_enabled` / `pre_merge_not_enabled` / `spec_level_undetermined` / `git_failed` / `config_unreadable` —— R2 rework: `no_checkpoints_configured` 拆成 `audit_not_enabled` / `pre_merge_not_enabled` 两格, 空纳入集的合法格改判 pass, 见上) / `scope_source` (`explicit`|`diff`|`no_spec`|null) / `change_ids` [] / `checked_checkpoints` [] / `no_prior_checkpoints` bool / `results` [{`checkpoint`, `change_id` (str|null), `status`, `reason`, `enabled_by` (`explicit`|`adaptive:level_{N}`|`default`), `matched` [文件名], `matched_count`}] / `excluded_legacy_count` / `unattributed_count` / `unattributed` [文件名] / `scanned_dir_depth` (恒 `1`) / `bypassed` bool / `elapsed_ms`。`results` **恒为 list**, 消费方不得从 `matched == []` 推断 (与 `execution-modes.md:185` 探针条款同构)。
- **两个计数的作用域 (R1 勘正 + R2 补量级)**: `excluded_legacy_count` / `unattributed_count` / `unattributed` 是**全局单值**, 统计面 = `.aria/audit-reports/` **顶层**以**任一纳入校验的 checkpoint** 名开头的文件的并集 (不含未纳入的 checkpoint 的文件, 不含子目录)。**口径敏感 (R2 rework 实测)**: 同一份语料, 全 8 checkpoint 口径 = **170** 份, 本仓 `.aria/config.json` 实际 `checked = {post_planning, post_spec}` 口径 = **160** 份 (legacy 恒 6) ⇒ 全文引用该数字必须点名口径, 断言只写 `> 0` 不写死期望值 (见 SC-11)。`checked_checkpoints` 的顺序 = **`sorted()` 字典序** (不是 config key 序), 使断言可逐字比对。
- **消费方 fail-closed 义务** (写进 execution-modes.md): exit≠0 或 stdout 非 JSON 或 `schema_version` 未知 ⇒ 按 `fail` 处置 (拒绝执行 pre_merge 审计, 输出 ERROR), 不得按 PASS。
- 三态各有 audit trail 行 (取代 `:70-80` 单模板):
  - missing: `ERROR: pre_merge audit 前序 checkpoint 报告缺失: {cp}@{change_id} 配置 "{mode}" 但未找到 .aria/audit-reports/ 下含 "-{change_id}-" 或以 "-{change_id}.md" 结尾的 {cp} 报告 (真 legacy 不计入: N; 归属不明: M, 见下方清单)` + Fix 三项 (补跑 / 改 off / allow_incomplete);
  - `unattributed_count > 0` 时另起**一行** (全局一行, 不按 checkpoint 重复; 与 §1.2 计数表逐字同一句, R2 rework 消除原稿的两种拼法): `[WARN] unattributed reports (checked={cp1},{cp2},…): N 份 — <按字典序前 20 个文件名>[, … 其余 K 份见 stdout 的 `unattributed`]` —— 让写侧命名偏离当场显影, 不与 legacy 混计;
  - not_applicable: `[INFO] completeness gate: {cp}@{change_id} not_applicable ({reason}); 依据 = <机械事实: diff N 文件全部在 openspec/changes/{id}/ 或 .aria/audit-reports/ 下 (Phase A-only) | 无 tasks.md/detailed-tasks.yaml 且 proposal.md 无 ## Tasks 小节 | --no-spec 且 diff 不触 openspec/changes/>` — **必须 surface**, 不得折叠进「校验通过」; **S3 (`change_id=null`) 时该行渲染为 `{cp} (无 change_id)`**, 不渲染 `@null`;
  - present: `[OK] {cp}@{change_id}: N 份` (逐行, 使「通过」可核对归属)。
- 一个 PR 含多个 change (S1 多值 / S2 多 id): 逐 (checkpoint, change_id) 评估, **全部**非 missing 才 pass; ERROR 逐对列出。

### 2. audit-engine 接线 (SKILL.md)

- `## 输入参数` 表 (`:49-54`) additive 加 `change_id` (string | list, 否, 「被审 change 的 openspec 目录名; pre_merge 必传, 缺失时脚本走 S2-S4」)。
- `## 执行流程` (`:75-81`) 第 (2) 阶段描述改为「经 `completeness_gate.py` 机械执行, 三态 + fail-closed」; **新增 fenced bash 调用块** (F7, 与 `:123-125` 探针块并列)。
- `## 配置依赖` (`:381-388`) 两个 `allow_*` 键的注释各补一句: `allow_incomplete_checkpoints` 补「豁免 missing / scope_unresolved, **不**豁免输入矛盾 (`no_spec_contradicted`)」; `allow_dangling_change_ids` 补「completeness gate 的 S1 锚点校验**继承**本键 —— true 时锚点缺失降为 `[WARN]` + 按零报告评估, 与 `pre-write-validation.md` Step 1 同源」(R2 rework 新增)。
- `execution-modes.md` §Pre-merge (`:23-82`): 互补说明 (`:25-30`) 补一句「#26 自本 spec 起带 change 维度」; **Step 1 (`:37-38`) 的 config-loader 表述保留为编排层语义, 但补一句「脚本侧等价实现: 直读 `.aria/config.json` + 内联缺省与旧配置兼容映射 (config-loader 无程序化入口), 相等性由 SC-21 锁」** (R2 rework); Step 3 (`:46-52`) **追加 `mid_implementation` 排除条款** (与 `mid_post_spec` 同构书写, 见 §1.3); Step 4-5 改为调用行 + 1.2 规则 + 1.3 三态表 + 1.4 契约; `:68-80` 校验失败输出改三态模板; **`:44` 与 `:82` 两处豁免文案统一为同一句** (§1.1 的形态, 消除现存两种拼法)。

### 3. phase-c-integrator (调用方)

- `SKILL.md:133-136` pre_hook 步骤 4 (步骤 4 的正文占 `:133-136`, `:137` 是「5. 处理 verdict」) 追加两行: `- change_id: 本 cycle 的 spec 目录名 (来自 A.1 spec 路径 / claim track 的 spec dir; Level 1 传 no_spec=true)` 、`- repo_path / diff_repo_path: 锚点面恒主仓根, diff 面 = 本次 C.2 合并的目标仓根 (子模块合并 → 子模块根, 与 :252 的执行上下文契约一致)` 与 `- base: 主干的**远程跟踪 ref** (本项目 `origin/master`, 不是裸 `master`; 与 :253/#137 同源, 缺失即 argparse exit 2)` (**`--base` 是必填项, R2 rework 补 —— 原稿的接线三行漏了它, 照抄会直接红**); 步骤 5 前插「4.5 completeness gate 三态处置: fail → 阻塞并输出 ERROR; not_applicable → workflow report 必带 `[INFO]` 行; `unattributed_count > 0` → 必带 `[WARN]` 行」(镜像 `:265` 的 surface 义务写法)。
- `:157` `audit_report: ".aria/audit-reports/pre_merge-{timestamp}.md"` 是旧 schema 残留 → 勘正为 `report-storage.md` 的 5-field 形态 (描述性, 顺手)。

### 4. 文档同步面 (Rule #3)

| 文件 | 位置 (@301641b) | 改动 |
|---|---|---|
| `audit-engine/references/execution-modes.md` | `:23-82` (含 `:15` / `:37-38` / `:46-52` / `:44` / `:82`) | §2 所述; 删除 `{checkpoint_name}-*.md` 两行通配; Step 3 加 `mid_implementation` 排除 **+ 枚举源过优先级链 (`checkpoints` 显式 > `adaptive_rules` > 默认 off, 与 `:15` 同句)**; `:44` 与 `:82` 豁免文案统一 |
| `audit-engine/SKILL.md` | `:49-54` / `:75-81` / `:385-388` / `:427-433` 相关文档 | 输入参数 + 调用块 + 配置注释 + 相关文档加 `completeness_gate.py` 契约指针 |
| `audit-engine/references/report-storage.md` | `:34-39` §向后兼容 | 追加四句 (**R2 rework: 后两句是 §1.2b 两条枚举边界的落点文件, 原稿只写「schema 文档写明」未点名任何文件 ⇒ 既无落点也无核验, 可静默不做**): (1)「completeness gate 归属匹配认**含 `-{spec_id}-` 或以 `-{spec_id}.md` 结尾**的文件 (末段形态是本仓 62 份真实报告的实际形态)」; (2)「真 2-field 旧 schema 不计入并以 `excluded_legacy_count` 显影; 有 id 段但 id 不在 `openspec/{changes,archive}` 内的以 `unattributed_count` + 文件名清单显影 —— 两者**不得**混计」; (3)「**子目录内的报告不计入完整性证据**: 完整性扫描只读 `.aria/audit-reports/` 顶层 (`scanned_dir_depth: 1`)」; (4)「**不以任何 checkpoint 名开头的文件** (如 `*-audit-trail.md` 族) 三个桶都不收 —— 既不进 `matched`, 也不进 `unattributed_count` / `excluded_legacy_count`」。SC-13 对 (3)(4) 各加一条逐字 grep |
| `audit-engine/references/pre-write-validation.md` | `:3` 关联行 | #26 互补描述补 change 维度 |
| `phase-c-integrator/SKILL.md` | `:133-136` / `:157` | §3 |
| **旧 report schema 散文残留 (R1 rework 补全, 形态族穷举)** | `phase-a-planner/SKILL.md:267` / `phase-b-developer/SKILL.md:204` / `phase-b-developer/SKILL.md:277` | 三处与 `phase-c-integrator:157` **逐字同构** (`.aria/audit-reports/{cp}-{timestamp}.md`), 一并勘正为 `report-storage.md:8` 的 5-field 形态。原稿只改 1 处 ⇒ 修完仍留三份误导性范例。全仓该形态实测恰 **4 处** (`grep -rn 'audit-reports/[a-z_]*-{timestamp}\.md' skills/`); `report-storage.md:37,43` 与 `report-format.md:5` 的 `{checkpoint}-{timestamp}.md` 是**合法的向后兼容描述**, 不动 |
| `aria/CHANGELOG.md` + 版本引用点 | — | PATCH **v1.71.2** (bump 前 `ls-remote --tags` + 读同伴 handoff `<vNEXT>`, memory 并行发版撞号; 撞号面见 待 owner 复议 #4); 主仓 **16** 版本字符串点 + gitlink (**R2 rework 订正, 原写 14**): README×4 文件共 11 处 (`README.md:8,242` + `README.{zh,ja,ko}.md:3,10,244` 各 3) + `VERSION:24` + `CLAUDE.md:139,141` + **`docs/architecture/system-architecture.md:189`** + **`docs/architecture/version-scheme.md:23`** —— 后两处正是 CLAUDE.md §版本管理「发布同步面」逐字列出的发版面 (PR #190 审计补入), 有 `plugin-version-arch-docs-match` 机械兜底 (`.aria/state-checks.yaml`), 漏改会在归档门转红; 并发轨 #195 对同一面独立枚举亦为 16 |
| `aria-plugin-benchmarks/ab-suite/audit-engine.json` | `evals[]`; 文件内 `version` 字段现值 **1.0.0** | 新增定向 eval id 3 (descriptive 形态与 id 1/2 一致)。文件内 `version` 是否随 eval 增补而 bump: **随 catalog 惯例, 本 spec bump 到 1.1.0** 并在 PR 描述点名 (原稿未说明该字段动不动) |
| `aria-plugin-benchmarks/ab-suite/version.yaml` | `version` (**套件**版本, R2 rework 重测现值 = **1.5.0**, `last_modified: 2026-09-05`) + `changelog[]` | **1.5.0 → 1.6.0**。**R2 rework 订正 (本轮唯一 5/5 全席独立命中)**: R1 写的「现值 1.4.0, 目标 1.5.0」在落盘时就已过期 —— 同伴轨 `a1-entry-claim-duplicate-work-guard` 的 `5697477` 经 `ecb6296` 合进本地 master 后, `version.yaml:1` 已是 `1.5.0` 且 changelog 顶条即该轨 ⇒ **目标号已被占用, 撞号已经发生而非未来风险**, 照原字面执行会覆写一条已发布的 changelog 条目。同轨的「在飞」陈述亦作废: `a1-entry-claim-duplicate-work-guard` **已归档** (`openspec/archive/2026-09-06-a1-entry-claim-duplicate-work-guard`, PR #202 merged `9f25a66` —— 本文头部 gitlink 行自己就写着该 PR 已 merge)。**真正在飞、同抢 v1.71.2 的并发轨是 Aria#195 `handoff-multibranch-subdir-path-fidelity`** (其 `proposal.md:352` 现推荐改走 MINOR `v1.72.0` 但仍待 owner 拍板, `v1.71.2` 未出局): 它的落点 (`state-scanner/collectors/handoff_multibranch.py` / `scan.py` / `state-snapshot-schema.md`) 与本 spec 触点**零交叠**, 唯一共享面是版本号与 `ab-suite/version.yaml`。缓解不变: 改这两个文件前 `git fetch --all` + `git -C aria ls-remote --tags origin` + 读同伴 handoff 的 `<vNEXT>`, 撞号顺延; `skills_covered` / `total_eval_cases` 按程序化重算, 不手填 |

### 5. 向后兼容 (消费方 grep, 起草时全 skill 树 + 主仓)

- `allow_incomplete_checkpoints` / `missing_checkpoint` / `Completeness Gate` 的消费方: 仅 `execution-modes.md` / `audit-engine/SKILL.md` / `pre-write-validation.md` / `CHANGELOG.md:3020` (历史条目) — 全 skill 树 `--include=*.py --include=*.json --include=*.yaml` **零代码消费方**; 主仓外仅 `ab-results/2026-04-23-v1.16.2-patch/` 历史 benchmark (不动)。
- 文件名 schema 消费方: `state-scanner/scripts/collectors/audit.py:52,62-114` (只挑 `-aggregated.md`/`-aggregate.md` 候选 + token 正则解析时间戳) 与 `aria-dashboard/references/parse-rules.md:79-103` (glob `*.md` + 文件名前缀 fallback checkpoint) — 本 spec **只读**文件名, 不改 writer schema, 两者零影响。
- config 键: 零新增; 读 `audit.enabled` / `audit.mode` / `audit.checkpoints` / `audit.adaptive_rules` / `audit.allow_incomplete_checkpoints` / `audit.allow_dangling_change_ids` (**直读 + 内联缺省与旧配置兼容映射**, R2 rework, 见 §1.4)。`audit.scope_skip_paths` 在本 spec **零消费** ((b) 判据已换成 Phase A-only)。**第二副本没有消失, 只是换了内容**: R1 以「`scope_skip_paths` 零消费」为由宣告 `013d0274` 自然消解, 但 config-loader 无程序化入口 ⇒ 脚本必须内联 (a) `DEFAULTS.json` 的 audit 缺省集与 (b) 旧配置兼容映射表 —— 规模比原来那份更大。处置不是「假装没有」而是**用相等性断言封住漂移**: SC-21 在测试期对插件树的 `DEFAULTS.json` 与 `config-loader/SKILL.md` §旧配置兼容层逐字比对。
- audit-engine 输入 `change_id` additive 可选; 其它 checkpoint 调用方 (phase-a-planner / task-planner / phase-b-developer / phase-d-closer) 不受影响 (门仅 pre_merge 触发, `execution-modes.md:32`)。
- 行为变化面: 只有 `audit.enabled=true ∧ checkpoints.pre_merge != off` 的采用方 (本仓不在其中, F1)。对这些采用方共 **五条**行为变更 (原稿只列第一条, R1 补到三条, R2 rework 重写第 3 条并补第 4/5 条):
  1. 曾经的假绿会变成 `missing` 阻断 —— 这是修复本身;
  2. **`allow_incomplete_checkpoints` 的豁免面被收窄**: `execution-modes.md:41-44` 原义是「跳过校验, 继续执行」(整门不算), 本 spec 改为「仍逐对评估三态并全部留痕, 只把 missing / S4 降为 `bypassed`; S3 的输入矛盾错**不被豁免**」⇒ 已开该旗标的采用方会新看到 exit 2 的 `no_spec_contradicted`;
  3. **空纳入集的处置分格 (R2 rework 改写)**: 「audit 已启用、只开 pre_merge、其余全 off」(官方 config-example 场景 A/B 与 legacy `agent_team_audit_points: ["pre_merge"]` 都落这一格) **仍 pass**, 只是新增一行 `[INFO] 无前置 checkpoint 纳入校验` 的留痕 ⇒ 对这批采用方**零行为变化**; 而 `audit.enabled != true` 或 `pre_merge == off` 时脚本被直调, 从「静默 pass」变成 exit 2 (`audit_not_enabled` / `pre_merge_not_enabled`) —— 这两格在有调用方守卫的生产路径上不可达 (`phase-c-integrator/SKILL.md:131-132` 早退)。R1 写的「空集一律 exit 2」会把第一批采用方永久硬阻, 已作废;
  4. **S1 锚点校验继承 `allow_dangling_change_ids` (新增)**: 开了该键的迁移期采用方, 锚点缺失在新门下也只记 `[WARN]` 并按零报告继续评估, 不 exit 2 —— 与 `pre-write-validation.md:16-18` 写盘侧行为对齐;
  5. **坏 config JSON 的语义与 config-loader 散文刻意不同 (新增)**: `config-loader/SKILL.md:37` 是「警告 + 返回默认值」, 本门取 `config_unreadable` exit 2 (fail-closed) —— 否则损坏的 config 会静默落成空纳入集。
  五条迁移文案全部写进 CHANGELOG (「若历史 change 报告用旧 schema, 需补跑或临时 allow_incomplete」+ 上面 2-5 四条的处置)。

## Impact

| Type | Description |
|------|-------------|
| **Positive** | 门的 PASS 与被审 change 相关: 其他 change 的报告不再构成证据; 「缺失」与「不适用」可分辨且各有 audit trail 行; change_id 在 pre_merge 有了输入通道; 规程可测 (hermetic 红转绿) |
| **Risk** | R-a 采用方历史 change 报告**不服从写侧命名约定** → 收窄后转 missing (假红形态)。**R1 rework 改写了量级**: 本仓 786 份报告里, 归属不明 (`unattributed`) 实测 **170 份**、真旧 schema 只有 **6 份** —— 这不是「少量历史包袱」, 而是写侧散文约定的真实执行水平 (读侧机械化了, 写侧仍是散文)。缓解: `excluded_legacy_count` 与 `unattributed_count` **分开显影** + `unattributed` 文件名进 audit trail + CHANGELOG 迁移文案 + `allow_incomplete_checkpoints` 既有豁免。**写侧强制**不在本 spec 范围, 记 Phase D 缺口 issue |
| **Risk** | R-b `--no-spec` 是调用方声明, 交叉核验只到「diff 不触 openspec/changes/」; Level 2 PR 若 proposal 早已在 master 且分支不碰 change 目录, 声明 `--no-spec` 可绕过。**R2 rework 补第二条弱化路径**: 该核验本身会被**陈旧 base ref** 悄悄削弱 —— `--base` 若传裸本地分支名 (子模块 detached HEAD / 本地 `master` 落后于 `origin/master`, 本项目常态), `merge-base HEAD <base>` 偏移且**不报错**, diff 偏小 ⇒「不触 `openspec/changes/**`」假通过 ⇒ 全部 checkpoint 记 `not_applicable/level1-no-spec` 的假绿。缓解: `--base` 契约改为远程跟踪 ref + 陈旧时输出 `[WARN] base ref 陈旧` (§1, SC-22) + 声明进 audit trail (`scope_source=no_spec` + `[INFO]` 行) 可事后核对; 更强核验 待 owner 复议 #2 |
| **Risk** | R-c `post_brainstorm` / `mid_implementation` 为条件性产物, 采用方若启用且本 change 未触发, 新规则下转 missing (旧规则被他人报告掩盖)。缓解: `mid_implementation` **已在 Step 3 显式排除** (R1 rework, 与 `mid_post_spec` 同构 —— 原稿让它走 1.3(b) 不成立: (b) 只在 diff 全是 docs 时生效, 而最常见的「code diff + 进度未过 50% 阈值」仍会假红); `post_brainstorm` 见 待 owner 复议 #1 |
| **Risk** | R-d 脚本引入 subprocess (git diff / merge-base) — 与 file-scope 过滤同命令, 失败落 `git_failed` exit 2 (fail-closed), 不 crash |
| **Risk** | R-e (R1 rework 新增) 跨仓形态: 子模块 PR 时锚点面与 diff 面不同仓, 单参数会两个方向都坏 (详见 §1)。缓解: `--repo-path` / `--diff-repo-path` 分离 + 跨仓时禁用 (b) 通道 + SC-17 |
| **Risk** | R-f (R2 rework 新增) (b) 通道的**路径形状判据**天生量不到「实现工序是否发生」, 只能量「diff 长什么样」。已收窄两处 (排除 `openspec/archive/**` 的归档-only 与 openspec 语料型交付物, 纳入 `.aria/audit-reports/**` 使本仓真实 Phase A 分支可达, 见 §1.3), 但**判据 species 未变**; 残余形态 (如 Phase A 分支顺手改了一个 change 目录外的 `.md`) 仍靠 fail-closed 兜。存废与判据换代 待 owner 复议 #7, SC-5 的反事实集把放行面钉死 |
| **Risk** | R-g (R2 rework 新增) 脚本内联 config-loader 的缺省集与旧配置兼容映射 = **第二副本**, 上游改 `DEFAULTS.json` 或映射表而不改脚本会静默漂移。缓解: SC-21 在测试期对上游两文件逐字比对 (相等性断言, 上游一改即红); 根治 (给 config-loader 加程序化入口) 另案 |

## Tasks

- [ ] **B.0 语料冻结产物** (SC-2 前置): 生成 `corpus-freeze.md` 落**本 spec 目录内**, 内容 = 取样时刻的 `.aria/audit-reports/` 全量文件名 + 每份的**真实归属标注** (change_id 或 `legacy` / `unattributed`) + 取样命令与时刻。SC-2/SC-4 的期望值从这份产物的**标注列**取, 固化成字面数字, **不得**由被测谓词现算。**标注必须双列交叉 (R2 rework 补, 防自指恒绿以更软形态回归)**: Phase B 是无人值守, 标注者与谓词实现者是同一执行体, 共享同一套「文件名里哪段是 change_id」的心智模型 ⇒ 标注会跟着规则走而不是跟着事实走。故第一列取**与文件名无关的机械源** —— 报告 frontmatter 的 `context:` / `spec_id:` 字段 (本仓 78 份有) 或写盘时刻仓内活跃 change 目录的 git 历史; 第二列才是名匹配; **两列不一致的条目单列成表**供人裁, 不得由实现者自行取舍
- [ ] TDD RED: `tests/test_completeness_gate.py` 先写 **SC-1~SC-10 + SC-15~SC-17 + SC-18 + SC-19~SC-22** (含 issue case-1 / 本仓 case-2 两条 hermetic 复现), baseline 全红。**R2 rework 补 SC-18~SC-22**: 原稿的 RED 清单漏了 SC-18 (复议项可证伪化) 使其无任务承接, SC-19~SC-22 是本轮新增/重写项
- [ ] 实现 `scripts/completeness_gate.py` (stdlib only; `--repo-path` / `--diff-repo-path` / `--base` / 作用域 S1-S4 含 `--no-renames` 与 `allow_dangling_change_ids` 继承 / 匹配规则含末段形态与对称排除 / 三态 / 两个排除计数 / **Step 3 枚举源过 `checkpoints` 显式 > `adaptive_rules` > 默认 off 的优先级链** / **空纳入集三格 (A error / B pass+`no_prior_checkpoints` / C error)** / 报告目录缺失按零报告 / stdout 15 键契约 / exit code; **config 直读 + 内联缺省与旧配置兼容映射**)
- [ ] 改写 `execution-modes.md` §Pre-merge: Step 3 加 `mid_implementation` 排除 + Step 4-5 + 三态输出模板 + 消费方 fail-closed 义务 + `:44`/`:82` 豁免文案统一
- [ ] audit-engine SKILL.md: 输入参数 `change_id` + fenced bash 调用块 + 配置注释 + 相关文档指针
- [ ] phase-c-integrator SKILL.md: pre_hook 传 change_id / repo_path / diff_repo_path + 4.5 三态处置 + `:157` 勘正
- [ ] **旧 schema 散文残留形态族清扫 (4 处)**: `phase-a-planner:267` / `phase-b-developer:204,277` / `phase-c-integrator:157`; 清扫后重跑 `grep -rn 'audit-reports/[a-z_]*-{timestamp}\.md' skills/` 应为 0 命中
- [ ] report-storage.md §向后兼容 + pre-write-validation.md 关联行
- [ ] AB (**两读法并集, 不取豁免**): (1) **照跑**两个真 eval 套件验漂移 —— `audit-engine.json` (2 evals) + `phase-c-integrator.json` (3 evals); (2) `phase-c-integrator-pre-merge-gate.json` **不跑 AB 臂** (无 `evals` 键, `type=workflow_skill_subextension`), 改为跑其 8 条 fixture 绑定的单测 `python3 -m unittest test_pre_merge_gate.GateCheckTests -v` 并把该文件的形态差异写进 rule6_note (R2 rework; 认定本身列 待 owner 复议 #9); (3) `audit-engine.json` 新增定向 eval id 3 + `version.yaml` **1.5.0 → 1.6.0** (bump 前 `git fetch --all` + `ls-remote --tags` 查 #195 撞号); (4) 经 `/skill-creator` 真跑, 结果落 `ab-results/<date>-pre-merge-completeness-gate-change-scope/`; (5) 套件缺口 issue (aria-plugin)
- [ ] 枚举范围两条边界落地 (§1.2b): `iterdir()` 非递归 + `scanned_dir_depth` 输出 (**并已补进 §1.4 顶层键集**) + 无 checkpoint 前缀形态三桶都不收; **落点 = `report-storage.md` §向后兼容的第 (3)(4) 句** (§4 同步表), 核验 = SC-13 两条逐字 grep + SC-19 运行时断言
- [ ] 活体 dogfood: 在本仓对本 spec 自身与一个假想 id 各跑一次脚本 (SC-11), 证据抄进 handoff
- [ ] **ship 前基线重取**: `git fetch --all` + 确认主仓 `origin/master` 的 `aria` gitlink 现值 (R2 实测: 远端与本地已全面对齐, gitlink = `301641b`, 同伴 PR #202 已合, **无分叉**) + `git -C aria ls-remote --tags origin` 与并发轨 #195 的 `<vNEXT>` 复核撞号
- [ ] 版本 v1.71.2: aria 5 文件 + 主仓 **16** 个版本字符串点 + gitlink (**无前置排队**, 原稿的「排在同伴 v1.71.1 主仓同步之后」是幻影); 归档门 (SC-12)
- [ ] Phase D: #199 / aria-plugin#161 回帖 + 关闭; F8 config 注册面缺口立 issue (含把两个 `allow_*` 键补进 DEFAULTS.json); **写侧命名约定无强制** 另立 issue (R-a 的 170 份 unattributed 是它的实证); **config-loader 无程序化入口** 另立 issue (R-g 的第二副本是它的实证); 复议项 **#1-#9** 结论回写本 spec

## Success Criteria (可证伪; 每条自问「机制没实现会红吗」)

| SC | 断言 | 核验 |
|---|---|---|
| SC-1 | **hermetic case-1 (issue 复现)**: tmp 仓 (`git init -b master` —— **必须显式 `-b master`**, 现代 git `init.defaultBranch` 可能是 `main`, 否则 `--base master` 的 merge-base 解析失败落 `git_failed`; 一次 commit; feature 分支改 `openspec/changes/x/tasks.md` + `src/a.py`), **fixture 必须同时建锚点 `openspec/changes/x/proposal.md`** (否则按 S1 直接 `change_id_unanchored` exit 2, 与本条期望的 exit 1 冲突 —— R1 勘正), config `post_implementation=convergence`, 目录放 37 份 `post_implementation-R1-<ts>-other-{i}-code-reviewer.md` + 4 份 `post_spec-…-x-…`, `--change-id x` → `results` 含 `{post_implementation, x, missing}`, `verdict=fail`, exit 1, stderr/stdout 文案含 `post_implementation@x`。反事实: 把规则退回 `{checkpoint}-*.md` 通配 → 判 present → 红 | `test_completeness_gate.py::test_issue_case1_other_change_reports_do_not_count` |
| SC-2 | **hermetic case-2 (本仓文件名形态, R1 全面重写)**: fixture 语料取自 **B.0 冻结产物 `corpus-freeze.md`** (spec 目录内, 可追溯), **按名形态族穷举**, 每族至少 3 份: (F-a) 末段族 `…-{id}.md` 无 role 段; (F-b) role 后缀族 `…-{id}-{role}.md`; (F-c) `-aggregated`/`-aggregate` 族; (F-d) 无 `-R\d+-` 族 (`CONVERGED` / `FINAL` / `R5.5` / `R1prime` / `usN-mN` / `A1-` 席位前缀); (F-e) 真 2-field legacy 族; (F-f) unattributed 族 (截断/别名 id)。**期望值 = 冻结产物里的独立人工标注归属**, 逐字等值 (非 ≥1), **不得由被测谓词现算** —— 原稿写「= 清单中含 `-{id}-` 的份数」是由被测规则自身推导的**自指恒绿**, 规则漏掉多少形态它都绿 (本轮 Critical 1 正是被它放过)。**选样硬约束**: 所选 id 在 fixture 中**必须同时有 F-a 与 F-b 两族样本** —— 原稿选的 `pre-merge-gate-no-run-for-branch` 实测 72/72 份全是中缀形态、末段形态 **0 份**, 对唯一会红的形态族结构上失明。**两条反事实**: (1) 把规则 2 退回纯中缀 `-{id}-` → F-a 族少计 → 红; (2) 位置解析 (按第 4 段取 spec_id) 在 `A1-` 与 `R5.5` 形态上少计 → 红 | 同文件 `::test_real_corpus_shapes_token_bounded_match` |
| SC-3 | 有界包含排除 (F3), **三型各一 case** (R1 补后缀/中缀): (1) **前缀型** id 集 {`aria-orchestrator`, `aria-orchestrator-divestiture`} —— 只有后者的报告在场 → 前者 `missing`; 两者各有报告 → 各 `present` 且 `matched` 互不交叉; (2) **后缀型** {`gate-scope`, `pre-merge-gate-scope`} —— 只有后者的报告在场 → 前者 `missing`; (3) **中缀型** {`gate`, `pre-merge-gate-scope`} 同上。**反事实**: 谓词退回原稿的 `c'.startswith(id + "-")` 单向写法 → case (2)(3) 判 present → 红 (锁死「与被修 bug 同型的假绿」这条不变量) | `::test_bounded_containment_excludes_all_three_shapes` |
| SC-4 | **两个计数分离** (R1 重写): fixture config **明确写** `{post_spec: convergence, post_planning: convergence}` (两 checkpoint 都纳入, 消除「计数是全局还是纳入集并集」的歧义 —— 契约见 §1.4「两个计数的作用域」)。语料放: 真 2-field `post_spec-2026-04-11T0530Z.md` + `post_planning-2026-04-11T0530Z.md`; 末段形态 `post_spec-R1-2026-05-01-x.md` (x 在 `C` 内); 截断 id `post_planning-R2-CONVERGED-trunc-id.md` (`trunc-id` 不在 `C`) → `excluded_legacy_count == 2` **且** `unattributed_count == 1` **且** `unattributed == ['post_planning-R2-CONVERGED-trunc-id.md']` **且** 末段那份计入 `x` 的 `matched` (不进任何排除计数)。**反事实**: 用原稿的合并定义 (「不含任何 `-{c}-`」) → `excluded_legacy_count` 变 4 → 红 | `::test_legacy_vs_unattributed_counts_split` |
| SC-5 | not_applicable (b) **新判据** (R1 重写): diff 仅 `openspec/changes/x/proposal.md` + `openspec/changes/x/tasks.md` + `.aria/audit-reports/post_spec-R1-2026-01-01-x-tech-lead.md` (Phase A-only 的**本仓真实形态**, R2 rework: 判据已纳入 `.aria/audit-reports/**`) → `post_implementation@x` = `not_applicable/phase-a-only-no-implementation`, `verdict=pass`, stdout 含 `[INFO]` 与 `not_applicable`。**七条反事实, 每条独立断言红** (原稿标题写「四条」正文却列五条 —— R2 rework 一并订正为逐条计数): (1) diff 加 `src/a.py` → `missing` + fail; (2) **diff 加 `aria/skills/audit-engine/SKILL.md`** (纯 `.md` 的处方性 Skill 变更, 落在旧 `scope_skip_paths` 的 `*.md` 里) → **必须 `missing`**, 不得 not_applicable —— R1 Critical 3 点名的那一格; (3) diff 加 `docs/x.md` → `missing` (`docs/` 亦不再豁免); (4) diff 为空 (仍传 `--change-id x`) → `missing` (空集不放行); (5) 跨仓形态 `--diff-repo-path != --repo-path` 且 diff 仅 openspec (**显式传 `--change-id x`**, R2 rework 补 —— 跨仓时 S2 从 diff 仓解析不到本仓的 change 目录, 不传会先落 S4 exit 2 而测不到本条) → **仍 `missing`** (§1 fail-closed 条款); (6) **归档-only PR** (diff 全部在 `openspec/archive/2026-01-01-x/` 下, 本仓 `62de051` 形态) → **必须 `missing`**, 不得 not_applicable —— R2 Major `b9e07647` 点名的那一格: 归档是 D.2 工序, 它证明实现已发生; (7) **交付物本身是 openspec 语料的 cycle** (diff = `openspec/changes/x/proposal.md` + `openspec/archive/2025-12-16-spec-drafter/proposal.md`) → **必须 `missing`** | `::test_phase_a_only_not_applicable` / `::test_md_and_docs_diff_still_missing` / `::test_empty_diff_is_missing` |
| SC-6 | not_applicable (c) **新判据** (R1 重写): `post_planning=convergence`。(1) change 目录仅 `proposal.md` 且该 proposal **无 `## Tasks` 小节** → `not_applicable/no-a2-artifact`; (2) 放一份 3 行 `tasks.md` → `missing` (「做了但简单」不豁免); (3) **无 `tasks.md` 但 `proposal.md` 含 `## Tasks` 内联清单 (Level 2 标准形态, `templates/proposal-minimal.md:27-31`) → `missing`, 不得 not_applicable** —— 这条是本轮 Critical 4 的红线, 缺它则每个 Level 2 change 都自动豁免 post_planning; (4) 归档态 `archive/2026-01-01-x/` 同样三分支。**反事实**: 判据退回原稿「只看两个文件是否存在」→ case (3) 判 not_applicable → 红。**自证**: 对本 spec 自身目录跑 (3) 分支 (本 spec 正是内联 `## Tasks` 无 tasks.md) 必须得 `missing`/`present`, 与头部「审计计划: post_planning convergence」一致 | `::test_post_planning_a2_artifact_includes_inline_tasks` |
| SC-7 | 作用域: 无 `--change-id`、diff 触 `openspec/changes/{a,b}/` → `scope_source=diff`, `change_ids=[a,b]`, 逐对评估, `b` 缺一项即 fail; 都不触且无 `--no-spec` → exit 2 `change_scope_unresolved`; `--no-spec` 且 diff 触 change 目录 → exit 2 `no_spec_contradicted`; `--no-spec` 合法 → 全部 `not_applicable/level1-no-spec` + `scope_source=no_spec` + **`change_ids == []` 且 `results` 每条 `change_id is None`** (R2 rework 补: 原稿未定义这一格的取值); `--change-id typo` → exit 2 `change_id_unanchored` (归档态 `archive/2026-01-01-x/` 也算锚)。**R2 rework 新增三格**: (a) **rename 语义** —— fixture 把 `openspec/changes/x/proposal.md` 用 `git mv` 移到 `openspec/archive/2026-01-01-x/proposal.md` 并 commit, 无显式 id → S2 仍取到 `x`, `scope_source=diff`; **反事实**: 去掉 `--no-renames` → 只剩目的侧路径 → 落 S4 exit 2 → 红; (b) **锚点逐字化** —— `C` 含 `aria-orchestrator-divestiture` 而不含 `orchestrator`, `--change-id orchestrator` → exit 2 `change_id_unanchored`; **反事实**: 用 `pre-write-validation.md:25` 的裸 glob `archive/*-orchestrator/` → 判有锚 → 落 `missing` → 红; (c) **`allow_dangling_change_ids` 继承** —— 同一 typo 输入 + config `audit.allow_dangling_change_ids=true` → **不 exit 2**, 记 `[WARN] dangling change_id: orchestrator` 并按零报告评估 (⇒ `missing`, 或叠加 `allow_incomplete_checkpoints` 后 `bypassed`); **反事实**: 不继承该键 → 仍 exit 2 → 红 | `::test_scope_resolution_matrix` / `::test_rename_and_anchor_semantics` / `::test_allow_dangling_inherited` |
| SC-8 | Step 3 枚举 (R1: 补 `mid_implementation` 与排序契约): config `{post_spec: convergence, post_planning: on, mid_post_spec: convergence, mid_implementation: convergence, post_closure: convergence, pre_merge: challenge, post_implementation: off}` → `checked_checkpoints == ['post_planning', 'post_spec']` (**`sorted()` 字典序**, §1.4 已定义, 消除「config key 序 vs sorted」的实现歧义; 字符串 `on` 纳入; `off` 与**四个**排除项 `pre_merge`/`post_closure`/`mid_post_spec`/`mid_implementation` 不纳入)。**注**: 本 fixture 不含 `post_brainstorm` 键 —— 待 owner 复议 #1 无论怎么裁, 本条断言都不变; #1 的两个分支各自的 SC 见 SC-18 | `::test_step3_enumeration` |
| SC-9 | 豁免 **三路全断言** (R1 补 S4-bypassed + 文案对齐): (1) `allow_incomplete_checkpoints=true` + 一项 missing → `verdict=bypassed`, exit 0, `bypassed=true`, 文案逐字含 `[WARN] incomplete checkpoint gate bypassed: missing=` (**与 `execution-modes.md:82` 同字面** —— 原稿造的 `bypassed by config: missing=` 是 SOT 两种拼法之外的第三种, 已删); (2) **S4-bypassed**: 同旗标 + 无法解析作用域 → `verdict=bypassed`, exit 0, `error_kind=null`, `scope_source=null`, `change_ids=[]`, `results=[]`, 文案含 `scope_unresolved=1`; (3) 同旗标下 `change_id_unanchored` 仍 exit 2 (`verdict=error`) | `::test_bypass_semantics` |
| SC-10 | 契约: 四种 verdict 下 stdout 均可 `json.loads`, 顶层键集逐字 = §1.4 的 **15 项**列表 (含 `unattributed_count` / `unattributed` / **`scanned_dir_depth`** / **`no_prior_checkpoints`**), `results` 每条含 **`enabled_by`**, `results` 恒 list; `--base` 缺失 → argparse exit 2; 坏 config JSON → `config_unreadable` exit 2 (**该格只在脚本直读 config 时可达** —— 若经 config-loader 的「坏 JSON ⇒ 返默认值」语义, 该输入只会落成空纳入集, 断言恒绿, 见 §1.4)。**R2 rework**: 键集从 13 补到 15 项, 消除与 SC-19(a) 的结构性互斥 (原稿 §1.4 列表不含 `scanned_dir_depth` 而 SC-19(a) 断言 stdout 含它 ⇒ 两条不可能同绿) | `::test_stdout_contract` |
| SC-11 | **活体**: 在本仓 (post_spec 审计 CONVERGED 后) `--change-id pre-merge-completeness-gate-change-scope --base master` → post_spec `present` (**≥6**: R1 五席 + 聚合各 1 份, 实测该 6 份文件名全为 role/aggregated 后缀族) 且 post_planning **`present` 或 `missing`** —— **不得**是 `not_applicable/no-a2-artifact` (本 spec 有内联 `## Tasks`, 见 SC-6 case 3; 原稿把 `not_applicable` 写进期望是 Critical 4 的自钉恒绿); 另断言 `unattributed_count > 0` 且 `unattributed` 非空 —— **只断言 `> 0`, 不写死期望值** (R2 rework: 该数字随口径变 —— 全 8 checkpoint 口径实测 **170** 份, 而本仓 `.aria/config.json` 的 `checked = {post_planning, post_spec}` 口径实测 **160** 份, §1.4 定义的统计面是后者; 原稿括注写 170 是全 8 口径, 与本节统计面对不上, 会让 Phase B 误以为期望值可写死); 假想 id `zz-not-a-change` → exit 2 `change_id_unanchored`; 输出抄进 handoff。此为手动 dogfood (F1: 本仓 pre_merge off, 生产不会调) | 命令 + handoff 证据 |
| SC-12 | 既有测试全绿: `cd aria/skills/audit-engine/tests && python3 -m unittest discover -s . -p 'test_*.py' -v`; 跨 skill 消费方 `cd aria/skills/state-scanner/tests && python3 -m unittest discover -s . -p 'test_*.py'` (collectors/audit.py 语料测试) 与 `aria/skills/phase-c-integrator/tests` 同命令 — 三者 0 failure; 归档门对本 spec `spec_complete.py` 判 `completeness_gate.py` alive (SKILL.md bash 块, F7), 无 dead-code block | 命令 |
| SC-13 | 文档机检 (R1 勘正): `grep -c '{checkpoint_name}-\*\.md' execution-modes.md` = 0; `grep -c 'completeness_gate.py' audit-engine/SKILL.md` ≥ 1 且命中行在 ```` ```bash ```` 块内; `grep -c 'change_id' phase-c-integrator/SKILL.md` 较基线 +≥1; **`grep -rn 'audit-reports/[a-z_]*-{timestamp}\.md' skills/` = 0 命中** (四处残留全清, 非只 `phase-c-integrator:157`); `grep -c '不计入' report-storage.md` ≥ 1; **`grep -c '子目录内的报告不计入完整性证据' report-storage.md` = 1** 且 **`grep -c '三个桶都不收' report-storage.md` = 1** (R2 rework 新增 —— §1.2b 的两条枚举边界原本「schema 文档写明」却未点名任何文件、也无机检, 可静默不做); **`grep -c 'adaptive_rules' execution-modes.md` 较基线 +≥1** (Step 3 枚举源过优先级链); `execution-modes.md` 内 `bypassed` 两处文案逐字相同; **调用串机械护栏 = 计数恰 2** (`grep -c 'completeness_gate.py' ` 在 SKILL.md fenced bash 块与 execution-modes.md 各 1, 照 `execution-modes.md:152` 竞品探针「SC-17 计数恰 2」先例) —— 原稿写「三份文件的调用串逐字相同」不可执行: §1 只规定**两处**调用串, §3 给 phase-c-integrator 的是参数与 4.5 处置、不含调用串 | grep |
| SC-14 | Rule #6 **两读法并集** (R1 改写, R2 rework 收敛照跑面): (a) **照跑两个真 eval 套件** —— `audit-engine.json` (2 evals) + `phase-c-integrator.json` (3 evals) 各跑一次 with/without 臂, 结果落 `ab-results/`, 无回归; **`phase-c-integrator-pre-merge-gate.json` 不在 AB 臂上** —— 它 `type: "workflow_skill_subextension"`、**无 `evals` 键**、8 条 `fixtures[]` 各绑一条单测, `sum(len(evals))` 对它计 0 ⇒ 「对 0 个 eval 断言无回归」恒真 = 假证据; 改为断言 `python3 -m unittest test_pre_merge_gate.GateCheckTests` 8 条 fixture 对应用例全绿, 并在 ab-results README 记明该文件的形态。本 spec 自引的先例同判 (`archive/2026-08-23-pre-merge-gate-no-run-for-branch/proposal.md:284` 把它记作 fixtures 并明写「照跑 = 测量剧场」, `:228` 的处置是往 catalog 登记 fixture 而非跑 AB 臂)。**认定本身列 待 owner 复议 #9** (Rule #10: 实施者不得自行删闸 —— 此处不是删闸, 是认定该文件结构上从不在 AB 臂内, 请 owner 确认); (b) **定向 fixture** —— eval id 3 登记进 `audit-engine.json` (descriptive 形态: 给定目录清单 + config + change_id, 要求写出调用命令行、三态逐对结果、`[INFO]`/`[WARN]`/ERROR 措辞; expectations 含「不得把其它 change 的报告当证据」「末段形态 `…-{id}.md` 必须计入」「not_applicable 必须 surface」三条) + `version.yaml` bump; 真跑一次, 回退本 spec 后该 eval 的 with 臂应转差 (可证伪实证写进 ab-results README); (c) **套件缺口 issue** 号写回本 spec | 文件 + issue |
| SC-15 | **空纳入集三格** (R1 新增短路, R2 rework 重写为分格 —— R1 的三个 fixture 全落在调用方早退拦下的**生产不可达**态, 唯一可达的那格零覆盖): (1) **格 A** config 为 `{}` / 无 `audit` 块 / 文件不存在 → `checked_checkpoints == []`, `verdict=error`, `error_kind=audit_not_enabled`, exit **2** (fail-closed; 这三格生产不可达, 只覆盖直调与误接线); (2) **格 C** `{audit:{enabled:true, checkpoints:{pre_merge:'off'}}}` → `error_kind=pre_merge_not_enabled` exit 2; (3) **格 B — 唯一生产可达的空集人群, 原稿零覆盖**: `{audit:{enabled:true, mode:'manual', checkpoints:{post_brainstorm:'off',post_spec:'off',post_planning:'off',mid_implementation:'off',post_implementation:'off',pre_merge:'convergence',post_closure:'off'}}}` (**逐字取自官方 `config-loader/config-example.md:381-400` 场景 A**) → `checked_checkpoints == []`, **`verdict=pass`, exit 0, `no_prior_checkpoints=true`, `bypassed=false`**, stdout/trail 含 `[INFO]` 与 `无前置 checkpoint 纳入校验`; **反事实**: 退回 R1 的「空集一律 error」→ 该合法采用方 exit 2 硬阻 → 红; (4) **legacy 映射两格**: `{experiments:{agent_team_audit:true, agent_team_audit_points:['post_spec','pre_merge']}}` 且无 `audit` 块 → `checked_checkpoints == ['post_spec']` (**非空**, 证明未绕过映射; **反事实**: 不内联映射的实现 → 空集 → 红); 同结构但 `agent_team_audit_points:['pre_merge']` → 落格 B 判 pass (**反事实**: 判 error → 红) | `::test_empty_checkpoint_set_three_buckets` / `::test_legacy_config_mapping_inlined` |
| SC-16 | **`mid_implementation` 排除** (R1 新增): config `{mid_implementation: convergence, post_spec: convergence}`, **传 `--change-id x` 且建锚点 `openspec/changes/x/proposal.md`** (R2 rework 补 —— 原稿只写 diff 含 `src/a.py` 而不触 `openspec/changes/**`、也不传 `--change-id`, 按 §1.1 会先落 S2→S4 `change_scope_unresolved` exit 2, 与期望的 `verdict=pass` 直接冲突; 同族漏清扫另见 SC-5 case (4)(5) 已补 `--change-id`), diff 含 `src/a.py` (**非** docs-only, 即旧 (b) 通道不生效的最常见形态), 目录只有属于 `x` 的 post_spec 报告 → `checked_checkpoints` **不含** `mid_implementation`, `verdict=pass`。**反事实**: 不加排除条款 → `mid_implementation@x = missing` + fail → 红 (阈值未触发的合法不产出被当缺失) | `::test_mid_implementation_excluded` |
| SC-17 | **跨仓执行上下文** (R1 新增): 造主仓 tmpA (含 `.aria/audit-reports/` + `openspec/changes/x/`) 与子模块 tmpB (无 `openspec/` 无 `.aria/`, diff 含 `skills/foo/SKILL.md`)。(1) `--repo-path tmpA --diff-repo-path tmpB --change-id x` → 锚点解析成功 (不报 `change_id_unanchored`)、报告在 tmpA 找、diff 取自 tmpB; (2) 缺省 `--diff-repo-path` 时等于 `--repo-path` (向后兼容); (3) 跨仓时 (b) 通道禁用 (见 SC-5 case 5)。**反事实**: 单参数实现传 tmpB → exit 2 `change_id_unanchored` → 红; 传 tmpA → diff 取错仓, (b) 误判 not_applicable → 红 | `::test_split_repo_and_diff_paths` |
| SC-18 | **复议项可证伪化** (R1 新增, 防「owner 裁完仍无测试证伪」): 待 owner 复议 #1 (`post_brainstorm` 排除) 两个分支各配一条 case —— 采纳: config 含 `post_brainstorm: convergence` → `checked_checkpoints` 不含它; 不采纳: 含它且零报告 → `missing` + fail。Phase B 按 owner 裁决**只保留其一**并删掉另一条, 不得两条都不写 | `::test_post_brainstorm_exclusion_decision` |
| SC-19 | **枚举范围两条边界 (2026-09-07 补, §1.2b; R2 rework 换掉一个恒绿反事实并补 (c))**: (a) tmp 仓 `.aria/audit-reports/` 顶层放一份属于本 change 的合规报告, **子目录** `sub/` 里再放一份同样合规的 → `matched_count == 1`, 结果里不含子目录那份, stdout 含 `scanned_dir_depth: 1`; **反事实**: `iterdir()` 换成 `rglob()` ⇒ `matched_count` 变 2 ⇒ 红。(b) 顶层放 `<change_id>-audit-trail.md` (含 change_id 但**不以 checkpoint 名开头**) 且无其它报告 → 该 (checkpoint, change_id) 判 `missing`, 且该文件三个桶都不收 (`matched` / `unattributed_count` / `excluded_legacy_count`)。**反事实换新 (原写「去掉规则 1 的 `startswith` ⇒ 判 present ⇒ 红」经机械代入为假, 该断言恒绿)**: change_id 在该文件名里位于**串首、左侧无连字符**, 规则 2 的两个分支 (`-{id}-` 子串 / stem 以 `-{id}` 结尾) **都不命中** ⇒ 去掉规则 1 它仍不会被判 present; 本仓真实样本 `sibling-spec-probe-audit-trail.md` 对 id `sibling-spec-probe` 实测规则 2 零命中可直接反证。改用**真能翻转**的变异体: 去掉 `unattributed` 定义里的「以 checkpoint 前缀开头」条件 ⇒ 该文件被收进 `unattributed`, `unattributed_count` 由 0 变 1 ⇒ 红。(c) **规则 1 的前缀约束单独设格 (R2 rework 新增, 补上 (b) 守不住的那条不变量)**: config 只纳入 `post_spec`, 顶层只放 `post_planning-R1-2026-01-01-x-tech-lead.md` → `post_spec@x` 判 `missing`, `matched_count == 0`, 且该文件不进 `post_spec` 口径的任何计数; **反事实**: 去掉规则 1 的 `startswith(f"{checkpoint}-")` ⇒ 它经规则 2 的 `-x-` 命中被 `post_spec` 收走 ⇒ `matched_count` 变 1 ⇒ 红。**语料依据 (R2 rework 逐字重取)**: 冻结快照 `3f4b379` 内真实存在子目录 `pr19-submodule-scan/` `wf-r1fix/`, 以及 5 份 `*-audit-trail.md` (`a1-entry-claim` / `linked-issue-field-availability` / `linked-issue-normalization` / `premerge-gate-mainbranch-failclosed` / `sibling-spec-probe`, 与 §1.2b 表格逐字同集) | `test_completeness_gate.py::test_enumeration_boundaries` |
| SC-20 | **Step 3 枚举源过优先级链** (R2 rework 新增, 锁 Critical `ca4cd11f`): (1) **场景 C** config **逐字取自** `config-loader/config-example.md:419-440` (`mode: adaptive` + `adaptive_rules.level_2=convergence` + `checkpoints` 只显式写 `post_spec: challenge` / `post_closure: off`), change `x` 的 `proposal.md` 头部含 `> **Level**: Minimal (Level 2 Spec)` → `checked_checkpoints` **必须含 `post_implementation`** (由 `adaptive_rules.level_2` 启用) 且其 `results` 条目 `enabled_by == 'adaptive:level_2'`; `post_spec` 的 `enabled_by == 'explicit'`。**反事实**: 枚举合并后对象的字面值 (DEFAULTS 把 `post_implementation` 填成 `"off"`) ⇒ 它不被枚举 ⇒ 「该跑的没跑」查不出 ⇒ 红 (与被修 bug 同型的假绿)。(2) **场景 B** config 逐字取自 `:402-417` (adaptive 且**无** `checkpoints` 块), Level 3 change → `checked_checkpoints` 非空且全部 `enabled_by == 'adaptive:level_3'`; **反事实**: 字面枚举 ⇒ 八键全 `"off"` ⇒ 空集 ⇒ 红。(3) **Level 不可解析**: change 目录的 `proposal.md` 无 Level 行且 `mode=adaptive` 且该 checkpoint 无显式值 → `error_kind=spec_level_undetermined` exit 2 (不猜) | `::test_step3_priority_chain` |
| SC-21 | **config 直读 + 内联缺省/映射的相等性** (R2 rework 新增, 锁 Major `84d49ad0` 与 R-g): (1) 脚本内联的 audit 缺省集 (`enabled` / `mode` / `adaptive_rules` / 八个 `checkpoints` 键与值) **逐字等于** 测试期读到的 `config-loader/DEFAULTS.json` 的 `audit` 子集对应字段 —— 用 `importlib` 文件直载脚本模块取其常量, 与 `json.load(DEFAULTS.json)` 比对; (2) 脚本内联的旧配置兼容映射表 **逐字等于** `config-loader/SKILL.md` §旧配置兼容层的三条 (`post_spec` / `post_implementation` / `pre_merge` → `"convergence"`), 且触发条件为「`experiments.agent_team_audit === true` **且**无 `audit` 块」两条同时成立 (只满足一条 → 不映射); (3) 坏 JSON → `config_unreadable` exit 2 (**不**按 config-loader 散文的「警告 + 返默认值」处理, 否则该输入会静默落成空纳入集)。**反事实**: 把脚本常量里任一 checkpoint 缺省改成 `"convergence"` / 把映射表删一条 ⇒ (1)(2) 红 (上游一改即红, 这正是第二副本的漂移守卫) | `::test_inlined_defaults_equal_upstream` / `::test_inlined_legacy_mapping_equal_upstream` |
| SC-22 | **环境边界三格** (R2 rework 新增): (1) **base 陈旧告警** —— tmp 仓造本地 `master` 落后于 `origin/master` 两个 commit 的形态, `--base master` → 输出含 `[WARN] base ref 陈旧`, 且**不阻断** (verdict 由其余判据决定); 传 `--base origin/master` → 无该 WARN; **反事实**: 不做该比对 ⇒ 陈旧 ref 静默偏移 merge-base、S3 交叉核验假通过 ⇒ 红。(2) **报告目录不存在** —— `.aria/audit-reports/` 整个不存在 → **不抛 `FileNotFoundError`**, `verdict` 按零报告评估 (`missing` 或 not_applicable), stdout 仍可 `json.loads` 且 `scanned_dir_depth == 1`, trail 含 `[WARN] .aria/audit-reports/ 不存在`; **反事实**: 直接 `iterdir()` ⇒ crash ⇒ 红 (首次启用 pre_merge 的采用方正是这种状态)。(3) **`--base` 解析失败** (传不存在的 ref) → `error_kind=git_failed` exit 2, 不 crash | `::test_env_boundaries` |

## rule6_note (Rule #6 — 判据表第三行, SOT `standards/conventions/skill-benchmark-exemption.md` §2-§3)

> **R1 rework 说明**: 档位本身在 post_spec R1 出现 **1 席判第二行 / 2 席判第三行**的分歧 (双方对事实无分歧: 套件 2 evals、关键词零命中, 三席实测一致; 分歧在「套件覆盖外」的解释)。本 spec **不靠裁决取豁免**, 改为**执行两读法的并集** —— 第二行要求的「照跑」和第三行要求的「三义务」全做。档位标签本身仍列 待 owner 复议 #5, 但无论怎么裁, Phase B 的动作集不变。

- **处方性 · 运行时指令面 (第二行动作: 照跑, 零裁量)**: `execution-modes.md` §Pre-merge Step 3-5 + 三态模板 + 消费方 fail-closed 义务; `audit-engine/SKILL.md` 调用块与 `change_id` 参数; `phase-c-integrator/SKILL.md` pre_hook 4/4.5。**照跑面 (R1 补全, R2 rework 收敛)**: 真 eval 套件两个 —— `ab-suite/audit-engine.json` (文件内 `version` 1.0.0, 2 evals) + `ab-suite/phase-c-integrator.json` (3 evals), 两者**实存**且本 spec 改其处方面 (pre_hook 步骤 4/4.5), **照跑 with/without 臂**。`ab-suite/phase-c-integrator-pre-merge-gate.json` **形态不同, 不进 AB 臂**: `type: "workflow_skill_subextension"`, **无 `evals` 键**, 8 条 `fixtures[]` 各带 `test_case_in_unit_tests` 指向 `test_pre_merge_gate.GateCheckTests.*` (= 绑定单测的契约夹具集), `version.yaml` 的 `total_eval_cases` 按 `sum(len(evals))` 对它计 **0** ⇒ 「跑它的 with/without 臂」在其上无结构对应物, 对 0 个 eval 断言「无回归」是恒真的假证据; 其 `spec`/`issue` 也指向 C.2.4 CI 门 (Aria#60) 而非本 spec 改的 audit-engine pre_hook。处置 = **跑其 8 条 fixture 绑定的单测** + 在 ab-results README 记明形态差异, 与本 spec 自引先例一致 (`archive/2026-08-23-pre-merge-gate-no-run-for-branch/proposal.md:284` 明写「照跑 = 测量剧场」, `:228` 的处置是登记 catalog fixture)。认定列 待 owner 复议 #9 (Rule #10: 不由实施者自行删条)。
- **第三行三义务 (同时做)**: (1) 点名行为 — A. 编排者在 pre_merge 入口用**被审 change 的作用域**调用 `completeness_gate.py` 并按三态路由, 不再把「目录里有同 checkpoint 报告」当通过; B. `not_applicable` 必须以 `[INFO]` 行 surface 其机械依据, 不得折叠进「校验通过」; C. `unattributed_count > 0` 时必须以 `[WARN]` 行列出文件名。**为什么现有套件测不到**: `audit-engine.json` 的 2 evals 全是 per-round 竞品探针场景 (id 1 sibling_found 渲染 / id 2 not_established 措辞), 无一进入 pre_merge gate (`grep -ic 'completeness\|missing_checkpoint\|allow_incomplete'` = 0)。(2) 定向 fixture — eval id 3 (SC-14b), 可证伪: 回退 SOT 后 with 臂对 A/B/C 三 expectation 应失分; (3) 套件缺口 — aria-plugin 立 issue「audit-engine AB 套件零覆盖 pre_merge completeness gate」(Phase D 任务; SOT §3 第 3 条要求成文缺口 —— 「套件的盲区是债, 不是豁免理由」)。同族先例走第三行且三义务齐备: `archive/2026-08-23-pre-merge-gate-no-run-for-branch/proposal.md:282-284` (缺口挂 aria-plugin#127)。
- **描述性 / 纯代码** (脚本 / 测试 / report-storage 注 / 四处 `{timestamp}` 勘正 / CHANGELOG): substitute = **SC-1~SC-10 + SC-13 + SC-15~SC-17 + SC-19~SC-22** (R2 rework 与头部 Rule #6 行对齐成同一集合 —— 原头部写 `SC-1~SC-13`、本行写 `SC-1~SC-10 + SC-13 + SC-15~SC-17`, 两处互不相等, Rule #6 的合规证据面自身不自洽)。`description` 字段零变动。

## 待 owner 复议

1. **Step 3 是否追加排除 `post_brainstorm`** (R-c): 它与 `mid_post_spec` 同为条件性产物 (brainstorm 是 A.0.5 可选, `audit-engine/SKILL.md:60`), 且其产物 (`docs/decisions/DEC-*` / `.aria/brainstorm-*`) 与 change_id 无机械关联。**推荐默认: 追加排除**, 理由与 `:51-52` #79 条款同构 (「启用即会误阻」)。**R1 勘正**: 原文写「不采纳则 SC-8 的排除集少一项」不成立 —— SC-8 的 fixture config 本就不含 `post_brainstorm` 键, 两个分支都不改 SC-8。两分支各自的可证伪断言已补进 **SC-18**, owner 裁完 Phase B 只保留其一。**注**: `mid_implementation` 已不在本条范围 —— 它在 R1 rework 中直接排除了 (与 `mid_post_spec` 同因同构, 见 §1.3), 不作为复议项。
2. **`--no-spec` 交叉核验强度** (R-b): 现设计 = 「diff 不触 `openspec/changes/**`」。可选加固: 同时要求 `refs/aria/coordination` 无本容器 active claim 指向某 spec dir (需读 state-scanner claim 契约, 跨 skill 依赖)。**推荐默认: 本 spec 不加固**, 声明进 audit trail 可事后核对; 加固另案。
3. **可配置下界 N** (issue 修法 3, 候选 C): **推荐默认: 不做**, 按 change 收窄后 N=1 即「≥1 份属于本 change」; 若要, 需先补 F8 的 audit 段 config 注册面。
4. **版本号**: PATCH v1.71.2 (SOT 规程 + 脚本, 无新 Skill)。**R2 rework 更新并发面**: 真正在飞、同抢 v1.71.2 的是 **Aria#195 `handoff-multibranch-subdir-path-fidelity`** (其 `proposal.md:352` 现推荐改走 MINOR `v1.72.0` 但仍待 owner 拍板 ⇒ `v1.71.2` 未出局); `a1-entry-claim-duplicate-work-guard` **已归档** (PR #202 merged), 不再是并发轨。`ab-suite/version.yaml` 的**套件**版本**撞号已经发生**: 现值已是 `1.5.0` (同伴轨占用), 本轨顺延到 **1.6.0** (见 §4)。bump 前仍 `git fetch --all` + `git -C aria ls-remote --tags origin` + 读同伴 handoff `<vNEXT>`。

**以下三条为 post_spec R1 审计留下的 conflicted / 分歧项 (R1 rework 新增)**:

5. **Rule #6 档位标签** (`d94ee0bc` vs `55a7db0e`, 1 席 vs 2 席): 走判据表第二行 (处方性·运行时指令面 ⇒ 照跑 AB, 零裁量) 还是第三行 (套件覆盖外 ⇒ substitute + 三义务)。双方对事实无分歧, 分歧在「套件覆盖外」是指「本轮套件没有 eval 到达该状态」(第三行成立, 有归档先例 `2026-08-23-pre-merge-gate-no-run-for-branch:282-284`) 还是「只有结构性不可测才算, 覆盖缺口应照跑并补」(第二行, SOT §2 附加约束「指令流程变动 ⇒ 一律第二行」)。**本 spec 的处置: 不等裁决, 两读法并集执行** (照跑三个既有套件 + 补定向 fixture + 缺口 issue), 故裁决不改变 Phase B 动作集, 只影响 rule6_note 的标签写法。**推荐默认: 保留第三行标签 + 并集执行**。
6. **§1.3(c) 判据口径** (`4e504aa5` vs `368b926b`, conflicted): 两侧量的不是同一件事 —— code-reviewer 量「已有 post_planning 报告却被判 not_applicable」的**已观测误放** = 0 例; backend-architect / knowledge-manager 量「A.2 确已执行但产物内联 ⇒ 判据说未执行」的**结构性入口** = 每个 Level 2 change。两组数字不互斥 (那些 change 本就没跑 post_planning, 不会进 code-reviewer 的反例集)。**本 spec 的处置: 已按 fail-closed 方向改成「任何 A.2 产物 (含内联 `## Tasks`)」** —— 收窄豁免面在 Rule #10 下永远是安全侧, 不需等裁决; 若 owner 认为「内联 Tasks 也不算 A.2 产物」, 那是**放宽**豁免面, 须由 owner 明示 (AI 不得自行放宽)。
7. **§1.3(b) 是否保留任何 diff 类 not_applicable 通道** (`8c7972ce`): R1 已把基于 `scope_skip_paths` 的原判据**删除**, 换成「Phase A-only PR」(与 issue 报告方独立论证放行的那格同义)。knowledge-manager 更进一步认为 (b) **没有合法触发场景** (文档型 diff 下 post_implementation 仍会照跑并产出报告 ⇒ 命中 present, (b) 唯一被触发的时刻就是「本该跑却被跳了」)。**R2 rework 追加两条实证 (`b9e07647`, 2 席)**: (1) **假绿向** —— 原判据把 `openspec/archive/**` 也算进「B.2 未发生」, 而本仓 `62de051` 是 Level 3 已 ship 的 change 的**归档-only 提交** (三文件全在 archive 下), 按原判据会拿免检票, 而三态 first-match 下 `present` 优先 ⇒ (b) 唯一被触发的时刻恰是「post_implementation 报告确实缺失」= 门该拦的那一次; 同型的还有「交付物本身就是 openspec 语料」的 cycle。(2) **空转向** —— 本仓真实 Phase A 分支惯例把审计报告落 `.aria/audit-reports/` (实测 `813e82c` 12/2, `f634d83` 6/1, `2c8eaa6` 1/1), 该目录不在 `openspec/**` 下 ⇒ 原判据在本仓规范工作流里几乎不可能触发。**本 spec 的处置 (不等裁决, 因为两向都是收窄或对齐现实, 非放宽)**: 判据改为「全部变更文件 ⊆ `openspec/changes/{作用域内 id}/**` ∪ `.aria/audit-reports/**`」, 并在 SC-5 补两条反事实 (归档-only PR / openspec 语料型 cycle 均**必须** `missing`)。**仍待 owner 裁的是存废本身**: **推荐默认 = 保留收窄后的 (b)** (它对应 issue 里真实发生过的 Phase A-only PR 场景, SC-5 的七条反事实把放行面钉死); 若 owner 认同「零合法触发场景」的论证, 则整条 (b) 删除, SC-5 相应改为「Phase A-only 亦 missing」。两分支都不影响其余条款。**判据 species 未变**这一点已记入 Impact R-f。

8. **空纳入集判 `pass` 还是 `error`** (R2 Critical `fdb30703`, 3/5 席): 「`audit.enabled=true` ∧ 只开 `pre_merge` ∧ 其余全 off」这一格 —— 官方 `config-loader/config-example.md:381-400` 场景 A、`:402-417` 场景 B、以及 legacy `agent_team_audit_points: ["pre_merge"]` 经兼容映射后都落这里, 且在有调用方守卫的路径上它是**唯一可达的空集人群**。**本 spec 的处置 (v3): 判 `pass` + `no_prior_checkpoints=true` + `[INFO]` 留痕** (§1.4 格 B), 理由 = Rule #10 白名单**第一类**「配置显式关闭是 owner 的配置决定, 是合法豁免来源」(`configured-gate-authority.md:35`) + `audit-engine/SKILL.md:410-411` 的空集 `pass-through (不降级)` 先例; R1 的「一律 error」会让这批合法采用方**永久硬阻且无逃生口** (`allow_incomplete_checkpoints` 明示不救)。**请 owner 拍板的是语义选择本身**: 保持 `pass`, 还是保留 `error` 并另开一个可配置逃生口 (若后者, 需新增 config 键 ⇒ 与 F8 注册面缺口同面)。SC-15 已把两分支的可证伪断言都写好, 裁完只保留其一。
9. **`phase-c-integrator-pre-merge-gate.json` 是否算 Rule #6 照跑面** (R2 Major `4f360230`, 3/5 席): 该文件 `type: "workflow_skill_subextension"`、**无 `evals` 键**、8 条 `fixtures[]` 各绑一条单测, `total_eval_cases` 对它计 0 ⇒ 「跑 with/without 臂」无结构对应物, 对 0 个 eval 断言无回归是恒真假证据; 其 `spec`/`issue` 指向 C.2.4 CI 门 (Aria#60) 而非本 spec 改的 audit-engine pre_hook; 本 spec 自引的先例 (`archive/2026-08-23-pre-merge-gate-no-run-for-branch/proposal.md:284`) 明写「照跑 = 测量剧场」。**本 spec 的处置 (v3): 照跑面收敛为 `audit-engine.json` + `phase-c-integrator.json` 两个真 eval 套件, 该文件改为跑其 8 条 fixture 绑定的单测并在 rule6_note 点名形态差异** —— 这不是「删闸」(它结构上从不在 AB 臂内), 但按 Rule #10「已启用的审计检查点不得由 AI 自行豁免」, 认定权归 owner, 故列此条请复核。若 owner 认为仍须以某种方式「跑」它, 需同时给出该动作的可执行定义 (否则 SC-14(a) 在它上面不可证伪)。

## References

- SOT (@`301641b`): `aria/skills/audit-engine/references/execution-modes.md:23-82,37-38,44,51-52,82,152,185` · `audit-engine/SKILL.md:47-54,75-81,105,123-125,381-388,403-419` · `references/report-storage.md:8-39` · `references/pre-write-validation.md:11-31` · `references/report-format.md:5` · `phase-c-integrator/SKILL.md:126-157,252-265,299` · `phase-c-integrator/scripts/path_coverage.py:1-50,547-549` · `agent-team-audit/references/audit-points.md` (checkpoint 触发定义) · `state-scanner/scripts/collectors/audit.py:52-114` (文件名 token 解析先例) · `state-scanner/scripts/lib/spec_complete.py:924-930` (liveness 分类; **该行号只对 `301641b`**, @`0545f86` 是 `:903`) · `config-loader/DEFAULTS.json:118-123` (`scope_skip_paths` 缺省; audit 键集**不含**两个 `allow_*`) · `config-loader/SKILL.md:8-10` (`user-invocable: false` / `allowed-tools: Read, Glob` —— **无程序化入口**, 目录内零 `.py`) · `config-loader/SKILL.md:37` (坏 JSON ⇒ 警告 + 返默认值) · `config-loader/SKILL.md:305-331` §旧配置兼容层 (`experiments.agent_team_audit` ⇒ 三键 `convergence` 映射) · `config-loader/config-example.md:280` (优先级链) · `:381-400` 场景 A / `:402-417` 场景 B / `:419-440` 场景 C · `phase-a-planner/SKILL.md:250,267` · `phase-b-developer/SKILL.md:204,277` (旧 schema 散文残留)
- 规范: `standards/conventions/skill-benchmark-exemption.md` §2-§3 (Rule #6; **判据表 `:26-31`** + **附加约束 `:33`**; `:35` 是「## 3. 第三行不是逃生舱」标题 —— **R2 rework 行号勘正**, 原写「`:25-33` 判据表 + `:35` 附加约束」两处都错格) · `standards/conventions/configured-gate-authority.md:28-40` (Rule #10 白名单; **`:35` 第一类「配置显式关闭」** 是 §1.4 格 B 的依据, `:38-40` 第四类及其边界是 §1.3 (b)(c) 的依据) · `standards/openspec/project.md:114-118` (Levels 表; **Level 2 = proposal.md 在 `:117`, Level 3 = proposal.md + tasks.md 在 `:118`**) · `standards/openspec/templates/proposal-minimal.md:27-31` (Level 2 模板自带内联 `## Tasks`)
- 先例: `openspec/archive/2026-08-23-pre-merge-gate-no-run-for-branch/proposal.md` (同类 Level 2 标杆; `not_applicable` 语义封闭集 §Why `:55-57`「归进 not_applicable = fail-open, 污染 #122 钉死的语义封闭集」—— 本 spec 的 (b)/(c) 收窄与该裁定同向; `:282-284` 第三行三义务先例) · `openspec/archive/2026-09-04-sibling-spec-probe/` (audit-engine 脚本 + stdout 契约 + 消费措辞三档形态) · `openspec/archive/2026-07-31-phase-c-gate-path-coverage-not-applicable/` (#122 not_applicable 原始定义)
- 现场: Aria#199 issue 原文 · `.aria/triage-comment-199.md` / `.aria/triage-report-199.json` (2/2 复现) · aria-plugin#161 · aria-plugin#127 (第三行先例) · 本仓语料统计 (F2/F3/F4 数字 = **R1 rework 时 python 全枚举**, 786 份 `.md` / `C`=152)
- 审计: `.aria/audit-reports/post_spec-R1-2026-09-06T155000-000Z-R1-pre-merge-completeness-gate-change-scope-{aggregated,tech-lead,backend-architect,qa-engineer,code-reviewer,knowledge-manager}.md` (R1 五席 + 聚合) · `.aria/audit-reports/post_spec-R2-2026-09-07T044500-000Z-R2-pre-merge-completeness-gate-change-scope-{aggregated,tech-lead,backend-architect,qa-engineer,code-reviewer,knowledge-manager}.md` (R2 五席 + 聚合; C2/M9/m16, conflicted 0, 本文 v3 逐条处置的来源)
