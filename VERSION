# Aria 项目版本信息

> **版本**: 1.7.5
> **最后更新**: 2026-08-16

## 版本号

```
1.7.3
```

## 说明

- **Major (1)**: 方法论核心框架稳定
- **Minor / Patch**: 累积演进 (v2.0 运行时参考实现层 + 多终端协调 + state-scanner 机械化等); 精确逐版本变更不在本快照, 见下方各 SOT。

> 主项目版本反映整体方法论成熟度, ≠ 各子模块版本之和 (见 `standards/conventions/version-management.md` §5.3)。
> 详细变更史: 插件 → `aria/CHANGELOG.md` (SOT) | 规范 → `standards/openspec/project.md` §Version History | 主项目 → `docs/handoff/` + git log。

## 子模块版本

| 子模块 | 版本 | 仓库 |
|--------|------|------|
| aria (插件) | v1.66.5 | https://github.com/10CG/aria-plugin |
| standards | v2.2.3 | https://github.com/10CG/aria-standards |
| aria-orchestrator | v2.0.0 | 内部 (10CG Lab) |

> 各子模块当前 gitlink SHA 见 `git submodule status` (指针 SOT), 本表只记语义版本避免 SHA 快照过期。

## 对应 Tag

```
v1.7.3
```

> ⚠️ 实际 git tag 最新为 `v1.5.0` —— 主项目自 v1.5.0 后经 VERSION 文件 bump 演进但未补 tag。
> 按 §4.3 (VERSION 须与 git tag 一致), 是否补打 `v1.6.0`..`v1.7.3` 由 owner 决定。
