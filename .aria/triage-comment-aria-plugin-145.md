## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.66.1` |
| Current | `1.66.1` |
| Gap | same |

reported == current == 1.66.1 (same); 但 case-2 证实 `Command was:` 回显**先于 1.66.1** (1.65.5 同样回显), 归因勘正成立; 1.66.1 新增第二处 `Triggering segment:`。

### Code Path

`secret-guard.sh` 裸文件名 collector 未解析 (实址 `aria/hooks/secret-guard.sh`, 存在); spec 路径已归档至 `openspec/archive/2026-08-18-secret-guard-per-segment-evaluation/`。

### Git History

No recent commits matched on cited files (collector skipped: cited paths 均为裸文件名/子模块相对路径, 主仓解析不到)

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | 1 (main only) |

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/3`

- **case-1** — 输入: `printf JSON{command:'nomad var put secret/demo value=FAKE_PLACEHOLDER_NOT_A_SECRET_9x9'} | bash aria/hooks/secret-guard.sh 2>&1 >/dev/null | tail`
  - 预期: exit 2 且 stderr 末行含 'Command was: nomad var put secret/demo value=FAKE_…'
  - 实测: exit=2; stderr 含 'Command was: nomad var put secret/demo value=FAKE_PLACEHOLDER_NOT_A_SECRET_9x9' 与 'Triggering segment: nomad var put secret/demo value=FAKE_PLACEHOLDER_NOT_A_SECRET_9x9' (两行都回显整段)
  - match: `True`
- **case-2** — 输入: `同输入 | cache 1.65.5 旧副本 | grep -c 'Command was:'`
  - 预期: issue 归因勘正: 暴露面先于 v1.66.1 存在
  - 实测: 1 (旧版同样回显 Command was:) — 先于 v1.66.1; 1.66.1 新增的 'Triggering segment' 行是第二处
  - match: `True`
- **case-3** — 输入: `grep -n 'Command was|Triggering segment' aria/hooks/secret-guard.sh`
  - 预期: 两处发射点都要纳入脱敏范围 (issue 提醒)
  - 实测: secret-guard.sh:936 'Command was: $command' (heredoc) + :939 echo "Triggering segment: $seg" >&2 — 两处均走 stderr, exit 2 时被 harness 作为拦截原因回喂给 AI (chat-visible)
  - match: `True`

### Verdict Rationale

case-1 逐字复现: 假值命令被拦 (exit 2) 且 stderr 回显整条命令两次 (`Command was:` + `Triggering segment:`); case-3 定位发射点 `secret-guard.sh:936` (heredoc) 与 `:939` (`echo … >&2`), 两处都走 stderr → exit 2 时 harness 把 stderr 作为拦截原因回喂 AI ⇒ chat-visible, 触 Rule #7。severity=major: 非负 rule 的暴露面, 但已普查 909 transcript 未见真值泄漏 (issue 自述), 且只在 AI 把真值内联到命令行 (本就是被拦的反模式) 时才成立。action=next-cycle: 小改动 —— 两处回显改为**结构保留的脱敏** (如 `key=value` / 长 token 的值段替换为 `[REDACTED]`, 保留命令骨架 + pattern 名 + 段序号, 诊断价值基本不损); 须**同时**覆盖 :936 与 :939 (issue 已提醒, 只改一处会留下唯一残留面)。归档 tracker Aria#183 与本项无关。

---

*Generated by `/issue-triage` v1.66.1 — Ref: 10CG/aria-plugin#145*
