## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.66.1` |
| Current | `1.66.1` |
| Gap | same |

reported == current == 1.66.1 (same); 本项就是 1.66.1 逐段判定引入并**申报**的行为变化。

### Code Path

唯一引用路径 `openspec/changes/secret-guard-per-segment-evaluation/proposal.md` 已不存在 (`exists=false`) —— **非描述错误, 是 spec 已于 2026-08-18 归档**: 现址 `openspec/archive/2026-08-18-secret-guard-per-segment-evaluation/proposal.md` (§转出)。

### Git History

No recent commits matched on cited files (collector skipped: cited paths 均为裸文件名/子模块相对路径, 主仓解析不到)

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | 1 (main only) |

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/3`

- **case-1** — 输入: `printf JSON{command:'set -o posix; set | grep foo'} | bash aria/hooks/secret-guard.sh (aria @ 3b97c35 = v1.66.1) vs cache 1.65.5 副本`
  - 预期: v1.66.1 exit 0 (放行), 旧版 exit 2 (拦截) — 2→0
  - 实测: new(1.66.1) exit=0 / old(1.65.5) exit=2
  - match: `True`
- **case-2** — 输入: `同上, command='set -o posix && set | grep buildid'`
  - 预期: 同 2→0
  - 实测: new exit=0 / old exit=2
  - match: `True`
- **case-3** — 输入: `对照: command='set | grep foo' (无 set -o posix 段)`
  - 预期: (issue 未声称) 用于确认失配确由跨段上下文引起
  - 实测: new exit=0 / old exit=0 — 单段本就不拦, 拦截依赖跨 `;`/`&&` 的 `set -o posix … set |` 上下文, 逐段判定切断了它
  - match: `True`
  - 注: 版本 gap=same (1.66.1); 无 in-flight PR/分支; 该项是 #128 明确不修的转出项, 属新 spec 范围

### Verdict Rationale

case-1/2 用 1.66.1 canonical hook 与 1.65.5 旧副本做判别式对照, `set -o posix` 接分号/`&&` 再接 `set` 管到 grep 的两种写法均 **2→0** 逐字复现; case-3 对照单段写法两版都 0, 证实拦截依赖跨 `;`/`&&` 上下文, 逐段切断了它。无 in-flight PR/分支。severity=major: 安全 guard 的**已知 fail-open 回归** (SC-19 实测 51 族), 虽已申报+测量但未兜底。action=next-cycle: 需要新 spec (issue 自述『需完整 shell 语法解析, 属另一 spec 范围』)。⚠️ 供 spec 起草时评估的一个未验证候选 (非结论): 『整条 + 逐段两遍匹配取 OR』—— 单调只增拦截, 能一并恢复 51 族 2→0, 且 #170 型 (逐段才拦) 仍拦; 风险是把逐段判定消掉的整条误报重新带回 (#143 残余误报面), 须用 SC-19 探针集 + SC-1 变体语料双向实测后再定, 不要凭推理采纳。

---

*Generated by `/issue-triage` v1.66.1 — Ref: 10CG/aria-plugin#138*
