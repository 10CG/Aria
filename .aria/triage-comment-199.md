## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.69.1` |
| Current | `1.70.0` (aria master 已到 v1.71.0 `985e629`) |
| Gap | behind |

v1.70.0 / v1.71.0 对 `skills/audit-engine/references/execution-modes.md` 该节零改动 (`git diff 0545f86 origin/master -- <file>` 为空), 缺陷仍在。

### Code Path

issue 引用的 `skills/audit-engine/references/execution-modes.md` 在本仓路径为 `aria/skills/audit-engine/references/execution-modes.md`:

| 位置 | 实况 |
|---|---|
| `:54-61` Step 4 | 匹配模式 `{checkpoint_name}-*.md` / `{checkpoint_name}-*-*.md`; 「任意文件匹配 → 该 checkpoint 通过 / 无文件匹配 → missing_checkpoint」— 与 issue 引用逐字一致, 全节不出现 change_id / spec_id |
| `:46-52` Step 3 | 排除清单 (pre_merge / post_closure / mid_post_spec) 只处理「误阻」; 「误放」无条款 — issue 的定位准确 |
| 实现形态 | `grep -rl allow_incomplete_checkpoints\|missing_checkpoint aria/skills/ --include=*.py` 零命中: **纯散文规程**, 由编排者机械执行, 没有可打补丁的脚本 |
| `references/report-storage.md:8` | 命名 schema `{checkpoint}-R{round}-{timestamp_ms}-{spec_id}-{agent_role}.md` — **change_id 维度在文件名里已经存在**, 修法 1 (收窄为 `{checkpoint_name}-*-{change_id}-*.md`) 零新增信息依赖 |

### Git History

step 4 collector 本次未返回数据; 人工核: 该节自 2026-04-23 (#26) 落地后只加过 mid_post_spec 排除条款 (#79), 无 fix 候选。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | 仅主 worktree (master) |

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/2`

- **case-1** (SOT 文本): Step 4 / Step 5 原文如上, 无 change 维度。**match**
- **case-2** (在本仓 `.aria/audit-reports/` 上按 Step 4 通配模拟, 被审 change = 假想新 change, 自身零份报告): `post_spec-*.md` 命中 499 份, `post_implementation-*.md` 命中 3 份 (归属 `aria-secret-guard-plugin-default-orchestrator`, `audit-drift-guard`, `state-scanner-mechanical-t3`), 属于被审 change 的 **0** 份 → 两者均「通过」→ PASS。与报告方仓 (37 份他人 / 0 份自己 → PASS) 同型。**match**
  - 注: 本仓 `audit.checkpoints.post_implementation = off`, 所以 Aria 自身的 pre_merge 目前只会把 post_spec 纳入校验 — 同样的通配, 同样零 change 维度。

### Verdict Rationale

这是 SOT 规程层的设计缺陷, 不是环境差异: 门的判据只问「目录里有没有这个 checkpoint 的任何报告」, 与被审 change 无关, 项目历史越长越必然 PASS —— 正是 issue 说的「与它要防的失效同型」。判 `major`: 一个启用的 Rule #8 前置闸对目标对象零信息量 = 假绿 (与本仓 memory 里「零证据不得当正证据」的不变量直接冲突), 但它不阻断主流程、也不吞数据。判 `next-cycle`: 修法涉及 SOT 规程改写 + 「不适用」通道设计 (Phase A-only PR 的 post_implementation 该判 N/A 而非 missing), 宜起 Level 2 Spec; 建议组合 **修法 1 + 修法 2**: Step 4 按 `{spec_id}` 收窄 (旧 schema `{checkpoint}-{timestamp}.md` 的历史报告不含 spec_id, 需成文为「不计入」), 并给 Phase A-only / diff 零触代码的 change 一个显式 `not_applicable` 出口 (对齐 phase-c-integrator C.2.4 已有的 `not_applicable` 语义), 使「缺失」与「不适用」可分辨。修法 3 (下界 N) 可作为 1 的参数化形式一并考虑。

---

*Generated by `/issue-triage` v1.70.0 — Ref: 10CG/Aria#199*
