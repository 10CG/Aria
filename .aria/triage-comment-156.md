## Triage Report

**Verdict**: `duplicate-of-#154` | **Severity**: `critical` | **Recommended Action**: `close`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.47.0` |
| Current | `1.55.0` |
| Gap | behind |

⚠️ 与 #154 同因:`v1.47.0` 是 `aria-report` 版本抽取缺陷的产物(详见 #154 triage 的 Deviation Note 偏离 2),不代表真实版本。

### Code Path / Git History / In-flight

与 #154 完全相同,不重复。见 [#154 的 triage comment](https://forgejo.10cg.pub/10CG/Aria/issues/154)。

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/2`

| case | 输入 | 结果 | match |
|---|---|---|---|
| case-1 | 逐段比对 #156 与 #154 的 body | 标题逐字相同;复现步骤、stderr、根因 3 点、修复建议、环境(macOS Darwin 22.6.0 / zsh / v1.47.0)全部同构,#156 是 #154 的压缩改写 | ✅ |
| case-2 | 与 #154 case-1 相同的 `enable -n readarray` shim 复现 | `exit=2` + 同一 FATAL 输出。缺陷真实,但归属 #154 | ✅ |

时间线:#154 创建于 `2026-07-09T01:10:43Z`,#156 创建于 `2026-07-09T14:21:50Z`。两者同为 `aria-report` 自动生成。

### Verdict Rationale

#156 与 #154 描述同一缺陷,无新增信息、无独立复现价值,且创建时间在后。以 **#154 为 canonical**,本条关闭并指向 #154。

缺陷本身是 `critical`(fail-closed `exit 2` 拦死 `Bash` 与 `Read|Edit|Write|MultiEdit` 两个 PreToolUse matcher,macOS bash 3.2 上会话死锁),修复在 #154 跟踪。

> 注:重复提交本身可能也是 `aria-report` 的一个可用性问题 —— 同一 bug 在 13 小时内被自动生成两次,提示缺少「提交前查重」。若属实,值得单独提。

---

*Generated by `/issue-triage` v1.55.0 — Ref: 10CG/Aria#156*
