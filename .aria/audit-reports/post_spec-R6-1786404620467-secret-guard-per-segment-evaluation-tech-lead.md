---
checkpoint: post_spec
round: R6
review_target: v8 (commit 4923380)
spec: secret-guard-per-segment-evaluation
timestamp: 1786404620467
date: 2026-08-10
seat: tech-lead
seat_id: TL6
lens: 架构一致性 + 收敛判定
verdict: REVISE
critical_count: 0
major_count: 4
minor_count: 4
new_issues: 4
recurrence_issues: 4
ready_for_a2: no
authored_versions: v6 (R5-fix)
---

# R6 · tech-lead

## v8 复核通过的部分

v8 新写入的**每一条事实性断言实跑复核, 全部属实, 无一条编造**: 行号 691 · 13 处判据
行号清单 `342 347 358 361 364 368 372 375 383 386 390 394 397` · `[[:space:]]+`10 /
`*`12 / any 13 · 141=139+2 · 81/79/7/5/1 · 四条 canonical exit · 转出 11 的 9 条
复现命令 9/9 一致。

`! for …` 确实不含任何 `BLOCK_CHARS`, 是真正只靠 `BLOCK_KW_RE` 判定的 fixture ——
v8 避开了当初咬死 `case` 的那个坑。SC-16 反事实 8+7 与 17 项算术自洽。

## Major

- **TL6-F1** (新) — SC-14 反事实表「仅命令位置 (v5 的规范文本)」行 × A-5 应为 `false`
  (v5 清单含 `in`, `in`+空白+`for` 直接命中), 表里写 `true`; 转红列应为「2 条红
  (A-5 · B-2)」。**且表里没有「含 `in` 的 v6 旧清单」这一行 —— 而这正是 A-5 被创造
  出来要鉴别的那个实现**。对照 SC-6 给新 `!` fixture 配了「漏 `!` → 该条红」行,
  同一次 W-1 编辑产出的两条配对 fixture, 一条给了反事实行另一条没给。
- **TL6-F2** (新, v8 修订动作自身引入) — Task 1.10 正文首句要求「版本 bump 落地并经
  marketplace 刷新后」, 而 Task 1.10a 才是 bump, 排在 1.10 之后。v8 的改动理由只论证了
  1.10a 与 1.11 的关系, **整段推理从未把 1.10 纳入检查**, 却在同一句声称顺序已反映实际。
  真实顺序 `1.10a → 1.11 → 1.9 → (ship) → 1.10`, 写死顺序 `1.9 → 1.10 → 1.10a → 1.11`。
- **TL6-F3** (复发, 第 5 次) — SC-17 (删重复用例 + 断言无重名) 全 spec 无 Task 承载。
  `grep -c 'FP-fix timeout run-env' secret-guard.test.sh` = **2**, 缺陷真实存在。
  v8 刚为 B-10 修掉同型缺口却没做同类清扫。
- **TL6-F4** (复发, 第 3 次) — SC-6 关键字型 5 条只写死 `until`/`select` 两条,
  `for`/`while`/`if` 仍只有关键字名。而 `while`/`if` 最惯用写法带 `[[ ]]`, 会被
  `BLOCK_CHARS` 先行捕获 → 与 `case` 完全同型的结构性恒绿。`case` 那次的教训正是
  「限定转述一次就丢」。

## Minor

- **TL6-F5** (复发) — SC-9a「5 类须在 Phase B 前枚举写死」既无枚举也无 Task。
  R4 点名后的处置是**把承诺写下来而非写死它**。它是 rule6_note substitute 的
  dogfood 组件本体。
- **TL6-F6** (新) — `:520`「SC-6 全部 15 项」未随 v8 同步 (v8 把 `:480` 的 1/15 改成
  1/16, 同一约定的另一处没改)。另: `:470`「共 17 项」与 `:480`「1/16」之间无分母说明。
- **TL6-F7** (新) — 转出 11 的 FIX 候选正则**同时删掉了 `do|then|else|elif` 四个位置
  分支**, 正文一字未提; 实测它顺手也治了 `redo`/`strengthen` 假阳性, 但代价面零分析。
- **TL6-F8** (复发) — 测试套件目标总数全 spec 未定义, 而 SC-11 (`366/360`)、SC-17
  (−1)、Task 1.5 (大幅增加)、SC-13 (须一致) 四处同时钩在它上面。`corpus_census.py`
  的口径不覆盖测试用例总数。

## 收敛判定

**严重度维度 = 真收敛**: `R1 6C+19M → R2 5C+12M → R3 4C+13M → R4 6C+13M+17m →
R5 5C → R5.5 0C+4M+9m → R6 本席 0C+4M+4m`。R5 的 5 条 Critical **无一复现**。

**缺陷注入维度 = 仍在平移**: F1 与 F2 都是 **v8 修订动作自身产出的新缺陷** ——
本 cycle 第六个连续版本出现「改一处冒一处」。换执笔人把注入的严重度从 Critical 压到
Major/minor, **但没有把注入率压到零**。

**更深的信号**: F1 / F3 / F6 全是「同一次编辑里, 同一个约定的多个落点只同步了一部分」。
根因 —— **这份 spec 六轮修订全部是「按 finding 清单逐条打补丁」, 从未做过一次
「按判据全 spec 清扫」**。这是判 no 的真正理由, 比任何单条 finding 重要。

## 进 A.2 的收口清单 (8 条编辑 + 1 道机械闸)

必修 4 (F1/F2/F3/F4) · 应修 4 (F5/F6/F7/F8) · **闸**: 执笔方跑一次全 spec 机械清扫,
对四条已知判据做穷举核对并输出核对表 (每条给命令与结果):

- (a) 每条 SC 反查 Task, 无承载者标红 (本 cycle 已抓 5 次, 穷举一次永久关闭)
- (b) 每条 Task 反查前置依赖, 检查编号序与依赖序是否一致
- (c) 每个跨 SC 引用的计数反查源头, 与该 SC 当前值比对
- (d) 每条断言 `safe_to_split()` 的 fixture 反查是否被 `BLOCK_CHARS` 先行捕获

**核对表全绿即可进 A.2, 不需要第七轮审计席。**

## owner 定案异议

无。一条附注: `!?` 不收这条裁定, 其依据的记录面不完整 (owner 看到的描述是「移到从属
可选项」, 而候选正则实际同时删了四个位置分支) —— 四条理由在完整描述下**依然全部成立**,
不建议重开, 但转出 11 正文应补这一句。
