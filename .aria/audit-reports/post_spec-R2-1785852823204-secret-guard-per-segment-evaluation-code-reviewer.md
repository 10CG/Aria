---
agent: code-reviewer
round: R2
verdict: REVISE
critical_count: 1
major_count: 3
minor_count: 5
r1_resolved: 8/12
---

# post_spec R2 — secret-guard-per-segment-evaluation (code-reviewer 视角)

审计对象: `/home/dev/Aria/openspec/changes/secret-guard-per-segment-evaluation/proposal.md` (owner 再缩范围后版本)
参照物: canonical `aria/hooks/secret-guard.sh` @ `af87cae` (698 行) / `aria/hooks/tests/secret-guard.test.sh` (798 行) / forgejo aria-plugin #128 / Aria#172
方法: 全部数字机械重算 (quote-aware 分段器 + 语料 dump) + canonical hook 直调实跑 (39 次) + forgejo API 取原文。**未修改仓库任何文件** (插桩副本在 scratchpad)。
验收环境遵守: 全部行为证据来自 `bash aria/hooks/secret-guard.sh` 直调, **未**使用仓内 harness hook 链 (确认其 cache = `1.63.0`, `~/.claude/plugins/cache/10CG-aria-plugin/aria/1.63.0/`)。

---

## Phase 1: 规范合规性

**判定**: PASS (可进入 Phase 2)

- Level 2 结构完整; Why / What(4) / 关键决策(8 行) / Impact / 转出(7) / rule6_note / Tasks(1.1-1.7) / SC(1-11) 齐备。
- Rule #6 框定 (deterministic detector hook → structural substitute, 不走 `/skill-creator` AB) 与同 hook 先例一致, substitute 点名 SC-1/SC-5/SC-2/SC-3。
- Rule #1/#5 满足。缩范围裁定与「转出」清单一一对应, 无越界实现。
- 头部「R1 `6C+19M+16m`」经五份 R1 报告 frontmatter 求和核对: C=1+1+0+1+3=**6**, M=2+6+2+3+6=**19**, m=2+5+1+3+5=**16** — **完全正确**。

---

## Phase 2: 代码质量 / 事实准确性

### 一、R1 我方 1C+6M+5m 的核销 (8/12 完全解决)

| R1 编号 | 本版处置 | 核销结论 |
|---------|---------|---------|
| **C-1** fail-safe 方向标反 + 二义 + SC-7 描述性 | §3 改写为「已知欠拦非保守」, 决策表第 4 行同步; SC-7 重定向为跨段 fail-open 锁 | **部分** — 方向已勘正 (实测复现: `` x=`cat /opt/.env; true >/dev/null` `` 不切=0 / 切=2), 但「不解析 vs 整体留段内」互斥措辞仍在, 且**该行现在零 SC 覆盖** → 见 M-3 |
| **M-1** `:653` 等三处指向 pre-#170 旧副本 | 正文改 `:663`, 其余两处行号整段删除 | **解决** — 实测 `aria/hooks/secret-guard.sh:663` = `if [[ $has_filter -eq 0 ]]` ✅ |
| **M-2** 「切错=安全回归」被证伪 | §2 明写「R1 实测证伪」, 理由改为语义正确性; 决策表第 3 行同步; SC-4 换成真锚点 | **解决 (优秀)** — 见下方「优点 1」, 新 fixture 经实跑验证确实可证伪 |
| **M-3** SC-8 冻结绝对毫秒恒红 | 改「相对同机即时基线, 20 轮中位数, ≤50%」; 决策表第 7 行注明 60ms 是 `jq` 非 bash | **解决** |
| **M-4** heredoc 零覆盖部分证伪 | §3 括注勘正: heredoc 已有 1 条 (`#157 heredoc-style nomad get`), `$()` 4 / 反引号 1 确无分隔符 | **解决** — 与我方枚举逐字一致 |
| **M-5** 迁移口径 + 5 条换行用例落枚举外 | Impact 声明口径 = 顶层 `;` `&&` `\|\|` `\|`; 另列 5 条换行; SC-2 纳入 | **部分** — 结构解决, 但数字 stale (M-1) + 纳入方式无检出力 (M-2) |
| **M-6** `.claude/scripts/` 本地副本漂移 | owner 裁定删除 (主仓 `5fab5b8`), spec 记录「不含本地副本」+ Aria#172 + 验收环境声明 + rule6_note dogfood 限制 | **解决 (超预期)** — 实测 `.claude/scripts/` 已不存在, `settings.json` `hooks` = `{}`, `5fab5b8` commit message 记录了 SOT §6 dual_install investigate 结论 |
| **m-1** 误引「347 用例」 | 整句删除 (全文已无 347) | **解决 (删除式)** |
| **m-2** Task↔SC 映射错位 | Tasks 全表不再点名 SC | **部分** — 错位消失但 traceability 变弱, SC-9 无 Task 承载 → 见 m-4 |
| **m-3** `guard:ack` 无对应 SC | 移出决策表, 仍以 §What.4 bullet 断言「维持命令级」 | **部分 (未闭)** — 断言仍在, 锁仍无 → 见 m-3 |
| **m-4** 「2 条引号内分隔符」口径未写 | 改为点名两条 anchor, 不再报计数 | **解决 (改述式)** |
| **m-5** `:62` 未验证项落定 | 结论已写进 §2 正文 | **解决** |

**无静默丢弃项** — 12 条全部可追溯到「解决 / 改写 / 显式移出」。

### 二、数字与引用终检 (全部实跑重算)

| 断言 | 核实方式 | 结论 |
|------|---------|------|
| `:663` 全局开关 | canonical `sed -n 663p` | ✅ |
| **141** pattern | 机械解析数组 `:402-:656` 非注释非空行 | ✅ 141 |
| `grep -c "^  '"` = 139 + 2 条双引号 ssh pattern | 实跑 `grep -c` = **139**; `:506` `:507` 两条 `"ssh[^\|]*['\"]…"` | ✅ 勘正属实 |
| **305** 条 `bash_case` | 插桩 dump 计数 = 305 (`grep -c` 得 306 含函数定义行) | ✅ |
| **12** 条把 `\|` 编码进正则的 pattern | 数组内含字面 `\|` 的 pattern 计数 | ✅ 12 |
| 按 `\|` 切「4 条真泄漏由 2 翻 0」 | 对 want=2 且含顶层 `\|` 的用例逐条 pipe-split 后逐段实跑 | ✅ **恰 4 条** (`base64 -d \| bash bypass` / `R2-C-3: find \| xargs cat` / `R2-C-3: xargs cat .env` / `#69 tar .ssh piped to ssh exfil`) |
| `has_filter` **13** 处 subprocess | `:318-:401` 区 `grep -qE` 计数 = 13 | ✅ |
| **366** / **360** (zsh 在场与否) | 实跑 `PASS: 366 / 366`; zsh 族 6 条 (`:714-:719`), 本机 `/usr/bin/zsh` 在场 | ✅ 366−6=360 |
| 同机零变更 **111-213ms** | 引自 R1 code-reviewer 实测 | ✅ 数值属实 (口径措辞见 m-1) |
| ~**294ms** (4 段 fork 放大) | 未复现 — 见 m-2 | ⚠️ 量级偏高 |
| v1.65.5 → **v1.65.6** PATCH | `aria/.claude-plugin/plugin.json` = `1.65.5` | ✅ |
| 主仓 **5fab5b8** | `git show` = "chore(hooks): 移除 .claude/scripts 本地 hook 副本" | ✅ |
| **Aria#172** | forgejo API: `open`, 标题「plugin cache 停在 1.63.0…」 | ✅ 且 cache 实测确为 1.63.0 |
| comment **17512** / **17545** | forgejo API 取回: 17512 = Triage `confirmed`/`critical`; 17545 = 「更正 — issue 原文给的分隔符集合是错的」 | ✅ (17545 内数字差异见 m-5) |
| triage 三条 `;` + 一条 `&&` 改前 exit=0 | canonical 直调 4/4 = exit 0; 对照 `cat /opt/.env` = exit 2 | ✅ SC-1 baseline-failing 前提成立 |
| `cat /opt/.env \| head -5 >/dev/null` 本版内不变 | 直调 = exit 0 | ✅ SC-1 排除该条正确 |
| `set -o posix; set \| grep foo` 逐段后翻 0 | 整条 = 2; 两段分别 = 0 / 0 | ✅ SC-7 锁的现状成立 |
| `cat /opt/.env &>/dev/null` 是合法 credit | 直调 = exit 0 | ✅ 「`&` 不切」理由成立 |
| 语料统计 **65/49/16/15/1 + 5** | quote-aware 分段器重算: 含顶层 `;&&\|\|\|` = **65**, want=2 = **49**, want=0 = **16**, 其中纯管道 want=0 = **15**, 非纯管道 want=0 = **1** (`put: KNOWN-LIMIT compound credit leak`); 纯换行边界另 **5** 条 (4 拦 / 1 放行) | ✅ **与已定案数字完全一致** — 但 spec 正文未更新, 见 M-1 |

### 优点

1. **SC-4 的新 fixture 是全篇最硬的一处**。实跑验证: `python3 -c 'import os; print(open("/opt/.env").read())'` 整条 = **exit 2**, 而切错后段 1 (`python3 -c 'import os`) = **0**、段 2 (` print(open("/opt/.env").read())'`) = **0** ⇒ 切错**必 exit=0**。这条真正堵住了 R1 M-2 揭示的「被其他 pattern 巧合兜住」假绿路径 — 与 R1 两条老 anchor (切开后各有一段仍 exit=2) 形成鲜明对比。作者不仅承认证伪, 还换上了会真红的锚点。
2. **6 条被推翻断言的自陈清单 (头部 line 9)** 把「无一由作者自查发现」写进 spec, 并单点标注第 (6) 条是在修正前五条的重写里**新引入的** — 这是极高质量的元认知记录, 直接服务于转出项收口者。
3. **缩范围后的规则表是封闭集**, 每一行都有实测反例支撑 (`&`↔`&>` 冲突 / 换行↔heredoc / `\|`↔12 条正则编码), 无一条靠直觉。
4. **验收环境声明 + rule6_note 的 dogfood 限制**诚实标注「本 spec 无法用『我在本仓被拦』作证据」, 而不是假装 dogfood 成立 — 这是对 M-6 的正确收口方式。

---

## 问题

### Critical (必须修复)

**C-1. 转出 2 (块结构整体重定向) 是**本版自造**的误伤类, 却只有指针、没有锁、也没进迁移面 —— 且语料对其零覆盖, 全部回归 SC 会保持绿**

- 位置: `proposal.md:34` (§Why 子问题表第 2 行) + `:130` (转出 2) + `:106-:116` (迁移面) + `:119` (行为变更声明) + `:168` (SC-10)
- 实测 (canonical 直调, 按本版规则「顶层 `;` 切」手工分段):

  | 安全写法 (现状 exit=0) | 逐段后各段 exit | 逐段后整体 |
  |---|---|---|
  | `{ cat /opt/.env; echo x; } >/dev/null` | `{ cat /opt/.env`=**2** / ` echo x`=0 / ` } >/dev/null`=0 | **2 (误伤)** |
  | `for f in a b; do cat /opt/.env; done >/dev/null` | 0 / ` do cat /opt/.env`=**2** / 0 | **2 (误伤)** |
  | `if true; then printenv; fi >/dev/null` | 0 / ` then printenv`=**2** | **2 (误伤)** |

  3/3 复现 R1 tech-lead M-2 + qa M-2 的「5/5 误伤」。
- **问题不是「没修」, 而是三重缺失**:
  1. **无锁**。本版对**另一个**自造缺口 (跨段 fail-open) 做得很对: 转出 1 + **SC-7 显式锁定 `set -o posix; set \| grep foo` 现状 exit=0, 并写明「该用例转红 = 转出 1 已收口」**。块结构误伤是完全对称的自造缺口, 却连一条 `KNOWN-LIMIT` 用例都没有。这个不对称本身就是信号。
  2. **迁移面没算它**。`:110-:116` 的迁移表口径是「305 条语料中含分隔符者」, 而实测语料中 `{ …; } >/dev/null` / `for…done >/dev/null` 类用例 = **0 条** (机械 grep 确认)。于是 SC-2 / SC-3 / SC-11 在这个类上**结构上不可能变红** —— 属 memory `feedback_check_predicate_must_validate_against_real_data_range` 与 `feedback_noop_in_test_env_hardening_needs_mechanism_assertion` 的同类假绿。
  3. **行为变更声明只说了一半**。`:119` 写「此前被 credit 掩护的 `a; b` / `a && b` 形态将开始被拦」= 只描述**期望内**的拦截扩大; SC-10 要求的 2 条迁移写法也只服务于这一半。用户侧真正会踩的是**合法写法被打红**, 而 CHANGELOG 对此零要求。
- 为什么重要: 这是安全 hook 的**假阳性**面。假阳性的代价不是漏拦而是「用户被无理由挡住 → 学会用 `# guard:ack` 绕过 / 学会不用块结构」, 正是 spec 自己在决策表末行引用的 #170 事故链「被拦后无正解可循」。而且它 100% 由本版的切分规则引入, 「转出」只能转走**修复**, 转不走**披露与锁定**。
- 修法 (三选一起做, 不必改范围):
  1. 迁移表增行「块结构整体重定向 (语料 0 条, 用户面存在) | 新增误伤 | 归转出 2」并写明统计口径不覆盖它;
  2. 新增 SC (建议 SC-12, 与 SC-7 同形): 对 `{ cat /opt/.env; echo x; } >/dev/null` / `for … done >/dev/null` **锁定改后 exit=2 且标注 `KNOWN-LIMIT`, 注明「该用例转 0 = 转出 2 已收口」** —— 这样转出 2 也有红灯;
  3. SC-10 的迁移写法示例中至少 1 条针对块结构 (给出「块内逐条补 redirect」的正解), CHANGELOG 行为变更段显式列出该误伤类。

### Important (应该修复)

**M-1. 迁移表与 SC-3 仍写 68/52 —— 已定案数字 65/49 未落地**

- 位置: `proposal.md:110` (68) / `:111` (52) / `:161` (SC-3 「52 条」)
- 独立重算 (quote-aware 分段器 dump 305 条语料, 口径 = 顶层 `;` `&&` `\|\|` `\|`): **65 / 49 / 16 / 15 / 1**, 与作者权威计数器及 qa R2 交叉确认值逐个吻合。16 / 15 / 1 三个数 spec 已经是对的, 只有 **68→65** 与 **52→49** 两处 stale。
- 我 R1 报的 68/52 系我方口径缺陷 (未做转义/嵌套引号处理), 本轮以更严格的分段器复算后**推翻我自己的 R1 数字**, 确认 65/49 为准。
- 为什么重要: SC-3「52 条 expected=2 用例改后仍 exit=2」是可机械执行的验收断言, 数字错了会让 Phase B 逐条核对时对不上账 (差 3 条), 进而诱发「随手把数字改成实测值」而不追问差异来源。
- 修法: `:110` 68→**65**, `:111` 52→**49**, `:161` SC-3 的 52→**49**; 并在迁移表下注明口径 = quote/转义-aware 顶层扫描 (与 §1 规则表同一实现)。

**M-2. SC-2 对「换行被误当边界」检出力为零 —— 5 条换行用例全部实测不变, 「这 20 条即红」是假保护**

- 位置: `proposal.md:160` (SC-2) + `:116` (「本版不切换行 ⇒ 这 5 条零影响, SC-2 显式纳入」)
- 实测 (canonical 直调, 强制按换行切分后取各段最大 exit):

  | 用例 | want | 整条 | 换行切分后 | 各段 |
  |---|---|---|---|---|
  | `#157 multiline echo\nenv` | 2 | 2 | **2 (不变)** | 0 / 2 |
  | `#157 multiline set\necho\nprintenv` | 2 | 2 | **2 (不变)** | 0 / 0 / 2 |
  | `#157 first-line hit kept` | 2 | 2 | **2 (不变)** | 2 / 0 |
  | `#157 heredoc-style nomad get` | 2 | 2 | **2 (不变)** | 0 / 0 / 0 / 2 |
  | `#152 FP: multiline benign` | 0 | 0 | **0 (不变)** | 0 / 0 |

  **5/5 在误切换行下依然全绿。** 换言之 SC-2 的换行半边在任何实现下都不会红。
- 管道半边同样被夸大: 对 15 条纯管道 `expected=0` 用例逐条 pipe-split 实跑, 翻红的是 **13 条**, 另 2 条不红。故 SC-2 断言的「这 20 条即红」实际上限是 **13/20**, 且这 13 条全在管道侧。
- 为什么重要: SC-2 是 rule6_note 点名的 substitute 之一。一条被声称有检出力、实际对半个覆盖面是恒绿的验收门, 比没有更糟 —— 它会让 R3/Phase B 误以为换行行为已被锁住。真正锁住换行的是 **SC-5 的机制断言「换行→1 段」**, 那条是好的。
- 修法: SC-2 改写为「15 条纯管道 `expected=0` 用例改前改后不变 (误把 `\|` 当边界时其中 **13 条**转红, 已实测); **换行族的 5 条同样锁定不变, 但注意 exit-code 层面对误切换行不敏感 —— 换行的真锚点是 SC-5 的机制断言**」。即: 把换行的举证责任明确移交 SC-5, 不在 SC-2 冒领。

**M-3. 决策表第 4 行 (`$()`/反引号/heredoc 不解析留段内) 零 SC 覆盖, 且「不解析」与「整体留段内」措辞仍互斥 (R1 C-1 残余层)**

- 位置: `proposal.md:53` (§1 规则表末行) + `:61-:67` (§3) + `:97` (决策表第 4 行) + SC-5 (`:163`) 的覆盖清单
- 方向问题已解决 (R1 C-1 第 2 层), 但另两层仍在:
  1. **措辞仍互斥**: 「不解析」字面 = 扫描器只感知引号, 那么 `` `cat /opt/.env; true >/dev/null` `` 里的 `;` 就是顶层 `;` ⇒ **会切**。要「整体留在所在段」必须跟踪 `$(`/反引号/heredoc 定界 —— 那就是解析。实施者两种读法都能自称合规。
  2. **无验收**: SC-5 的覆盖清单是「单命令 / `a; b` / `a && b` / `a \| b` / 引号内 `;` / `\;` / 换行 / `a & b`」共 8 项, **不含 `$(a; b)` / 反引号 / heredoc**。SC-7 已被改派去锁跨段 fail-open。于是这一行既无机制断言也无 exit 断言。
- 两种读法产出的行为**确实不同** (实测): `` x=`cat /opt/.env; true >/dev/null` `` 整条 = **exit 0**; 若切, 段 1 (`` x=`cat /opt/.env ``) = **exit 2**。即「切」会把这条从放行变成拦截 —— 是**行为变更**, 但 spec 声称本形态归转出 5 (保持欠拦)。若实施者按字面「不解析」写, 转出 5 的前提当场作废, 且该变更不在迁移面与 CHANGELOG 里。
- 语料对此同样零检出: `$()` 4 条 / 反引号 1 条内部均无分隔符 (R1 已枚举, 本轮复核一致) ⇒ 全量回归恒绿。
- 修法: (a) 措辞改为「**最小定界跟踪**: `$(`…`)` / 反引号 / heredoc 体内不切 (不做语法解析, 只跟踪定界符配对)」, 删「不解析」; (b) SC-5 覆盖清单补三项机制断言: `` x=`a; b` ``→**1 段** / `x=$(a; b)`→**1 段** / heredoc 体含换行与 `;` →**1 段**。这与建议里「凡涉及切/不切一律用分段函数求值表述」一致。

### Minor (建议修复)

**m-1. 「相邻差 92%」口径不准**: `:100` 决策表写「同机零变更 5 轮 111-213ms, **相邻差 92%**」。R1 五轮实测 111/148/213/153/181 的**相邻**最大差是 148→213 = **44%**; 92% 是 min(111)↔max(213) 的**轮间极差**。SC-8 (`:166`) 的措辞「轮间散布可达 92%」是对的, 只有决策表这处沿用了我 R1 报告里同样不准的「相邻」二字 —— 属我方 R1 的措辞缺陷被忠实继承 (memory `feedback_spec_inherits_upstream_dec_errors`), 建议一并改为「轮间极差 92%」。

**m-2. ~294ms 未能复现, 建议标注为单次量级参考**: `:36` / `:134` 引 backend R1「4 段多耗 ~294ms」。同口径估算: `has_filter` 13 处 `echo \| grep -qE`, 4 段 = 3 段增量 = 39 次 pipeline; 本机实测 39 次同形 pipeline = **156ms**, 130 次 = 452ms (≈3.5ms/次) ⇒ 增量量级约 **137-156ms**, 与 294ms 差约 2 倍。结论方向 (fork 按段数线性放大) 不受影响, 且 SC-8 已改成「实测并写进 spec」, 故只建议把 294 标注为「单机单次实测, 量级参考」, 避免它被当成基线再被继承一轮。

**m-3. `guard:ack` 命令级语义仍无锁 (R1 m-3 未闭)**: `:83` 仍以规范性 bullet 断言「维持命令级 (整条豁免), 不下沉段级」, 但该条只是从决策表移到 §What.4, 锁没补上。ack 判定在被重构的判定循环上游, 风险低, 但「有规范性断言就该有可证伪锁」的原则不变。建议 SC-5 或 SC-6 附加一句「复合命令 + `# guard:ack: <reason>` 改后仍 exit=0」(一行断言, 成本极低)。

**m-4. Tasks↔SC 追溯链断开, SC-9 无任务承载**: Tasks 1.1-1.7 已不再点名 SC (R1 m-2 的错位以「删映射」方式消解)。逐条比对后: SC-1/2/3/4/6/7 落在 1.3 隐含, SC-5→1.3, SC-8/SC-11→1.4, SC-10→1.5 —— 唯 **SC-9 (canonical 直调 dogfood 脚本, 5 类命令形态)** 在 1.1-1.7 中**无任何任务承载** (1.4 的「全量回归」是测试套件, 不是 dogfood 脚本)。另 Task 1.6 (`secret-hygiene.md` 计数回填 366→新值) 无对应 SC。建议 1.4 补「+ SC-9 dogfood 脚本」, 1.6 挂一条机械断言 (grep 新计数)。

**m-5. 与 comment 17545 的数字差异未标注**: `:47` 写「该论证比原稿的『毁 13 条正确用例』更硬」, 而 spec 头部引为权威的 comment 17545 原文写的是「照做会毁掉 **15 条**本来正确的用例」。本轮实测: 15 条是「纯管道 `expected=0`」的**总数**, 其中按 `\|` 切后真正翻红的是 **13 条** ⇒ **spec 的 13 是更准的数**, 17545 的 15 偏松。建议在 `:47` 或 `:6` 处补半句「(17545 原文写 15 = 纯管道 want=0 总数; 实测真翻红 13)」, 免得下一轮又有人拿 17545 来「勘正」spec。

---

## 建议

1. **把「转出 = 免披露」的隐含等式掐掉**。本版对转出 1 做得完全正确 (SC-7 锁现状 + 写明转红即收口), 对转出 2 只给了指针。建议立一条本 spec 内的通则: **凡由本版切分规则自造的行为缺口 (转出 1/2/5), 一律配一条 `KNOWN-LIMIT` SC 锁定现状**, 使每个转出项收口时都有确定的红灯。目前三者里只有转出 1 有。
2. **R1 建议 1 (「凡涉及切/不切一律用分段函数求值表述, 不用 exit code」) 只落实了一半**。SC-5 是范本, SC-4 换了真锚点也很好; 但 SC-2 (换行半边) 与决策表第 4 行仍停在 exit-code 层, 而这两处**恰恰就是本轮实测证明 exit-code 无检出力的地方** (5/5 不变 / 语料 0 覆盖)。把 M-2 与 M-3 的修法都落到 SC-5 的机制断言清单里, 一处解决两个问题。
3. **语料盲区应显式成表**。本轮确认三个类别在 305 条语料里是**结构性零覆盖**: 块结构整体重定向 (0 条)、`$()`/反引号内部分隔符 (0 条)、跨段上下文 pattern (仅 `set -o posix` 1 条)。建议在 Impact 里加一小节「语料盲区」列出这三项 + 一句「故 SC-3/SC-11 对其恒绿, 不构成证据」—— 这会挡住 Phase B 「全绿即安全」的误读, 也是 memory `feedback_noop_in_test_env_hardening_needs_mechanism_assertion` 的直接应用。

---

## 评估

**是否可以继续?** 需要修复 (未 CONVERGED)

**理由**: 本版质量相对 R1 有实质跃迁 —— 我方 R1 的 12 条有 8 条完全解决、4 条部分解决、**零静默丢弃**, 且核心举证 (SC-4 新锚点、`&`/换行/`\|` 三条不切的实测依据、6 条自陈被推翻断言) 逐条经 canonical 直调复现属实, 141/305/12/13/366/360 与 65/49/16/15/1+5 全部机械重算吻合。但仍有一条 Critical: 转出 2 的块结构误伤是**本版切分规则自造**的假阳性类, 语料零覆盖使全部回归 SC 结构上恒绿, 而它既无 `KNOWN-LIMIT` 锁、又不在迁移面、CHANGELOG 也无要求 —— 与本版对转出 1 的严谨处理形成不对称, 会让 Phase B 在「全绿」下把用户面误伤直接 ship 出去。另 M-1 (已定案的 65/49 未落到正文与 SC-3)、M-2 (SC-2 换行半边检出力实测为 0)、M-3 (决策表第 4 行零 SC + 措辞互斥) 三条都会直接影响 Phase B 的代码形态或验收有效性。四条修法均为文字级、不改范围, 一轮可收敛。
