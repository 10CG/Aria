{
  "spec_key": "a1-entry-claim-duplicate-work-guard",
  "fixes": [
    {
      "finding_id": "C1 — 两个 A.1 落点的 allowed-tools 结构性不支持本机制",
      "severity": "critical",
      "root_cause": "Spec 把「宿主」当成可任意挂载的位置，没把宿主的能力声明当作需要实读核验的事实断言；而实读显示 phase-a-planner / spec-drafter 正文各自零 shell 命令 —— 缺 Bash 不是笔误而是这两个纯文档 skill 的既有设计。",
      "edit_target": "§3「入口覆盖 (S6)」——在现有双落点列表之后插入新小节 §3.1；同步改 Impact 表（新增两行）与 Success Criteria（新增 SC-20）。",
      "exact_change": "§3 双落点列表下方插入：\n\n```\n#### §3.1 宿主能力前置 (post_spec R1/C1 — 实读核验)\n\n| 宿主 | `allowed-tools` 实读现值 | 本机制所需 | 处置 |\n|---|---|---|---|\n| `phase-a-planner/SKILL.md:9` | `Read, Write, Glob, Grep, Task, Skill` | `Bash` (跑 phase1_gate) + `AskUserQuestion` (§2.3 请裁) | **扩容为** `Read, Write, Glob, Grep, Task, Skill, Bash, AskUserQuestion` |\n| `spec-drafter/SKILL.md:11` | `Read, Write, Glob, Grep, AskUserQuestion` | `Bash` | **扩容为** `Read, Write, Glob, Grep, AskUserQuestion, Bash` |\n\n**这是能力面扩张，不是笔误修正**：实读二者正文 (`grep -n 'python3|```bash'`) 命中 **0** —— 它们此前从未执行过任何 shell 命令，本 Spec 让它们首次获得任意 shell 执行能力。owner 须知情。\n\n**先例**：`phase-b-developer` / `phase-c-integrator` / `phase-d-closer` 三个 phase skill 均已持 `Bash`（`phase-a-planner` 是唯一例外）；`aria-report` 已同时持有 `Bash, Read, AskUserQuestion`，Bash+AskUserQuestion 并存有先例。\n\n**为什么不用「phase-a-planner 经 `Skill` 委派给 spec-drafter」这条更窄的路**：`phase-a-planner:66` 的 `skip_if: has_openspec: true` 分支**不进 spec-drafter**（选用既有 Spec 而非起草），而该分支同样可能撞上竞品轨 ⇒ 委派方案对它零覆盖。故取双授权。\n```\n\nImpact 表新增两行：\n- `skills/phase-a-planner/SKILL.md` 那一行的「变更」列追加：`+ frontmatter allowed-tools 增 Bash, AskUserQuestion`，来源列追加 `R1/C1`\n- `skills/spec-drafter/SKILL.md` 那一行的「变更」列追加：`+ frontmatter allowed-tools 增 Bash`，来源列追加 `R1/C1`\n\nSC 表新增：\n`| **SC-20** (代码) | 读两个 SKILL.md 的 frontmatter | `phase-a-planner` 的 allowed-tools 含 `Bash` 且含 `AskUserQuestion`；`spec-drafter` 的含 `Bash` | 现状两者皆缺 ⇒ 必红。宿主 `state-scanner/tests/test_coordination_default_lockin.py`（既有读-SKILL.md 断言宿主） |`",
      "why_it_closes": "C1 的三层缺口逐层关闭：(a) 事实断言写进 Spec 且标注实读出处；(b) 处置给到字段级最终值，实现者无需推断；(c) SC-20 让断言错误在测试上红 —— 若将来某版把 Bash 从 frontmatter 拿掉，测试立刻失败。同时补上 C1 指出的「Impact 表零覆盖该字段」。",
      "risk_of_self_recurrence": "有，且形状与本 finding 完全相同：§3.1 这张表本身又是一组「对既有代码的事实断言」（现值列 + 先例列）。缓解有二：(1) 每格标注实读出处 `<file>:<line>`，可被下一席一条命令证伪；(2) SC-20 把「扩容后应有的值」机械钉住，断言错则测试红。残余风险在「先例列」（aria-report 等）—— 它不被任何 SC 覆盖，若我抄错先例，无人会红。这一格我已实读 `for f in aria/skills/*/SKILL.md` 全量输出确认，但它仍是零机械保护的软断言。"
    },
    {
      "finding_id": "C2 — §2.2 只换了 heartbeat 的匹配键，没指定谁调、何时调",
      "severity": "critical",
      "root_cause": "§2.2 的整个因果链搭错了机制：它认为保护窗由 heartbeat 新鲜度决定，于是去修 heartbeat 的匹配键。实读表明 (a) `linked_issue_overlaps` 对 heartbeat/TTL/stale 的 grep 命中为 0，可见性纯由 status 决定；(b) 真正杀死 A.1 claim 的是 `gc.sweep_stale_active`，而它有一个 §2.2 完全没看见的高频生产调用者。",
      "edit_target": "§2.2「保护窗 (spike S1 定案)」整节重写（含其两条 blockquote）；决策记录 D4 行重写；SC-5/SC-6/SC-7 三条替换；Impact 表 `claim_lifecycle.py` 行替换为 `gc.py` + `constants.py`。",
      "exact_change": "§2.2 整节替换为：\n\n```\n#### §2.2 保护窗 (spike S1 定案 + R1/C2 实读修正)\n\n事故窗实测 **48–72h**。原版据 `STALE_TTL`=30min / `SWEEP_TTL`=24h 推出「保护窗短于事故窗」，并处方「改 heartbeat 匹配键」。**该推理链经实读修正如下**：\n\n| 原版断言 | 实读 | 结论 |\n|---|---|---|\n| STALE_TTL(30min) 缩短保护窗 | `linked_issue_overlaps` (`collision.py:207-234`) 对 `heartbeat`/`TTL`/`stale` 三词 grep 命中 **0**；staleness 只走 reconcile 的同-track_id 路径，而 §2.1 保证两轨 track_id 必不同 | **不成立**，STALE_TTL 与本 Spec 无关 |\n| 缺 heartbeat 循环缩短保护窗 | 同上：可见性纯由 `status` 决定，与 heartbeat_at 新鲜度无关 | **不成立** |\n| SWEEP_TTL(24h) 缩短保护窗 | `gc.sweep_stale_active` 把 24h 未刷新的 active claim **durable 改写为 `abandoned`**，而 `abandoned` ∈ `_TERMINAL` ⇒ 从 overlap 消失 | **成立，且比原版估计的更严重** |\n\n**⚠️ 谁在扫（原版完全没看见）**：`phase-d-closer/SKILL.md:52` 的 D.2b 收尾命令**逐周期**带 `--sweep-stale`，而 sweep 是 **ref-wide** 的（`gc.py:361-364` docstring 自陈 \"Cross-container by design\"）。⇒ **任意一条不相干的轨走完自己的十步循环，就会顺手把你 25h 前的 A.1 claim 判死。** 这不是理论风险，是每个 cycle 都在跑的路径。\n\n**处置 = 让 A.1 claim 不被 24h 档扫掉，而不是发明一个没人调的 heartbeat**：\n\n1. `constants.py` 新增 `A1_SWEEP_TTL: int = 259200  # 72h`，取值 = S1 实测事故窗上界；\n2. `gc.sweep_stale_active` 的 TTL 改为按 `record.phase` 分档：`record.phase == \"A.1\"` ⇒ 用 `A1_SWEEP_TTL`，其余一律 `SWEEP_TTL`（24h 逐字节不变）；\n3. **仍然有限**：72h 后照扫 ⇒ 不引入永久泄漏（`gc.py` 存在的理由 defect (c) 不被破坏）。\n\n**「谁调、何时调」的答案 = 不需要任何人调。** 保护窗由 sweep **消费侧**的 TTL 分档给出，不新增任何周期性调用者 —— 这正是原版判否「再调 phase1_gate」的那条理由（依赖 AI 记性）所要求的形状，而被采纳的 heartbeat 方案**并不满足**它（R1/C2 的 CR 补刀）。\n\n> **`--phase A.1` 字面量是承重的**：§2 调用模板写 `--phase A.1`，`gc` 侧谓词必须逐字节比 `\"A.1\"`。`--phase` 是自由字符串（`phase1_gate.py:1190` 无 `choices`），写成 `A1` / `a.1` 即静默失配 ⇒ 见 SC-22。\n\n> **heartbeat by-track 变体移出本 Spec**（原 D4）：它治的病是「fresh session_id 匹配不上」，而本 Spec 的唯一消费面 (`linked_issue_overlaps`) 根本不读 heartbeat ⇒ 在本 Spec 内引入它 = 新增第二个零生产调用点的函数变体（`heartbeat()` 现有调用点 0，`constants.py:43-44` 自陈）。属 `feedback_paper_fix_antipattern` 形状。若要治 sweep 对长 Phase B 的误杀，另开 Spec。\n```\n\nD4 行替换为：\n`| D4 | A.1 claim 保护窗改由 `sweep_stale_active` 的 **phase 分档 TTL** 给出（A.1 ⇒ 72h），不引入 heartbeat 调用者 | **R1/C2 实读**: overlap 可见性零 staleness 过滤 (`collision.py:207-234`)；真实杀手是 `phase-d-closer/SKILL.md:52` 每周期带的 `--sweep-stale`（ref-wide）。heartbeat 方案在本 Spec 无消费面 ⇒ 移出 |`\n\nSC-5/6/7 三条替换为：\n`| **SC-5** (代码) | phase=`A.1` 的 claim，heartbeat_at 距今 25h，跑 `sweep_stale_active` | **不被** sweep（仍 active） | 现状 24h 一刀切必红 |`\n`| **SC-6** (代码) | 同上但距今 73h | **被** sweep 为 `abandoned` | 「A.1 永不过期」的实现必红（防止把 24h 换成 ∞） |`\n`| **SC-7** (代码) | phase=`B.2` 的 claim，距今 25h | **被** sweep（24h 档逐字节不变） | 把分档写成全局提升的实现必红 |`\n`| **SC-22** (代码) | 读 `phase-a-planner`/`spec-drafter` SKILL.md 的调用模板与 `gc.py` 的分档谓词 | 两侧 phase 字面量**逐字节相等** | 一侧写 `A.1` 另一侧写 `A1` 必红 |`\n\nImpact 表：删 `skills/state-scanner/lib/claim_lifecycle.py` 行；新增\n`| `skills/state-scanner/lib/gc.py` | `sweep_stale_active` TTL 按 `record.phase` 分档 | **R1/C2** |`\n`| `skills/state-scanner/lib/constants.py` | 新增 `A1_SWEEP_TTL = 259200` (72h) | **R1/C2** |`",
      "why_it_closes": "C2 问的是「谁调何时调」。本修法给出的答案是「无人需调」并证明它成立 —— 因为把机制从「生产端刷新」搬到了「消费端分档」，而消费端 (`sweep_stale_active`) 已有确凿的高频生产调用者 (`phase-d-closer/SKILL.md:52`)。CR 的补刀（判否再调 phase1_gate 的理由原样适用于 heartbeat）也被关闭：新方案零调用者，那条理由对它不适用。SC-5/6/7 从「测一个没人调的函数」变成「测一个每周期都在跑的路径」。",
      "risk_of_self_recurrence": "有两处，都实在：(1) **`\"A.1\"` 字面量分裂** —— 我用一个自由字符串做承重谓词，这正是本 Spec §2.1 批评「省字段即免义务」的同族病（约定散在两个文件、无机械回声）。已用 SC-22 做双向钉合，但 SC-22 自己是文本比对，若将来第三处（如 Layer 2 orchestrator）也写 phase，SC-22 看不见它。(2) **72h 这个数是我从 S1 的事故窗上界直接搬的**，没有独立依据说「72h 之后就安全」—— 它只是把恒定失效换成 72h 后失效，属显式收窄而非根治，必须以此措辞写进 §6 而不是当成已覆盖。另：本 fix 让 D4/§2.2/SC-5~7/Impact 四处同步改动，任一处漏改即产生新的自相矛盾（原版三轮的典型失败形状）。"
    },
    {
      "finding_id": "C3 — §1 的字段格式对真实语料 0/13 匹配",
      "severity": "critical",
      "root_cause": "§1 规定了一个「应然」格式却没对现存语料做 fit 检验，也没给「字段值 → `--linked-issue` 实参」的提取规则 —— 于是同一串既要给人读（链接+状态注解）又要给机器解析，两个用途互相污染。",
      "edit_target": "§1 第 2 条「格式固定」整条替换为新的 §1.2（canonical token 双层格式 + 字符级提取规则 + 作用域）；第 3 条「机械校验」补作用域；新增 SC-25；本 proposal 头部补该字段（dogfood）。",
      "exact_change": "§1 第 2 条替换为：\n\n```\n2. **格式 = canonical token + 自由散文（双层）**。实测 141 篇语料中 12 篇有 `**关联 Issue**:` 字段，**11 篇非「无」者全部是 markdown 链接形**，裸 `<org>/<repo>#<n>` 命中 **0/11** ⇒ 原版的单一裸形规格对现实为空集。实际书写形态含 `Forgejo [#134](url)` / `aria-plugin [#95](url)` / `[10CG/Aria #166](url)` / `Forgejo [aria-plugin#17](url)` 四种，且每篇尾部都带状态散文。\n\n   **规格**（字符级，唯一 SOT，§4 探针亦引用本条）：\n\n   ```\n   > **关联 Issue**: `10CG/aria-plugin#122` — [#122](https://forgejo.10cg.pub/10CG/aria-plugin/issues/122) (open, triage=confirmed)\n   ```\n\n   1. **提取器** = 该行 `**关联 Issue**:` 之后的**第一个 inline-code span**（一对反引号之间的内容），记为 canonical token；无 code span ⇒ 该 proposal 记 `NO_TOKEN`；\n   2. **token 合法值**二选一：字面 `无`，或前置 Spec 的归一可解析的 `<org>/<repo>#<n>`；\n   3. token 之后的任意散文 / 链接 / 括注**不参与机器解析**，自由书写 —— 现有书写习惯零成本保留；\n   4. **多 issue**：token 内用 `,` 分隔（实测 1/11 篇为真多 issue：`archive/2026-07-11-secret-guard-bash3-multiline-hardening` 含 #154/#157/#152/#156）。`--linked-issue` 只收单值 ⇒ **本 Spec 定义：取第一个 token 传给 `--linked-issue`，其余仅供人读**，第二及以后的 issue **不参与 overlap 匹配**（落 §6 缺口，不假装覆盖）；\n   5. **`--linked-issue` 实参 = canonical token 逐字节**，不做二次加工 —— 这是「不把整个 markdown 链接喂进匹配」的机械保证。\n\n   > **为什么是 code span 而不是「整行」**：整行既承载人读信息又承载机器实参，正是前置 Spec 要治的格式病在上一层复现（R1/C3 (b)）。code span 把两个用途在字符级隔开，且它是 AI 逐字照抄时最不易带出多余字符的载体。\n```\n\n§1 第 3 条「机械校验」末尾追加：\n```\n   **作用域 = `openspec/changes/**/proposal.md` 单层，不扫 `openspec/archive/`**。实测有字段的 12 篇里 10 篇在 `archive/` 下 ⇒ 限 `changes/` 后上线当日待判对象仅 3 篇（`linked-issue-normalization` / `phase-c-integrator-ci-path-coverage` / 本 Spec），**三篇全在本 Spec 交付范围内改到合规 ⇒ check 上线即绿**，不产生「129 篇恒黄」。这同时让第 4 条「不追溯」从口头承诺变成机械事实（path 前缀过滤）。\n```\n\n本 proposal 头部（第 8 行 `前置依赖` 之前）插入：\n```\n> **关联 Issue**: `无` — 本 Spec 由 `a1-entry-claim-duplicate-work-guard` 原版 post_spec R1 审计发现，无独立 issue（dogfood：本行即 §1.2 规格的实例）\n```\n\nSC 新增：\n`| **SC-25** (代码) | 对 §1.2 给出的四种历史书写形态 + 新 canonical 形态 + 无 code span 形态逐一跑提取器 | canonical 形态提取出裸 token；历史形态与无 code span 形态一律判 `NO_TOKEN`（**不猜**） | 提取器若对 markdown 链接做「宽容解析」会把整串或错串吐出 ⇒ 必红 |`",
      "why_it_closes": "C3 的两个子伤各自关闭：(a)「上线即恒红/129 篇恒黄」——作用域限 `changes/` 后待判对象降到 3 篇且三篇都在本 Spec 内改绿，可当场证伪；(b)「照抄字段值传 --linked-issue 收到整个 markdown 链接」——第 5 条把实参钉死为 code span 内容，第 3 条把散文明确排除在解析之外。KM/CR 的 dogfood 缺口由头部字段关闭，且该行本身就是规格实例。",
      "risk_of_self_recurrence": "有，且是本条最该盯的一处：**我新定义的格式对存量语料同样是 0/11 匹配** —— 与它要治的病一模一样。区别只在我给了作用域裁剪（限 `changes/`）把「存量不匹配」变成结构性无关，而不是靠「不追溯」这句承诺。若下一版有人把作用域悄悄放宽到 `archive/`，这条 fix 会当场退化成它要治的病。⇒ 作用域是本条的承重条款，不是实现细节，必须与规格写在同一段（我已如此放置）。次要风险：「第一个 inline-code span」这条提取规则本身在「散文里先出现了别的 code span」时会取错 —— 但规格要求 token 紧跟冒号，SC-25 未覆盖这种畸形输入，属残余。"
    },
    {
      "finding_id": "C4 — 对 _TERMINAL 的事实断言与代码相反",
      "severity": "critical",
      "root_cause": "§2.4 凭记忆写下状态集而未实读；且实读后发现真正的问题不是「少显了一个终态」，而是 `unknown` 这个「读不懂的 claim」被当终态静默丢弃 —— 那是零证据当正证据，与 §2.5 的 fetch_degraded 同一病。",
      "edit_target": "§2.4 首段（`done`/`abandoned`/`yielded` 那句 + `collision.py:210` 那句）整段替换；SC-8 场景列重写；新增 SC-24。",
      "exact_change": "§2.4 首段替换为：\n\n```\n实读 `collision.py:210`：`_TERMINAL = (\"done\", \"abandoned\", \"unknown\")` —— **三值，不含 `yielded`，含 `unknown`**。据此三分：\n\n| status | 现状 | 语义（实读依据） | 本 Spec 处置 |\n|---|---|---|---|\n| `yielded` | **今天已可见**（不在 `_TERMINAL` 内） | 主动让出，仍是候选（`concurrent_tracks.py:30`） | **不改**。原版称它被 skip 是错的 ⇒ 从 SC-8 移出，否则该子例 baseline 即绿 |\n| `done` / `abandoned` | 被 skip | 真终态 | `--include-terminal` 时显出。A.1 场景下 `done` 恰是最该看见的信号（「对方已经做完了」） |\n| `unknown` | 被 skip | **不是终态**：`claim_schema.py:18/51/226` —— 它是 schema_version 不被本读者识别时的 **reader-side sentinel**，「NEVER written by a live session」 | **无论 `--include-terminal` 与否都必须显出**，且措辞为「**未能核实**（存在一条 claim，但本读者读不懂其 schema 版本）」，**不得**与 `done`/`abandoned` 合并成「已终结」 |\n\n> **为什么 `unknown` 必须无条件显出**：它是「有一条竞品 claim 但我读不懂」，即零证据；把零证据当「无碰撞」正是 §2.5 已识别的病，`unknown` 是它在 status 维度的同形实例。跨版本容器并存时（Layer 2 镜像滚动升级）这条路径必然被踩到。\n\n> ⚠️ **同文件有两个同名常量**：`collision.py:210`（在 `linked_issue_overlaps` 内，三值）与 `collision.py:307`（另一函数，两值 `(\"done\",\"abandoned\")`）。本 Spec 要改的是 **`:210` 那个**。实现者改错位置会得到一个「测试半绿、生产不动」的结果。\n```\n\nSC-8 场景列改为：「同 issue 他轨 claim 为 `done` / `abandoned`（**不含 `yielded` —— 它今天已可见**）」\n\nSC 新增：\n`| **SC-24** (代码，CLI 全链路) | 同 issue 他轨 claim 的 schema_version 不被识别（status 读作 `unknown`），**不带** `--include-terminal` | 该 claim **出现在** `linked_issue_overlap[]`，且带可区分标记（措辞「未能核实」而非「已完成/已放弃」） | `:210` 现状 skip 必红；把 `unknown` 与 `done` 合进同一档的实现也必红 |`",
      "why_it_closes": "C4 的三项逐条关闭：`yielded` 的错误断言被撤回并从 SC-8 移出（消除 baseline-即绿 的假 SC）；`unknown` 从「完全没讨论」变成有独立处置 + 独立 SC + 实读依据；R2/M3 的 `yielded` 归属在表格第一行被显式重新记载（关闭「重写中丢失」）。额外收益：点名 `:210` vs `:307` 双常量，堵住实现期改错位置这条本项目已四次实证的路。",
      "risk_of_self_recurrence": "低但非零。诚实点：我这段新文本自己就是一张「对既有代码的事实断言表」—— 与 C4 同形。三格依据我都逐条实读过（`collision.py:210` 与 `:307` 用 grep 全量列出、`claim_schema.py:18/51/226` 与 `concurrent_tracks.py:30` 直读），且 SC-8/SC-24 把 `yielded` 与 `unknown` 两个最易写反的格钉成可红的测试。真正无机械保护的是「`:307` 是另一函数」这句 —— 它只影响实现者导航，写错不会红。另一残余：`unknown` 无条件显出会让 overlap 在跨版本期变噪，本 Spec 未给降噪出口。"
    },
    {
      "finding_id": "C5 — D6「无任何函数支持释放别的容器的 claim」为假",
      "severity": "critical",
      "root_cause": "D6 把一条实读为真的窄断言（`release_claim_by_track` 只匹配自己 container）过度推广成全称否定，没有反向枚举其他写入路径；而审计给出的危害描述（30min 阈值）自身也与代码不符。",
      "edit_target": "§2.3 第二个选项下的 blockquote「『接手』不是一键动作」整段重写；D6 决策行依据列重写；新增一条 minor 级顺手勘正进 Impact。",
      "exact_change": "§2.3 blockquote 替换为：\n\n```\n> **「接手」不是一键动作 —— 但理由不是「没有这样的函数」(R1/C5 实读修正)**：\n>\n> - **窄断言为真**：`release_claim_by_track` 的匹配集含 `rec.container == resolved.container_id`（`claim_lifecycle.py:422-428`）⇒ 它释放不了别人的 claim；\n> - **全称否定为假**：跨容器写路径**存在** —— `gc.sweep_stale_active`（`gc.py:338`），经 `release_gate.py --sweep-stale` 暴露，其 docstring `gc.py:361-364` 自陈 \"Cross-container by design\"，且 `phase-d-closer/SKILL.md:52` **每周期都在调它**；\n> - **但它不构成「接手」原语**，三条实读理由：(a) 目标不可指定 —— 无差别扫全 ref；(b) 终态不可指定 —— 只写 `abandoned`；(c) 时机不可指定 —— TTL 闸门（`gc.py:341` 默认形参 `SWEEP_TTL`，`release_gate.py:141` 未覆写 ⇒ **实际阈值 24h**）。\n> - ⚠️ **两处文档把阈值写错成 STALE_TTL(30min)**：`release_gate.py:225` 的 `--sweep-stale` help 与 `state-scanner/SKILL.md:176`，而代码用 `SWEEP_TTL`(24h)。本 Spec 顺手勘正这两处措辞（描述性勘正，Rule #6 判据表第一行）—— 它是把「24h 的 GC」误读成「30min 的抢占」的直接来源。\n>\n> ⇒ **定向跨容器 release 仍不在本 Spec 引入**：写别人的 claim 是权限面变更，应独立评估。措辞即定义，避免实现者以为有一键路径。\n```\n\nD6 依据列替换为：\n`| D6 | 「接手」= **两步人工**，不引入**定向**跨容器 release | **R1/C5 实读**: 全称否定为假 —— `gc.sweep_stale_active` 是跨容器写路径且每周期在跑；但它 TTL-gated(24h)、不可定向、只写 `abandoned` ⇒ 不是接手原语。定向 release 是新权限面 |`\n\nImpact 表新增：\n`| `skills/state-scanner/scripts/release_gate.py` + `skills/state-scanner/SKILL.md` | `--sweep-stale` 的 TTL 措辞勘正 STALE_TTL→SWEEP_TTL(24h) | **R1/C5** |`",
      "why_it_closes": "D6 的定案（不引入跨容器 release）保留，但依据从一个被证伪的全称否定换成三条可逐条实读复验的、限定明确的理由。审计自身的危害描述（30min ⇒ 几乎所有并发轨可达）被纠正为实际的 24h，并指出误读来源是两处文档笔误 —— 这满足「危害描述与建议修法方向一致」：既然危害是文档写错阈值，修法就是勘正文档，而不是去改 sweep 的权限模型。",
      "risk_of_self_recurrence": "有一处需要点名：我在这里说「TTL-gated ⇒ 不构成接手原语」，但 §2.2 的 fix 恰恰**动了这个 TTL**（A.1 档 72h）。两条 fix 在同一函数上相遇：C5 说「TTL 是它不危险的理由」，C2 说「我要改 TTL」。二者不冲突（C2 只延长、只对 A.1、且仍有限），但下一版若有人据 C5 的措辞去缩短或去掉 TTL，就会同时打掉 C5 的安全论证 —— 这正是「多条 fix 互相拆台」的形状。故 §2.2 的分档条款里我已写明「仍然有限」，两处措辞需保持一致。另：三条理由 (a)(b)(c) 全是软断言，无 SC 覆盖。"
    },
    {
      "finding_id": "C6 — include_terminal 传递链漏了第 0 段",
      "severity": "critical",
      "root_cause": "传递链从 CLI 往下数到调用点就停了，没往下数到被调函数本身 —— 与 R2-fix 那次「指错函数」是同一种「只审自己写的那几段、不审链条端点」的盲区。",
      "edit_target": "§2.4 的「`include_terminal` 的传递链（三段缺一不可）」列表改为四段；Impact 表新增 `lib/collision.py` 行；新增跨 Spec 合并序声明（与 M1 的 §0 合并落笔）。",
      "exact_change": "列表标题与首项改为：\n\n```\n**`include_terminal` 的传递链（四段缺一不可 —— R1/C6：原版从第 1 段起数，漏了第 0 段）**：\n\n0. **`lib/collision.py` 的 `linked_issue_overlaps` 加形参**。实读现签名（`collision.py:177-181`）：\n   ```python\n   def linked_issue_overlaps(claims, own_track_id, own_linked_issue) -> \"list[dict]\":\n   ```\n   改为：\n   ```python\n   def linked_issue_overlaps(claims, own_track_id, own_linked_issue, *, include_terminal: bool = False) -> \"list[dict]\":\n   ```\n   **keyword-only + 默认 `False`** 两项都是承重的：前者防位置实参错位，后者保 Phase B 既有语义逐字节不变（对齐 §非目标）。缺这一段，按原版字面在 `_main():1232` 加关键字参数会直接 `TypeError`。\n1. `phase1_gate.py` 新增 CLI flag `--include-terminal` (store_true);\n2. **在 `_main()` 的现有调用处** (`phase1_gate.py:1232-1234`) 传该关键字参数 —— **不碰** `run_gate` / `_run_gate_impl` 签名;\n3. A.1 调用模板**显式带该 flag**。\n```\n\n（原第 2 段的 R3/C2 blockquote 原位保留，仅把「三段」字样改为「第 1-3 段」。）\n\nImpact 表新增：\n`| `skills/state-scanner/lib/collision.py` | `linked_issue_overlaps` 增 keyword-only 形参 `include_terminal=False`；`_TERMINAL` 的 `unknown` 分档 (C4) | **R1/C6 + R1/C4** |`",
      "why_it_closes": "把链条端点补齐，且给出了可直接落笔的完整签名（含 `*` 与默认值），实现者无需推断。keyword-only + 默认 False 同时消化了「不改既有调用方行为」这个约束 —— 这也是与姊妹 Spec SC-8「签名与 schema 不变」共存的技术前提（见 M1 的合并序条款）。Impact 表补上 C6 点名的零覆盖。",
      "risk_of_self_recurrence": "有一处真实的：我新写的这段本身也是一条链，而它的**第 4 段**（消费面：谁读 `linked_issue_overlap[]` 里新出现的终态条目并按 status 分档渲染）我放在了 §2.3 而不是这张表里 —— 若下一版只按这张表实现，会得到「CLI 能吐终态条目但没人按 status 分档渲染」= 有记录无路由（memory `fix-recurs-in-fallback`）。缓解：SC-8 已拆为 `-a`(CLI 字段) / `-b`(渲染措辞) 两条（见 M10），第 4 段由 SC-8b 兜。但表里不写出来仍是我这版的结构缺陷，属知情取舍。"
    },
    {
      "finding_id": "M1 — 两 Spec 间归一能力职责真空",
      "severity": "major",
      "root_cause": "两份 Spec 各自把 `collision.py` 当成「对方负责的地方」：姊妹 Spec 声明「只改内部谓词、签名不变」，本 Spec 的 Impact 表干脆不列它 —— 但本 Spec 有两处依赖调用它，且 C6 还要改它的签名。没有任何一份文件写着合并顺序。",
      "edit_target": "新增 §0「跨 Spec 契约（与 `linked-issue-normalization` 的分工与合并序）」，置于 §1 之前；§2.1 的 basename 行依据列一并修正。",
      "exact_change": "§What Changes 下、§1 之前插入：\n\n```\n### §0 跨 Spec 契约 (与 `linked-issue-normalization` 的分工与合并序)\n\n两份 Spec 都动 `lib/collision.py`，职责与顺序钉死如下（R1/M1）：\n\n| 项 | 姊妹 Spec | 本 Spec |\n|---|---|---|\n| `linked_issue` 归一谓词 | **实现方**（`collision.py` 内部比较谓词，五步字符级算法） | **消费方，不重实现** |\n| `linked_issue_overlaps` 签名 | 声明「不变」（其 SC-8） | 加 **keyword-only 且带默认值**的 `include_terminal`（§2.4 第 0 段） |\n| `_TERMINAL` 的 `unknown` 分档 | 不涉 | 本 Spec（§2.4） |\n\n**为什么两者不冲突**：姊妹 SC-8 的可验语义是「既有调用方零改动」；`*, include_terminal: bool = False` 对既有 3 位置实参调用点逐字节无影响 ⇒ 该 SC 仍绿。**但这是本 Spec 单方面的读法** ⇒ A.2 须在姊妹 Spec 的 tasks 里交叉登记本条，不得只写在这边。\n\n**合并序：姊妹先 ship，本 Spec 后。** 反序落地的后果：`linked_issue` 匹配仍是裸 `!=`，主机制在真实语料上恒漏报（姊妹 Spec §Why 已实证 A/B 两族格式并存）⇒ 本 Spec 的 §2.3 消费面会稳定渲染「无碰撞」，即假绿。\n\n**归一的边界（原版 §2.1 表述错位，一并勘正）**：`raw_track_id` 的归一职责在 `run_gate` **内部**（`derive_track_id`，`track_id.py:61-77`：lower → `/`.`_`→`-` → 截断 64 → 非 ASCII/超长走 sha 回落）；`standards/conventions/session-handoff.md:246` 明写「**调用方不预先归一**」。⇒ §2.1 的 `basename` 一行不得写「经前置 Spec 归一」—— 那是 `linked_issue` 的归一，作用对象不同，混用会在 track-id 侧引入第二套归一实现。\n```\n\n§2.1 表格 `basename` 行改为：\n`| `basename` | **传未归一原串**；`/` `.` `_` → `-` 由 `derive_track_id` (`track_id.py:71`) 在 `run_gate` 内部完成，调用方不预先归一 (`session-handoff.md:246`) | 避免 track-id 侧出现第二套归一实现 |`\n\nImpact 表新增（KM 点名的两份 standards 文档）：\n`| `standards/conventions/session-handoff.md` | §2.3.x 追加：A.1 阶段 claim 的 `phase` 字面量 `A.1` 与其 72h sweep 分档（track_id 自称 SOT，须同步） | **R1/KM** |`\n`| `aria/skills/state-scanner/docs/coordination-ref-schema.md`（若存在，否则记为不适用并说明） | `unknown` 在 overlap 面的可见性语义 | **R1/KM** |`",
      "why_it_closes": "职责真空被一张表填满（谁实现、谁消费、谁改签名），合并序被显式写出并给出反序后果，交叉登记义务落到 A.2 而不是停在意向。附带关闭了一处审计未点名但同源的错位：§2.1 把 `linked_issue` 归一误用到 `track_id` 上 —— 若不改，实现者会在 track-id 侧再造一套归一，正是 M1 描述的真空在下游复现。",
      "risk_of_self_recurrence": "两处：(1) **「姊妹 SC-8 仍绿」是我单方面的解释** —— 我在替另一份 Spec 的验收标准做语义裁定。已写明「这是本 Spec 单方面的读法，A.2 须交叉登记」，但若 A.2 漏做，这条就退化成一句没人认的自我授权（形状同 memory `feedback_cross_doc_claim_verify_at_target`）。(2) 我新增的 `coordination-ref-schema.md` 一行**没有实读确认该文件存在** —— 我只在 `claim_schema.py:16` 的注释里见到该路径被引用。故我在 exact_change 里写成条件形（「若存在，否则记为不适用并说明」）而不是断言，这是知情的降级，不是遗漏。"
    },
    {
      "finding_id": "M2 — §5「放弃必 release」× §2.1「id 不含 slug」互相拆台",
      "severity": "major",
      "root_cause": "§2.1 让 track-id 只由 issue 决定，于是「同一 issue 的多个探索方向」在 id 维度不可分；§5 又要求「放弃某方向就 release」，而 release 按 track_id 释放全部匹配 ⇒ 两条各自成立的条款在同一 track_id 上相撞。",
      "edit_target": "§5 表格「探索性放弃」行整格重写；SC-14 改写（并入 M9 的重标）；§非目标追加一条。",
      "exact_change": "§5「探索性放弃」行的处置格替换为：\n\n```\n**先厘清**：§2.1 的 track-id 三段（basename / number / container_uuid）**都不含方向信息** ⇒ 同 issue 的三个方向共享**同一** track_id；而 `release_claim_by_track` 释放该 `(container, track_id)` 下的**全部** active claim（`claim_lifecycle.py:433-435`，注释明写 \"ALL matches are released (review I1)\"）。\n\n⇒ **规则（R1/M2）**：\n- **放弃某一个方向 ⇒ 不 release**（会连坐掐掉同 issue 保留中的方向）；\n- **判定整个 issue 不做（全部方向放弃）⇒ 必须** 调：\n  ```bash\n  python3 \"${CLAUDE_PLUGIN_ROOT:-aria}/skills/state-scanner/scripts/release_gate.py\" \\\n    --raw-track-id \"<与 A.1 认领时逐字节相同的原串>\" --status abandoned --repo-path \"<主仓根>\"\n  ```\n  （`--raw-track-id` **必给** —— `release_gate.py:236-237` 要求 `--raw-track-id / --sweep-stale / --gc` 至少其一，只给 `--status` 会被 argparse 拒绝。此即 R1 minor/BA。）\n- **同 issue 派生出两个都要做的 Spec 时**：后起的那个**不得**复用同一 track_id（否则先 ship 的一方 D.2b release 会连坐掐掉后者），须走回落分支 `<spec-slug>-<container_uuid>` 另起 id，并在其 proposal 头部 `关联 Issue` 字段照常填同一 issue（overlap 靠 `linked_issue` 而非 track_id 工作，§2.1「职责分离」原文即此意）。\n```\n\n§非目标追加：\n`- **不**为「同 issue 多方向」引入方向维度的 track-id 分段（会重新打开 R2/C1 的自排除问题）；多方向的可分性由上面的「后起 Spec 走回落分支」规则承载。`",
      "why_it_closes": "M2 指出的连坐路径被规则本身堵住（放弃单方向不 release），而 §5 原本要防的病（claim 永久 active）由「全部放弃时必 release」+ §2.2 的 72h 有限 sweep 双重兜底。同时补上 BA 的 minor（命令缺 `--raw-track-id`），因为它就在同一格里。",
      "risk_of_self_recurrence": "这是本轮我最不放心的一条。新写的「后起 Spec 走回落分支」规则自己带一个隐含前提：回落分支 `<spec-slug>-<uuid>` 含 slug，而 §5 的第二行明写「回落分支改名须 release 旧 + acquire 新两步」⇒ 我为了修 M2 引入的规则，把更多 Spec 推进了那条更脆的分支。也就是说：我用「拆台 A」换来了「拆台 B 的暴露面变大」。这是知情取舍，不是无风险 —— 且它使 SC-1 的「改名 id 不变」对更大比例的场景为假（与另/CR-M1 的分支限定必须同步落笔，否则两条 fix 又互相拆台）。"
    },
    {
      "finding_id": "M3 — §4 探针的「同 issue」匹配谓词全文未定义",
      "severity": "major",
      "root_cause": "§4 用「同 issue」这个自然语言词承载了一个必须字符级定义的谓词；且若直接沿用 §1 的 canonical token 规则，探针在自己的 motivating case（两篇 #122 proposal，实测**都不含** canonical token）上恰好失效。",
      "edit_target": "§4 第二个 bullet「扫描范围含归档」之后插入新 bullet「同 issue 谓词」。",
      "exact_change": "§4 插入：\n\n```\n- **「同 issue」谓词（字符级，R1/M3）**：探针对每份 proposal 提取 issue 标识集合，两份的集合**有交集**即命中。提取分两层，**顺序固定**：\n  1. **canonical 层**：§1.2 的提取器（`**关联 Issue**:` 后第一个 inline-code span），产出 `<org>/<repo>#<n>` 集合，经前置 Spec 的归一化为比较键；\n  2. **URL 回落层**（仅探针用）：canonical 层为 `NO_TOKEN` 时，从该行**所有** URL 中按 `/<org>/<repo>/issues/<n>` 抽取。实测 11/11 有字段的存量 proposal 该层可解析 ⇒ 探针对存量语料可用；\n  3. 两层皆空 ⇒ 该 proposal 对探针**不可见**（落 §6 缺口，不猜标题、不猜 slug）。\n\n  > **作用域分离是承重的**：URL 回落**只用于探针的只读比对**，**绝不**用于产生 `--linked-issue` 实参 —— 后者仍严格取 canonical token（§1.2 第 5 条）。混用会把脏串喂进匹配面，即前置 Spec 要治的病换条路径复现。\n\n  > **motivating case 自检（可证伪）**：`openspec/changes/phase-c-integrator-ci-path-coverage/proposal.md:18` 与 `openspec/archive/2026-07-31-phase-c-gate-path-coverage-not-applicable/proposal.md:6` 均无 canonical token，但两者的字段行 URL 均可抽出 `10CG/aria-plugin#122` ⇒ **经第 2 层互相命中**。若只实现第 1 层，探针在第 5 次事故的真实语料上返回空 —— 即它在自己的立项理由上失效。\n```\n\nSC 新增：\n`| **SC-26** (代码) | 以上述两篇真实 proposal 的字段行原文为夹具 | 探针判**命中**，且命中键为 `10CG/aria-plugin#122` | 只实现 canonical 层的实现必红（这正是 §4 的 motivating case） |`",
      "why_it_closes": "谓词从自然语言变成两层字符级规则 + 固定顺序 + 明确的不可见分支；并用 §4 自己的立项案例做 SC 夹具，使「谓词是否成立」可当场证伪而不是等实现期才发现。作用域分离条款同时防止这条 fix 污染 C3 的成果。",
      "risk_of_self_recurrence": "有，形状明确：我给探针开了一条 C3 明令禁止的「宽容解析」口子。两者靠「作用域分离」共存，而作用域分离是**一句散文约定**，无机械回声 —— 实现者把 URL 回落层复用到 `--linked-issue` 构造上，没有任何测试会红。这是本条已知的、未闭合的弱点：正确的补法是让 URL 回落层的函数签名/模块位置就使它不可能被 `--linked-issue` 路径引用（如放在 audit-engine 的探针脚本内而非 state-scanner 的共享 lib），但那会与「单一 SOT」的诉求冲突。我倾向前者并把这个取舍留给 A.2 定，不在本轮硬定。"
    },
    {
      "finding_id": "M4 — 「进模板」只做了一半",
      "severity": "major",
      "root_cause": "§1 的两条腿（模板 + 机械校验）各自落在 plugin 分发不到的位置：模板改的是 `spec-drafter` 内嵌预览而非模板 SOT，校验改的是主仓项目级 `.aria/state-checks.yaml` ⇒ 采用方两条腿都拿不到，只继承了「会退化」的那一半。",
      "edit_target": "§1 第 1 条「进模板」与第 3 条「机械校验」各自重写；Impact 表新增两行。",
      "exact_change": "§1 第 1 条替换为：\n\n```\n1. **进模板（两处，缺一则采用方拿不到）**：\n   a. **模板 SOT** `standards/openspec/templates/proposal-minimal.md` —— 实读该文件现无任何 issue 字段，而 `spec-drafter/SKILL.md:429` 正指向它。在其头部 blockquote 增：\n      ```\n      > **关联 Issue**: `<org>/<repo>#<n>` 或 `无` — {链接与状态注解，自由书写}\n      ```\n      （standards 是共享子模块 ⇒ 独立 PR + 主仓 gitlink bump，见 Impact）；\n   b. **`spec-drafter/SKILL.md` 的 Level 2 预览段**（`:137-142`）同步增该行，否则 AI 照预览抄仍不带字段。\n   无关联时**显式写 `无`**，不留空 —— 空与「忘了写」不可区分。\n```\n\n§1 第 3 条替换为：\n\n```\n3. **机械校验（可分发，不只落主仓）**：\n   a. 校验逻辑落 **`aria/skills/spec-drafter/scripts/linked_issue_field_check.py`**（stdlib-only，随 plugin 分发；`spec-drafter` 现无 `scripts/`，目录新建 —— `run_all_tests.sh` 按 `find skills -type d -name tests` 自动发现，无需注册）；\n   b. 主仓 `.aria/state-checks.yaml` 只放**调用**该脚本的一条 check（`severity: warning`，与既有 8 条同档）；\n   c. `spec-drafter/SKILL.md` 登记该脚本，采用方把同一条 check 抄进自己的 `.aria/state-checks.yaml` 即可获得同等回声。\n   **severity: warning**（advisory-over-hardlock）。\n```\n\nImpact 表新增：\n`| `standards/openspec/templates/proposal-minimal.md` | 模板 SOT 增「关联 Issue」字段（standards 子模块：独立 PR + 主仓 gitlink bump） | **R1/M4** (承接 R1/M1 丢失的那一半) |`\n`| `skills/spec-drafter/scripts/linked_issue_field_check.py` + `tests/` | **新增**（目录新建）；主仓 state-checks 只做调用壳 | **R1/M4** |`",
      "why_it_closes": "两条腿都被抬到 plugin/standards 分发面：模板改 SOT 且同步内嵌预览（防「AI 照预览抄」这条实际路径），校验把逻辑放进随 plugin 分发的脚本、主仓只留调用壳。M4 点名的「R1/M1 的 standards 一半在重写中丢失」也随之补回。",
      "risk_of_self_recurrence": "有一处：`standards` 是共享子模块，改它意味着本 Spec 的交付跨三个 repo（主仓 / aria / standards），而本 Spec 的 §Why 恰恰在讲「多轨并发互不知情」—— 跨 repo 交付把自己暴露在同一类风险下（CLAUDE.md 多远程两条硬约束 + memory `feedback_submodule_pointer_post_merge_bump`）。这不是措辞能消的，属 A.2 必须排序的实际成本，我已在 Impact 里写明「独立 PR + gitlink bump」而非轻描淡写。另：我**未**把 `spec-drafter/SKILL.md:110/:134` 的 `standards/openspec/changes/` 违反 Rule #5 一并修 —— 见 deferred。"
    },
    {
      "finding_id": "M5 — phase1_gate.py:1229 的 `if args.linked_issue:` 门控整块",
      "severity": "major",
      "root_cause": "§6 的缺口表按「机制覆盖不到谁」列举，漏了最大的一类：机制的**输入本身缺席**。而按 §1 自己的 9% 统计，无输入才是多数路径。",
      "edit_target": "§6 缺口表新增首行；§2.3 告警渲染规则新增一条三态区分；新增 SC-27。",
      "exact_change": "§6 缺口表**首行**插入（放首位，因为它是最大的一项）：\n\n```\n| **双方（或任一方）无「关联 Issue」字段** —— 实测存量语料 **129/141 = 91%** 无该字段 | 无界 | **无**。`phase1_gate.py:1229` 的 `if args.linked_issue:` 门控整块 ⇒ 无字段时 `linked_issue_overlap` 键**根本不出现在 JSON 里**（不是空数组），主机制与 §4 探针**同时无输入**。§1 的 warning check 是唯一杠杆，而它只提醒、不产生匹配键 |\n```\n\n§2.3「告警须含」之后新增：\n\n```\n- **三态必须可辨（R1/M5 + R1/M6）**，消费面措辞不得合并：\n  | JSON 形态 | 含义 | 渲染 |\n  |---|---|---|\n  | `linked_issue_overlap` **键缺席** | 本轨未提供 `--linked-issue`（无字段） | 「**未检测** —— 本 Spec 无「关联 Issue」字段，主机制无输入」 |\n  | `linked_issue_overlap: []` | 已核实，无同 issue 他轨 | 「无碰撞」 |\n  | `linked_issue_overlap_error` 非空 | 检测过程异常降级 | 「**未能核实**」 |\n  「未检测」与「无碰撞」渲染成同一句，等于把 91% 的路径伪装成已核实 —— 与 §2.5 的 fetch_degraded 同一病。\n```\n\nSC 新增：\n`| **SC-27** (代码，CLI 全链路) | 三次调用：(a) 不传 `--linked-issue`；(b) 传但 ref 内无同 issue 他轨；(c) 传但 `read_claims` 抛异常 | 三次输出**逐 JSON 形态可辨**：(a) 键缺席；(b) `[]`；(c) `[]` + `linked_issue_overlap_error` 非空 | 现状 (a)(b) 已可辨、(c) 与 (b) **不可辨** ⇒ (c) 子例必红 |`",
      "why_it_closes": "M5 的两半各自关闭：缺口表补上最大一项并给出实测比例（91%），不再让 §6 显得比实际覆盖得多；消费面从「渲染一个数组」升级为「区分三态」，使「没有输入」不能再被静默呈现为「已核实无碰撞」。SC-27 把三态钉成 CLI 层可红的断言。",
      "risk_of_self_recurrence": "有，是本项目已实证的形状：我新增了 `linked_issue_overlap_error` 这个诊断键 —— **有记录 ≠ 有路由**（memory `fix-recurs-in-fallback`）。若渲染侧不消费它，这个键就是又一个「已 ship ≠ 能用」的字段，与 §2.5 点名的 `fetch_degraded`（docstring 预留却从未被赋值）一模一样。缓解：我把渲染规则和 JSON 形态写在**同一张表**里，且 SC-8b/SC-10b（M10 拆出的行为条）覆盖渲染侧。但渲染侧只能由 AB fixture 覆盖，强度弱于代码类 —— 这是诚实的残余，不是已闭环。"
    },
    {
      "finding_id": "M6 — :1235-1237 的 `except → []` 才是「零证据当正证据」的真实落点",
      "severity": "major",
      "root_cause": "§2.5 只盯住 `GateResult.error` 这个它已经知道的通道，没有反向从「哪些代码路径能把零证据写成 `[]`」出发枚举 —— 于是治了一个能红的地方，漏了真正会绿的地方。",
      "edit_target": "§2.5 第二个 bullet「fetch 降级须进 error 契约」整条重写；SC-10 改写（与 M10 的拆分合并落笔）。",
      "exact_change": "§2.5 第二个 bullet 替换为：\n\n```\n- **降级必须在两个通道都留痕，缺一即假绿**：\n  1. **`GateResult.error`**：`phase1_gate.py:210` 的 docstring **早已预留 `\"fetch_degraded\"` token 但从未被赋值**（又一个「已 ship ≠ 能用」）⇒ fetch 降级时实际赋值；\n  2. **`linked_issue_overlap` 的 except 分支（真实落点，R1/M6）**：实读 `phase1_gate.py:1235-1237` ——\n     ```python\n     except Exception as exc:  # fail-soft\n         logger.warning(\"phase1_gate: linked_issue overlap check skipped (%s)\", exc)\n         out[\"linked_issue_overlap\"] = []\n     ```\n     这条 fail-soft 路径把**任何**异常（ref 读失败 / 解析失败 / 归一崩溃）都写成 `[]`，与「已核实无重叠」逐字节不可分。**只治通道 1 ⇒ SC-10 可绿而病仍在。**\n     处置：except 分支保留 `[]`（不改既有键语义，Phase B 消费方零改动），**追加 additive 键** `out[\"linked_issue_overlap_error\"] = <非 secret 的短 token>`。\n  3. 两个通道的消费面措辞统一为「**未能核实**」，**不得**渲染成「无碰撞」——零证据不得当正证据。\n```",
      "why_it_closes": "把 §2.5 的治疗对象从「它已知的那个通道」扩到「实读枚举出的全部能写零证据为空集的路径」，并明确指出只治通道 1 会导致 SC-10 绿而病在。additive 键的选择保证不破坏 §非目标的「Phase B 语义逐字节不变」。",
      "risk_of_self_recurrence": "两处：(1) 我又新增了一个诊断键，与 M5 的 fix 同源风险（有记录无路由）—— 两条 fix 恰好共用同一个键，好处是路由点只有一个、坏处是两条 fix 一起成或一起败；(2) 更根本的：我保留了 `except Exception` 这个**过宽的捕获**。真正的根治是收窄捕获范围让非预期异常冒泡，但那会改变 fail-soft 契约（advisory 不得阻断 gate）⇒ 我选择了留住宽捕获 + 加可观测键。这意味着「异常被吞掉」这个类别本身没消失，只是变得可见了 —— 这是知情取舍，应在 §6 而非 §2.5 里再声明一次。本轮我没加这条声明，属本 fix 的已知不足。"
    },
    {
      "finding_id": "M7 — §4 只扫默认分支 ⇒ in-flight 竞品结构性不可见",
      "severity": "major",
      "root_cause": "§4 一边把「只扫默认分支」当实现细节写在规模上限里，一边在 §6 把探针记为覆盖 (a)(b) 两场景 —— 覆盖归功没有减去自己的取样面限制；且「各自默认分支」这个取法从未定义。",
      "edit_target": "§4「规模上限」bullet 重写并前置一条「取样面」bullet；§6 表格中 §4 的两处「覆盖它的机制」归功收窄。",
      "exact_change": "§4 新增 bullet（置于「规模上限」之前）：\n\n```\n- **取样面 = `enforced_remotes` × 各自默认分支，定义如下（R1/M7）**：\n  1. `enforced_remotes` 语义**必须**照 `multi_remote.py:270-286` 的 F5′ 契约：**非空列表 = 显式白名单；`[]` 或 `None` = auto-discover 全部 remote，不是空集**。把 `[]` 编码成「什么都不扫」会让每个默认采用方的探针静默变成 no-op（该注释块把这一点标为 🔴 trap，本仓已踩过一次）；\n  2. 「默认分支」取法：`git symbolic-ref refs/remotes/<remote>/HEAD` → 失败回落 `<remote>/master` → 再失败则**该 remote 记 `degraded` 并披露**，不静默跳过（朴素做法在第二 remote 上直接失效 —— 多数镜像 remote 没有 `HEAD` 符号引用）；\n  3. **取样面的结构性盲区**：未合入默认分支的 in-flight 竞品（feature 分支上的 proposal）**看不见**。这不是可调参数，是取样面的定义结果。\n```\n\n「规模上限」bullet 中「同库 `handoff_multibranch.py` 已因 440 条远端分支踩坑并做了 scan cap」一句后追加：\n`（实读 `scripts/collectors/handoff_multibranch.py:108-112, 326-338`：cap 三层可配 env > config > default **20**，超限走 `soft_error(\"handoff_multibranch_branch_cap\")` 披露被丢弃范围 —— 本探针照同一形状实现）`\n\n§6 表格：\n- 「legacy 轨」行的「覆盖它的机制」列改为：`**§4 探针部分覆盖 —— 且仅当竞品已合入默认分支**（S6: 7/9 身份属此类；in-flight 者不可见）`\n- §4 bullet「盲区声明」中「只看得见**已 push** 的竞品」改为「只看得见**已 push 且已合入被扫 remote 默认分支**的竞品」",
      "why_it_closes": "三项各自关闭：取法从未定义 → 给出三步确定性算法含失败处置；`[]` 语义 → 直接引用已成文的 F5′ trap，防止实现者复现一个本仓已踩过的坑；覆盖归功 → §6 的两处措辞都减去了取样面限制，使「自述盲区」与「覆盖归功」不再互相矛盾。",
      "risk_of_self_recurrence": "低。三条都是把既有的、已成文的机械契约引进来，不是我新造的规则 —— 复发风险主要在引用准确性，而 `multi_remote.py:270-286` 与 `handoff_multibranch.py:108-112/326-338` 我都逐行实读过。残余：`git symbolic-ref` 回落链的第三步「记 degraded 并披露」没有 SC 覆盖（SC-18 只覆盖探针整体 fetch 失败），单个 remote 降级而其余成功这个中间态无人测。"
    },
    {
      "finding_id": "M8 — §3 双落点是本版核心杠杆，SC 零覆盖",
      "severity": "major",
      "root_cause": "SC 表按「机制正确性」组织，双落点属「机制被装在哪」——这一维在 SC 表里没有位置，于是本版最大的杠杆恰好落在验证面的空白处。",
      "edit_target": "Success Criteria 新增 SC-21（与 C1 的 SC-20 相邻，同宿主）。",
      "exact_change": "SC 表新增：\n\n```\n| **SC-21** (代码) | 读 `phase-a-planner/SKILL.md` 与 `spec-drafter/SKILL.md` | 两者**各自**含独立标题级认领步骤，锚点字面 `A.0 - REQUIRE claim`；且该步骤块内含 `phase1_gate.py`、`--linked-issue`、`--include-terminal`、`--phase A.1` 四个字面量 | 任一落点缺失 / 认领被塞回 A.1 的 YAML 动作列表（R3/M6 的原病）必红 |\n```\n\n并在该行下方加注：\n`> 宿主与写法直接套用本仓同型先例 `state-scanner/tests/test_coordination_default_lockin.py:53-56` 的 `test_phase_b_require_claim_present`（它对 Phase B 两个落点做的正是同一件事）—— 扩它，不另起文件。`\n\n§3 双落点列表末尾追加：\n`> **锚点字面量 `A.0 - REQUIRE claim` 是承重的**：SC-21 与 SC-22 都按字面比对，改锚点措辞即静默失去两条 SC 的保护。取 `A.0` 与 Phase B 的 `B.0` 对称（`phase-b-developer/SKILL.md:86`）。`",
      "why_it_closes": "把本版核心杠杆钉进可红的结构化测试，且直接复用 QA 点名的既有先例（同一文件、同一断言形状），实现成本接近零、不引入新宿主。锚点字面量被显式声明为承重项，使「改措辞导致 SC 静默失效」这条路被写下来而不是留给下一轮发现。",
      "risk_of_self_recurrence": "有一处，且形状与 memory `invariant-dimension` 完全吻合：SC-21 是**存在性**断言（文本在不在），而它要防的失败是**行为性**的（AI 会不会真跑那条命令）。存在性检查对「步骤写在那里但被跳过」天然免疫 —— 这正是 §Why 自证的病。所以 SC-21 只能锁住「机制被装上了」，锁不住「机制被执行了」；后者只有 rule6_note 的定向 fixture 能覆盖。我已把它标为「代码」类而非「行为」类（对齐 M9 的三值重标），但必须承认：本版最大杠杆的**行为面**仍只有 AB fixture 兜底，强度低于代码类。"
    },
    {
      "finding_id": "M9 — SC-9/SC-14 标「代码」但实测对象是 SKILL.md 散文",
      "severity": "major",
      "root_cause": "SC 表只有「代码 / 行为」二分，而本 Spec 有大量断言的对象是 SKILL.md 文本 —— 它既非可执行代码也非 AI 行为，被硬塞进「代码」栏后会退化成文本存在性检查却顶着代码类的可信度。",
      "edit_target": "Success Criteria 的「验证面分层」blockquote 表格由两行改三行；SC-9 与 SC-14 逐条重标并重写。",
      "exact_change": "「验证面分层」表格替换为：\n\n```\n| 类 | 宿主 | 可机械断言 | 它锁不住什么 |\n|---|---|---|---|\n| **代码** | `state-scanner/tests/`（既有）+ `audit-engine/tests/`、`spec-drafter/tests/`（新建） | ✅ | — |\n| **文本** | 同上宿主，但断言对象是 SKILL.md / 模板字面量 | ✅（存在性） | **锁不住「写了但被跳过」** —— 与 §Why 自证的病同维度 |\n| **行为** | **定向 AB fixture**（rule6_note 第 2 条） | ⚠️ 只能由 eval 覆盖，**不冒充结构化测试** | — |\n```\n\nSC-9 拆为两条：\n`| **SC-9a** (**文本**) | 读两个 A.1 落点的 SKILL.md | 认领步骤块内含 `skip_if:` 且列出「`coordination.enabled` 显式 false」 | 无条件调用（无 skip_if）必红。**注**: 属存在性检查，锁不住运行期真跳过 |`\n`| **SC-9b** (行为) | 项目 config 显式写 `state_scanner.coordination.enabled=false` | A.1 **零调用** phase1_gate，不写 claim、不推远端 | 定向 fixture；「照跑不误」的臂应可分辨。**fixture 必须显式写 `false`，不得用缺键**（见另/CR-M8） |`\n\nSC-14 替换为：\n`| **SC-14** (代码) | 同 issue 三个方向共享同一 track_id，其中一个方向被放弃 | 该 track 的 active claim **仍然存在**（放弃单方向不 release，§5）；只有「整个 issue 不做」才 `abandoned` | 现状若按原版「放弃必 release」实现 ⇒ `release_claim_by_track` 连坐释放全部 ⇒ 必红。**原 SC-14「claim 状态为 abandoned」与既有 `test_release_abandoned_roundtrips` 重复且恒真，已删** |`",
      "why_it_closes": "三值分类让「文本类」不再冒充「代码类」，且在表里直接写明文本类锁不住什么 —— 这比把它降级更有用，因为它同时告诉下一版哪些地方还需要行为类补位。SC-9 拆分后两半各归其类；SC-14 从与既有测试重复的恒真断言，换成 M2 的反向对照（在现状必红），一举关闭 M9 的两半并承接 M2。",
      "risk_of_self_recurrence": "中等。新增「文本」类本身有个诱惑：它比「行为」类便宜得多，下一版容易把本该做 fixture 的条目都归进文本类然后宣称有覆盖 —— 即用一个更弱的绿换掉一个诚实的空。缓解是我在表里加了「它锁不住什么」这一列，使降级必须写明代价。但没有任何机制强制这一列被填 ⇒ 它是纪律不是机制，而本 Spec 的 §Why 恰恰在论证纪律不足以替代机制。这条自指的不足我不试图在本轮消解，只如实记下。"
    },
    {
      "finding_id": "M10 — SC-8/SC-10 把「CLI 可验证字段」与「消费层措辞」捆在一条断言里",
      "severity": "major",
      "root_cause": "一条 SC 承载两个不同验证面（CLI 只吐 JSON、不产文案），导致该 SC 在任一面达成时都可被声称通过，两面都无法被单独证伪。",
      "edit_target": "SC-8 与 SC-10 各拆为 `-a` / `-b` 两条。",
      "exact_change": "SC-8 替换为两条：\n`| **SC-8a** (代码，CLI 全链路) | 同 issue 他轨 claim 为 `done`/`abandoned`，命令带 `--include-terminal` | `linked_issue_overlap[]` 中**出现**该条目，且 `status` 字段原值回显 | `_TERMINAL` skip 的现状必红；**且只测库函数的实现在「参数没接到 CLI」时会绿** ⇒ 断言层必须是 subprocess，套用既有 `TestPhase1GateLinkedIssueCli` 模板 |`\n`| **SC-8b** (行为) | 同上 JSON 输入喂给消费面 | 渲染措辞按 `status` **分档**：`done`→「对方已完成」/ `abandoned`→「对方已放弃」/ `unknown`→「未能核实」 | 定向 fixture；「三档渲染成同一句」的臂应可分辨 |`\n\nSC-10 替换为两条：\n`| **SC-10a** (代码，CLI 全链路) | (i) fetch 降级；(ii) overlap 检测抛异常 | (i) `error == \"fetch_degraded\"`；(ii) `linked_issue_overlap_error` 非空且 `linked_issue_overlap == []` | 现状 (i) `error: null`、(ii) 与正常空集不可辨 ⇒ 均必红 |`\n`| **SC-10b** (行为) | 同上两种 JSON 喂给消费面 | 两者均渲染「**未能核实**」，**不得**渲染「无碰撞」 | 定向 fixture；渲染成「无碰撞」的臂应可分辨 |`\n\n并在 SC 表头加注：\n`> **拆分规则（R1/M10）**：任何一条 SC 若同时断言「CLI/库的可验证字段」与「消费层文案」，一律拆为 `-a`（代码）/ `-b`（行为）两条。CLI 只吐 JSON，不产文案 —— 捆在一起的断言两面都不可证伪。`",
      "why_it_closes": "两条 SC 的两个验证面各自获得独立的宿主、独立的红条件；并把拆分规则上升为表级约定，防止后续新增 SC 重犯。SC-8a 明确指向 QA 点名的既有 subprocess 先例，实现路径确定。",
      "risk_of_self_recurrence": "低，但有一个连带账要认：拆分使行为类 SC 从 2 条（SC-11/SC-12）增至 5 条（+SC-8b/9b/10b），而 rule6_note 第 2 条只承诺「上述三条各一个 eval」。⇒ 若不同步把 rule6_note 的 fixture 清单从 3 条扩到 5 条，就会出现「SC 表里有 5 条行为类断言、fixture 只建 3 个」的账面不平 —— 正是 M8/M9 那类「承诺与验证面不匹配」的形状。rule6_note 第 1 条的点名行为列表须同步补 (d) 终态按 status 分档渲染、(e) coordination.enabled=false 时零调用。这一步我在本条里点出但没有单独列 fix，属本轮 exact_change 的依赖项，A.2 必须一并落。"
    },
    {
      "finding_id": "另/CR-M8 — coordination.enabled 未在 DEFAULTS.json 注册，SC-9 极性两读",
      "severity": "major",
      "root_cause": "finding 的结论（SC-9 极性需钉死）对，但给的理由（未注册 ⇒ 两读）不成立：默认值有 SOT 也有锁定测试，真正的两读只存在于另一个消费面的缺键语义。",
      "edit_target": "§2.5 第一个 bullet 末尾追加；SC-9b 的 fixture 条件（已在 M9 的 exact_change 内写入，此处给依据段）。",
      "exact_change": "§2.5 第一个 bullet 末尾追加：\n\n```\n  > **默认值不是两读的（R1「另」/CR-M8 结论对、理由需换）**：`coordination.enabled` **默认 `true`（opt-out）**，SOT 是 `config-loader/SKILL.md:134-136`（不是 `DEFAULTS.json` —— 该 key 按设计不在其中），且 `state-scanner/tests/test_coordination_default_lockin.py:32` 已把「默认必须是 true」钉成测试。\n  >\n  > **真正的两读在别处**：`runtime_probe._resolve_enabled_when` 的**缺键 = off** 是通用探针契约、不随动（`config-loader/SKILL.md:137` 自陈的已知边界）⇒ 同一个缺键，config 消费面读作 `true`、探针消费面读作 `off`。\n  >\n  > ⇒ **SC-9b 的 fixture 必须显式写 `\"enabled\": false`，不得靠缺键**；靠缺键的 fixture 在两个消费面上会得到相反结论，测出来的东西取决于走了哪条路。\n```",
      "why_it_closes": "保住了 finding 成立的那一半（SC-9 极性必须钉死、fixture 不能靠缺键），换掉了不成立的那一半（未注册 ⇒ 默认不明），并把真正的歧义精确定位到 `runtime_probe` 的缺键语义 —— 这是可逐条实读复验的。符合任务对「结论对但理由错」的要求：不只改措辞，而是找到了成立的理由。",
      "risk_of_self_recurrence": "低。这段全部是对既有成文契约的引用（SKILL.md:134-137 + 锁定测试），我逐行实读过。残余风险：我断言「该 key 按设计不在 DEFAULTS.json」—— 我实读了 grep 结果（`coordination` 在 config-loader 的 json 内零命中）确认它不在，但「按设计」这三个字是我的推断，依据是 `test_coordination_default_lockin.py` 的 docstring 明写「没有 python 解析点 —— config-loader SOT」。若这其实是遗漏而非设计，我的措辞会把一个 bug 追认成契约。建议 exact_change 落笔时把「按设计」改为「实测不在，且锁定测试以 SKILL.md 为 SOT」。"
    },
    {
      "finding_id": "另/CR-M1 — SC-1「改名 id 不变」无分支限定，对 91% 无 issue 场景为假，与 SC-15 断言相反",
      "severity": "major",
      "root_cause": "SC-1 从「四个历史钉子」表继承而来，钉的是原始版的失败；重写引入了「无 issue 回落分支」后，该钉子的适用范围缩小了，但 SC-1 的文本没跟着收窄。",
      "edit_target": "SC-1 场景列加分支限定；§5「slug 改名」行同步；并与 M2 新增的「后起 Spec 走回落分支」规则对齐。",
      "exact_change": "SC-1 替换为：\n`| **SC-1** | 原始版 (spec-slug ⇒ 改名孤儿) | **有关联 issue 的主分支**：slug 改名前后 track-id **不变**（id 三段皆不含 slug） |`\n\n紧随其后加一行注：\n`> **分支限定是必须的**：无关联 issue 的回落分支 `<spec-slug>-<container_uuid>` **仍含 slug ⇒ 改名即改 id**，由 SC-15 覆盖（release 旧 + acquire 新两步）。两条 SC 的适用分支互斥、不矛盾；不写限定则 SC-1 与 SC-15 字面相反，且按 §1 实测统计，落在**回落分支**的才是多数（91% 无字段）。`\n\n§5「slug 改名」行的处置格首句改为：\n`**分支相关**：有关联 issue 的主分支 ⇒ track-id 不含 slug，改名不改 id，问题从源头消失；**无关联 issue 的回落分支仍含 slug** ⇒ 须走 release 旧 + acquire 新两步（`release_claim_by_track` 按 `(container, track_id)` 定位、不依赖 session，可照字面实现）。**M2 的「同 issue 后起 Spec 走回落分支」规则会扩大落在本分支的比例**，两条须一并读。`",
      "why_it_closes": "SC-1 与 SC-15 从字面相反变成适用分支互斥且显式互引；同时把「多数场景其实走回落分支」这个实测事实写进限定注里，防止读者按 SC-1 的表述以为改名问题已从源头消失。",
      "risk_of_self_recurrence": "有，且是我这轮引入的：M2 的 fix 主动把更多 Spec 推向回落分支，因此我在这里必须把两条 fix 显式互引（已写入 exact_change 末句）。若 A.2 只落其中一条，就会出现「SC-1 说改名不改 id」与「实际多数轨改名即改 id」并存 —— 即两条 fix 互相拆台的教科书形态。这也是我把互引句写进 §5 而不是只写进 SC 注的原因：两处都必须能看见对方。"
    },
    {
      "finding_id": "另/CR-M5 — §2.3 强制请裁与 AD10 冲突，Layer 2 无人值守未定义",
      "severity": "major",
      "root_cause": "§2.3 的「必须经 AskUserQuestion 请裁」假定了一个交互式人类在场，而 v2.0 的 Layer 2 是无人值守容器（AD10 明定人类参与点仅 1 个：S7_AWAITING_MERGE 签字）—— 该分支在本 Spec 里完全没有定义。",
      "edit_target": "§2.3 第三个 bullet「不硬阻断」之后新增一条无人值守分支。",
      "exact_change": "§2.3 末尾追加：\n\n```\n- **无人值守分支（Layer 2 / AD10，R1「另」/CR-M5）**：`AskUserQuestion` 通道在无人值守容器内不存在。overlap 非空且无人可问时：\n  1. **不自行放行**（Rule #10：「继续起草」是对已知碰撞的处置决定，属 owner 权限面）；\n  2. **也不硬阻断**（撞 §非目标与 advisory 契约）；\n  3. **处置 = 上报并挂起该轨**：把告警全文（含 §2.3「告警须含」六项）写入本 cycle handoff 的待复议段 + 审计报告入口段，状态标 `awaiting_owner`，由 Layer 1 主管 triage 决定另起 / 并轨 / 停。\n  > 这与 AD10 一致：它不新增人类参与点，而是把决定**路由**到既有的那一个（主管/owner 面），而不是让 Layer 2 自己裁。\n  > **判据（可机械）**：`AskUserQuestion` 不可用 ⇒ 走本分支；不得以「本次碰撞看起来无害」为由跳过上报（memory `no-self-exempt-gates`）。\n```",
      "why_it_closes": "把一个此前未定义的执行环境分支补成有明确处置的三步，且处置方向与 AD10、Rule #10、advisory 契约三者都不冲突（不放行、不阻断、上报路由）。判据给成「通道是否可用」这个可机械判定的条件，而不是「AI 觉得该不该问」。",
      "risk_of_self_recurrence": "有一处需要盯：第 3 步依赖 handoff 与审计报告两个写入面真的会被读。若无人读，`awaiting_owner` 就是又一个「有记录无路由」的状态标（与 M5/M6 的诊断键同族风险），而且这次是在一个**没有人类在场兜底**的环境里 —— 后果比交互式场景更重。本 Spec 无法单方面保证 Layer 1 会消费该标记（那属 aria-orchestrator 面）⇒ 诚实做法是把「Layer 1 侧的 `awaiting_owner` 消费接线」记为 follow-up 而非假装本 Spec 已覆盖。我建议 exact_change 落笔时在该 bullet 末尾再加一句 follow-up 声明。"
    },
    {
      "finding_id": "另/KM — session-handoff.md 与 coordination-ref-schema.md 未入 Impact 表",
      "severity": "major",
      "root_cause": "Impact 表按「我改了哪些代码文件」组织，漏了「哪些文档是我改动内容的 SOT」——而 track_id 的 SOT 恰在 standards 侧。",
      "edit_target": "Impact 表新增两行（已在 M1 的 exact_change 中一并给出，此处为独立确认与去重说明）。",
      "exact_change": "沿用 M1 fix 中新增的两行，不重复添加；并在 Impact 表末尾追加一句：\n\n`> **SOT 同步义务**：本表除「我改了哪些文件」外，还须列出**被改内容的规范 SOT**。本 Spec 触及两处：`derive_track_id` 与 track_id 形态的 SOT 在 `standards/conventions/session-handoff.md §2.3.1`（`track_id.py:64-66` 的 docstring 明指该处）；claim schema / status 语义的 SOT 在 `docs/coordination-ref-schema.md`（`claim_schema.py:16` 引用）。改 `A.1` phase 语义与 `unknown` 可见性 ⇒ 两处须同步，否则重演 `layer-l-integration.md` 那类「活文档断言与实现相反」（本 Spec 已在 Impact 中列它一行）。`",
      "why_it_closes": "补上 KM 点名的两份 SOT 文档，并把「Impact 表须含 SOT」升为表级义务，使下一版新增改动时不再需要靠人记得。附带把本 Spec 自己已经列了 `layer-l-integration.md` 这件事变成正面先例引用。",
      "risk_of_self_recurrence": "我对 `docs/coordination-ref-schema.md` 只有间接证据（`claim_schema.py:16` 的注释引用），**未实读该文件存在**。故 M1 的 exact_change 里写成条件形。若它不存在，落笔时应改为「记为不适用 + 说明 claim_schema.py:16 的引用是悬空的（顺带成为一条新 minor）」。这是本条唯一的软点，我不装作已核实。"
    },
    {
      "finding_id": "MINOR-BATCH — R1 minor 五条 + 两处措辞勘正合并处理",
      "severity": "minor",
      "root_cause": "五条 minor 分属不同章节但共性是「引用/契约与实读不符」或「表格缺列」，逐条开条目会稀释注意力，合并为一次通读式修正。",
      "edit_target": "分散落点：§5（命令）· §4（exit code）· §2.1（行号）· SC-13~19（补列）· Impact 表（layer-l-integration.md 行）· 头部（关联 Issue 字段）。",
      "exact_change": "1. **`release_gate.py --status abandoned` 缺 `--raw-track-id`**（BA）—— 已在 M2 的 exact_change 中给出完整命令，此处不重复。\n\n2. **§4 exit code 与 SC-18 不齐，「无远端」恒非 0**（CR/QA）：§4「消费面」bullet 的 exit code 句替换为：\n   `exit code: **0 = 无命中 / 0 = 有命中 / 0 = 无远端可扫**（三者都不是错误；后两者各自在 JSON 里带可辨键 `hits[]` / `degraded`）；**非 0 仅用于探针自身失败**（脚本异常、超时）。`\n   SC-18 场景列同步拆：`探针 fetch 超时/异常 ⇒ degraded + exit 非 0`；**新增** `SC-18b：仓库无任何 remote ⇒ degraded + exit **0**`（与 SC-17 的 0 靠 JSON 键区分，不靠 exit code）。\n\n3. **`layer-l-integration.md:45` 的 `update_heartbeat()` 全仓不存在**（TL，实测 `grep -rn update_heartbeat aria/ standards/` 仅命中该行本身）：Impact 表中 `layer-l-integration.md` 行的「变更」列追加：`+ 勘正 :45 —— `update_heartbeat()` 全仓不存在（真实函数名 `claim_lifecycle.heartbeat`），且「`phase-b-developer` mid-cycle 每 10min」这条调度从未落地（`constants.py:43-44` 自陈零生产调用点）⇒ 改为如实记「无生产 heartbeat 调度器；A.1 保护窗由 sweep 侧 phase 分档 TTL 给出，见本 Spec §2.2」`\n\n4. **S3 spike 的 `identity.py:244` 行号误差**（KM）：§2.1 `container_uuid` 行的依据格中，把对 `get_container_id()` 的引用改为 `identity.py:191`（函数定义）+ `:218-222`（label 优先、uuid 兜底的实际取值逻辑），删去 `:244`。\n\n5. **SC-13~19 缺「怎么会红」列**（QA）：该子表加第 4 列「怎么会红」，逐条填。空缺不得留 `—`；若某条填不出可红条件，说明它不是 SC，应移出或改写。\n\n6. **本 proposal 自身缺「关联 Issue」字段**（KM/CR dogfood）—— 已在 C3 的 exact_change 中给出头部插入行，此处不重复。",
      "why_it_closes": "五条 minor 各自有明确落点与最终文本；其中第 2、3、5 条实际关闭的是三个不同的「假绿/恒红」隐患（无远端恒非 0 = 恒红；活文档断言相反 = 误导实现者；缺「怎么会红」列 = 该表最新最少被审的内容无红条件）。第 5 条附带给出一条判据（填不出红条件的不是 SC），把补列从抄写变成筛子。",
      "risk_of_self_recurrence": "第 5 条有真实的自复发风险：补「怎么会红」列时，最省事的写法是给每条填一句同义反复（如「不满足期望则红」），那样列填满了而信息量为零 —— 即 memory `feedback_false_green_dual_is_permanent_red` 的另一面。我已把判据句写进 exact_change 作为筛子，但判据本身仍是纪律。第 3 条的勘正文本里我引用了本 Spec §2.2 的新方案，形成文档间的正向依赖：若 §2.2 在后续轮次又被改，这行勘正会指向一个不存在的方案 —— 属新引入的耦合，A.2 需登记。"
    }
  ],
  "deferred": [
    "【heartbeat by-track 变体 —— 原 §2.2 处置 / D4 / SC-5~7 / Impact 的 claim_lifecycle.py 行】不修，改为移出本 Spec。理由不是「不重要」而是「在本 Spec 内没有消费面」：实读 collision.py:177-234，linked_issue_overlaps 对 heartbeat / TTL / stale 三词 grep 命中 0，overlap 可见性纯由 status 决定 ⇒ 改 heartbeat 匹配键对本 Spec 的唯一消费面零影响。在本 Spec 引入它 = 新增第二个零生产调用点的函数变体（heartbeat() 现有生产调用点 0，constants.py:43-44 自陈），正是 memory feedback_paper_fix_antipattern + feedback_completion_signals_vs_runtime_invocation 的形状。C2 问的「谁调何时调」在本 Spec 范围内没有成立答案，硬编一个调用者只会把问题推到实现期。若要治「sweep 对长 Phase B 会话的误杀」，另开 Spec 并在那里回答调度问题。",
    "【D6「不引入跨容器 release」的定案】不改，只换依据。C5 证伪的是它的**理由**（全称否定为假：gc.sweep_stale_active 是跨容器写路径且每周期在跑），不是它的**结论**。定向跨容器 release 仍是权限面变更，本 Spec 不引入 —— 硬改结论会在一份已经不收敛过三轮的 Spec 里新开一个权限面。",
    "【spec-drafter/SKILL.md:110 与 :134 把 Level 2 输出位置写成 standards/openspec/changes/{feature}/proposal.md，违反 CLAUDE.md 不可协商规则 #5】本轮不修。它是我在核验 M4 时顺带实读发现的**既有独立缺陷**，与本 Spec 的机制无因果关系。顺手修会扩大 blast radius 且这条改动零审计记录（本 Spec 已因「新表面未经审」被 R1 点名）。正确处置：开独立 issue，走自己的 triage。已在 new_surfaces 中登记以免丢失。",
    "【owner-container 与 claim container 段的口径统一】维持原 Spec 的 follow-up 判定，不动。它牵动 handoff frontmatter 规范（standards 变更），且 S6 已论证入口覆盖率才是杠杆。R1 未对此提出新证据。",
    "【CR 处方 (ii)「track-id 形态 × 生命周期动词影响矩阵」】不在本轮以矩阵形式落笔。三个动词（放弃 / 改名 / 终态）的下游已分别由 M2（放弃）、另/CR-M1（改名）、C4（终态）三条 fix 逐条覆盖，且三处已互相引用。再加一张矩阵是同一内容的第四份表述，会新增一个需要与三处保持同步的面 —— 而本 Spec 三轮不收敛的形状恰恰是「同一事实分散在多处、改一处漏其余」。CR 处方 (i)「Spec 事实断言逐条实读清单」则**已采纳**：C1/C4/C5/C6/M1/M7/另-CR-M8 七条 fix 全部按「实读出处 + 现值 + 处置」三段式落笔。"
  ],
  "new_surfaces": [
    "§0「跨 Spec 契约」整节 —— 全新章节，零审计记录。其中「姊妹 Spec SC-8 在加 keyword-only 默认形参后仍绿」是我单方面对**另一份 Spec 的验收标准**做的语义裁定，姊妹 Spec 那边没有任何人认过。",
    "§2.2 的「phase 分档 sweep TTL」整套方案 —— 新常量 A1_SWEEP_TTL=72h、gc.sweep_stale_active 按 record.phase 分档。这是本轮最大的新承重设计，改的是一个**每周期都在生产中跑**的 GC 路径（phase-d-closer D.2b），零审计记录。72h 这个数只有「等于 S1 实测事故窗上界」一个依据。",
    "「--phase A.1」字面量成为承重谓词 —— 一个自由字符串（phase1_gate.py:1190 无 choices）被提升为跨两个文件（SKILL.md 模板 / gc.py 谓词）的匹配键。SC-22 做了双向钉合，但第三处写入者（如 Layer 2 orchestrator 直调 phase1_gate）不在 SC 视野内。",
    "§1.2 的 canonical token 格式（inline-code span 提取器）+ check 作用域限 openspec/changes/ —— 全新格式规格，对存量语料 0/11 匹配，靠作用域裁剪而非「不追溯」承诺来避免恒黄。作用域这一条是承重条款，零审计。",
    "§4 的「URL 回落层」及其与 §1.2 的作用域分离 —— 我为了让 M3 在自己的 motivating case 上成立，开了一条 C3 明令禁止的宽容解析口子。两者靠一句散文约定隔离，**无任何机械回声**：实现者把回落层复用到 --linked-issue 构造上不会有测试变红。这是本轮我最不放心的新表面。",
    "linked_issue_overlap_error 这个 additive 诊断键（M5 + M6 共用）—— 新键，且两条 fix 一起成或一起败；其消费路由只有行为类 fixture 兜底，强度低于代码类。",
    "§2.3 的无人值守分支（awaiting_owner 上报路由）—— 全新分支，零审计。第 3 步依赖 Layer 1 侧真的消费该标记，而那属 aria-orchestrator 面，本 Spec 无法单方面保证；建议同时记 follow-up。",
    "M2 新增的「同 issue 后起 Spec 须走回落分支另起 track_id」规则 —— 它主动把更多轨推向含 slug 的回落分支，从而扩大了 SC-15（改名两步）的暴露面。是我用「拆台 A」换「拆台 B 暴露面变大」的知情取舍。",
    "SC 表新增的「文本」类（M9 三值重标）—— 它比行为类便宜得多，构成把本该做 fixture 的条目降级归类的诱惑。我加了「它锁不住什么」一列作为代价披露，但那是纪律不是机制。",
    "两个 A.1 落点的 allowed-tools 扩容 —— phase-a-planner 与 spec-drafter 此前正文各自零 shell 命令（实读），本 Spec 让两个随 plugin 分发、user-invocable 的 skill **首次**获得任意 shell 执行能力。这是安全/能力面变化，需 owner 明确知情，不是笔误修正。",
    "spec-drafter/SKILL.md:110 与 :134 的 Rule #5 违规（Level 2 输出位置写成 standards/openspec/changes/）—— 本轮实读发现、判为 deferred，此处登记以免在 deferred 列表之外丢失。本 Spec 会编辑该文件的相邻段落，属「路过但未修」的已知缺陷。",
    "docs/coordination-ref-schema.md 是否存在**未经实读**（仅见 claim_schema.py:16 的引用）。M1 与另/KM 两条 fix 的 Impact 行按条件形写，若该文件不存在，这条引用本身是一条新 minor。"
  ]
}