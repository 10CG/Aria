# Post-Spec Audit Report — US-012 (2026-04-12T0800Z)

> **Audit Type**: post_spec (3 轮收敛)
> **Checkpoint**: 2 Spec + 1 User Story 审计
> **Target**:
>   - `docs/requirements/user-stories/US-012.md`
>   - `openspec/changes/state-scanner-multi-remote-parity/proposal.md`
>   - `openspec/changes/phase-c-integrator-push-enforcement/proposal.md` + `tasks.md`
> **Agent Team**: Tech Lead, Knowledge Manager, QA Engineer, Code Reviewer (4 agents)
> **Rounds**: 3
> **Final Vote**: 4/4 PASS (收敛 at Round 3)

## Round History

| Round | Vote 统计 | 关键事件 |
|-------|-----------|---------|
| 1 | 4/4 REVISE | 25+ MAJOR findings identified |
| 2 | 4/4 PASS | 全部 MAJOR 修复, 残留 12 MINOR |
| 3 | 4/4 PASS | 跨 agent 共识 MINOR 已修复, 其余保持 (收敛) |

## Round 1 MAJOR Findings (全部已修)

### Cross-Spec
1. **helper 交付形式错误** — "Bash 函数调用" → "SKILL.md 指令块 + JSON schema 契约"
2. **合并顺序循环依赖** — 明确为 helper (T1) → Spec A → integrator (T2/T3)
3. **配置命名空间不一致** — 统一为顶层 `multi_remote.*` 共享 + skill 级 `null` 继承
4. **Schema 重复定义** — helper SKILL.md 为 canonical single source of truth

### Spec A (state-scanner)
5. AC 非二元判定 (全绿/N commits/reason 字段缺失/diverged 无覆盖) → 全部改为二元可测
6. 向后兼容 Consumer Inventory 缺失 → 新增 `submodule_drift` + `aria-dashboard` 消费点清单
7. verify_mode 触发协议未定义 → CLI > config > 默认优先级表
8. helper fallback schema 一致性缺 AC → 明确 "fallback 必须产出 canonical schema"
9. local_refs staleness 边界 → 新增 `local_refs_stale` + `no_local_tracking_ref`

### Spec B (phase-c-integrator + helper)
10. verify 总超时 30s 数学矛盾 → 改为 `max_per_remote_seconds = 34s` (4×5s + 14s)
11. Race condition AC 矛盾 → helper 返事实, integrator 决阻断
12. "Everything up-to-date" 边界 → 明确 `success = post_remote_head == pre_local_head` 严格判定
13. 指数退避实现语言 → Python (非 Bash, 跨平台可靠)
14. 子模块 per-remote matrix gating 缺失 (本次事故根因) → 笛卡尔积守卫算法
15. C.2.5 vs branch-manager 边界 → 明确表格 (branch-manager 仅 origin, C.2.5 所有)
16. fail_on_partial_push + read_only_remotes 优先级 → read_only > fail_on_partial_push
17. 跨平台 timeout → timeout/gtimeout/Python wrapper 三级降级
18. helper shallow clone / detached HEAD / 未 fetch refs 守卫 → 全部补充
19. eval 编号统一映射 → eval-10/11/hlp-1~4/int-1

### US-012
20. Forgejo Issue 引用 → "待创建 (归档前需补)"
21. Related 字段 → US-008
22. Layer 3 验收标准归属 → 删除实现细节, 引用 Spec B

## Round 2 MINOR Findings (跨 agent 共识部分已修)

**已修**:
- enforced_remotes 不存在 remote 语义不对称 → D11 (Spec A soft-fail) + D13 (Spec B hard-fail) 消歧
- 子模块 detached HEAD 分支推断 → D14 + AC 新增 "警告不阻断"
- README skill 数量表述 → internal 5→6, user-facing 保持 30

**Round 3 保持 (接受为实现期待办)**:
- `_timeout_wrapper.py` 未在目录结构声明 (T1.1 实施时补)
- eval-int-1 AB fixture/mock 需求 (T2.5 实施时定)
- `max_retries=3` 命名歧义 (实际 4 attempts, 实施时加注释)
- fallback 检测用相对路径 (CLAUDE_PLUGIN_ROOT 建议)
- "shell vs skill 层调用" 措辞技术化
- Forgejo Issue 归档前检测 D8 (D.2 前补)
- Spec A AC 条数接近 Level 3 (保持 Level 2 可接受)

## 最终产出

| 文件 | Status |
|------|--------|
| `docs/requirements/user-stories/US-012.md` | Approved (in_progress) |
| `openspec/changes/state-scanner-multi-remote-parity/proposal.md` | Approved |
| `openspec/changes/phase-c-integrator-push-enforcement/proposal.md` | Approved |
| `openspec/changes/phase-c-integrator-push-enforcement/tasks.md` | Approved |

## 下一步

Phase A.3 (Agent 分配) → Phase B (开发)。
开发顺序: T1 helper → Spec A 集成 → T2 integrator → T3 文档。
