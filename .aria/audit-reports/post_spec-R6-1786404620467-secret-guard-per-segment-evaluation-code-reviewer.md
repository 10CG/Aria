---
checkpoint: post_spec
round: R6
review_target: v8 (commit 4923380)
spec: secret-guard-per-segment-evaluation
timestamp: 1786404620467
date: 2026-08-10
seat: code-reviewer
seat_id: CR6
lens: 规范符合性 + 事实核验
verdict: REVISE
critical_count: 0
major_count: 3
minor_count: 4
facts_verified: 64
facts_falsified: 4
ready_for_a2: no
---

# R6 · code-reviewer

## Phase 1 规范合规性 — PASS

对照 `standards/openspec/templates/proposal-minimal.md`: 六节齐备 + Aria 扩展节齐备。
SC 编号 1–18 连续无缺 (9 拆 a/b), Task 1.1–1.11 连续 + 1.3b / 1.10a 插入,
转出 1–11 完整 (6、7 划线注销, Task 1.9 开 9 张票, 数目吻合)。
Rule #1 / #5 / #6 / #10 均满足。两条提交符合 Conventional Commits。
全文无带圈数字 / 希腊字母。

## Major

- **CR6-M1** — §Impact「行为变更 (穷尽声明): **只有一类**」被本 spec 自己的 §What.5 +
  SC-7 证伪, 漏报的那一类方向是 **fail-open**。命中的是 `risky_patterns` 里唯一那条
  `.*` pattern。`:319` 还把转出 1 归进「**非本版引入**」的「维持现状泄漏」—— 跨段
  fail-open 恰恰是**本版引入**的。**与 SFH-C1 独立同现。**
  自建逐段模拟器扫 305 条语料: `2→0` 翻转 **0 条** ⇒ SC-3 / SC-11 / 全语料对该方向
  零鉴别力, 整份 spec 只有 SC-7 那 2 条手写 fixture 覆盖它。
- **CR6-M2** — **W-1 / W-2 与 tech-lead m-1..m-7 在仓内不存在任何记录**, 而 v8 全部改动
  挂在它们身上, `:91` 还写着「见 **W-1** 引用的复核报告」。
  ```
  grep -oE '\bW-[0-9]+\b' notes → 只有 W-3 W-4
  grep -rln 'W-1' .aria/audit-reports/ → 无
  ```
  对照 R1–R5 各席门牌号逐个抽查**都真实存在**。直接撞上本 spec 写在转出抬头的纪律
  「不得只引用未提交的审计报告」, 并使「换人执笔」这条 owner 处置失去可审计性。
  另有命名撞车: 同一份 proposal 里「R5 code-reviewer m-5」与「tech-lead m-5」是两个东西。
  > 本席独立复现 `:91` 那条无源可查的断言, **实质成立** (`KEEP=DETECT` / `THIRD=MISS`)。
  > 这是留痕问题, 不是内容问题。
  > **落盘补记**: 已按 owner 2026-08-10 裁定补档为 `post_spec-R5.5-*`, 编号改带席位前缀。
- **CR6-M3** — 「141 条 pattern 里 **16 条**含 `\b`」是全 spec 唯一**没有分配给计数器**的
  抽取型数字, 而它恰好是三种口径给三个答案的那一类:
  ```
  bash 求值数组后逐元素判 *'\b'*   → 16   ← 正确
  对 pattern 字面量做文本扫描       → 16   (前提: 正确处理 :507 双引号内的 \")
  grep -c '\\b' 于 402..656         → 17   ← 多算 :650 纯注释行
  本席第一版 python 扫描器          → 15   ← 在 :507 的 \" 处截断
  ```
  三个数各差 1, `:507` 正是那条双引号 `ssh` pattern —— 与 `141→139` 两次踩坑**同一根源**。
  该 16 最终核定**正确**。

## Minor

- **CR6-m1** — SC-14 反事实表 `(^|\n)` 行只把错误施加于关键字正则, 未施加于同精度要求的
  `exec`/`time` 正则 ⇒ B-1 那格应为 `false`, 该行应是 **3 条红**。
  而 Task 1.1(a)(b) 明写两行同精度、照抄同一段位置交替 ⇒ 实现者写错时**必然两处一起错**,
  「只错一处」才是不自然的那支。表里选的是不自然那支。**与 QA6-M1 独立同现。**
  > 本席的表另给出「仅命令位置」行 A-5 = `true`, 与 TL6-F1 的 `false` 不一致 ——
  > 根因是行标签「仅命令位置 (v5 的规范文本)」有两种读法 (v5 的位置清单 vs 当前清单去掉
  > 词边界), 两席各按一种读法计算。**该二义本身是 finding。**
- **CR6-m2** — SC-6 反事实 `(^|\n)` 条的分母 `1/16` 未随 v8 的 16→17 同步 (裸 `^` 那支已
  按 17 项更新)。实测 17 项下红的仍是 1 条, 属分母陈旧非分子错。
- **CR6-m3** — SC-6「关键字型 5 条」只写死 `until`/`select`, `for`/`while`/`if` 仍只有
  关键字名 —— 正是 `:472` 承诺要消除的那个形态。**与 TL6-F4 独立同现。**
- **CR6-m4** — §Impact 摘要句「aria 子模块 3 交付文件 + 5 版本文件 + 主仓 gitlink」
  在主仓侧只列 gitlink, 与紧随其后的 14 点表不自洽。14 点表逐行核过**行号全部正确**。

## 我核过且成立的事实断言 (60/64)

**行号/路径**: `:663` has_filter 全局开关 · `:691` `Command was:` · `:416` nomad put ·
`:302`/`:311` 两处 ack 且**确在** filter detection (`:318`) 之前 · `:658`/`:662` 匹配循环 ·
`:394` `| wc -l` · Filter detection–Risky read patterns 区间实测 `318..401` ·
13 处判据行号逐个吻合 · §6 那条抽取命令**实跑产出恰 13**。

**数字** (全部自写扫描器复算): 141 · 139+2 · `[^|]` 81 · `[^|]*` 79 · `[^|]+` 7 ·
兼有 5 · `.*` 1 · 「只数单引号得 79/77/7」精确复现 · 12 条 pattern 把 `\|` 编码进正则 ·
按 `|` 切恰 4 条真泄漏 2→0 · `[[:space:]]+` 10 / `*` 12 / any 13 · credit 含 `\b` 2 处 ·
语料 305 · 含换行 6 · 含顶层边界 65 = 49 拦 + 16 放 · 16 中 15 纯管道 + 1 真边界 ·
纯换行边界 5 = 4 拦 + 1 放 · 测试 **366/366 全绿 (实跑)** · zsh 6 条 → 366/360 ·
SC-17 的重复用例真在 `:641`/`:673` 字节级重复且全语料仅此一处重名 ·
语料 6 条 exec/time 落命令位置 = 2 真 + 4 `timeout`。

**外部事实** (bash 5.2.15 + glibc 2.36): `type -t time`=keyword / `exec`=builtin ·
`(?:…)` rc=2 · `\b`/`\s`/`\w` 支持 · `[[ $'a\nb' =~ ^b ]]` 不匹配 ·
`[[ =~ ]]` 编译失败**静默走 else、零 stderr、`set -euo pipefail` 下不中止** ·
`bash -n` 合法性 7 条全对, 省掉 `)` 的 case 语法错。

**内联复现命令**: 转出 9 (含它点名的**自证伪裸写法确实自证伪**) · 转出 10 (假值, 未回显
真值) · 转出 11 三组 · 转出 2/3/4/5/8 全部 exit=0 · Why 段三条 · SC-1 五形态改前 exit=0。

**可满足性**: 自建逐段模拟器跑真 canonical —— **SC-3 的 49 条一条不翻, 0 flips**;
与 SC-7 无死结 (两条 SC-7 fixture 不在语料内); 全语料唯一翻转 = KNOWN-LIMIT 的 0→2。

**环境/ship 面**: SOT = 1.65.5 · cache `1.63.0` 与 `1.65.5` 并存 · `1.65.5` 副本与
canonical **cmp 字节相同** · `.claude/scripts/` 确于主仓 `5fab5b8` 移除 ·
v8 声称的四处勘正全部落地, 残留出现均在版本演进表的历史叙述里, 属正确用法。

## owner 定案异议 (备复议, 不阻塞)

转出 11 的 `!?`「不收」—— 四条不采理由**没有一条是技术性的**, 而被推迟的这一半是
**v7 自己刚引入的**。`:399` 自己划的线是 `do`/`then`/`else`/`elif` 同类问题「在 v6 就
已存在」, 而 `!` 是 v7 加的。「一起立案一起治」把一个**本轮新造的**误降级面与四个**存量**
面捆在一起推迟, 与 memory `feedback_freeze_task_must_coland_with_volatile_state_phase`
方向相反。**不主张改判** (方向确实 fail-safe, 第三次改同一正则的风险论据实在), 但建议
转出 11 正文显式标出「`!` 属本轮新引入 / 四个 `do` 系属存量」, 否则读票的人会以为五处同龄。
