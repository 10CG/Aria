---
checkpoint: post_spec
mode: convergence
rounds: 6
converged: null
oscillation: false
overridden_by_user: false
degraded: false
drift_terminated: false
drift_check_skipped: false
drift_warning: false
is_refocus: false
verdict: PASS
timestamp: 2026-09-15T11:10:00.000Z
context: openspec/changes/rule6-description-change-trigger-eval-lane/proposal.md
agents: [qa-engineer]
---

# post_spec Round 6 — qa-engineer 席

> 被审 SHA `c3a5903` (v7, owner 2026-09-15 裁定收窄范围 + max_rounds 5→7)。方法: 全程只读仓库, 未编辑任何仓库内文件。**未读**主控自检脚本之外的任何其他席 R6 报告 (独立性要求)。临时文件全部落 `/tmp/claude-1000/-home-dev-Aria/e91a9d6a-05ff-4b26-af79-8d903c21a292/scratchpad/r6-qa/`。
>
> 方法与 R4/R5 不同之处: 本轮**不复用**主控 `selfcheck_v7.py` 的代码, 而是自己从 proposal.md 重新切片提取 (`build_v7.py`)、自己重新实现 10 条 SC 判定 (`land_and_sc.py`, 独立的 `slice_section`/`core_lines`/正则), 跑通后再与主控脚本的构造/判定逻辑逐点对照复核, 确认两者独立收敛到同一结论后, 再补跑主控脚本未覆盖的反事实。

## R5 对账

本席 R5 报告 2 条 Findings, 逐条判定如下:

1. **[major] SOT §2 自主模式条款无 SC 锁** — **closed**。v7 新增 SC-12, 字面要求 SOT 含核心句的那一行同时含 `unattended == true` / `不做 description 改动` / `放弃整个任务` / `写明` 四项, 且 CLAUDE.md 含指向句、SOT 含 GLM 未验证句各恰 1 次。已实测反事实「整句删除 SOT 自主条款」→ SC-12 正确转红 (`缺: ['unattended == true', '不做 description 改动', '放弃整个任务', '写明']`), 其余 9 条 SC 不受牵连仍绿, 精确定位, 未过度触发。

2. **[minor] SOT §3 边界注写回旧形不被抓** — **closed**。v7 的 SC-10 在原有「`grep -cF "description hunk 不走本节"` = 1」之外, 新增「且该行同时含「照跑场景 1」与「另须跑场景 4b」」的字面要求 (proposal.md SC-10 正文明确写"R5 qa 席反事实实测"作为依据)。已实测反事实「边界注写回 v3/v4 旧形」→ SC-10 正确转红 (`边界注行缺"照跑场景1"或"另须跑场景4b"`)。

**对账结果: closed 2 / partially 0 / open 0。**

## Findings

无。本轮对主控自检脚本的构造忠实性做了独立复核 (未发现失真), 并针对任务指定的 9 类反事实 + 2 类自行补充的规则性反事实 (D 编号注入、SC-8 标题重复回归) 全部实跑, 均被现有 SC 正确捕获, 无遗留 open/partially 项, 也未发现新的 critical/major, 亦未发现任何"不改就不能进 Phase B"的 minor (探索性反事实找到的文档深度类缺口均判非阻塞, 理由见「观察」)。

## 观察

**1. 主控自检脚本 (`selfcheck_v7.py`) 忠实性复核结论**: 独立重写构造与判定逻辑后, 基线 (正确落地) 与全部可比反事实的判定结果与主控脚本完全一致, 未发现主控脚本的构造方式与 v7 转录范围 (T3「只把 YAML 模板、紧随其后的合规条款与「两套编号不同轴」一句写进 SOT §4.1」) 或 SC 字面有出入。具体核对点: (a) D4 转录范围——独立提取的 `D4_BULLETS` 恰为 3 条 bullet, 不含 `OQ-7`/「无机械 enforcement」/「authoring 路径与过渡期」段, 与 T3 字面一致; (b) D2/D3 整节搬入手册 §4b 的方式——两份脚本都是整段落字符串拼接, 未做任何改写, 与 D2 标题本身「(写进手册 §场景 4b, SOT §2 引用)」的字面指示一致; (c) 三处 D1 旧句在真实文件里各恰命中 1 次的前提, 已用独立 `python3` 命令对仓库现状重新验证 (非沿用脚本内的 assert); (d) 十条 SC 的判定逻辑逐条按 proposal.md 原文重新实现 (未查看主控脚本的实现代码后再誊写), 结果吻合。**结论: 主控自检可信, 不构成本轮判断的单一信息源风险。**

**2. 主控自检脚本本身的反事实覆盖面小于本任务要求, 但补测后未发现新缺口**: `selfcheck_v7.py` 只跑了 13 个反事实 (a-m), 未覆盖任务明确要求的「禁令改"跳过这一部分"」「删"写明"半句」「混入 T4 / Why 第 1 条」。本席补跑后 (`perturb_v7.py` 测试 1-11、`perturb_v7c.py` 测试 D 编号注入), 全部被 SC-12 / SC-11 正确捕获, 无一漏检。另单独验证了「"写明"替换为同义词"说清"」这一更隐蔽的变体 (非删除, 是同义改写), SC-12 同样正确转红——确认 SC-12 是字面子串匹配, 对同义替换零容忍, 与 T1「逐字」落地要求的设计意图一致 (不是缺陷)。

**3. 探索性反事实发现的文档深度缺口 (均判非阻塞, 计入观察不入 Findings)**: 针对"v7 新引入内容有没有配 SC"做了 8 项额外探测 (`perturb_v7b.py`), 发现以下内容删除后 10 条 SC 全绿、无一转红:
   - 手册 D2「同批参照臂」整段定义 bullet (含"为什么必须有它"的 run_eval.py 异常处理理由)。**非阻塞理由**: "同批参照臂"这一操作要求本身在 D3 前置表第 5 行「同批参照臂与负控」与 D2「作废」「fail 的后果」两个 bullet 里都有独立提及 (三处冗余), 丢的只是最详细的那一份理由陈述, 执行者仍能从冗余处知道"必须跑参照臂", 只是少了 why。
   - 手册 D2「只承诺已验证的两类破坏」整段——纯限局文档陈述 (与 R5 判过的 D6 GLM 未验证句同类)。
   - 手册 D2「套件」整段 (含 should/should-not 各 10、近似误触须覆盖相邻任务、"新套件须经 owner 审阅后才能作门")——该段落的核心合规要求("owner 审阅"闸)在 SOT §4.1 D4 bullet 里独立复述一次 (冗余); 且该段服务的受众主要是 D5.2 跟进 issue 覆盖的其余 41 个 skill (本 cycle T4 本身因 OQ-3 未裁而 deferred), 不影响本 cycle 可执行任务。
   - SOT §4.1「`scenario4b` 所用套件未经 owner 审阅 ⇒ 不合规」条款——同上, 手册侧有冗余表述。
   - SOT §4.1「`description_changed: yes` 而 scenario1/4b 为空 ⇒ 不合规」条款——该条款是 SOT §2 核心句「`description` 变动另须跑场景 4b」这条**无条件**义务的派生推论, 核心义务本身由 SC-1/9/10/12 独立锁定, 派生推论丢失不影响义务本身是否成立, 只影响审阅 rule6_note 时的查表便利性。
   - SOT §6 第三条里的「(未穷举)」括注——纯 hedge 措辞。
   - 手册 4a 标题文字写回旧描述 (丢失"可选"字样)——D5.1 原文「4a 是可选优化」这句未被要求转录进手册 (T2 未列), 且即使 4a 未标"可选", SOT §2 核心句本身对"任何 description 变动"无条件要求跑 4b, 不依赖 4a 标题措辞, 不存在"经 4a 优化后可以跳过 4b"的字面漏洞。

   以上 8 项按任务给的 major 门槛 ("不改就会让规则执行者做错事、或让某条 SC/判据不可执行、或事实不成立") 逐项核验: 均有冗余覆盖或纯属限局/hedge/未纳入转录范围的说明性文字, 不满足门槛, 判非阻塞。

**4. 一处未被任何 SC 覆盖、但确认不影响可执行性的 T2 子项**: T2 要求「§固定测试集 vs 临时测试 表加 trigger 行」, 但十二条 SC 里没有任何一条检查这张表是否真的加了 `ab-suite/trigger/` 行 (SC-2 检查的是 D3 前置表, 不是这张表)。核实: 该表要传达的规则 ("`ab-suite/trigger/*.json` 归 `ab-suite/` 版本化规则管") 在 D2「套件」bullet 里已有「**沿用 `ab-suite/` 版本化规则**」的独立陈述, 也确认 T4 本身的判据 SC-4 (本轮任务范围外) 直接检查真实文件是否升版, 不依赖这张表。判非阻塞, 建议未来若有空档可补一条轻量 SC, 但不是本轮收敛的必要条件。

**5. 任务项 3 —「自主模式禁令收窄后的可证伪性」分析**: 除 SC-12 的字面存在性检查外, 该禁令句在本 Spec 范围内**没有**、也**不可能有**其他机械可证伪手段——因为本 Spec 的自主模式交付物本身就只是"在 SOT 里写一句话", 没有任何配套代码改动 (D1 原文明示: 编排器/runner 的实际感知与处置能力全部推给 D5.6 跟进 spec, 本 Spec 不动 aria-orchestrator 代码)。这不是一个可以靠"加一条更强的 SC"解决的缺口, 因为没有运行时机制可供断言。据此判断: **这是可接受的范围收窄, 不构成"不改就不能进 Phase B"的缺口**, 理由三点: (1) owner 已在 2026-09-15 的 R5 降级裁定里明确要求把自主模式细节收窄为"只留一句禁令", 这正是该裁定的预期产物, 不是执行走样; (2) 该禁令句一旦落地即刻生效 (不依赖 D5.6 是否已落地——D5.6 落地前后唯一的差异是失败后编排器的处置质量, 即是否被打成 `container_crash` 并无脑重试, 这一差异已作为已知代价写进 Impact 段, 有对应跟进 issue D5.7 追踪); (3) 与本项目其余"靠 AI 读规则自律 + 事后审计"而非"逐条机械网关"的既有模式 (如 Rule #6 判据表第四行"拿不准照跑"本身也不是机械可判的) 一致, 不是这条规则独有的短板。**残余的、值得记录但不升级为 finding 的一点**: D5.6 issue 的"硬前提"目前只是 proposal 正文里的一句话, SC-6 (本轮范围外) 只验证 issue 存在, 不验证 issue 正文是否带上这句硬前提——这是 T6 执行时的措辞忠实度风险, 不是本 Spec 设计缺陷, 与 D5.2/D5.3 两张issue 同等对待 (SC-6 对三张 issue 一视同仁地只查存在性), 不构成对 D5.6 的差异化质疑。

**6. 边界场景确认 (非缺陷, 记录留痕)**: 测试了「SOT 核心句在"两者不互相替代。"之后手动插入换行 (纯排版, 不删字)」这一场景——SC-1/SC-10 保持绿 (它们锚定的子串都在换行前的半句), SC-12 正确转红 (它要求的 4 个子串现在跨了行)。这不是 SC-12 的缺陷: T1 要求"逐字"落地, 而 proposal.md 源文件里这句新句本身没有内嵌换行, 真正的逐字复制粘贴不会引入这种换行; 会引入换行的操作 (手动重新排版) 本身已经偏离"逐字", SC-12 的转红是对该偏离的正确捕获, 而不是误报。

## Verdict

PASS — 0 critical / 0 major / 0 minor。

## Vote

PASS

## SC 实测记录

**方法**: 临时目录 `/tmp/claude-1000/-home-dev-Aria/e91a9d6a-05ff-4b26-af79-8d903c21a292/scratchpad/r6-qa/`。`build_v7.py` 独立从 proposal.md (v7, HEAD `c3a5903`) 逐字提取三处 D1 新旧句、SOT §3 边界注、D2 判据整段、D3 前置表整段、D4 yaml 块 + 紧随其后三条 bullet (断言其恰 3 条且不含 `OQ-7`/「无机械 enforcement」/「authoring 路径与过渡期」)、D6 第三条局限, 每处提取都带独立断言 (与 proposal.md 原文 count==1 核对), 不依赖/复用主控脚本的切片代码。`land_and_sc.py` 独立构造三份"正确落地"模拟副本并独立重写全部 10 条 SC 判定函数 (`sc1`..`sc12`, 各自带失败原因回显)。`perturb_v7.py`/`perturb_v7b.py`/`perturb_v7c.py` 做反事实。

### 0. 正确落地 → SC-1/2/3/5/7/8/9/10/11/12 基线全绿 (独立实现, 结果与主控脚本一致)

```
SC-1: PASS   SC-2: PASS   SC-3: PASS   SC-5: PASS   SC-7: PASS
SC-8: PASS   SC-9: PASS   SC-10: PASS  SC-11: PASS  SC-12: PASS
```

(SC-2 已额外用 shell 直接对基线目录 `test -e` 全部 11 个机读实证路径, 全部存在, 独立于 python 脚本核实。SC-4/SC-6 按任务范围排除, 未纳入本轮测试——两者分别依赖真实 `ab-suite` 文件 diff 与 Forgejo issue 存在性, 不是纯文本落地可模拟的对象, 这是任务给定的 SC 清单本身排除它们的原因, 非本席遗漏。)

### 1. 任务指定反事实 (9 类, 逐类落地 11 次具体扰动)

| # | 操作 | 目标 SC | 结果 | 判读 |
|---|---|---|---|---|
| 1 | 整句删除 SOT §2 自主运行时条款 | SC-12 | **翻红** (缺 4 项子串) | 符合预期, R5 major 已 close |
| 2 | 删 CLAUDE.md「; 自主运行时的处置见 SOT §2」 | SC-12 | **翻红** (指向句次数≠1) | 符合预期 |
| 3 | 「放弃整个任务」改写为「跳过这一部分」 | SC-12 | **翻红** (缺"放弃整个任务") | 符合预期, 精确命中该字面替换 |
| 4 | 删「并在最终消息里写明是哪个 skill、为什么要改」半句 | SC-12 | **翻红** (缺"写明") | 符合预期 |
| 4b | 「写明」同义改写为「说清」(非删除) | SC-12 | **翻红** (缺"写明") | 确认字面匹配对同义词零容忍, 设计意图内 |
| 5 | 删「fail 的后果」整行 | SC-9 | **翻红** (无行同时含两串) | 符合预期 |
| 6 | 删「作废」整行 | SC-9 | **翻红** (缺"连续2轮", 因两者同行) | 符合预期 |
| 7a | 手册 4b 混入「OQ-7」 | SC-11 | **翻红** (命中 OQ-7) | 符合预期 |
| 7b | 手册 4b 混入「T4」 | SC-11 | **翻红** (命中 T4) | 符合预期, 任务点名的三种混入之一, 主控脚本未测 |
| 7c | 手册 4b 混入「Why 第 1 条」 | SC-11 | **翻红** (命中 Why 第) | 符合预期, 主控脚本未测 |
| 7d (补) | 手册 4b 混入「D2 所述」 | SC-11 | **翻红** (命中 D2) | 补全 SC-11 四个正则分支里唯一未测的 `\bD\d\b` |
| 8a | 删前置表第 1 行 | SC-2 | **翻红** (行号序列缺 1) | 符合预期 |
| 8b | 删前置表第 4 行 ([配置推导] 行) | SC-2 | **翻红** (行号序列缺 4) | 补测: 专门验证"恰一行[配置推导]"这一子判据锚定的正是第 4 行 |
| 8c | 删前置表第 6 行 | SC-2 | **翻红** (行号序列缺 6) | 符合预期 |
| 9 | SOT §3 边界注写回 v3/v4 旧形 | SC-10 | **翻红** (缺"照跑场景1"或"另须跑场景4b") | 符合预期, R5 minor 已 close |

### 2. 补充反事实 (回归测试 + 覆盖 SC-8/SC-9 更细粒度)

| # | 操作 | 结果 | 判读 |
|---|---|---|---|
| 10 | 「### 场景 4b」标题重复一次 (制造空 4b + 影子 4b) | SC-8 直接翻红, 且连带 SC-2/5/9/10 (因 section 提取只会截到第一个"4b"标题为止) | SC-8 存在的必要性得到验证——若无 SC-8, 这类结构性错误会通过其余 SC 的 section 提取失效间接暴露, 但 SC-8 提供了更直接、更早的信号 |
| 11 | 「连续 2 轮作废」改写为「多次作废」 | SC-9 翻红 (缺"连续2轮") | 符合预期, 对措辞替换零容忍 |
| 12 | 核心句处手动插入换行 (纯排版, 不删字) | SC-1/SC-10 仍绿, SC-12 翻红 | 见观察 6, 非缺陷, 是对"逐字"落地的正确要求 |

### 3. 探索性反事实 (排查"新引入内容有没有配 SC", 8 项, 结果见观察 3, 全部"无 SC 转红")

删「同批参照臂」定义段 / 删「为什么必须有它」理由句 / 删「只承诺已验证的两类破坏」段 / 删「套件」整段 / 删 SOT §4.1「套件未审阅⇒不合规」条款 / 删 SOT §4.1「description_changed:yes 不合规」条款 / 删 SOT §6「(未穷举)」括注 / 手册 4a 标题写回旧描述——8 项均无 SC 转红, 逐项非阻塞理由见观察 3。

### 4. 前置事实

```
CLAUDE.md / SOT / 手册现状仍是改前原句
(grep -crF "只验证触发面没被改坏" 三份文件均为 0, 独立验证), 本 Spec 仍是 Draft,
本轮反事实全部基于临时副本模拟, 未改动仓库任何文件。
```

### 5. 落盘脚本 (仅供复核, 未改动仓库)

`build_v7.py`(独立提取+断言) / `land_and_sc.py`(独立构造+10条SC独立实现) / `perturb_v7.py`(任务指定反事实1-11) / `perturb_v7b.py`(探索性反事实A-H) / `perturb_v7c.py`(D编号注入+换行边界), 均在 `/tmp/claude-1000/-home-dev-Aria/e91a9d6a-05ff-4b26-af79-8d903c21a292/scratchpad/r6-qa/`。中间产物 `extracted.pkl`/`docs_baseline.pkl` 同目录。
