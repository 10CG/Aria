---
checkpoint: post_spec
round: 1
converged: false
overridden_by_user: false
incomplete: false
---

# post_spec R1 — linked-issue-normalization

> **席位**: 5/5 (config `teams.post_spec` 全员) · **5/5 REVISE** · `scope_ok` 5/5 true
> **counts (各席)**: TL 1C/7M/3m · BA 1C/2M/2m · QA 2C/3M/1m · CR 2C/4M/4m · KM 0C/2M/2m
> **timestamp**: 1785700000000 · 审计对象: 主仓 `c6aa29a`

## 判定

**REVISE。** 去重后 **2 个 critical**, 两条都是**抽出/新增时引入**的, 不在被继承的三轮审计资产里。

---

## C1 — SC-4 的例子本身是坏的, 与规则 3 + SC-6 三者互斥
**4 席独立命中** (TL / BA / QA / CR) + **主控实读复验**

- **位置**: `proposal.md:107` (SC-4) vs `:57` (归一规则 3) vs `:110` (SC-6)
- **问题**: 规则 3 明写「`repo_basename` 空 ⇒ 不可解析」。`#007` 与 `#7` 的 `left` **就是空** ⇒ 不可解析 ⇒ 按规则 4 退回**原串精确比较** ⇒ `'#007' != '#7'` ⇒ **不命中**。而 SC-4 要求**命中**; SC-6 又把**同形态**的 `#5` / `10CG/#7` 列为不可解析范例。
- **QA 的实跑结论最刺**: 「用忠实实现验证 —— **连 100% 合规实现都无法通过 SC-4 字面读法**, 唯一能通过的是 SC-2 存在的目的就是要杀死的 `number_only` 退化实现。」
- **性质**: `#007 ≡ #7` 这条**决策本身是对的** (D1 的 int 比较正确), 坏的是**举例**没带 repo 段。
- **修法**: SC-4 例子改为 `aria-plugin#007` × `aria-plugin#7`; 并在规则 2 的自证例句同步 (它也用了裸 `#007`)。

## C2 — rule6_note 的 substitute 声称经实测不成立
**QA + CR + KM 三席实测**

- **位置**: `proposal.md:94` (rule6_note「SC-1~6 均在现状代码上可红」)
- **实测** (三席各自直接调用生产 `linked_issue_overlaps` 跑 baseline): 真正 baseline-red 的只有 **SC-1 / SC-3 / SC-4**; **SC-2 / SC-5 / SC-6 在现状代码上已经是绿的** (它们是负控 / 已知限 / 回落语义, 恰与裸 `!=` 巧合重合)。而**真正 baseline-red 的 SC-5b 反而不在枚举里** (rule6_note 说「1~6」, Impact 表 `:132` 说「1~8」)。
- **性质加重**: 我在同一份文档 `:96` 刚写下 owner 裁定要求的「**substitute 须实证而非声称**」, 然后在 `:94` 声称了一个未实证的范围。**这与本 Spec §Why 点名的「表面信号全绿」是同一个病。**
- **修法**: rule6_note 的枚举改为**实测所得**的 baseline-failing 集合 (SC-1 / SC-3 / SC-4 / SC-5b), 并注明其余为负控 (负控恒绿是正确的, 但不能算进 substitute 的证据面)。

---

## Major (去重后 8 条)

| # | 簇 | 席位 | 要点 |
|---|---|------|------|
| **M1** | **D2 的「回显双方原始串」是孤儿 MUST** | TL / CR | `_claim_to_dict` / `_gate_result_to_dict` 均无 `linked_issue` 字段, 而非目标 / Impact / SC 三面都把实现路径关死; **母 Spec 原文的指针「(见 §1 消费面)」抽出时被删而本 Spec 无 §1 消费面** ⇒ D2 唯一的代价缓解无处落地 |
| **M2** | **S5 追加的两条论据在本 Spec 范围内不成立** | TL / BA / CR | (a) `derive_track_id` **不消费 `linked_issue`** (全仓 grep 零命中), 且 track-id 派生是本 Spec 的**显式非目标** ⇒「与 derive_track_id 对齐」的论证在本 Spec 内落空; (b)「不是理论风险」的举证取自 **handoff prose 总体**, 与 SC-5 降级所用总体不同 —— **正是 S4 指控 R2 的同一错误**。⚠️ **S5 的结论 (加译码) 可能仍对, 但理由要重写** |
| **M3** | `number` 谓词欠定 + 抽出时措辞漂移 | TL / BA / CR | 母 Spec 写「纯数字」, 抽出漂成更宽的「能解析为非负整数」⇒ `int('1_0')==10` / `int(' 122')==122` / `int('+7')==7`。`int()` 派与 `isdigit()` 派对 `#+7` / `#1_000` / `# 122` **实测三对全分叉** |
| **M4** | §69 与 D4 沿用**已被 spike S4 推翻**的 R2/M2 数字 | TL | 24/10 实为 19/802, 真实输入总体为 0。**母 Spec 在同一 commit 里警告了这条, 本 Spec 原样吸收** |
| **M5** | `yielded` 归属未随抽出带走 + §接口面披露不完整 | TL | 「行为变化**仅为**原漏报现能报」不准确 —— ref 现有 **3 条 2026-07-14 的 `yielded`** 会因归一变成跨格式可达 |
| **M6** | 「今天就在因格式失配而漏报」**不可观测且被 ref 全枚举证伪** | TL | 两族的 issue 号**交集为空**; 唯一多条同号的 `#110` 共享 track_id ⇒ **ship 后现有数据零 delta**。⚠️ 缺陷是真的, 但「今天就在漏报」这句**当前无实例**, 应改为「一旦两族撞同号即漏报」 |
| **M7** | SC-8 不可机械证伪 | BA / QA / CR | 「Phase B 行为除多报外无差异」是散文级; 唯一操作化路径是既有 6 个测试, 而它们匹配侧**全部用逐字相同字符串**, 对新比较逻辑的 near-miss 判别力为零 |
| **M8** | 「审计资产继承」表**选择性引用** | KM / TL / CR | 略去 R2 的 MAJOR「取样口径不同」; 三族表**漏掉 prose 最高频的第四族** (`Aria #124` 形, 139 篇中 111 篇); 且继承表把本轮 scope 收窄到「抽出过程」, 而 S5 译码 + SC-5b 是**三轮之后新增的零审计承重条款** |

## Minor (7 条)

SC-7 的「语料全集」全文未定义, 且 D1 引的 18 元语料**从未入库、现已不存在**; 数学上任何「算 key 再比较」的实现都自动满足三性质, 判别力窄 (QA/CR) · 归一只译 `.`/`_`, **其他分隔符替代落在 SC-5/SC-5b 二分之间**, 既不归一也未记为已知限 (QA) · SC-5 的「被显式记为已知限」`:71`(a) 是循环定义, 第二种读法直接踩红 SC-8 (CR) · 空白剥离规定了 5 个位置中的 2 个 (CR/BA) · 抽出丢失母 §0 两条约束 (「不得用模糊匹配」禁令 + 步骤 4 的三条不可解析枚举) (TL) · 三处「母 Spec 已 spike-first 挂起」**在写下它的同一 commit 里即被证伪** (母 Spec 已改 Draft v2); ref 计数陈旧 (9→11, 13→16) (TL/KM) · `:39` 的 `phase1_gate.py` 实际在 `scripts/` 非 `lib/` (行号本身准确) (CR) · Impact 表未列 `linked_issue_overlaps` docstring 与 `SKILL.md:176` 的文案同步 (KM)

---

## 经实测确认**成立**的部分 (下轮免重复)

- **四处代码引用逐字准确**: `collision.py:217` / `phase1_gate.py:1232` / `phase-b-developer/SKILL.md:88-93` / `track_id.py:71`; **live 路径成立** (TL/KM)
- **D1 的等价关系加入 S5 译码后重新穷举, 依然零违例** (BA 独立复跑); **D3 的论域划分依然干净** (BA)
- **S5 译码与 `/` 切分顺序无关** (译码既不产生也不消耗 `/`, 两序同结果); **步骤 1 与步骤 3 的剥空白不重复** (CR 实测, 留证免重跑)
- **SC-3 确系全表唯一能区分「org 不参与匹配」两种设计的用例** (QA 逐条核对全部 SC 后确认)
- **两 Spec 职责边界无重叠** —— 问题是时序不是归属 (TL)
- **D1 的 int 比较裁决正确** —— C1 的问题出在**举例**而非该决策 (QA)
- **9 项指定核实点中 7 项精确匹配** (KM)

---

## 收敛提示

**两条 critical 都是「抽出/新增时引入」的, 不在被继承的三轮资产里** —— 这本身验证了母 Spec 里那句「本 Spec 的 post_spec 应聚焦**抽出过程**是否引入偏差」是对的判断, 但也说明抽出过程**确实引入了偏差**, 不能因「已审三轮」而免审。

**M2 值得单独注意**: S5 的**结论**(加 `./_ → -` 译码) 可能仍然正确, 但它在本 Spec 里给出的**两条理由都不成立**。修 fix 时不能只改措辞 —— 要么找到本 Spec 范围内成立的理由, 要么承认它属母 Spec 范围而移回去。

**AI 不预判裁决。**
