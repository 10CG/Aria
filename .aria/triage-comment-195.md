## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.69.1` |
| Current | `1.70.0` (aria master 已到 v1.71.0 `985e629`) |
| Gap | behind |

版本差 2 个 release, 但 v1.70.0 / v1.71.0 对 `collectors/handoff_multibranch.py` 的 `_list_handoff_files` / `_read_file_content` / `_get_file_commit_date` 零改动 (`git diff 0545f86 origin/master -- <file>` 为空), 缺陷仍在。

### Code Path

issue 引用的 `skills/state-scanner/scripts/collectors/handoff_multibranch.py` 在本仓路径为 `aria/skills/state-scanner/scripts/collectors/handoff_multibranch.py`, 三处定位实读一致:

| 位置 | 实况 |
|---|---|
| `_list_handoff_files` `:240-288` | `git ls-tree -r --name-only origin/<branch> -- docs/handoff` 递归; `basename = Path(path).name` 丢弃目录段; docstring 写「callers compose the full git-object path」 |
| `_read_file_content` `:301` | `ref = f"{_REMOTE}/{branch}:{_HANDOFF_TREE_PATH}/{filename}"` 固定前缀, 无法还原子目录 |
| `_get_file_commit_date` `:321` | **同一固定前缀** (`docs/handoff/<filename>`) — issue 未点名, 但它就是「updated_at 取到 mv 提交日期」的直接来源 |
| `:637-658` | git show 失败 → `soft_error` + 追加 `status: legacy` 假 track (`owner_container: unknown`, `updated_at` = 上面的 fallback) |

`docs/handoff/archive/2026-05-09-session-end.md` 是报告方仓的路径, 本仓不存在 (本仓 `docs/handoff/` 平铺, 因此 Aria 自身 scan 不触发)。

### Git History

step 4 collector 本次未返回数据; 人工核: 上述三个函数自 v1.69.1 起无提交触碰, 无 fix 候选。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | 仅主 worktree (master) |

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/2`

- **case-1** (子目录文件从未在顶层存在): `handoff_multibranch_git_show_failed … origin/master:docs/handoff/2026-05-09-session-end.md (other, rc=128)`; `tracks[]` 多出 `legacy:master:2026-05-09-session-end.md` (`legacy_count=1`); `updated_at=''` (fallback 同路径查不到)。**match**
- **case-2** (按 issue 步骤: 顶层 commit 2026-05-09 → `git mv` 进 `archive/` commit 2026-08-15): 同样假 legacy track, 且 `updated_at = 2026-08-15T12:00:00+00:00` = 归档那次 mv 提交的日期 — 「双重失真」逐字成立。**match**

### Verdict Rationale

两条 hermetic case 在当前代码上全部命中, 与 issue 描述 (错误文案、假 legacy track、mv 日期污染) 一致, 且根因是**契约与实现错配**而非环境差异 (三处调用方都按固定前缀拼路径, 与 `-r` 递归枚举不自洽)。判 `major`: 不是纯告警噪声 — 失真数据进 `tracks_multibranch` 看板并带误导性 `updated_at`, 任何把归档放子目录的采用方仓都 100% 触发 (每次 scan exit 10)。判 `next-cycle` 而非 hotfix: 数据只污染 legacy 分区 (collision 判定排除 `unknown`), 不阻断主流程。

**修法倾向** (与 issue 一致选 A + 附加建议): `_list_handoff_files` 保留 ls-tree 原样相对路径 (相对 `docs/handoff/`), 三个调用方直接用它拼 ref; `filename` 字段另派生 basename (`track_id` 的 legacy 形式 `legacy:<branch>:<filename>` 需决定用 basename 还是相对路径, 建议相对路径防同名碰撞); **git show 失败不再降级为 legacy track** — 只报 soft_error, 不进 `tracks[]` (「读不到」≠「老格式」)。B/C 两案会静默丢掉子目录里的交接历史, 不推荐。适合与其他 state-scanner collector 小修合一个 Level 1/2 批。

---

*Generated by `/issue-triage` v1.70.0 — Ref: 10CG/Aria#195*
