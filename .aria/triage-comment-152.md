## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.53.0` (目录) / `1.47.0` (VERSION 文件) |
| Current | `1.55.0` |
| Gap | behind |

缺陷在当前 `1.55.0` 依然存在,版本差不影响结论。关于「VERSION 文件 1.47.0」的附带发现见下方 case-4 —— **症状真,归因错**。

### Code Path

`step3_code` = `skipped`(issue 正文未以 `path:line` 格式引用文件),人工核对 `aria/hooks/secret-guard.sh`:

```
463:  '^[[:space:]]*printenv([[:space:]]+\||[[:space:]]*$)'
464:  '^[[:space:]]*env([[:space:]]+\||[[:space:]]*$)'
465:  '^[[:space:]]*/bin/printenv'
466:  '^[[:space:]]*/usr/bin/printenv'
```

四条 pattern 全部以 `^` 锚定整条命令开头 —— 与 issue 根因描述**一致**。同类 `^` 锚定还有 `:438-439`(`. .env` / `source .env`)。

### Git History

`step4_history` = `skipped`(无 cited paths)。未查到修复候选提交。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | 仅 main worktree `/home/dev/Aria` @ `master` |

**无人认领。**

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/4`

> hook 只解析命令字符串、从不执行它,故以下用例不产生任何 secret 输出(Rule #7 合规)。

| case | 命令字符串 | 结果 | match |
|---|---|---|---|
| case-1 | `env` | `exit=2` BLOCKED,匹配 `^[[:space:]]*env(…)` | ✅ |
| case-2 | `cd /tmp && env` | **`exit=0` 放行 —— 逃逸复现** | ✅ |
| case-3 | `true; printenv` | **`exit=0` 放行 —— 逃逸复现** | ✅ |
| case-4 | 核对附带发现「VERSION 文件内容 1.47.0 vs plugin.json 1.53.0」 | 症状真,归因错 | ❌ |

**case-4 详情**:VERSION 文件头部(`> **版本**:` 行)与 plugin.json **始终一致**(四个缓存安装 1.46.5 / 1.50.1 / 1.50.2 / 1.53.0 逐一核对通过)。你看到的 `1.47.0` 来自 `aria-report/SKILL.md:59` 的
`cat "$CLAUDE_PLUGIN_ROOT/VERSION" | grep -m1 '^[0-9]'` —— 它命中 `VERSION` 第 40 行、一个围栏代码块内的冻结示例串 `1.47.0`。这是 **`aria-report` 的抽取缺陷,不是 VERSION 文件漂移**,且同时污染了 #154 / #156 的环境信息段。建议单独立 issue。

### Verdict Rationale

主诉完全复现且根因描述准确:`^` 锚定使分隔符(`;` `&&` `||` `|`)之后的 `env` / `printenv` 全部逃逸,凭据可明文进入 transcript 与 prompt 缓存。这不是对抗性构造,是日常拼接写法(issue 步骤 3 的真实触发形态)。

严重度 `major` 而非 `critical`:它是 **secret-guard 拦截能力的缺口**(Rule #7 的执行载体被绕过),但不阻断主流程、不造成数据损坏。

`next-cycle` 而非 `hotfix`:与 #154 同文件、同 hook,应**并入同一修复 cycle**。#154 是 `critical`/`hotfix`,#152 顺带修复,共用一次回归测试与一次发版。

**修复注意**:
1. 不要只改 `env` / `printenv` 两条 —— `:438-439` 的 `source .env` / `. .env` 同样 `^` 锚定,需一并排查;
2. 分隔符集合至少覆盖 `;` `&&` `||` `|` `(` `` ` `` `$(`,并考虑 `\n`(多行命令);
3. 放宽锚定会提高误报率,需补足反向用例(如 `echo "env"` / `grep env file` 不应拦截)。

---

*Generated by `/issue-triage` v1.55.0 — Ref: 10CG/Aria#152*
