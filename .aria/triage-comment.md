## Triage Report

**Verdict**: `confirmed` | **Severity**: `critical` | **Recommended Action**: `hotfix`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.33.0` |
| Current | `1.33.0` |
| Gap | same |

报告版本与当前版本相同 (v1.33.0) — 非过期报告,bug 在当前 HEAD (`aria` submodule `44b3e00`) 中仍存在。

### Code Path

`aria/hooks/secret-guard.sh:111` (issue 中以 submodule 相对路径 `hooks/secret-guard.sh` 引用) 确认存在且与描述一致:

```bash
111: readarray -t _sg_fields < <(jq -r '(.tool_name | type), (.tool_name // ""), (.tool_input.command // ""), (.tool_input.file_path // "")' 2>/dev/null <<<"$input")
...
118: if [[ "$tool_type" != "string" ]]; then
```

- line 111 `readarray -t` 管道**无** `tr -d '\r'` — 与报告根因一致
- line 118 type 校验 `[[ "$tool_type" != "string" ]]` — CR 污染下 `"string\r" != "string"` 判真 → fail-closed exit 2
- *(triage.py Step 3 因 issue 路径相对 aria-plugin repo root、扫描发生在 Aria 主仓,误报 "file not found" —— 实际文件存在于 `aria/hooks/`,已人工核对。)*

### Git History

Step 4 skipped (collector 未运行)。手工核对:secret-guard.sh 为 v1.33.0 新增 hook,无修复 CRLF 的后续 commit。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | none (仅 main `/home/dev/Aria` @ master `f36950d`) |

**无任何 in-flight 修复** — 可安全起新 hotfix cycle,无重复工作风险。

### Reproduction

**Mode**: `auto` | **Hit rate**: `1/3` (case-1 复现;case-2 修复验证;case-3 Linux 对照)

| Case | Input | Actual | Match |
|------|-------|--------|-------|
| case-1 | 模拟 Windows native jq 的 CRLF 输出 → `readarray -t` → type 校验 | **BLOCKED** — `$tool_type` 含尾部 CR,`!= "string"` 成立,触发 fail-closed | ✅ 复现 |
| case-2 | 应用建议修复 `jq … \| tr -d '\r'` 后重跑同一 CRLF 输入 | allowed — CR 被剥除,type 校验通过 | 修复有效 |
| case-3 | Linux 原生 jq (LF-only) 跑真实 line 111 管道 | allowed — 输出干净 LF,无 CR | Windows-only 佐证 |

> 注: Linux 环境无法直接调用 Windows native jq,故 case-1 以**确定性模拟** CRLF 输出复现 issue 描述的精确机制 (jq 输出带 `\r` + `readarray -t` 仅 strip `\n`)。报告的技术分析与代码路径完全吻合,且修复 (case-2) 与平台隔离 (case-3) 均验证通过。

### Verdict Rationale

确认 (confirmed): (1) 代码路径 `secret-guard.sh:111` 确实缺 `tr -d '\r'`,line 118 type 校验在 CR 污染下必然 fail-closed (Step 3 + 代码核对);(2) case-1 确定性复现 issue 描述的精确机制;(3) 报告版本 = 当前版本,非过期。Severity critical: PreToolUse `*` matcher hook fail-closed 会阻断 Windows session 全部工具 (Bash/Read/Edit/Write),仅 Grep/Glob 幸免,且 `/plugin update` + `/reload-plugins` 无法恢复 — 等同 brick 整个 session。Recommended hotfix: 修复为单行 (`| tr -d '\r'`),属 P0 阻断应即时修。建议同时按 #61/#131 同源经验,系统性排查 aria-plugin 所有 `jq | readarray` / `jq | read` / `VAR=$(jq …)` 模式统一加 `tr -d '\r'`,并补 CRLF 输入的 cross-platform 回归测试。

---

*Generated by `/issue-triage` v1.33.0 — Ref: 10CG/Aria#132*
*Step 6 performed by AI (auto mode) — 2026-05-30*
