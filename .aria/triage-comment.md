## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.61.0` |
| Current | `1.62.0` |
| Gap | behind (1 minor) |

issue 针对 v1.61.0 有意留下的债;当前 v1.62.0 (并发轨主 spec ship) **未触及本 issue 的实现点** — `gate_result` yaml-only 分支与 `carry_forward` 提取路径在 v1.62.0 逐行核验仍为 issue 描述的形态,非 outdated report。

### Code Path

- `lib/spec_complete.py::_build_d_payload` — **:1115 精确命中** ✓
- `lib/spec_complete.py::gate_result` — def 在 **:1272** (issue 引 ":1298 起" 实为 yaml-only 分支起点 :1298-1327,轻微漂移,语义一致) ✓
- `collectors/openspec.py` `carry_forward_inventory` 只读 tasks.md — 声称成立,但**行号漂移**: 实际在 **:265-279** (`tasks_file = d / "tasks.md"` @ :266),issue 引 `:244` 现指向 design_deferred 逻辑
- 归档 spec `openspec/archive/2026-07-19-state-scanner-openspec-collector-false-green/proposal.md` 存在 ✓

### Git History

No recent commits matched on cited files (无 in-flight 修复痕迹)。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | main worktree only (master @ 60b33cc) |

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/3`

1. **case-1** (gate blanket 兜底): yaml-only fixture 含残留 (pending task + deferred 项) → `gate_result` 输出 `verdict=warn` + `unverified_claims=['archive-safety-net-source-unsupported']` + 非 None `d_payload` — 与声称一致;`:1298-1327` 仅 `.is_file()` 探测,从不打开 yaml。**match=true**
2. **case-2** (无法区分真干净): yaml-only fixture 全 done 无 deferred → 输出与 case-1 **逐字段相同** — blanket 噪声实锤。**match=true**
3. **case-3** (carry_forward 展示假绿): 合成 root 中 yaml 含 `[TODO: ...]` `[deferred: ...]` (均匹配 `_CARRY_FORWARD_RE`) 贡献 **0**;tasks.md 正控组同款标注**命中** — regex 正常,缺口精确在「yaml 从未被打开」(`openspec.py:266`)。**match=true**

### Verdict Rationale

三个声称在当前 v1.62.0 全部机械复现 (3/3),且正控组把根因收窄到「detailed-tasks.yaml 这一 task-planner path B 正常产出格式整体未被解析」— 同一病根同时导致归档 gate 对该类 spec 精度归零 (真干净/有残留输出无差别 → blanket tracker 噪声) 与展示侧 `carry_forward_inventory` 恒 0 假绿,属功能错误 → `major`。方向上 gate 是 fail-toward-warn (保守不失明),无 hotfix 紧迫性;无 in-flight 重复工作 → `next-cycle` 起独立 OpenSpec cycle。两处引用行号漂移 (`:244`→实际 `:265-279`;`:1298`→def :1272) 已核正,起 Spec 时按本报告钉位。

---

*Generated by `/issue-triage` v1.62.0 — Ref: 10CG/aria-plugin#113*
