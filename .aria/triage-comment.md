## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

三个缺陷全部在当前 aria-plugin **v1.59.1** 复现（report 版本 1.56.1 → 现 1.59.1，gap=behind，但缺陷均未修）。**无 in-flight 修复分支**。与 #110 / 主 spec `state-scanner-stale-refs-false-parity` **同类不同根**（详见 Rationale）。

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.56.1` (Aether env) |
| Current | `1.59.1` |
| Gap | behind（3 缺陷在 1.59.1 仍复现，非 outdated report）|

### Code Path

在 **`aria/` 子模块** 内逐一实地核验（triage.py Step 3 相对主仓根解析报 `exists:false` 是路径假象，非代码不存在）：

- `collectors/openspec.py:164-170` — 缺陷 1 early-return，`archive_dir` 于 :166 算出但早 return 前从未使用 ✅ 存在
- `collectors/openspec.py:244` — 缺陷 2 `tasks_file = d / "tasks.md"`，仅聚合 `tasks.md`，无 `detailed-tasks.yaml` 分支 ✅ 存在
- `collectors/_status.py:199` — 缺陷 3 done 家族 token `("done","complete")` + `_has_token` 词边界 `\bcomplete\b` ✅ 存在

### Git History

Step 4 (history) collector = `skipped`（exit 10 主因）。人工核 `openspec.py` 近 5 次提交（`39e1c21` runtime-probe / `8fe52da` #134 archive-gate / `8d000eb` #50 / …）**无一处理 #166**。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none（`tx-g2-g3-g4-collectors` / `hotfix-v1.22.1` 是 substring 误命中 "collect" 的旧分支，与本 issue 无关）|
| Worktrees | 仅主 worktree |

→ **无 in-flight 修复。**

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/3`

- **case-1（缺陷 1 [Major]）** — 构造 `openspec/` 存在、`changes/` 缺失、`archive/2026-01-01-foo/` 有 1 归档的树 → `configured=False, changes.total=0, archive.total=0`（磁盘实有 1），`errors=[]` **完全静默**，输出与非-OpenSpec 干净项目无异。✅ 复现
- **case-2（缺陷 2 [Major]）** — 静态核验 `openspec.py:244` 仅读 `tasks.md`，用 `detailed-tasks.yaml`（task-planner path B 合法产出）的项目 `d_payload` 恒 null 且无 soft_error → 归档安全网（openspec-archive Step 7 #95）恒空转且不可见。✅ 复现（源侧；端到端空转需与 Step 7 联动，本 triage 只核 collector 源）
- **case-3（缺陷 3 [Minor]）** — `_normalize_status('Completed'/'COMPLETED'/'Status: Completed')` 实调 → 全部 `unknown`（`Complete`/`Done` 正常）。根因 #101 substring-shadow 修复的词边界对合法词形变化 `Completed` 过度收紧。✅ 复现

### Verdict Rationale

三缺陷共享同一失效模式——**沉默的假绿**（一个在该失败时不失败的检查），根因清晰、修复路径明确（缺陷 1: layout-drift soft_error + 正交扫 `archive/`；缺陷 2: 支持 `detailed-tasks.yaml` 数据源或至少 soft_error；缺陷 3: done 家族加 `"completed"` token，已验证不重开 #101）。

**与 #110 / 主 spec 的关系**：#110（`sync_status`/`overall_parity` 在陈旧 remote-tracking ref 下撒谎）与主 spec `state-scanner-stale-refs-false-parity` 针对的是 **git 远程同步 collector**；#166 针对 **OpenSpec collector**——两者是「假绿」这一 **bug 类别** 的姊妹，但**代码路径、根因完全不相交**，非 duplicate。可作为**独立 next-cycle**（Level 2/3），不宜并入主 spec（避免绑定不相关的高风险 F3′ 大改）。

判为 `major` 而非 `critical`：非数据损坏、不阻断 state-scanner 主流程，但静默误报整个 OpenSpec 维度、已污染下游 handoff 8 周，且**任何走完十步循环的 aria 消费方归档完最后一个 spec 后迟早必踩**——影响面广，故 `next-cycle` 而非 `backlog`。

---

*Generated by `/issue-triage` v1.59.1 — Ref: 10CG/Aria#166*
