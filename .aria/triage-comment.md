## Triage Report

**Verdict**: `partial-repro` | **Severity**: `minor` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.36.0` |
| Current | `1.42.0` |
| Gap | behind (+6 minor) |

### Code Path(v1.42.0 实测)

- `aria/templates/session-handoff.md:1-50` — **模板已含** 5 字段 frontmatter + 完整派生规则(`track_id` 规范化函数 / `owner_container` = git email local-part + `~/.aria/container-id`,缺失回退 hostname / `phase` enum)。**v1.22.x+ 即存在,早于报告版本 1.36.0**,且方案比 issue 建议的"hostname 默认"更丰富。
- `phase-d-closer/references/execution-steps.md` D.3 — action 含 "fill template",但 **grep frontmatter == 0**:无写后校验子步。
- `hooks/handoff-location-guard.sh:26` — 注释明示 "does NOT inspect file content or frontmatter"(只管路径)。
- `collectors/handoff_multibranch.py` — legacy fallback 正常(`owner_container='unknown'` / `track_id='legacy:<branch>:<filename>'`),但**零告警** — 退化完全静默。

### In-flight

Remote PRs / local branches / worktrees: **none** — 起 cycle 安全。

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/3`

- **case-1** (match=false): 模板注入机制已存在(instruction 层)→ issue 标题主张"不自动注入"不准确。
- **case-2** (match=true): D.3 无写后 frontmatter 校验,AI 跳过模板/手写 handoff 时静默缺失。
- **case-3** (match=true): collector 对缺 frontmatter 静默 legacy fallback,无 soft_error — 与 SilkNode 2026-05-31 实地事故一致(两份并发 handoff 全落 legacy,看板失去识别能力且**无人知道**)。

### Verdict Rationale

注入机制(模板 + 派生规则)v1.22.x+ 已存在,issue 的方案请求(1)(2)(3)(4)实质已实现或更优 → 不复现"缺注入"。但 SilkNode 事故揭示的**真缺口是 enforcement**:三层(D.3 / hook / collector)全部不校验、不告警,ad-hoc handoff 静默丢失多终端识别能力。修复方向 = **enforcement 而非注入**:(a) D.3 写后 frontmatter 自校验子步(grep 验证 5 字段);(b) handoff collector 对 latest pointer 目标缺 frontmatter 时发 soft warning(仅 latest,避免历史 legacy 刷屏)。Severity `minor`(协调可见性退化,机制存在,手动补齐可恢复 — SilkNode 实测已证)。

---

*Generated by `/issue-triage` v1.42.0 — Ref: 10CG/Aria#137*
*Step 6 performed by AI (auto mode) — 2026-06-10*
