## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.37.0` |
| Current | `1.38.0` |
| Gap | behind (+1 minor) |

issue 报告于 v1.37.0 时代,当前 v1.38.0。已在当前工作树代码层确认 bug 仍存在,非"旧版本已修"。

### Code Path

issue 引用 `collectors/interrupt.py` + `.aria/workflow-state.json`(triage.py 从 repo root 解析短路径标 `not found`,实为解析 artifact)。实际路径核实:
- `aria/skills/state-scanner/scripts/collectors/interrupt.py` ✅ 存在 — 仅读 `.aria/workflow-state.json`,无 git 层检测
- `aria/skills/state-scanner/scripts/collectors/git.py` ✅ 存在 — 设 `detached_head` 但不区分"普通 detached" vs "rebase 暂停"
- 描述与代码**一致**。

### Git History

No recent commits matched on cited files(Step 4 因短路径未解析而 skip;手工 grep 确认零修复 commit)。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | 仅 main (`/home/dev/Aria` master) |

→ 无 in-flight 处理,起新 cycle 安全。

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/2`

- **case-1**(实测): 临时 git repo 创建 `.git/rebase-merge/`(模拟暂停 rebase),git status 明确 "currently rebasing branch master";但 `collect_interrupt_state` 返回 `status=none`、`collect_git_state` 返回 `detached_head=False` 且**无** `git_operation_in_progress` 字段 → 完全忽略 pending rebase。**match=true**
- **case-2**(代码核查): `grep -rn 'rebase-merge|rebase-apply|MERGE_HEAD|CHERRY_PICK|git_operation_in_progress' scripts/` 零命中 → 代码层无任何 git 操作检测。**match=true**

### Verdict Rationale

bug 在当前 v1.38.0 确定性复现(实测 + 代码核查双证)。`interrupt.py` 仅消费 `.aria/workflow-state.json`,`git.py` 的 `detached_head` 在 rebase-暂停态甚至返回 False(因 `git branch --show-current` 仍报 master),两条路径都漏检 `.git/rebase-merge|MERGE_HEAD|...`。后果是阶段 2 推荐可能在 pending git 操作上给出 checkout/分支建议,破坏中间态(issue 实证已与 #133 ship 混乱叠加)。Severity `major`:影响推荐安全性的功能性缺陷,但触发前置(残留 git 操作)较罕见且无必然数据损坏,未达 critical。Action `next-cycle`:有确定性修复方案(git.py 加 `git_operation_in_progress` 枚举 + 阶段 2 入口断言消费 + snapshot additive bump),适合走完整 Spec 而非 hotfix。

---

*Generated by `/issue-triage` v1.38.0 — Ref: 10CG/Aria#135*
*Step 6 performed by AI (auto mode) — 2026-06-04*
