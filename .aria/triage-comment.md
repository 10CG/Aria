## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | (未标注) |
| Current | `1.65.0` |
| Gap | n/a |

issue 未标注报告版本; 当前 v1.65.0 的 `skills/session-closer/scripts/handoff_autofill.py` 仍含缺陷代码, 非 outdated report。

### Code Path

- `skills/session-closer/scripts/handoff_autofill.py::grep_unchecked_tasks` — ✅ 存在 (L160-175), L166-167 只拼 `tasks.md` 路径, 无任何 `detailed-tasks.yaml` 分支, 与 issue 引用逐行一致。
- 复用目标 `skills/state-scanner/scripts/lib/detailed_tasks.py` (#113 parser SOT) — ✅ 存在, 含 `_split_task_blocks` / `is_done_status` 等可复用接口。
- 实证 spec `openspec/changes/state-scanner-gate-yaml-datasource/` 已归档至 `openspec/archive/2026-07-22-state-scanner-gate-yaml-datasource/`; 归档不影响缺陷本身 (任何未来 yaml-only spec 都会触发)。

(collector 抽取的 `tasks.md` / `collectors/openspec.py` 等裸文件名 "not found" 为路径抽取 artifact, 已人工核对真实路径。)

### Git History

Step 4 skipped (collector 未跑); 人工核对: 当前 aria submodule (v1.65.0, `5a9ca18`) 该函数无后续修复提交。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | none (仅主 worktree master) |

### Reproduction

**Mode**: `auto` | **Hit rate**: `2/2`

- **case-1** (主缺陷): 隔离 fixture — `changes/yaml-only-spec/` 只含 `detailed-tasks.yaml` (2 task 全 pending, 无 tasks.md)。`grep_unchecked_tasks` 返回 `[]`, 报 0 未完成, 实际 2 pending。**match: true**
- **case-2** (对照): 同目录 `changes/md-spec/tasks.md` 含 2 个 `- [ ]`。正确返回 2 个 unchecked。缺陷限定在 yaml-only 分支缺失, 与 issue 描述一致。**match: true**

### Verdict Rationale

当前版本代码逐行核对 + 隔离 fixture 双 case 复现均命中: yaml-only spec 在 §2 carry-forward 汇编中被静默跳过, 产生「0 未完成」假绿 — 与 #113 已修的三处消费方同根因, 本处为跨 skill 第四处。修法按 issue 建议复用 `lib/detailed_tasks.py` parser SOT, 注意跨 skill import 路径与 `lib` 顶层名绑定毒化 (#113 已踩坑)。severity=major: 功能性假绿, 直接误导下一 session 的 carry-forward 判断; 非 critical (不损数据、不阻断主流程)。

---

*Generated by `/issue-triage` v1.65.0 — Ref: 10CG/aria-plugin#121*
