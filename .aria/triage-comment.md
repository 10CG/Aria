## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.28.0` |
| Current | `1.36.0` |
| Gap | behind (+8 minor) |

报告版本 v1.28.0 落后当前 v1.36.0，但**核心 gap 在当前版本仍存在**：Layer L (multi-terminal-coordination) 已 ship v1.22.0，但默认 opt-out，且未覆盖 UPM 记录约定本身。非"已修待升级"。

### Code Path

无 cited paths（feature-request，非具体行 bug）。相关组件：state-scanner Layer L (`state_scanner.coordination.*`)、UPM/handoff line-prepend 约定、phase-d-closer 收尾。

### Git History

multi-terminal-coordination Spec 已归档 `openspec/archive/2026-05-20-multi-terminal-coordination/` (v1.22.0) — Layer L 存在但 opt-out。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | none |

无 in-flight，可安全起新 cycle。

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/3`

- **case-1 (merge thrash)** ✅: 并发同区编辑（line-3 指针 prepend + 同一 followup row）→ git 冲突是确定性行为。**本 triage session 亲历同类**：在 Aria `latest.md` line-prepend + 5 SOT 文件与 sister 并发 ship 反复冲突，靠 fetch+rebase 机械合并。
- **case-2 (Layer L opt-out)** ✅: 确认 Layer L 已 ship v1.22.0，但 `state_scanner.coordination.enabled` 默认 `false`；项目未启用 → phase1_gate / claim / cross-owner collision 检测不触发，没拦住 thrash。
- **case-3 (附带 index.lock)** ✅: 并发 git 操作产生瞬时 `.git/index.lock` — 本 session 3-repo ship 期 4× 命中，已立 memory `feedback_stale_git_index_lock_recovery`。

### Verdict Rationale

设计 gap 真实且机制确定性成立（并发同区编辑必致 merge 冲突；Problem 2 矛盾记录源于 `updated_at`=Prisma @updatedAt 不被 raw SQL 触发的真实陷阱）。Layer L 已存在但 **opt-out + 不覆盖 UPM 记录约定 + phase-d 无 fetch-gate** → 三处可改进。`major`：Problem 2 矛盾 prod 归属是严重的工作流正确性问题、merge thrash 耗时显著，但无代码级数据损坏、不硬阻断、且 Layer L 提供部分缓解，故非 `critical`。`next-cycle`：需 OpenSpec 承载报告人的 3 项建议（Layer L 主动引导 + UPM 约定 concurrent-safe + phase-d fetch-gate），非 hotfix。

---

*Generated by `/issue-triage` v1.36.0 — Ref: 10CG/Aria#133*
*Step 6 performed by AI (auto mode) — 2026-05-30*
