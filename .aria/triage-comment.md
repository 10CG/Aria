## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.62.0-phase4-rule6` |
| Current | `1.63.0` |
| Gap | behind (设计/方法论 issue，版本差不影响成立) |

### Code Path

- `CLAUDE.md` — ✅ 存在 (639 行，本会话 trim 后)。
- `AB_TEST_OPERATIONS.md` (:153 / :188) — ✅ 存在于 `aria-plugin-benchmarks/AB_TEST_OPERATIONS.md`。triage.py 按仓根解析报 not-found 是**路径假象** (文件在子目录)。人工核实两行**逐字吻合** issue 引用：`:153` = 「如果 without_skill 也能轻松通过这个 expectation…应该替换」；`:188` = 「without_skill: 不加载任何 skill 直接执行」。
- 证据 artifact `ab-results/2026-07-20-v1.62.0-phase4-rule6/` — ✅ 存在 (benchmark.json / eval-10 / eval-11 / grading-summary.md / skill-snapshot-v1.61.0)。

### Git History

No recent commits matched on cited files (设计 issue，无对应代码修复轨迹)。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | none (仅 main) |

**无任何 in-flight 修复。** 起 cycle 无撞车风险。

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/3`

- **case-1**: cited 文件 + 证据 artifact 真实存在，`:153`/`:188` 逐字吻合 → ✅ match。
- **case-2**: `evidence_grade` / `gitlink_integrity` / `fail-CLOSED` / `overall_parity` 在 CLAUDE.md **各 1 次**，与 issue「各一次」claim 吻合 → ✅ match。
- **case-3**: 污染是**结构性**——本会话 CLAUDE.md trim (删 v1.52–v1.58 方法论轨) 使 `stale_unverified` 掉到 0，但 4 个核心术语仍在保留的 stale-refs 条目内 → **污染减小未消除**，反证 option C 前提「in-repo baseline 恒被污染」 → ✅ match。

### Verdict Rationale

三条结构性前提 (CLAUDE.md 自动加载污染 baseline / `:153` 判据在污染下反噬测试集 / genre confound) 均可**机械核实为真**，证据 artifact 完整。故 `confirmed`。

`major`：污染直接侵蚀 Rule #6 (不可协商规则) 的 AB 执行——不仅让分数偏高，还会顺着 `:153` 把**对真实采用者有区分度的断言删掉**，磨钝测量工具本身。但即时危害已被作者含住 (该轮 `without_skill` 0.75 标注不可用、未据此下「skill 有价值」结论、A10/B4 已拆条 ab-suite v1.5.0)，非 data corruption / 阻断主流程，故非 critical。

`next-cycle`：需在 A/B/C/D 间做**设计决策** (作者倾向 C+D：接受 in-repo 恒污染 → Rule #6 问题从「skill vs 无 skill」改「新版 vs 旧版」+ 改 `:153` 判据为「三臂全过先语义分档再决定拆/删」；D 正交——钉死产出形态「实执行 vs 描述推演」)。这是 brainstorm + Level 2/3 OpenSpec cycle，非 hotfix。**补一条 triage 期证据**：本会话缩小污染面但未消除，实证污染的结构性，为 option C 增一分。

---

*Generated by `/issue-triage` v1.63.0 — Ref: 10CG/aria-plugin#116*
