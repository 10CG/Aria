## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.69.1` |
| Current | `1.70.0` (aria 子模块 `0545f86`; aria master 已到 v1.71.1 `301641b`) |
| Gap | behind |

版本差 3 个 release (v1.70.0 / v1.71.0 / v1.71.1), 但 `git diff 0545f86 origin/master -- skills/issue-triage/scripts/collectors/_version.py skills/issue-triage/scripts/triage.py skills/issue-triage/SKILL.md` 为空; `_version.py` 自 2026-05-13 `d2d7cb6` (issue-triage T1 首版) 起从未被触碰, 缺陷仍在。

注意: 本仓 (Aria) 跑 Step 0 得 exit 0 / `current=1.70.0` / `triage_tool_version=1.70.0` —— 这是 path 1 (`aria/.claude-plugin/plugin.json`) 恰好命中的**对照组**, 不是反证; 见下方 case-1 对照。

### Code Path

issue 引用的文件在本仓路径需加前缀 `aria/skills/issue-triage/scripts/` (step 3 collector 把裸文件名相对仓根判 `file not found`, 是引用路径省略前缀, 非事实错误)。实读 `aria/skills/issue-triage/scripts/collectors/_version.py`:

| 位置 | 实况 |
|---|---|
| `:114` `_discover_current_version(project_root)` | 签名与 issue 一致; docstring 明写 5-path chain |
| `:128` p1 `project_root / "aria" / ".claude-plugin" / "plugin.json"` | 播种 `triage_tool_version` (`:131`) |
| `:135` p2 `project_root / ".claude-plugin" / "plugin.json"` | 播种 `triage_tool_version` (`:139`) |
| `:143` p3 `project_root / "VERSION"` | 不播种 |
| `:149` p4 `project_root / "package.json"` | 不播种 |
| `:155` p5 `project_root / "pyproject.toml"` | 不播种 |
| `:160` | 全 miss 兜底 `("unknown", None)` |
| `:227-229` | `current == "unknown"` -> `collection_status: error` + `soft_error("version_discovery_failed", "all 5 version paths failed")` |
| `:242` | `_triage_tool_version = triage_tool_version or "unknown"` |
| `triage.py:121-124` `_step_has_data` | 只认 `status == "ok"`, step2 error 必然使 `steps_with_data <= 4` -> `triage.py:219` `EXIT_PARTIAL` (10) |

5 条全部是 `project_root / <literal>`, 无任何下探; 与 issue 引用的代码块逐字一致。

### Git History

step 4 collector 返回空 (`likely_fix_candidates=[]`); 人工核: `_version.py` 全史仅 1 次提交 (`d2d7cb6` 2026-05-13), 无 fix 候选。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none (aria 远程仅 `github/feature/aria-issue-triage-sop` 为已合并的原始功能分支) |
| Worktrees | 仅主 worktree (master `16b5bf1`) |

Forgejo 按「version collector」/「triage_tool_version」搜 issue 无同题重复。

### Reproduction

**Mode**: `auto` | **Hit rate**: `3/3`

- **case-1** (collector 单元级, hermetic 仓: 仓根无 5 个探测位置, 版本在 `sub-cli/VERSION` 与 `sub-plugin/VERSION`): `_discover_current_version(root)` -> `('unknown', None)`; `collect_version` -> `collection_status: error`, `current: unknown`, `gap: null`, `_triage_tool_version: unknown`, soft_error `version_discovery_failed`。对照组 `/home/dev/Aria` 根 -> `('1.70.0', '1.70.0')`。**match**
- **case-2** (端到端, 同仓 `triage.py --issue 10CG/Aria#200 --project-root <mono>`): `EXIT=10`; stderr 逐字 = issue「错误输出」两行; report `triage_tool_version='unknown'`, `steps_with_data=3/5`。本机 `/home/dev/Aether` 实测布局与 issue 一致 (仓根五处全无, `aether-cli/VERSION`=1.19.0, `aether-plugin/VERSION`=1.12.5), 报告方「连续 4 次全命中」前提属实。**match**
- **case-3** (连带断言核对): SKILL.md `:108` gate 判据 = `triage_tool_version` 存在且为 string, **无** `!= "unknown"` 检查; schema `:26-29` 为 `type: string` 无 enum/pattern, `"unknown"` 过校验; 代入 case-2 report: `isinstance str = True`, gate 不 abort, 模板尾行 (SKILL.md `:238`) 渲染为 `vunknown`。issue 建议 3 的替代源 `${CLAUDE_PLUGIN_ROOT}/.claude-plugin/plugin.json` 本机存在 (cache `aria/1.71.1/`, version `1.71.1`), 与被 triage 项目布局无关。**match**

### Verdict Rationale

三条 case 全部命中, 根因是代码级事实 (`_version.py:128-160` 五条 `project_root / <literal>` 无下探) 而非环境差异; 本仓不触发只是探测位置巧合命中, 任何仓根无版本文件的 monorepo 采用方 100% 每次 triage exit 10, Step 2 版本差恒不可用。判 `major`: 不只是告警噪声 —— 六步 SOP 里 Step 2 整步失效, 且 `triage_tool_version` 这个 anti-hand-author 防伪字段在 monorepo 上恒为 `unknown`, gate 静默失去判别力 (issue 对 gate 行为的描述经实读核对属实)。判 `next-cycle` 而非 `hotfix`: exit 10 按契约仍生成 report、triage 可继续, 不阻断主流程, 也不污染数据。

**修法倾向** (与 issue 三条建议一致, 建议 3 优先且独立落地): (1) `triage_tool_version` 改从 `${CLAUDE_PLUGIN_ROOT}/.claude-plugin/plugin.json` 读 (`triage.py` 自身 `__file__` 向上两级即 plugin 根, 无需环境变量), 与项目版本解耦, 直接恢复 gate 判别力并消掉 `vunknown`; (2) `.aria/config.json` 加 `issue_triage.version_file` 逃生口 (SKILL.md config 表同步加行); (3) 有界下探 (深度 1-2) 命中多个时报 `ambiguous` 列候选。附带: gate 判据宜显式加 `triage_tool_version != "unknown"`, 否则 (1) 落地前该字段仍是合法字符串。适合与 issue-triage 其他小修合一个 Level 2 批 (改 `_version.py` 逻辑 + SKILL.md 描述, Rule #6 按 hunk 判: collector 逻辑走 SC 级结构化测试 substitute, SKILL.md gate 判据改动属处方性照跑 AB)。

---

*Generated by `/issue-triage` v1.70.0 — Ref: 10CG/Aria#200*
