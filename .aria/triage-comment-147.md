## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.66.0` |
| Current | `1.66.1` |
| Gap | behind (1 patch) |

1.66.1 (#128 secret-guard, bash hook 面) 与本 issue 的 Python subprocess 面无交集, 版本差不影响结论。

### Code Path

Issue 以表格列路径, 机械抽取未命中 (cited_paths 空), 逐处人工核验:
- `phase-c-integrator/scripts/ci_backends/aether.py` — :153/:176 `text=True`; :156 `except (TimeoutExpired, FileNotFoundError, OSError)`; :180 重试循环仅接 `TimeoutExpired`。两处均接不住 `UnicodeDecodeError`, 与 issue 描述一致。
- 全仓 `text=True` 文件数实测 **27** (issue 初筛 26, 语料增长 +1, 量级一致)。

### Git History

Step 4 skipped (cited_paths 机械抽取为空)。人工核验: 无修复该类缺陷的 commit。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | none |

### Reproduction

**Mode**: `auto` | **Hit rate**: `4/4`

1. 异常族归属 + 实抛: `issubclass(UnicodeDecodeError, OSError)=False` / `ValueError=True`; `text=True` 下实抛且 OSError 族 isinstance=False; bytes+`surrogateescape` 对照不抛 — 与 issue 复现段逐字一致。
2. `aether.py` 元组核实: 命中 (见 Code Path)。
3. 26 文件初筛: 实测 27, 命中。
4. 先例放大器: `pre_merge_gate.py:310` 明文「异常轴取 path_coverage.py 的 `(TimeoutExpired, FileNotFoundError, OSError)` 元组; 重试轴取 aether.py `RETRY_BACKOFF`」— 同形元组确被立为异常轴先例 (出处是 path_coverage.py 而非 aether.py, 实质主张成立)。该文件自身靠「⛔ 不传 `text=True` + surrogateescape」躲过, 且 `references/pre-merge-gate-empirical-traps.md` #4/#5 已把本坑与「代理码位下游 `json.dumps` 才炸」坑一并记录 — issue 对修法 B 代价的预警与仓内既有经验吻合。

### Verdict Rationale

机制 4/4 复现, 生产调用点 (aether.py 两处) 逐行核验命中, 且仓内已有文档把它记为经验坑但未清理存量 — `confirmed`。severity `major`: 触发面窄 (需子进程输出非 UTF-8) 但后果是收集器/gate 脚本未捕获崩溃。action `next-cycle` 而非 hotfix: 类级缺陷 21 处存量待逐处复核, 且修法 A (逐处补 `ValueError`) vs B (统一 bytes+surrogateescape+出口净化) 是设计取舍待 owner 裁 — 建议起 Level 2 spec, 把「出口净化」(traps.md #5) 写进 SC。

---

*Generated by `/issue-triage` v1.66.1 — Ref: 10CG/aria-plugin#147*
