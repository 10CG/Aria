## Triage Report

**Verdict**: `confirmed` | **Severity**: `major` | **Recommended Action**: `next-cycle`

---

### Version

| Field | Value |
|-------|-------|
| Reported | `1.62.2` |
| Current | `1.64.0` |
| Gap | behind (破损自始存在, 版本差不影响成立 — v1.64.0 无相关修复轨迹) |

### Code Path

- `skills/tdd-enforcer/examples/python/tests/test_calculator.py` — ✅ 存在 (triage.py 报 not-found 为路径假象, 文件在 `aria/` 子模块下; 与 #116 triage 同形)。
- `src/calculator.py` + `src/__init__.py` — ✅ 结构与 issue 描述逐项吻合。
- `conftest.py` / `pytest.ini` / `pyproject.toml` — ✅ 确认**三者均不存在** (issue 的根因前提成立)。
- `README.md` — ✅ `:42` / `:58` / `:77` 三处教裸 `pytest tests/test_calculator.py -v`, 按文档跑必失败。

### Git History

No recent commits matched on cited files (step4 collector skipped; 人工核实 v1.62.2→v1.64.0 无 examples 目录变更)。

### In-flight

| Category | Matches |
|----------|---------|
| Remote PRs | none |
| Local branches | none |
| Worktrees | none (仅 main) |

**无 in-flight 修复, 起 cycle 无撞车风险。**

### Reproduction

**Mode**: `auto` | **Hit rate**: `4/4`

- **case-1**: 目录结构 + 零 path 配置文件 + README 裸 pytest 指引 → ✅ 全部吻合。
- **case-2**: 隔离 venv (pytest 9.1.1) 内 `cd tests && python3 -m pytest -q` → **逐字复现** `ModuleNotFoundError: No module named 'calculator'`, `Interrupted: 1 error during collection` → ✅ match。
- **case-3**: 对照 `PYTHONPATH=../src python3 -m pytest -q` → `14 passed, 1 skipped` — **与 issue 数字逐字吻合** → ✅ match。
- **case-4**: 宿主环境恰无 pytest → 实证「环境依赖假绿」形态 (SKIP 顶替 FAIL, "exit 0 绿在缺依赖上"); 本次复现须专门建 venv 才触发真实 FAIL → ✅ match。

### Verdict Rationale

根因前提 (src/ 不在 path、无任何 pytest path 配置)、失败形态 (收集期 ImportError)、对照实验 (PYTHONPATH 即通过) 三者全部机械复现, 且 README 教的命令本身就踩此坑, 故 `confirmed`。定 `major` 而非 minor: 它不只是教学示例破损 — 在任何装了 pytest 的环境, 跨 skill 测试入口 `run_all_tests.sh` (Aria #168) 会**恒 exit 1**, 恒红信号会被学会忽略, 侵蚀 runner 对真实失败的拦截力 (与 #119 详情恒空叠加后更甚)。修复是确定性小改 (conftest.py 一个文件), 无 hotfix 紧迫性, 故 `next-cycle`; 建议与姊妹 #119 打包一个小 cycle。

---

*Generated by `/issue-triage` v1.64.0 — Ref: 10CG/aria-plugin#118*
