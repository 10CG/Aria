{
  "_comment": "Aria 项目级配置 — 首次启用 issue_scan 用于 Phase 1.13 dogfooding (2026-04-09)",
  "version": "1.0",

  "workflow": {
    "auto_proceed": false
  },

  "state_scanner": {
    "confidence_threshold": 90,
    "auto_execute_enabled": false,
    "auto_execute_rules": ["commit_only", "quick_fix", "doc_only"],
    "audit_log_path": ".aria/audit.log",

    "sync_check": {
      "enabled": true,
      "check_submodules": true,
      "check_remote": false,
      "warn_after_hours": 24
    },

    "issue_scan": {
      "_comment_submodules": "2026-04-15 启用 scan_submodules + stage_timeout 12→20 作为 state-scanner-submodule-issue-scan Spec 的 dogfooding — 防止 Aria meta-repo 模式下 aria-plugin / aria-orchestrator / aria-standards 的 issue 被漏扫",
      "enabled": true,
      "platform": "forgejo",
      "platform_hostnames": {
        "forgejo": ["forgejo.10cg.pub"],
        "github": ["github.com"]
      },
      "cache_ttl_seconds": 900,
      "cache_path": ".aria/cache/issues.json",
      "stage_timeout_seconds": 20,
      "api_timeout_seconds": 5,
      "limit": 20,
      "label_filter": [],
      "scan_submodules": true
    },
    "coordination": {
      "_comment": "DEC-20260704-002 dogfood: 启用 Layer L advisory 认领闸门 (interactive-session-dedup-coordination TASK-019). enabled=true 使 phase1_gate.run_gate 在 cross-owner/self-multi-container collision + 用户进 Phase B 时被 AI 编排层调用; mode=advisory (放行+写推 claim+surface). runtime 探针 coordination-gate-invocation 从此有真实生产调用对象。",
      "enabled": true,
      "mode": "advisory"
    }
  },

  "tdd": {
    "strictness": "advisory"
  },

  "benchmarks": {
    "require_before_merge": true,
    "skill_change_block_mode": "warn"
  },

  "experiments": {
    "agent_team_audit": false,
    "agent_team_audit_points": ["pre_merge"]
  },

  "audit": {
    "_comment": "Step-by-step rollout (DEC-20260519-001). post_spec enabled 2026-05-19. post_planning enabled 2026-07-04 per DEC-20260704-001 (mid_post_spec dogfood on a concurrent A.3 draft found a per-artifact transcription drift + a shared spec-wording drift → post_planning audits A.2/A.3). Remaining checkpoints kept off until dogfood-verified; mid_post_spec deferred pending a Phase-B SMOKE-triggered dogfood.",
    "enabled": true,
    "mode": "convergence",
    "max_rounds": 4,
    "checkpoints": {
      "post_brainstorm": "off",
      "post_spec": "convergence",
      "post_planning": "convergence",
      "mid_implementation": "off",
      "post_implementation": "off",
      "pre_merge": "off",
      "post_closure": "off"
    },
    "teams": {
      "post_spec": ["aria:tech-lead", "aria:backend-architect", "aria:qa-engineer", "aria:code-reviewer", "aria:knowledge-manager"],
      "post_planning": ["aria:tech-lead", "aria:backend-architect", "aria:qa-engineer", "aria:code-reviewer", "aria:knowledge-manager"]
    }
  },

  "orchestrator": {
    "dispatch_policy": {
      "tier1_action": "dispatch_ready",
      "tier2_action": "approval_required",
      "tier3_action": "human_only",
      "dry_run": false
    }
  }
}
