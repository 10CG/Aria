---
type: pending_owner_decision
spec_id: linked-issue-normalization
opened_at: 2026-08-06
status: pending
blocking: A.2
items: 7
audit_context: post_spec R1′ (5/5 REVISE) + R2′ (5/5 REVISE)
---

# Owner 决策清单 — `linked-issue-normalization`

> **为什么一次性给你**: post_spec 跑了 R1′ 与 R2′ 两轮 5 席全额审计, 浮出的**不可由 AI 单方处置**的事项已攒到 7 条。它们互相有依赖 (Q6 的答案会让 Q1–Q4 中的几条自动消失), 逐条问会让你重复读同一批上下文。
>
> **读法建议**: 先看 **Q6** (它可能让一半问题消失), 再回头看其余。
>
> **AI 的角色**: 每条给推荐与理由, **不代裁**。Rule #10 — 已启用闸门的处置、跨 Spec 边界归属、Rule #6 合规判定, 都不在 AI 的权限内。

---

## 背景速览 (30 秒)

- **变更本体**: `lib/collision.py::linked_issue_overlaps` 的**一个内部比较谓词** —— 把裸字符串 `!=` 换成归一后比较。Level 2。
- **两轮审计结果**: R1′ 5C/~14M → R2′ 1C/~11M。数字在降, **但 R2′ 的 ~11 条 major 里 10 条是 R1′-fix 亲手引入的**。
- **算法本体已被反复证实站得住**: 传递性 200,000 三元组 0 违例 · `normalize` 顺序无关 (全 Unicode 码点验证) · 全部 `file:line` 零偏差 · 11 行判读表 11/11 全对 · baseline artifact 17/17 + `6 passed` + 1322 tests OK。
- **问题不在算法, 在 Spec 的体量与它引出的边界**: 466 行 / 17 条 SC / 8 条 D 行 / 16 项零审计表面 / 5 条跨 Spec 挂起。**审计装置已比被审对象大。**

---

## Q6 ⭐ 先看这条 —— 要不要缩范围?

**问题**: 本 Spec 是否应把范围收窄为「**只交付归一比较谓词 + SC-1~6b**」, 把 fixture / 等价关系断言 / 可导出单元 / D2 半幅缺口**整体推给母 Spec 或后续 change**?

**为什么现在问**: 母 Spec (`a1-entry-claim-duplicate-work-guard`) 经 R1→R2→R3 三轮不收敛, **破局的不是加轮, 是你裁的「缩范围 + spike-first」**(handoff §4.3 已记)。本 Spec 现在走到了同一个位置: 两轮审计, 修 12 造 10。

| 选项 | 内容 | 代价 |
|---|---|---|
| **(a) 缩范围** ⭐ 推荐 | 保留: 归一规则五步 · SC-1/1b/2/3/4/5/5b/5c/6/6b/10/11 · rule6_note substitute · D1–D8。**移出**: SC-7 (等价关系) + §SC-7 fixture 口径整节 + SC-8a/8b/8c 冻结断言 + SC-9 | **Q1/Q2/Q4 直接消失** (它们全挂在被移出的部分上); Spec 从 466 行回到 ~250 行; 未审表面从 16 项降到 ~8 项。**代价**: D1 的等价关系主张退回「已论证、未留证」并成文为已知限 |
| (b) 维持现范围 | 全部保留, 按三类补救方案继续 (见 Q7) | Q1–Q5 全都要答; 且每轮 fix 仍会造新缺陷 |
| (c) 整体推迟 | 本 Spec 挂起, 等母 Spec 解封后合并处理 | 那条 live 缺陷继续挂着 (虽已实测「已装填未击发」, 无存量症状) |

**AI 推荐 (a)**, 理由: 被移出的四块**全部**是 R1′/R2′ 缺陷的高发区 —— SC-7 的判别力自陈「结构上不可红」、fixture 口径引出 G1 不可实现的下界、SC-8a 冻结与母 Spec 撞车、SC-9 与 D2 半幅纠缠。而**留下的部分是两轮审计里唯一从未被质疑过的**: 归一算法本体 + 它的行为 SC + substitute 证据面。

---

## Q1 — D2 半幅缺口的归属 (原 X1)

**事实** (两席各自去母 Spec 实读, 结论一致): 本 Spec 写「该半缺口由母 Spec 闭合」, 但母 Impact 表那行只列 `--include-terminal` / `_main():1232` 关键字参数 / `error` 带 `fetch_degraded` **三项**, 母 SC-1~19 **无一条**断言输出含 `linked_issue`, 且母 Spec 自陈行为类 SC「只能由 eval 覆盖, **不冒充结构化测试**」。⇒ **两边都无 owner。**

| 选项 | 代价 |
|---|---|
| **(a) 纳入本 Spec** | 给 `_claim_to_dict` **与** `_gate_result_to_dict` 两处加 additive 键 (加键不改签名); 非目标第一条须从「不改 `phase1_gate.py`」收窄为「不改 CLI 参数面 / `run_gate` 签名」 |
| (b) 归母 Spec | 须去母 Impact 表加行 + 加一条**代码类** SC (不能只靠行为类 SC-11) |
| **(c) 成文为已知限, 两边都不做** ⭐ 推荐 | 零成本; 代价是「回显原串」这个 D2 缓解**永久只有半幅**, 须在 D2 与 §极性段如实标注 |

**推荐 (c)**, 理由: 该缺口的后果是「看不见自己传的是什么」, 而 advisory 的消费者是**人**, 人知道自己传了什么。真正致盲的只有「转述该 JSON 的下游」—— 那是假设的未来消费者, 今天不存在。**用一个已知限换掉一条跨 Spec 依赖, 划算。** 若选 (c), 无论如何须修 §极性段现在那句「由母 Spec 闭合」(它已被证伪且 `git diff` 显示从未改过)。

---

## Q2 — 归一是否作为可导出单元交付 (原 X2)

**事实**: 母 Spec §2.1 逐字规定 track-id 派生的 `basename`「经前置 Spec 归一 (含 S5 的 `./_ → -`)」⇒ 要求一个可 import 的单元。本 Spec 通篇写「内部比较谓词」, 且 **SC-8a 把签名冻成 committed 测试**。母 Spec §2.4 自己写着「⇒ `lib/collision.py` 必须进 Impact 表」—— **而它没进母 Spec 的 Impact 表**。

| 选项 | 代价 |
|---|---|
| **(a) 本 Spec 导出纯函数** ⭐ 推荐 | 导出 `normalize_linked_issue(value) -> tuple[str,int] \| None` (`None` = 规则 4 的不可解析) + 进 Impact 表 + 一条 SC 冻其返回契约。**成本极低 —— 该函数无论如何都要写, 只是决定它是不是私有** |
| (b) 母 Spec 重实现 | **正是本 Spec 把算法钉到字符级要防的事** (两个实现者同规格得相反结果, 已在本项目实证过) |

**推荐 (a)**。若 Q6 选 (a) 缩范围, 本条仍需答 (导出与否和 fixture 无关), 但 SC-8a 会被移出 ⇒ 冻结冲突自动消失。

---

## Q3 — `include_terminal` 形参归属 (原 X3)

**事实**: 母 Spec §2.4 要求给 `linked_issue_overlaps` 增 keyword-only 形参 `include_terminal: bool = False`, 并**逐字写「该协调项须 owner 确认」**。而本 Spec 的 SC-8a 写「改签名 / **加默认值**的实现必红」⇒ 母 Spec 落地当天必须改本 Spec 刚提交的冻结测试。

| 选项 | 代价 |
|---|---|
| **(a) SC-8a 改为前三位参数冻结** ⭐ 推荐 | 期望改为「**前三位**逐字为 `(claims, own_track_id, own_linked_issue)` 且三者无默认值」+ 注明 keyword-only 追加由母 §2.4 引入、届时更新本 SC 不视为回归 |
| (b) 维持严格冻结 | 母 Spec 承担改测试成本并在其 tasks 显式列出 |

**推荐 (a)**。若 Q6 选 (a), SC-8a 被移出 ⇒ 本条消失。

---

## Q4 — 两层解的主/辅定位 (原 X4)

**事实**: 母 §1.2+§1.3 的「抽取规则 + 单一 canonical 形态 (B 族)」与本 Spec 的「比较时归一」是**同一问题的两层解**, 两份 Spec 都没写谁是主防线。母 §1.3 实测「直接拿字段值喂归一 **OK=0 / 不可解析=13**」⇒ 本 Spec「未来输入总体 = 由字段值**直接**喂入」的定义与之矛盾。

**唯一合理的定位 (待你确认)**: 归一 = **存量已落盘 / 人工直传 `--linked-issue` / `coordination.enabled=false` 三条路径的兜底**; 抽取规则 = **新写入路径的主防线**。

定后须同批订正: 本 Spec 第三个总体的定义 + 母 Spec `:8` 的「恒漏报」措辞 (在 §1 落地后它只对存量 16 条成立)。

---

## Q5 ⭐ — rule6_note 的 Rule #6 自我豁免 (原 X5, R2′ 新增, **两席独立命中**)

**这是唯一一条涉及不可协商规则本身的**。

**事实**: 我在 rule6_note 里补的「三要素对照」, **第 1 条自陈**「本 hunk 属『行为变更后的语义追加』, **严格说不完全等同于**该 §2 具名的『溯源注释 / 行号勘正 / 术语修正』三个子类型」, 然后**自创一条枚举外理由**把判定拉回判据表第一行, 结论写「⇒ 三要素齐备」。

而 `standards/conventions/skill-benchmark-exemption.md`:
- §2 措辞是「**仅当**变动是事实性同步 (三子类型) …**才可能**落进第一行」= **必要条件, 非加权评分**;
- §4 明写「AI **不可以**在决策表之外自创理由…落进『拿不准』格时, **默认照跑**」;
- 其 `Source incidents` 记录的两起真实误判之一, 起因逐字是「**以『未改 SKILL.md 指令面』自我豁免**」—— **与我这段的论证结构同形**, 且就发生在同一个 state-scanner skill 家族内。

| 选项 | 代价 |
|---|---|
| **(a) 照跑一次 AB** ⭐ 推荐 | 改动只有 `SKILL.md:176` 一行, AB 成本低; **一次性消除争议**, 且不留「AI 可自证任何看起来不改行为的追加都算描述性」的先例 |
| (b) 你个案裁定可接受 | 须在 rule6_note 注明**这是一次性个案而非通用先例** |
| (c) 从 Impact 撤下 SKILL.md 同步行 | 回到 U-2 的方案 B —— 留一处已知文档漂移, 违 Rule #3 (这正是你 U-2 时否决它的理由) |

**推荐 (a)**。理由: 这是**闭合枚举的覆盖外分支**, memory `feedback_written_exception_exact_condition_match` 的判据是「覆盖外分支**上报而非自搭桥**」。我搭了桥。照跑一次一行的 AB 是最便宜的正解。

---

## Q7 — 后续补救走哪条路 (不是「跑不跑 R3′」, 是「用什么工具」)

R2′ 把缺陷分成三类, **只有第三类适合用「再审一轮」解决**:

| 类 | 本轮实例 | 建议工具 |
|---|---|---|
| **机械同步缺失** | 行号失效 · 计数 stale · SC 枚举漏 · 自指语料漂 (共 6+ 处) | **写一个 Spec 一致性检查器** (仿 artifact 的漂移守卫)。加轮只能发现, 不能防止 |
| **全称句自我证伪** | D8「任意两个」· rule6_note「三要素齐备」· SC-10「其余全部」· R1′ 的「结构上不可能」 | **一次定向 sweep** (grep 任意/全部/唯一/均/结构上不可能, 逐条检验)。是写作习惯不是知识缺口 |
| **覆盖维度盲区** | strip 第三点 · `#` 方向 · `/` 方向 · casefold · number | **一次穷举变异测试** —— qa 已建好 harness。归一谓词的维度是**有限集**, 逐维建变异体、要求每维有 killing SC。**一次清零, 不要每轮挖一两个** |

**AI 推荐: 三条全做, 且都在 R3′ 之前做完。** 做完后若还要跑闸门, 那一轮才有意义 (它审的是「穷举之后还剩什么」而不是「上一轮 fix 又造了什么」)。

---

## 附: 三条与本 Spec 无关但已逾期的

| # | 项 | 状态 |
|---|---|---|
| 🔴🔴 | **凭据轮换** `FORGEJO_TOKEN` / `NEXUS_API_TOKEN` / `CF_ACCESS_CLIENT_SECRET` | hard cap `2026-08-02`, **已逾期 4 天**。AI 完全无法代劳 |
| 🟡 | **silknode waiver** | `expires_at 2026-08-05` **已过期**, custom checks 8/8 → **7/8**。三条路: 关闭豁免 / 续期 / 升级为 `standards/governance/` |
| 🟡 | **terminal 语义分歧未开 issue** | `reconcile.py:57-58` 的 `"yielded" is NOT terminal` 与 `claim_lifecycle.py:317` 正面冲突, 3 个取值散在 9 个站点。两轮审计都点名「建议单开 issue」, 至今无号 |

---

## 回复格式建议

逐条给字母即可, 例如: `Q6=a, Q1=c, Q2=a, Q3=(随 Q6 消失), Q4=确认, Q5=a, Q7=全做`。
不确定的写「?」, 我会补充那条的信息再问。
